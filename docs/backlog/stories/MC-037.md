---
id: MC-037
title: New screenshots grow the corpus and fill the held-out set
slug: new-screenshots-grow-the-corpus-and-fill
epic: EPIC-07
type: chore
status: in-review
phase: REVIEW
branch: story/MC-037-new-screenshots-grow-the-corpus-and-fill
depends_on: [MC-036]  # the split field and its validation must exist first
required_gates: []    # gate ids that are optional for the repo but binding for THIS story
---

## Context

[`EPIC-07`](../epics/EPIC-07.md) `## Stories` item 1, second half. MC-036 gave
the manifest a validated `split` field and assigned every pre-existing entry to
`"tuning"`. The held-out set is empty. This story fills it, with **new
screenshots that no v1 tuning has ever seen**, and puts floors under both sets
so the emptiness cannot come back.

Read MC-036's `## Context` first; the contamination argument is there and is not
repeated. The one-line version: the existing 28 entries are contaminated — six
investigations fitted thresholds against them — so held-out has to be built from
new files, and this story is the only place new files can arrive.

**The user supplies and marks the screenshots. An agent cannot.** `corpus.md`
is explicit that a mark is a person's call and that nothing in the repository
can check a rectangle is the right rectangle. This is MC-018's situation
repeated, and MC-018 is the procedure to copy: `chore` under SCAFFOLD, the user
drops files into `fixtures/corpus/` and fills in the manifest, the Lead PO
writes the validation and runs the gates.

**Why `chore` and not `feature`.** Same reason as MC-036, verified there:
`paths.conf` classifies `fixtures/**` and `**/tests/**` as `test`, GREEN may
write neither, and there is no production code that could make a criterion about
corpus composition pass. See MC-036 `## Context`.

**Gate that fails if this story's artifact breaks: `unit` (required).** The new
floors go in `crates/engine/tests/corpus_manifest.rs`, non-`#[ignore]`d, for the
reason MC-036's `## Out of scope` gives. `required_gates` is `[]` because `unit`
already binds. It must stay that way: a count assertion parked in `corpus.rs` or
`corpus_accuracy.rs` runs only under the optional, `slow` `integration` gate and
would let `All required gates passed` print over an unfilled held-out set.

### The hard constraint nobody will think of until it bites

`MAX_BYTES` is 60 MB (MC-018 AC-4, so the repository stays clonable without
LFS), and the corpus is **22 MB across 28 files today**. That leaves ~38 MB, and
the format of a new capture matters more than its count:

| Kind | Observed per-file | 34 new files would cost |
|---|---|---|
| 2560x1440 PNG capture | 0.77 – 1.47 MB (~1.1 MB) | ~37 MB → total ~59 MB, **over the line in practice** |
| 2560x1440 JPEG capture | 0.21 – 0.28 MB (~0.25 MB) | ~8.5 MB → total ~31 MB |
| even mix, 17 of each | — | ~23 MB → total ~45 MB |

So: a PNG-heavy growth of this size does not fit, and the ceiling is a DONE
story's acceptance criterion — raising it is the user's call, not an agent's.
An even mix fits comfortably and keeps the existing 18 PNG / 8 JPEG / 2 WebP
balance from skewing, which matters because JPEG ringing near a panel edge is
exactly the kind of thing a v2 edge rule will be sensitive to. **Recommendation:
roughly half the new captures as JPEG, and do not exceed ~17 new PNGs.**

### Numbers proposed here need the user's confirmation before this story leaves PLANNED

Criteria freeze on leaving PLANNED, so this is the moment. Everything in the
next section marked **[confirm]** is a proposal with reasoning, not a settled
fact. The reasoning is given so it can be argued with.

**Why 20 held-out marked entries and not fewer.** Read out of MC-018 AC-1's own
reasoning, which is already a `const` in the test file: *"Below this the corpus
cannot support a 'nine in ten' claim with any resolution: at twenty entries one
miss is five percent."* The bar EPIC-07 inherits is 20 of 21 with zero clips —
about 95%. A held-out set of 15 moves 6.7% per entry, so the reported number
would be decided by which screenshot happened to land in it. 20 is the smallest
set on which a 95% claim means anything, and it is a number this project already
committed to rather than one invented here.

**Why 34 new files and a 29:20 marked split.** 21 existing marked entries are
locked to tuning by MC-036, so the ratio is not free: to reach 20 held-out the
corpus needs at least 20 new marked entries, and the tuning set should grow too
— v2 is fitting two new signal classes (2D structure, known site furniture) and
fitting them on 21 entries is how v1 got here. 28 new marked entries gives
tuning 29 (21 + 8) and held-out 20, a 59:41 split, which is the usual shape for
a corpus this small where held-out resolution is the binding constraint. 6 new
flag entries, 4 of them held-out, keeps flags near their current quarter share
so the flag-and-copy behaviour is also measured on unseen data.

**Coverage beats count, and it is not a slogan.** v1's failure causes cluster by
site furniture: a site's navigation bar is pixel-identical across every
screenshot from that site, which is the whole premise of EPIC-07's story 3. A
screenshot from a site the corpus does not already contain is worth several from
one it does. Twenty held-out entries from one reader would measure almost
nothing.

### Two questions only the user can answer

**Q1 — is the split stratified by site, or site-disjoint?** They measure
different things and the difference is not cosmetic.

- *Stratified* (every site appears in both sets): measures whether a rule
  generalises to unseen **pages**. Optimistic for anything that learns site
  furniture, because the held-out entries share a pixel-identical navigation bar
  with entries the rule was tuned on.
- *Site-disjoint* (whole sites held out): measures whether a rule generalises to
  an unseen **reader**. Honest, and pessimistic for story 3 by construction —
  which may be the right answer to get, since story 3's approach is exactly the
  one stratification would flatter.

**Recommendation: stratified, plus at least one site held out entirely.** Both
numbers then come out of one corpus, and a gap between them is itself the
finding. It costs only that the user contributes enough files from one extra
site to make it a whole-site holdout.

**Q2 — does every entry gain a `site:` tag?** AC-4 below cannot be written
without one: "spans at least N sites" is not checkable unless the site is
recorded. A `site:<slug>` tag needs no parser change — `documented_tag_vocabulary()`
reads any table row whose first cell is a backticked tag — but it does need the
user to name the site for each of the 28 existing entries as well as the new
ones, because an agent reading a site off a screenshot is guessing, and
`corpus.md` is explicit that a guessed tag is worse than an absent one. If the
user declines, **AC-4 is dropped at PLANNED** and site coverage stays an
instruction to the person collecting files rather than an assertion.

### Settled with the user on 2026-09-18, before this story left PLANNED

Everything above marked **[confirm]** was put to the user and answered. The
answers are below; the criteria that follow are the answers, not the proposals,
and they are frozen from here.

| Question | Answer |
|---|---|
| **Q1** split rule | **Stratified, plus at least one whole site held out.** Recorded in AC-6 and enforced by AC-4's "≥2 unseen sites" clause — see below |
| **Q2** `site:` tag | **Held-out entries only** — see the revision below. The pre-EPIC-07 28 carry no `site:` tag |
| AC-1 held-out marked floor | **20**, as proposed |
| AC-2 held-out flag floor | **4**, as proposed |
| New file count | **24, not 34** — the user's change. See below |
| AC-4 `S` (corpus spans ≥S sites) | **Dropped.** See below |
| AC-4 `H` (held-out spans ≥H sites) | **4** |

**Q1 needs no assertion of its own, and that is worth spelling out.** Tuning is
frozen at exactly the 28 pre-EPIC-07 entries and every new entry is held out, so
a site appears *only* in held-out precisely when no pre-EPIC-07 entry carries
it. "At least one whole site held out" and AC-4's "at least 2 sites carried by
no pre-EPIC-07 entry" are therefore the same clause, and the stricter of the two
is the one written down. A second assertion saying the same thing in different
words is a second thing to keep in step, not a second check.

**Why that clause says "no pre-EPIC-07 *entry*" and not "no pre-EPIC-07 *marked*
entry"**, which is how AC-4 originally phrased it. A site carried only by a
pre-EPIC-07 *flag* entry would count as unseen under the looser wording, and it
is not: MC-026's decision gates were fitted against the flag entries too. The
tighter wording is also the mechanically simpler one — `PRE_EPIC_07_ENTRIES` is
a flat list of 28 names with no rect/flag distinction in it.

**Why 24 new files and not 34: the user declined to grow the tuning set.**
`## Context` argued tuning should grow to 29 marked because v2 is fitting two
new signal classes on the 21 that got v1 here. The user's ruling is that
held-out is what the new files are for, and tuning stays at its 21 marked / 7
flag. So the 24 new files are 20 marked + 4 flag, **all held-out**, and the
split is 28 tuning : 24 held-out by entry, 21 : 20 by marked entry. The argument
for growing tuning is not withdrawn — it is deferred, and any later story that
wants it is adding files, not reassigning these.

*Consequence for the byte budget, which gets easier:* 24 files against 38.67 MB
of headroom (measured 2026-09-18: the corpus is 22,361,231 bytes = 21.33 MB of a
60 MiB `MAX_BYTES`). An even 12/12 PNG/JPEG mix costs ~16 MB → ~37 MB total.
Even **all 24 as PNG** now fits, at ~26 MB → ~48 MB, though it would leave only
12 MB of headroom for every story after this one. The mix recommendation stands
for the JPEG-ringing reason in `## Context`, not for the bytes any more.

### Q2 revised the same day, after the user began marking

The first answer to Q2 was "tag all 52". On working through it the user declined
to tag the pre-EPIC-07 28, and **the repository's own doctrine says they are
right**: `corpus.md` rules that *a guessed tag is worse than an absent one*.
Attributing a reader to a screenshot captured in January 2026 is precisely the
situation that manufactures guesses, and 28 unreliable tags would have produced
a check that looks rigorous while resting on recall nobody can audit — the exact
failure this corpus keeps writing rules against.

So the pre-EPIC-07 entries carry **no `site:` tag at all**, and AC-4 now
quantifies over held-out entries only.

**What replaces the per-file attribution, so Q1 stays enforceable.** The
"≥2 unseen sites" clause is the only thing that enforces Q1's whole-site
holdout, and it needs to know which readers the old corpus came from. Naming
that **as a set** is a different and far easier act of recall than attributing
28 files one by one: the user declares the readers the pre-EPIC-07 corpus was
captured from, that set is pinned as `PRE_EPIC_07_SITES` in `corpus.md` and read
into the test, and "unseen" means a held-out slug outside it.

This is a person's ruling pinned in `corpus.md`, which is how this corpus
already handles its marking rule, its diagonal gutters and its gutter-crossing
band. It is no weaker than the rest of the corpus and no stronger: `corpus.md`
is explicit that nothing in the repository can check a rectangle is the right
rectangle either. What it buys is that the claim is **written down and
falsifiable by the person who made it**, rather than implicit in 28 fields
filled from memory.

**Why `S` was dropped.** AC-4 proposed "the corpus spans at least S distinct
sites" with S "fixed with the user once they list the sites in the existing 21".
Nobody in this repository knows that number, and it is not knowable until the
user tags the existing entries — which happens *during* SCAFFOLD, after the
criteria freeze. A floor chosen before the count is known is decorative, which
is the specific failure AC-4's own control clause warns about. The two clauses
that survive — held-out spans ≥4 sites, ≥2 of them unseen — are checkable
without knowing anything about the existing corpus, and the whole-corpus site
count becomes a fact the tags record rather than an assertion nobody can
justify.

## Acceptance criteria

- **AC-1** — Given the loaded corpus, when the held-out entries with a rect are counted, then there are **at least 20**. *Control:* moving any one held-out marked entry to `"tuning"` must fail this test naming the count 19, and the whole-corpus `MIN_ENTRIES` test must stay green while it does — a floor that fires only when the total drops is not measuring the split.
- **AC-2** — Given the loaded corpus, when the held-out entries expecting `"flag"` are counted, then there are **at least 4**. *Control:* as AC-1, with a flag entry; and the test must fail when all held-out entries carry rects, which is the way this criterion is silently satisfiable if it is written as a count over all held-out entries rather than over flags.
- **AC-3** — Given MC-036's `PRE_EPIC_07_ENTRIES` list, when the manifest is validated, then **no** held-out entry's file name appears in it, and the held-out set is non-empty. This is MC-036's AC-2 read in the other direction; it was vacuous there because held-out was empty, and it is the assertion that makes the held-out number mean anything. *Control:* flipping one pre-EPIC-07 entry to `"held-out"` must fail it by name.
- **AC-4** — Given every **held-out** manifest entry, when validated, then each carries **exactly one** tag matching `site:<slug>` and present in `corpus.md`'s vocabulary table; the held-out set spans **at least 4 distinct sites**; and **at least 2** of those are outside `PRE_EPIC_07_SITES` — the set of readers the user declares the pre-EPIC-07 corpus was captured from, pinned in `corpus.md` and read into the test. **Entries in `PRE_EPIC_07_ENTRIES` carry no `site:` tag and are not required to**, by the user's ruling of 2026-09-18 recorded below. *Control:* a held-out entry with two `site:` tags, and one with none, must each fail by name; a `site:` tag absent from the vocabulary table must fail; and **collapsing every held-out entry onto slugs inside `PRE_EPIC_07_SITES` must fail the "2 outside" clause while leaving the "at least 4 sites" clause satisfied** — if the two clauses cannot be made to disagree, one of them is decorative.
- **AC-5** — Given the grown corpus, when `corpus_manifest.rs` runs, then every MC-018 and MC-033 assertion still holds unchanged: every named file exists and decodes, every rect lies inside its image, the nine required tags are still covered, every tag is in the documented vocabulary, the `diagonal-gutter` list is still exactly two entries, and the whole directory is still under 60 MB. New files do not get to weaken the existing checks.
- **AC-6** — Given `docs/wiki/corpus.md`, when this story is done, then its split section records **how** the split was assigned — **stratified, plus at least one whole site held out**, the user's ruling of 2026-09-18 — by whom and on what date, and states that the held-out set is scored once, at the end of a v2 attempt, and is never tuned against. It also records that the 24 new entries are held out in their entirety and that tuning was deliberately not grown, so the next agent does not read the 21:20 marked ratio as an accident. A split whose assignment rule is not written down is a split the next agent can quietly re-draw.

## Model guidance

Model: **default**. As MC-018: every criterion is a mechanical check on files
the user provides, and the only judgement — is this the right rectangle, is this
the right site — is the user's and the validator cannot check it.

**Oracle partition:**

| Kind | What |
|---|---|
| **Settled — read out, never re-derive** | `corpus.md`'s marking rule, the `diagonal-gutter` tolerance, the gutter-crossing band and the tag vocabulary; the 60 MB ceiling; the twenty-entry reasoning from MC-018 AC-1; MC-036's `PRE_EPIC_07_ENTRIES` list and the contamination ruling; the tight marks, by the user's decision of 2026-09-17. |
| **Oracle-free** | The counts in AC-1 (20), AC-2 (4) and AC-4 (4 sites, 2 unseen), and the 21:20 marked split ratio. Their justification is in `## Context`; they were **settled with the user on 2026-09-18** and are frozen. Each carries a control above. Changing one now is an `## Amendments` entry and an independent reproduction, not a quiet edit. |
| **Mechanical — pin exactly** | AC-3 and AC-5 in their entirety, and the tag-shape half of AC-4. |

## Out of scope

- **Any detection rule.** This story adds files and counts. It does not touch
  `crates/core/src/**`, `Tuning`, any threshold, or anything that looks at a
  pixel. Nothing here is scored against anything.
- **Scoring the held-out set.** Not once, not "just to see". The set's value is
  that it has never been looked at; the first look belongs to whichever EPIC-07
  story earns one, and `corpus.md` will say so (AC-6). Running the existing
  accuracy harness across the new entries to "check they are reasonable" spends
  the held-out set before v2 starts.
- **Re-marking anything.** The marks stay tight, by the user's decision of
  2026-09-17. MC-031 section 9 Option C was declined. No existing rectangle
  changes.
- **Changing any existing entry's `split`.** All 28 are `"tuning"` and stay so.
  If a new entry turns out to be a duplicate of an existing one, it is dropped,
  not swapped.
- **The column axis.** Settled at 20 of 21 and not reopened.
- **Applying `overhang-text`, or adding a third `diagonal-gutter` entry.** Both
  are a person's call under `corpus.md`, and both are a survey rather than a
  side effect of adding files. If the user tags new entries with either while
  marking, that is fine and needs no story; an agent inferring one is not.
- **Raising the 60 MB ceiling.** If the growth genuinely will not fit, stop and
  put it to the user — it is MC-018 AC-4 and a DONE story's criterion.
- **`gates.sh --fast` as sufficient evidence.** `integration` is `slow`, and
  `corpus.rs` / `corpus_accuracy.rs` decode every file in the corpus. Adding 24
  images makes that gate slower and is exactly the kind of change `--fast` hides.
  Run the full gates.

## Test plan

Written under SCAFFOLD; the shape is fixed here so the criteria are reachable.
All new tests are non-`#[ignore]`d, in `crates/engine/tests/corpus_manifest.rs`.

- Per-set counters over `corpus::load()`, split by `Split` and by `Expect`
  (AC-1, AC-2), each with its own vacuity guard in MC-018's style — a count
  assertion over an empty set is the failure mode here.
- The reverse-direction check against `PRE_EPIC_07_ENTRIES` (AC-3).
- The `site:` tag shape and coverage counts (AC-4), reusing
  `documented_tag_vocabulary()` with no parser change. Three separate
  assertions, not one: exactly-one-`site:`-tag-per-entry, held-out spans ≥4
  sites, and ≥2 held-out sites absent from every `PRE_EPIC_07_ENTRIES` entry.
  The last two must be able to disagree — the control depends on it.

## Collection outcome — what actually arrived, 2026-09-18

The files are in `fixtures/corpus/` and the manifest is written. All sixteen
existing `corpus_manifest.rs` tests pass against it (AC-5 satisfied), which is
the state SCAFFOLD starts from.

| | Count | Floor | AC |
|---|---|---|---|
| Held-out entries total | 31 | — | |
| …with a rectangle | 24 | 20 | AC-1 |
| …expecting `flag` | 7 | 4 | AC-2 |
| …that are pre-EPIC-07 files | 0 | 0 | AC-3 |
| …carrying exactly one `site:` tag | 31 | all | AC-4 |
| Distinct held-out readers | 7 | 4 | AC-4 |
| …outside `PRE_EPIC_07_SITES` | 3 | 2 | AC-4 |
| Corpus size | 58.73 MB | < 60 | AC-5 |

`PRE_EPIC_07_SITES` = `toongod`, `rolia-scans`, `w-network`, `xbato`.
Unseen readers: `manhwaclan` (2 entries), `kunmanga` (2), `demonicrevolution`
(1). Held-out readers by weight: `toongod` 10, `rolia-scans` 8, `w-network` 5,
`xbato` 3, `manhwaclan` 2, `kunmanga` 2, `demonicrevolution` 1.

**Three things happened during collection that the tests must not paper over.**

1. **A duplicate was dropped, not swapped.** `2025-03-03 11_24_19.png` arrived
   as a held-out entry and is byte-identical (`sha256 0a1344bfc456…`) to the
   pre-EPIC-07 tuning entry of the same name. Six investigations fitted
   thresholds against that file; holding it out would have produced exactly the
   fake-rigour number AC-3 exists to prevent. Removed per `## Out of scope`.
   Held-out went 32 → 31. **AC-3's test must catch this case**, because it is
   not hypothetical — it happened on the first attempt.
2. **`w16` and `w18` were collapsed into `w-network`.** They were read off a
   rotating subdomain (`w18.pickmeupgacha.com`) and are one reader template.
   Two slugs for one reader inflates the distinct-site count and can make a
   reader look unseen while its twin sits in tuning. The full reasoning is in
   `corpus.md`.
3. **The unseen-reader number rests on 5 entries across 3 readers**, one of
   which has a single entry. It clears the floor and it is thin; `corpus.md`
   says to read a v2 unseen-reader result as a direction rather than a
   percentage. AC-4's floor is not raised to match — that would be inventing a
   requirement after seeing the data.

`PRE_EPIC_07_SITES` is the user's ruling given as a set, and their own wording
was that the three unseen readers are *likely* absent from the old 28. It is a
recall claim on the same footing as every rectangle here, recorded in
`corpus.md` with that confidence attached rather than laundered into a fact.

## Collection brief — what was asked for

The part no agent can do. `corpus.md`: nothing in the repository can check that
a rectangle is the right rectangle, or that a site slug is the right site.

**24 new screenshots**, none of which any v1 tuning has ever seen:

- **20 marked** — each with the tightest axis-aligned rectangle containing all
  of one page's own artwork, under `corpus.md`'s marking rule. A panel's own
  overhang extends its rectangle; a neighbour's overhang, or a caption box in
  the gutter, does not.
- **4 `flag`** — all-art or too blank to call.
- **Every one of the 24 is `"split": "held-out"`.** No existing entry's split
  changes.

**Site spread:** the 24 must span **at least 4 distinct sites**, and **at least
2** of those must be sites that appear nowhere in the existing 28. Beyond that
the weighting is yours — coverage beats count, and a screenshot from a reader
the corpus has never seen is worth several from one it has.

**Format:** roughly half JPEG. Not for bytes any more (all-PNG now fits) but
because JPEG ringing near a panel edge is exactly what a v2 edge rule will be
sensitive to, and the existing 18/8/2 balance should not skew further toward
PNG.

**Tags on the new entries:** the theme and gutter tags as usual, the format tag,
and exactly one `site:<slug>`. **Do not apply `diagonal-gutter` or
`overhang-text`** — both are a person's deliberate survey call under
`corpus.md`, both are `## Out of scope` here, and AC-5 pins the
`diagonal-gutter` list at exactly its current two entries.

**And the 28 existing entries each gain a `site:<slug>` too** — you name them.
This is the Q2 answer and there is no way around it: an agent reading a site off
a screenshot is guessing, and `corpus.md` says a guessed tag is worse than an
absent one.

Each new slug also needs a row in `corpus.md`'s `<!-- tag-vocabulary -->` table,
or `every_tag_in_the_manifest_is_in_the_documented_vocabulary` fails — which is
the check working, not a problem.
- AC-5 is the twelve-plus existing tests, unmodified. If any of them needs
  editing to accommodate a new file, the new file is wrong.

## Scaffold inventory

**No production source was written.** Every path this story touches classifies
as `test` or `docs`:

```
$ bash -c '. .claude/hooks/lib.sh; classify "fixtures/corpus/manifest.json"; \
           classify "fixtures/corpus/Screenshot (56).png"; \
           classify "crates/engine/tests/corpus_manifest.rs"; classify "docs/wiki/corpus.md"'
test test test docs
```

| File | Kind | Covered by |
|---|---|---|
| `fixtures/corpus/manifest.json` | fixture | all twenty-two tests in `corpus_manifest.rs`; mutated in probes P1–P6 |
| `fixtures/corpus/*.png` (31 new) | fixture | `every_corpus_file_decodes_with_the_engines_own_decoder`, `every_expected_rect_is_non_empty_and_within_its_image`, `the_whole_corpus_fits_under_sixty_megabytes` |
| `crates/engine/tests/corpus_manifest.rs` | test (validator) | itself; the six new tests probed P1–P6 |
| `crates/engine/tests/corpus.rs` | test (accuracy) | itself — the `integration` gate; filtered to `Split::Tuning`, see `## Notes` |
| `crates/engine/tests/corpus_accuracy.rs` | test (accuracy) | itself — the `integration` gate; filtered to `Split::Tuning`, see `## Notes` |
| `docs/wiki/corpus.md` | docs | `every_tag_in_the_manifest_is_in_the_documented_vocabulary` (the seven `site:` rows) and `at_least_two_held_out_readers_are_absent_from_the_pre_epic_07_set` (the `<!-- pre-epic-07-readers -->` table) |

That classification is also why this is a `chore` under SCAFFOLD rather than a
`feature`: GREEN's permitted categories are `source,config,docs,harness`, with
no `test`, so a `feature` would arrive in GREEN unable to write three of these
four. MC-018 and MC-036 are the precedents.

**The 31 images are the story.** They are not incidental fixtures for a test —
they *are* the artifact, and the tests exist to stop them rotting. No agent
could have produced them: the rectangles are a person's judgement and
`corpus.md` is explicit that nothing in this repository can check a rectangle is
the right rectangle.

**`required_gates: []` was checked, not assumed.** `bash scripts/gates.sh --list`
reports `unit` as `required`, running `cargo test --workspace`; the six new
tests in `corpus_manifest.rs` are not `#[ignore]`d, so a required gate exercises
this story's artifact. The `## Out of scope` clause forbidding these tests from
moving into `corpus.rs` or `corpus_accuracy.rs` — both `#[ignore]`d, both
reachable only through the optional `slow` `integration` gate — is what keeps
that true.

## Gate probes

Required, and for the reason the PLANNED-phase note gives: this is SCAFFOLD, so
every assertion was written against a corpus that already satisfies it. None had
been observed to fail. One mutation, one run, one revert each, against
`fixtures/corpus/manifest.json`, with **the restore verified by hash rather than
assumed** — `sha256 b5f163ba72d3ca42…` before and after every probe, and all six
printed `restored: hash matches`. The suite is `22 passed; 0 failed` on the
restored tree.

Each probe is the control clause its acceptance criterion names.

### P1 — AC-1, the held-out marked floor

Five held-out marked entries moved to `tuning`, taking 24 → 19.

```
test the_held_out_set_carries_at_least_twenty_marked_entries ... FAILED
test the_manifest_parses_and_names_at_least_twenty_files_that_all_exist ... ok

AC-1: the held-out set carries 19 marked entries and needs at least 20. This is
MIN_ENTRIES' reasoning applied to the half of the corpus that now carries the
accuracy claim: at twenty entries one miss
```

**The second line is the probe.** The whole-corpus floor stayed green while the
split floor went red, which is the half of this control that catches a test
measuring the wrong thing: a floor that only fires when the *total* drops is not
measuring the split at all, and nothing about the first line alone would reveal
that.

**Departure from the control as written, recorded rather than quietly
reinterpreted.** AC-1's control says "moving *any one* held-out marked entry to
`tuning` must fail this test naming the count 19". That presumed a corpus at
exactly the floor; collection produced 24, so one entry leaves 23 and the test
correctly passes. Five were moved instead, which is the smallest mutation that
crosses the floor, and the count named is 19 as the control requires. The
criterion's assertion is unchanged and its purpose is fully discharged — see
`## Notes`.

### P2 — AC-2, the flag floor, and the way it is silently satisfiable

Four held-out `flag` entries turned into rects, taking flags 7 → 3. **Held-out
entries in total stayed 31.**

```
P2 held-out total: 31
test the_held_out_set_carries_at_least_four_flag_entries ... FAILED
test all_art_and_mostly_white_entries_expect_a_flag_and_not_a_rect ... FAILED

AC-2: the held-out set carries 3 entries expecting `flag` and needs at least 4.
The detector has two jobs - crop the croppable and decline the rest -
```

That the total held unchanged at 31 is the point. AC-2's control asks for the
test to fail "when all held-out entries carry rects", because a count written
over *held-out entries* rather than over *flags* is the way this criterion reads
as passing while measuring nothing. A count over the total would have seen 31
here and said nothing.

MC-018's `all_art_and_mostly_white_entries_expect_a_flag_and_not_a_rect` going
red alongside is correct and not noise: the mutated entries still carry
`all-art`, and a rect contradicts it. Two independent assertions caught one
mutation.

### P3 — AC-3, the contamination guard from the other side

`2026-01-05 13_33_41.png`, a pre-EPIC-07 entry, flipped to `held-out`.

```
test no_held_out_entry_is_a_pre_epic_07_file ... FAILED
test every_pre_epic_07_entry_is_in_the_tuning_split ... FAILED
test every_held_out_entry_carries_exactly_one_site_tag ... FAILED

assertion `left == right` failed: AC-3: no held-out entry may be a file that
existed at commit a453aaa. ...
  left: ["2026-01-05 13_33_41.png"]
```

Fails **by name**, as the control requires. MC-036's guard fires too — the same
invariant read from the tuning side — and the site-tag test fires because a
pre-EPIC-07 entry carries no `site:` tag and is not required to until it claims
to be held out. Three assertions, one mutation, all three correct.

*First attempt did not mutate anything at all* (`ok. 22 passed`): the target
`Screenshot (2744).jpg` was passed to awk with escaped parentheses, which the
dynamic regex did not honour, so nothing matched and the probe would have been
recorded as evidence of a test that never ran. Retried against a name with no
parentheses. A probe that silently mutates nothing looks exactly like a probe
that passed.

### P4 — AC-4, exactly one site tag, both directions

Two separate mutations, because "exactly one" fails in two ways and a probe of
one direction says nothing about the other.

**P4a — a second, separately valid site tag** on `2025-07-17 14_20_23.png`:

```
test every_held_out_entry_carries_exactly_one_site_tag ... FAILED
test result: FAILED. 21 passed; 1 failed

  left: ["2025-07-17 14_20_23.png: 2 site tags [\"xbato\", \"toongod\"]"]
```

**P4b — the site tag removed** from `2025-09-29 14_33_15.png`:

```
test every_held_out_entry_carries_exactly_one_site_tag ... FAILED
test result: FAILED. 21 passed; 1 failed

  left: ["2025-09-29 14_33_15.png: 0 site tags []"]
```

Both fail by name, and in both cases **only that test** fails — the isolation is
the evidence that this assertion, and not some neighbour, is what caught it.
The duplicate is a slug the vocabulary already documents on purpose: a made-up
one would also trip `every_tag_in_the_manifest_is_in_the_documented_vocabulary`
and the probe would stop isolating anything.

*P4a's first attempt produced invalid JSON* and took 21 of 22 tests down with a
loader panic. That is not a probe of anything — it demonstrates the loader
rejects malformed input, which no criterion here claims. Fixed and re-run.

### P5 — AC-4, the reader-count floor

Every held-out entry's `site:` tag rewritten, cycling over three readers.

```
test the_held_out_set_spans_at_least_four_readers ... FAILED
test at_least_two_held_out_readers_are_absent_from_the_pre_epic_07_set ... FAILED

AC-4: the held-out set spans 3 distinct readers and needs at least 4.
```

### P6 — AC-4, and the one control that decides whether any of this is decorative

Every held-out entry's `site:` tag rewritten, cycling over **the four
pre-EPIC-07 readers** — `toongod, rolia-scans, w-network, xbato`.

```
test at_least_two_held_out_readers_are_absent_from_the_pre_epic_07_set ... FAILED
test the_held_out_set_spans_at_least_four_readers ... ok
test result: FAILED. 21 passed; 1 failed

AC-4: 0 held-out readers are absent from the pre-EPIC-07 set, and at least 2 are
needed. These are the only entries that can answer 'does this general...
```

**This is the probe the criterion turns on.** AC-4's control says that
collapsing every held-out entry onto readers the corpus already carries "must
fail the '2 unseen' clause while leaving the 'at least 4 sites' clause
satisfied — if the two clauses cannot be made to disagree, one of them is
decorative". They disagreed: the reader-count test is **`ok`** on the same tree
where the unseen-reader test is red. The two assertions are measuring different
things, and the stronger one is not riding on the weaker.

P5 and P6 together bracket it: three readers fails both, four *known* readers
fails only the unseen one. Neither alone would have shown that.
## Notes

**The `integration` gate scored the held-out set the first time it ran, and
that is why two files outside the test plan changed.** This is the finding the
story's `## Out of scope` warned about — *"`gates.sh --fast` as sufficient
evidence... `corpus.rs` / `corpus_accuracy.rs` decode every file in the corpus.
Run the full gates"* — arriving in a worse form than the warning anticipated.
The warning was about *speed*. The actual defect was that both suites call
`corpus::load()` and score **every** entry, with no reference to `Split`
anywhere in either file:

```
$ for f in crates/engine/tests/*.rs; do printf "%-45s Split refs: %s\n" "$f" "$(grep -c Split "$f")"; done
crates/engine/tests/corpus.rs                 Split refs: 0
crates/engine/tests/corpus_accuracy.rs        Split refs: 0
crates/engine/tests/corpus_manifest.rs        Split refs: 6
```

So the moment the thirty-one screenshots landed, v1's detector ran across them
and four tests that passed under MC-036 failed:

```
WARN         integration (127s, exit 101, optional)

test no_marked_page_is_reported_ambiguous ... FAILED
test no_crop_clips_a_marked_page ... FAILED
test no_crop_clips_a_marked_page_on_the_column_axis ... FAILED
test the_two_pages_closest_to_chrome_are_ambiguous_again_one_step_above_the_band ... FAILED
test result: FAILED. 5 passed; 4 failed
```

`integration` is optional, so `All required gates passed` printed over it. That
combination — an optional gate carrying the only check that touches the thing a
story built — is the failure mode this story's own `## Context` describes for
`required_gates`, showing up from the other direction.

**Two rulings, both the user's, 2026-09-18.**

*The suites are filtered to `Split::Tuning`, in this story.* `corpus.rs` gains
`tuning_only()` and `corpus_accuracy.rs::run_corpus()` filters inline. No
threshold moved, no detection rule changed, nothing under `crates/core/src/**`
was touched — `## Out of scope` holds. The second reason is the one that makes
this not merely permitted but necessary: **MC-019's, MC-026's and MC-032's
numbers were measured over the twenty-eight pre-EPIC-07 entries**, and without
the filter their denominator silently becomes fifty-nine. Not filtering would
have changed what every recorded v1 claim means. The filter restores those
suites to what they measured when their numbers were written down; it does not
narrow them.

After the change, `cargo test -p cropper-engine --release --test corpus --test
corpus_accuracy -- --ignored` is `9 passed; 0 failed` and `5 passed; 0 failed`,
in 3.07s and 2.25s — faster than before, because they now decode 28 files
rather than 59.

*The held-out set is still held out.* `corpus.md`'s rule is that contamination
is **using** a held-out result to choose, adjust or reject a threshold, or
reading a per-file table while a rule is still being changed. Neither happened:
v1 is frozen and closed, no v2 rule exists yet to be influenced, and the
per-file tables in `.claude/state/gate-logs/integration.log` were deliberately
not opened — the four test *names* and the pass/fail line were read, and nothing
else. What was observed is a single aggregate fact, that v1 does worse on the
new screenshots than on the old. The full reasoning and the ruling are recorded
on `docs/wiki/corpus.md` rather than only here, because that page is what the
next agent reads.

**The lesson, stated so the next split-like thing does not repeat it:** MC-036
added `split` and enforced it in `corpus_manifest.rs`, and nothing made the
suites that actually *score* the corpus respect it — because held-out was empty
and the omission could not bite. It bit on the first day it could. A field that
is validated in one file and assumed in every other is not enforced.

**AC-1's control clause presumed a corpus at exactly the floor, and is
discharged on its own terms rather than literally.** It reads "moving *any one*
held-out marked entry to `tuning` must fail this test naming the count 19".
That is arithmetic for a held-out set of exactly 20 marked entries. Collection
produced 24, so moving one leaves 23 and the test correctly passes — the
control as literally written cannot be satisfied by this corpus.

P1 moves five, the smallest mutation that crosses the floor, and the count named
is 19 exactly as the control demands. **No acceptance criterion changed**:
AC-1's assertion is "at least 20", it is unchanged, and both halves of the
control's purpose are discharged — the test fails naming 19, and the
whole-corpus `MIN_ENTRIES` floor is observed staying green while it does. This
is recorded here rather than in `## Amendments` because nothing about what the
test asserts was altered; what changed is the size of the mutation needed to
reach the boundary, which is a property of the corpus that arrived, not of the
criterion.

Flagged to the product owner in the report accompanying this phase. If they
would rather it be an `## Amendments` entry, the wording to amend is AC-1's
control clause — "any one" to "enough to cross the floor" — and nothing else.

**Two probes had to be re-run because the first attempt did not probe
anything.** Both are recorded in `## Gate probes` rather than quietly
re-executed, because each is a way a probe can look like evidence and be none:

- P3's first attempt passed `Screenshot (2744).jpg` to an awk dynamic regex with
  escaped parentheses, which were not honoured; nothing matched, the manifest
  was unmodified, and the suite reported `ok. 22 passed`. A probe that mutates
  nothing is indistinguishable from a probe of a test that cannot fail.
- P4a's first attempt emitted invalid JSON and took 21 of 22 tests down with a
  loader panic. That demonstrates the loader rejects malformed input — true, and
  not something any criterion here claims. A probe whose blast radius is the
  whole suite has isolated nothing.

**`PRE_EPIC_07_SITES` is parsed off `docs/wiki/corpus.md`, not duplicated in
Rust.** The story text says it is "pinned in `corpus.md` and read into the
test"; it is implemented with the existing `documented_vocabulary()` helper
against a new `<!-- pre-epic-07-readers -->` marker, the same arrangement the
tag and split vocabularies already use. A second copy in Rust is the drift
MC-033 exists to remove, and this list is more likely to move than either of
the others — it widens the moment anyone identifies another reader in the old
twenty-eight.

**The branch was cut from `main` after PR #48 merged** (`99d602d`), so this PR
carries MC-037's artifact and not its criteria. The criteria landed separately
and deliberately: `check-boundaries.sh` diffs `## Acceptance criteria` against
`origin/main` with no PLANNED exemption, so settling them on the story branch
would have failed CI, and an `## Amendments` entry would have been the wrong
fix for changes made *during* PLANNED with the product owner.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-18T23:19:52Z
    commit: 99d602d (working tree had uncommitted changes)
    tree:   e594648b1372187ca3ee94a026a9605aeb3aac25
    result: pass (7 ran, 0 unconfigured, 0 known)

    PASS         format (0s)
    PASS         lint (1s, observed -)
    PASS         typecheck (0s, observed -)
    PASS         unit (13s, observed 47, floor 47)
    PASS         coverage (67s, observed 1717)
    PASS         integration (139s, observed 9)
    PASS         build (118s, observed -)

