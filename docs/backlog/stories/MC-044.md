---
id: MC-044
title: The lock-waiting tests hold under load
slug: the-lock-waiting-tests-hold-under-load
epic: EPIC-07
type: fix
status: done
phase: DONE
branch: story/MC-044-the-lock-waiting-tests-hold-under-load
depends_on: []      # none; independent of MC-043, which is in the same file
required_gates: []  # `unit` does not cover the harness; see "The gate question"
---

## Context

Found while driving [MC-040](MC-040.md) on 2026-09-22. The `one run at a time`
block of `.claude/tests/gates.test.sh` (~line 344 onwards) takes the gate-run
lock with a **live background process**:

```bash
sleep 30 & LIVE=$!
fake_lock "$LIVE" "$(hostname 2>/dev/null || printf '%s' "${HOSTNAME:-unknown}")"
```

and then makes **seven** `gates.sh` invocations — `--no-wait`, `--gate`,
`--fast`, `--required`, `--audit`, `--list`, and finally the waiting run —
before the last four assertions fire. Under enough concurrent load those seven
invocations take longer than 30 seconds, the fixture owner dies during setup,
and `gates.sh`
**correctly** reports taking over a stale lock instead of waiting. The suite then
fails, having observed the behaviour a *different* assertion in the same file
exists to pin.

Observed during MC-040's GREEN, while several minutes-long release rebuilds were
running concurrently — `gates: 78 passed, 4 failed`:

```
    FAIL waiting is the default
         expected to contain: Waiting for it to finish
         actual:               note: the gate run lock was left behind by pid 3652562, which is gone. Taking it over.
    FAIL and it gives up out loud
         expected to contain: gave up waiting for the gate run lock
    FAIL saying nothing ran
         expected to contain: neither a pass nor a failure
    FAIL and exits 3
         expected: 3
         actual:   0
```

On an idle machine it passes: `bash scripts/selftest.sh gates` →
`82 passed, 0 failed`, `real 3m18.001s`. **So this is a flake, not a standing
red, and the suite is green today.** That is exactly what makes it worth a
story: a self-test that fails intermittently teaches everyone to re-run it, and
CI runners are loaded and slow, so this can fail on CI against a green tree.

### The obvious diagnosis is wrong, and was already made once

MC-040's GREEN concluded the machine had become ~25x slower, from timing
`bash scripts/gates.sh --list` at 73–154 s per invocation. **That timing is real
and reproduces** — re-measured at 103, 72, 96, 91, 142 s. It still cannot be the
explanation: the `gates` suite makes far more than eight such invocations and
completes in 198 s. The per-invocation cost belongs to the **outer shell path**
— a fresh top-level `bash` under this environment's command layer — and
invocations made as children of an already-running script do not pay it.

This is the same shape as the trap `project.conf` documents under `ci-factor`,
where a coverage gate's wall time was extrapolated to per-test time and read 14x
against a real 3.4x: *"Someone acting on the 14x would have spent a day making a
test faster that was already fine."* **Do not repeat the wrong diagnosis.** The
defect is not machine speed. The defect is that a test pins behaviour against a
wall clock.

### What the block is actually claiming, and which claim races

Read at PLANNED against `acquire_lock()` in `scripts/gates.sh` (~129–205). Five
distinct claims live in that block, and only one of them needs a live process:

| Claim | Needs a live owner? |
|---|---|
| A held lock blocks `--no-wait`, `--gate`, `--fast`, `--required` (exit 3, nothing runs) | No — it needs a lock that is *not taken over* |
| `--audit` and `--list` take no lock | No |
| Waiting is the default, gives up out loud, says nothing ran, exits 3 | No |
| A lock whose same-host owner is **gone** is taken over | No — it needs a *dead* pid, which is already deterministic |
| A lock whose same-host owner is **alive** is waited for, never taken over | **Yes** — this is the only one |

The first three are today driven by the `sleep 30` fixture and do not need it.
The fifth is the one real requirement, and the current fixture satisfies it only
for as long as the timer happens to outlast the setup.

### Two candidate mechanisms, named and not mandated

Both were read off `acquire_lock()` at PLANNED. **Neither is prescribed** —
confirm against `scripts/gates.sh` and `.claude/tests/gates.test.sh` rather than
taking this story's word, and if a third is better, take it and say why.

1. **Own the lock with a process that cannot die during the block.** The
   take-over branch requires `host = $LOCK_HOST` **and** `! kill -0 "$pid"`. A
   pid that is alive for the whole block by construction — the test runner's own
   — makes the live-owner case deterministic with **no production change at
   all**. This is the preferred direction precisely because it costs `gates.sh`
   nothing.
2. **An injected staleness signal** in `acquire_lock()`, so a lock can be made to
   look stale (or live) on demand. This was the direction raised when the flake
   was reported. It works, but it adds an input to the lock's take-over decision
   that exists only for tests, and AC-3 is where that has to be paid for.

The claims that need no live owner have a third option already proven in the
same file: `fake_lock 999991 some-other-host` is waited for and never broken,
because this host cannot see that process table. That is deterministic today.
**But note the trap** — converting *every* fixture to a foreign host would make
AC-1 pass and silently delete the fifth claim above. AC-2 exists to stop it.

### The gate question

`required_gates: []`, on [MC-039](MC-039.md)'s reasoning, re-checked here. No
`gate` line in `project.conf` runs the harness suite; they all run `cargo`, and
this story's artifact is `.claude/tests/gates.test.sh`. **CI covers it anyway**:
`.github/workflows/gates.yml` line 37 runs `bash scripts/selftest.sh` as a
blocking step with no `continue-on-error`, before `gates.sh --list`,
`gates.sh --audit` and `gates.sh` — and that ordering is why `selftest` is not
itself a gate: it guards the machinery `gates.sh` runs on, and a gate inside
`gates.sh` checking whether `gates.sh` works is circular.

There is one wrinkle MC-039 did not have: if the fix touches `acquire_lock()` in
`scripts/gates.sh`, then **every** gate run exercises the changed path, and the
full `bash scripts/gates.sh` run this story records in `## Gate results` is real
evidence for it. AC-3 turns that into a condition rather than a hope.

What the story still owes regardless: `bash scripts/selftest.sh` run locally and
its output pasted (AC-4).

## Acceptance criteria

- **AC-1** — *No assertion in the block depends on a timer.* Given `.claude/tests/gates.test.sh`, when the `one run at a time` block runs with **the live-owner fixture killed at any point during the block** — the condition load produces, induced deterministically — then `bash scripts/selftest.sh gates` reports `0 failed`. *Control:* applying that forced kill to the suite **as it stands today** reproduces a specific failure set at each of two induction points, and both are this story's RED:
  - **late** — the owner killed after the `--list` invocation and before the waiting run, which is what load actually did: exactly the four failures quoted in `## Context` (`waiting is the default`, `and it gives up out loud`, `saying nothing ran`, `and exits 3`), at `78 passed, 4 failed`.
  - **immediate** — the owner killed on the line after it is started: those four *plus* the eight that precede them (`a second run says what is holding it`, `and names the owner`, `and refuses rather than proceeding`, `with an exit code that is not a gate verdict`, `and runs nothing`, `--gate is locked too`, `--fast is locked too`, `--required is locked too`), at `70 passed, 12 failed` — because a lock whose owner is *already* dead is taken over before `--no-wait` is ever consulted.

  Both must be shown red before the change. After it both are vacuous, because the block starts no process that can be killed; that vacuity is the criterion being met, not a gap in it, and AC-2 is what stops it being reached by deletion. A probe that produces some other set of failures at either induction point is measuring something else and the story stops.
- **AC-2** — *Every claim the block draws is still drawn.* Given the changed block, then all five claims in `## Context`'s table are still carried by at least one assertion each, and the story names which assertion carries the fifth — *a lock whose same-host owner is alive is waited for and never taken over*. *Control:* the cheap fixes both satisfy AC-1 and destroy this one — deleting the live-owner case, or making every fixture a foreign-host lock, which is waited for because the host is unreadable rather than because the owner is alive. Neither is acceptable. No assertion is weakened, skipped, widened or deleted to reach green; if one cannot be made deterministic, that is a return to PLANNED, not a deletion.
- **AC-3** — *The production lock is unchanged, or the change is justified and exercised.* Given the story's diff, then **either** `scripts/gates.sh` is untouched, **or** every change to it is named in `## Notes` with the reason a test-side fix was insufficient, states what stops the new input being set in a real run, and is covered by the full `bash scripts/gates.sh` run recorded in `## Gate results`. *Control:* the default expectation is untouched — candidate 1 in `## Context` achieves AC-1 and AC-2 with no production change, and is to be tried first. A fix that adds a force-stale environment variable and nothing else would pass AC-1 and AC-2 while making the lock weaker in production than it is today; AC-3 is what makes that visible rather than invisible.
- **AC-4** — Given `bash scripts/selftest.sh`, when it runs in full, then every suite passes and the output is pasted in `## Notes`, together with the `gates` suite's wall time. No suite other than `gates.test.sh` is edited. *Control:* `selftest` is a CI step rather than a `gates.sh` gate and never appears in `## Gate results`, so this is the story's only in-story evidence that its artifact was verified. The wall time is recorded for the next person, **not** as a threshold — this machine's outer-shell timings are documented above as unreliable, and no criterion here is allowed to depend on one.

## Model guidance

Model: **default**. Every criterion is mechanical. The one thing that looks like
judgement — why the suite flakes — is settled in `## Context` and was reproduced
before the criteria froze; re-opening it is explicitly out of scope.

**Oracle partition:**

| Kind | What |
|---|---|
| **Settled — read out, never re-derive** | That this is a wall-clock race and **not** a slow machine. The four observed failures and the idle-machine baseline (`82 passed, 0 failed`, `3m18`). That the take-over branch turns on `host = $LOCK_HOST` **and** `! kill -0 "$pid"`. That the five claims in the table all have to survive |
| **Mechanical — pin exactly** | AC-1, AC-2, AC-3, AC-4 |
| **Oracle-free** | The mechanism. Which of the two named candidates (or a third) is used is the implementer's call, subject to AC-3's preference order, and must be confirmed against the code rather than adopted from this story |

## Out of scope

- **Re-diagnosing the machine's speed.** The ~100 s per top-level
  `bash scripts/gates.sh --list` is real, belongs to the outer shell path, and is
  not this story's problem. It is recorded in `## Context` so the next person
  does not spend the day the `ci-factor` note warns about. Do not chase it, do
  not "optimise" `gates.sh --list`, and do not file a performance story off it
  without a measurement that separates the outer shell from the script.
- **Changing the lock protocol's semantics.** What counts as stale, `LOCK_POLL`,
  the default `GATES_LOCK_WAIT` of 1800, the rename-before-remove take-over, the
  foreign-host rule — all correct and all out of scope. This story changes how
  the *tests* set the lock up, not what the lock does. The one permitted
  exception is a change made under AC-3, with its reason recorded.
- **Speeding up the `gates` suite.** A deterministic fixture will probably be
  faster than a 30 second sleep; that is a side effect, not a goal, and it may
  not be bought by dropping a case. AC-2.
- **Any other suite under `.claude/tests/`.** If another one races a clock, write
  it in `## Notes` and file a story; do not widen this one. The `two real runs,
  racing` block further down the same file starts real `gates.sh` processes on
  purpose — that is a different fixture with a different justification and it is
  not in scope unless it is *also* shown to flake, which it has not been.
- **The `work_count` documentation defect.** Same file, unrelated, and it is
  [MC-043](MC-043.md). That story adds assertions elsewhere in
  `gates.test.sh`; this one changes the `one run at a time` block. They do not
  depend on each other and can be done in either order.
- **`scripts/selftest.sh` itself**, and how it reports or retries. Making a flaky
  test deterministic is the fix; adding a retry to hide it is the thing the
  harness exists to prevent.

## Test plan

One file is touched: `.claude/tests/gates.test.sh`, the `one run at a time`
block. One assertion is **added**; nothing is edited, weakened or removed. The
fixture (`sleep 30 & LIVE=$!`, the `fake_lock` that follows it, and the
`kill "$LIVE"` after the waiting run) is deliberately left exactly as it is —
it is the reproduction, and GREEN is what repairs it.

### Level

Shell unit test against a throwaway project fixture (`make_project_fixture`),
which is where this logic lives: `acquire_lock()` in `scripts/gates.sh` is
driven by running the real script in a temp repo whose gates are `printf`s.
Nothing here needs a real toolchain, and no other level can falsify the claim —
the behaviour under test is a decision `gates.sh` makes about a lock directory.

### Which assertion carries which claim in `## Context`'s table

Labels below are the exact first argument to `_ok`/`_bad`/`assert_*`, i.e. the
string that appears after `FAIL` in the suite output.

| Claim | Carried by |
|---|---|
| 1. A held lock blocks `--no-wait`, `--gate`, `--fast`, `--required` (exit 3, nothing runs) | `a second run says what is holding it`, `and names the owner`, `and refuses rather than proceeding`, `with an exit code that is not a gate verdict`, `and runs nothing`, `--gate is locked too`, `--fast is locked too`, `--required is locked too` |
| 2. `--audit` and `--list` take no lock | `--audit takes no lock`, `--list takes no lock` |
| 3. Waiting is the default, gives up out loud, says nothing ran, exits 3 | `waiting is the default`, `and it gives up out loud`, `saying nothing ran`, `and exits 3` |
| 4. A lock whose same-host owner is **gone** is taken over | `a lock with no live owner is taken over`, `and the run proceeds`, `successfully` |
| 5. A lock whose same-host owner is **alive** is waited for, never taken over | **`a live owner's lock is never taken over`** — added in this story's RED |

**AC-2 names the fifth: `a live owner's lock is never taken over`.**

Before this story, claim 5 was carried only *implicitly* — by claim 3's four
assertions happening to succeed. That is exactly what made the flake readable as
"gave up waiting" rather than "broke a live lock", and it is what would let a
cheap AC-1 fix (converting the fixture to `fake_lock 999991 some-other-host`)
delete the claim without any assertion going red. The new assertion states it
directly, in the same shape the foreign-host case below it already uses:

```bash
case "$out" in
  *"Taking it over"*) _bad "a live owner's lock is never taken over" "broke it: $out" ;;
  *) _ok "a live owner's lock is never taken over" ;;
esac
```

It reads the `$out` of the `GATES_LOCK_WAIT=2 gates` waiting run (the same
variable claim 3's four assertions read), and it is placed after `and exits 3`
and before the `kill "$LIVE"`, so the owner's liveness is still whatever the
fixture made it.

### AC coverage

| AC | Covered by |
|---|---|
| **AC-1** — no assertion in the block depends on a timer | The whole block, run under the two induction points. The `late` and `immediate` probes are the controls; `late` is RED here, and its measured set now has **five** members rather than four. |
| **AC-2** — every claim still drawn, and the fifth is named | The table above. The new assertion is the mechanism that makes AC-2 enforceable rather than merely asserted in prose: the foreign-host shortcut that satisfies AC-1 by deleting claim 5 now fails this assertion's *sibling* premise — see the handoff's note on what GREEN must not do. |
| **AC-3** — production lock unchanged by default | Not a test. `scripts/gates.sh` is untouched by RED; `git diff` is the evidence. |
| **AC-4** — full `selftest.sh` green, no other suite edited | `git diff --stat` shows one test file. The full run belongs to GREEN. |

### Deliberately not covered

- The `two real runs, racing` block further down the same file. `## Out of
  scope` excludes it and it has not been shown to flake.
- `work_count` documentation — [MC-043](MC-043.md), same file, different block.
- Anything about machine speed. Settled in `## Context`; not re-derived.

## Handoff: RED -> GREEN

You start with no memory of RED. Everything you need is below.

### What RED changed

**One file, one addition.** `.claude/tests/gates.test.sh`, `+9` lines, no
deletions and no edits to any existing line:

```
 .claude/tests/gates.test.sh | 9 +++++++++
```

The addition sits immediately after `assert_eq "and exits 3" "3" "$rc"` and
immediately before `kill "$LIVE" 2>/dev/null; wait "$LIVE" 2>/dev/null`:

```bash
# The claim the four assertions above only imply: it waited because the owner is
# ALIVE, not because it happened to give up on something it had already broken.
# Stated directly, as the foreign-host case below states it, so that a fixture
# whose owner dies mid-block is caught here rather than read as a pass.
case "$out" in
  *"Taking it over"*) _bad "a live owner's lock is never taken over" "broke it: $out" ;;
  *) _ok "a live owner's lock is never taken over" ;;
esac
```

That is the assertion AC-2 requires the story to name for claim 5 of
`## Context`'s table. `scripts/gates.sh` is untouched (AC-3's default holds
into GREEN). No other suite under `.claude/tests/` is touched (AC-4).

### The command that runs these tests

```bash
bash scripts/selftest.sh gates          # the suite, as CI runs it
bash .claude/tests/gates.test.sh        # the same suite directly; what RED used
```

Both take **5–7 minutes on this machine right now** (four other Claude sessions
are loading it; `## Notes` records the same). Budget for it; do not assume a
hang.

### The observed failure — `late`, 2026-09-22

Reproduced with the untracked probe generator described below:

```
bash .claude/tests/.mc044-probe-gen.sh late     # mode=late  injections=1  lines=522 (source 521)
bash .claude/tests/.mc044-probe.sh
```

```
  one run at a time: two gate runs on one checkout
    FAIL waiting is the default
         expected to contain: Waiting for it to finish
         actual:               note: the gate run lock was left behind by pid 3702, which is gone. Taking it over.
         ... (the taken-over run's full output: PASS unit, "All required gates passed")
    FAIL and it gives up out loud
         expected to contain: gave up waiting for the gate run lock
         actual:               note: the gate run lock was left behind by pid 3702, which is gone. Taking it over.
         ...
    FAIL saying nothing ran
         expected to contain: neither a pass nor a failure
         actual:               note: the gate run lock was left behind by pid 3702, which is gone. Taking it over.
         ...
    FAIL and exits 3
         expected: 3
         actual:   0
    FAIL a live owner's lock is never taken over
         broke it: note: the gate run lock was left behind by pid 3702, which is gone. Taking it over.
         
         === gate: unit (required) ===
         printf 'Tests  47 passed (47)\n'
         Tests  47 passed (47)
         
         --- gate summary ---
         PASS         unit (0s, observed 47)
         
         (not recorded: no active story; use --story <id> to record it in one)
         
         All required gates passed (1 ran, 0 unconfigured, 0 known).
         CI runs one more script that this does not: bash scripts/check-boundaries.sh
         It is not a gate because it judges the COMMIT rather than the code - the phase in
         the committed frontmatter, the criteria against the base branch, and whether this
         very record still matches the tree. Run it after committing, before the PR.

gates: 78 passed, 5 failed

real	4m55.827s
user	0m48.056s
sys	2m6.034s
```

The first four are `## Amendments`'s `late` set verbatim. **The fifth is the new
assertion, and this is the run in which it was watched to fail** — it is written
against code that already exists, so it had to be *seen* red rather than assumed
red. It fails for exactly the right reason: the message names the take-over, not
a missing helper or a shell error.

### Counts — measured vs. predicted

`late` is measured. The other two are **predictions for you to check, not
measurements**; RED ran the suite once, deliberately, because each run costs
five to seven minutes.

| Run | Before RED | With the new assertion | Status |
|---|---|---|---|
| `late` (owner killed after `--list`) | 78 passed, 4 failed | **78 passed, 5 failed** | **measured above** |
| `immediate` (owner killed on the line after it starts) | 70 passed, 12 failed | 70 passed, **13** failed | predicted |
| unmodified, idle machine | 82 passed, 0 failed | **83** passed, 0 failed | predicted |

The arithmetic is 82 assertions + 1 = 83 in every row, and `late`'s measured
`78 + 5 = 83` confirms the total. If `immediate` reports anything other than 13
failures, or an unmodified run anything other than 83/0, stop and say so — that
is a different defect from the one this story describes.

### `gates.sh --fast` at the end of RED — green, and expected to be

`bash scripts/gates.sh --fast` was run to close RED and returned
`All required gates passed (5 ran, 0 unconfigured, 0 known)` in `3m26`
(`format`, `lint`, `typecheck`, `unit`, `coverage`; `integration` and `build`
skipped as `slow`). Exit 0.

That is the **correct** shape here and not a contradiction of RED. Every gate in
`project.conf` is a `cargo` command; none runs `scripts/selftest.sh` or reads
`.claude/tests/**`, which `## Notes` verified at PLANNED. So no gate can be red
for this story's artifact, and the `--fast` run tells you only that RED left the
Rust tree admissible — which it did, because RED touched no Rust and no config.
The red that matters is the probe output above. A `--fast` run is never
recorded and this paragraph is prose, not a gate record.

### The fixture: what you must change, and what you must not

**Change these** — they are the defect. Currently at
`.claude/tests/gates.test.sh` lines 376–377 and 411:

```bash
sleep 30 & LIVE=$!                                              # 376
fake_lock "$LIVE" "$(hostname 2>/dev/null || printf '%s' "${HOSTNAME:-unknown}")"   # 377
...
kill "$LIVE" 2>/dev/null; wait "$LIVE" 2>/dev/null              # was 411, now 420
```

`## Context` names two candidate mechanisms and mandates neither. Candidate 1 —
own the lock with a pid that is alive for the whole block *by construction*,
such as the test runner's own `$$` — is the preferred direction because it
costs `scripts/gates.sh` nothing and therefore keeps AC-3's default. Confirm it
against `acquire_lock()` yourself rather than taking the story's word; a third
mechanism is allowed if it is better and you say why.

**Do not change these.** Every assertion in the block is frozen for you, the
new one included. In particular:

- Do **not** convert the live-owner fixture to `fake_lock 999991 some-other-host`.
  It would make AC-1 green and silently delete claim 5: a foreign-host lock is
  waited for because this host cannot read that process table, not because the
  owner is alive. `a live owner's lock is never taken over` would then pass
  while asserting nothing about liveness. AC-2 exists to refuse this and
  `## Test plan` names the assertion so the deletion is visible.
- Do not weaken, widen, skip or delete any assertion to reach green. If one
  cannot be made deterministic, that is a return to PLANNED.
- `scripts/gates.sh` untouched is the default (AC-3). If you do touch it, every
  change goes in `## Notes` with the reason a test-side fix was insufficient
  and what stops the new input being set in a real run.

### What GREEN must confirm

1. **The new assertion still passes for the right reason after your fix.** It
   passing is necessary but not sufficient: `_ok` also fires when the output is
   empty or the run never happened. Check that the waiting run's `$out` still
   contains `Waiting for it to finish` (claim 3's assertion, right above yours)
   — those two together are what pin "waited *because* alive".
2. **Both probes are vacuous, and say so with a number.** After the fix the
   block starts no process that can be killed, so
   `bash .claude/tests/.mc044-probe-gen.sh immediate` and `... late` should
   print `injections=0`. The generator prints that count on every run — read it.
   A generator that injects nothing produces an unmodified copy, and a green run
   of an unmodified copy is *not* evidence of AC-1 unless you have confirmed
   `injections=0`. If an anchor still matches (`injections=1`), run the probe
   and require `0 failed`.
3. **`bash scripts/selftest.sh` in full, output pasted into `## Notes` with the
   `gates` suite's wall time** — AC-4, and the story's only in-story evidence,
   since no gate in `project.conf` runs `.claude/tests/**`.
4. The three claim-5-adjacent controls below still measure what they claim.

### Negative controls and their expected values

The suite does **not** fail at import — every assertion in the file executed in
the run above — so these are measured, not inferred, except where marked.

| Control | What it must show | Threshold / expected | Measured in RED |
|---|---|---|---|
| `late` probe against the **unfixed** suite | the new assertion *can* go red | `Taking it over` present in the waiting run's `$out`; assertion in the FAIL list | **fired**: `FAIL a live owner's lock is never taken over` / `broke it: … which is gone. Taking it over.`; suite `78 passed, 5 failed` |
| `another host's lock is never broken` (line 445, pre-existing) | the same `case` shape is capable of failing at all | `_bad` on `Taking it over` | passed in RED (`999991` on a foreign host is never taken over) — unchanged by this story |
| `a lock with no live owner is taken over` (claim 4) | a dead same-host owner **is** taken over, i.e. the two branches are genuinely distinguishable | contains `which is gone. Taking it over` | passed in RED — this is the control that proves the new assertion is not trivially true for every lock |

That third row is the one that matters: claim 4 and claim 5 read the *same*
string from the *same* code path and demand opposite outcomes. Both passing on
an unmodified idle run is what makes either mean anything. If a fix ever makes
both pass for the same reason, the pair has stopped working.

### Things discovered in RED that should shape GREEN

- The four claim-3 assertions and the new one all read the **same** `$out` from
  `out="$(GATES_LOCK_WAIT=2 gates)"`. Whatever fixture you choose must still
  leave the lock held at that line, or five assertions move together.
- `## Amendments` already explains why an owner that is dead *from the start*
  falls into the take-over branch before `NOWAIT` is consulted
  (`scripts/gates.sh` ~176: `host = $LOCK_HOST` **and** `! kill -0 "$pid"`).
  That ordering is why `immediate` costs twelve failures and `late` four. A
  fixture whose owner is alive by construction removes the ordering's
  relevance entirely; it does not require changing the ordering.
- The probe generator anchors on two **exact** source lines:
  `sleep 30 & LIVE=$!` and `assert_eq "--list takes no lock"   "0" "$rc"`
  (three spaces before `"0"`). Your fix will almost certainly break the first
  anchor — that is the point, but it means `injections=0` and not a passing
  probe is the evidence. See item 2 above.

### Untracked probe tooling — never commit

Three files, all dotfiles so `selftest.sh`'s `*.test.sh` glob ignores them, all
scratch and none ever committed:

- `.claude/tests/.mc044-probe-gen.sh <none|immediate|late>` — writes the probe.
  Read it before relying on it: it is an `awk` pass that copies
  `gates.test.sh` and inserts one `kill "$LIVE"; wait "$LIVE"` line after an
  exact-match anchor, then prints `injections=N`. RED read it and confirms it
  does what it claims; the `late` anchor is the `--list takes no lock` line, so
  the injected kill lands **before** the waiting run and after the new
  assertion's upstream setup, which is the load condition `## Context`
  describes.
- `.claude/tests/.mc044-probe.sh` — the generated copy. Regenerate, never edit.
- `.claude/tests/.mc044-probe-run.sh <dir>` — runs both modes and greps the
  failure list. Convenient but it does two full suite runs, so ~10–14 minutes.

Delete all three before the PR and check `git status` shows none of them.

## Regressions

<!-- Expected to be unused: AC-1's reproduction is this story's RED and belongs
     in the handoff, not here. Required only if the story returns to RED after
     GREEN or GATES. -->

## Amendments

- **AC-1**, corrected on 2026-09-22 while the story was still in PLANNED, by the
  orchestrator's PLANNED-phase check of whether the criteria are testable.
  Approved by the product owner in the `/complete-story MC-044` session, having
  been shown the measurement below.

  **What it said.** The block runs "with **every background process the block
  starts killed immediately after it is started**"; *Control:* that kill
  "reproduces exactly the four failures quoted in `## Context`"; and "a probe
  that produces some other set of failures is measuring something else and the
  story stops."

  **What it says now.** Two named induction points, each carrying the failure
  set it actually produces — `late` (the quoted four) and `immediate` (twelve).

  **Why.** The criterion was unsatisfiable as written. `acquire_lock()`
  (`scripts/gates.sh` ~176) tests `host = $LOCK_HOST` **and**
  `! kill -0 "$pid"` *before* it consults `NOWAIT`, so an owner that is dead
  from the start is **taken over** rather than waited for. The five `--no-wait`
  assertions and the three `--gate`/`--fast`/`--required` ones therefore fall
  too, and the literal induction can never yield the quoted four. The diagnosis
  in `## Context` is untouched and was not re-opened: the flake is a wall-clock
  race, and the `late` induction reproduces the field report verbatim. Only the
  induction *clause* was wrong.

  **Reproduced before it was accepted,** rather than reasoned about — the
  criteria were still frozen against `main`, and a claim that the contract is
  wrong is the shape of claim that most needs evidence. Probe: a copy of
  `gates.test.sh` with one `kill "$LIVE"; wait "$LIVE"` line injected at the
  chosen point, run directly.

  ```
  ########## mode=immediate ##########
      FAIL a second run says what is holding it
      FAIL and names the owner
      FAIL and refuses rather than proceeding
      FAIL with an exit code that is not a gate verdict
      FAIL and runs nothing
      FAIL --gate is locked too
      FAIL --fast is locked too
      FAIL --required is locked too
      FAIL waiting is the default
      FAIL and it gives up out loud
      FAIL saying nothing ran
      FAIL and exits 3
  gates: 70 passed, 12 failed
  real	4m52.708s

  ########## mode=late ##########
      FAIL waiting is the default
      FAIL and it gives up out loud
      FAIL saying nothing ran
      FAIL and exits 3
  gates: 78 passed, 4 failed
  real	6m40.924s
  ```

  The `late` set is identical to the field report in `## Context`, which is the
  evidence that the diagnosis frozen there is right and only the wording of the
  induction was wrong.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-22T20:39:36Z
    commit: 91cd4d7 (working tree had uncommitted changes)
    tree:   ea1e06e65c8c87dcb9ab553964aa9dfebbb88c54
    result: pass (7 ran, 0 unconfigured, 0 known)

    PASS         format (2s)
    PASS         lint (3s, observed -)
    PASS         typecheck (2s, observed -)
    PASS         unit (21s, observed 47, floor 47)
    PASS         coverage (70s, observed 1717)
    PASS         integration (121s, observed 9, floor 9)
    PASS         build (110s, observed -)

## Notes

### The gate check at PLANNED, re-verified rather than inherited

`advance-story` requires naming the gate that would fail if this story's
artifact broke, and refuses the story if no gate can. Checked against a real
`bash scripts/gates.sh --list` on 2026-09-22 rather than against the story's own
claim: all seven gates (`format`, `lint`, `typecheck`, `unit`, `coverage`,
`integration`, `build`) are `cargo` commands with cwd `.`. None runs
`scripts/selftest.sh` or reads `.claude/tests/**`, so **no gate, required or
optional, exercises this artifact** and there is nothing to escalate into
`required_gates`. That is [MC-039](MC-039.md)'s reasoning, which merged, and not
a new exemption invented here: the verifier is `.github/workflows/gates.yml`
line 37, a blocking `bash scripts/selftest.sh` step with no `continue-on-error`,
ordered *before* `gates.sh --list`, `--audit` and `gates.sh` — and that ordering
is why `selftest` is not itself a gate. `required_gates: []` stands.

Consequence to carry into RED and GREEN: `.claude/tests/gates.test.sh`
classifies as `harness` (`bash -c '. .claude/hooks/lib.sh; classify …'`), which
is writable in **every** phase. The phase lock therefore protects nothing on
this story, and the role boundary has to be held by hand.

### The machine was under real concurrent load during this story

Four other Claude Desktop sessions were running on this machine throughout.
That is the story's own subject matter rather than an aside, and it is why the
outer-shell timings here are worse than the ones `## Context` recorded: a single
`bash scripts/gates.sh --list` took ~120 s, and the `gates` suite took
`4m52`–`7m07` against the idle baseline of `3m18` in `## Context`. Recorded for
the next person, **not** as a threshold — see AC-4.

### GREEN: the fixture, the mechanism, and what was measured

**The change.** One file, `.claude/tests/gates.test.sh`, two hunks in the
`one run at a time` block. No assertion added, removed, weakened, reordered or
edited; no other suite touched; **`scripts/gates.sh` is untouched**, so AC-3's
default holds and there is nothing for AC-3 to justify.

- the live-owner fixture is now `LIVE=$$` with a comment explaining why,
  replacing `sleep 30 & LIVE=$!`;
- the `kill "$LIVE" 2>/dev/null; wait "$LIVE" 2>/dev/null` teardown becomes a
  comment: there is no longer a process to kill.

**Why this mechanism** — `## Context`'s candidate 1, confirmed against
`acquire_lock()` rather than taken on the story's word. The take-over branch
(`scripts/gates.sh` ~172–178) fires only when `[ "$host" = "$LOCK_HOST" ]`
**and** `! kill -0 "$pid"`. `LOCK_HOST` (line 112) is computed with the *same*
`hostname 2>/dev/null || ${HOSTNAME:-unknown}` expression the fixture uses, so
the host arm stays true, which is what keeps claim 5 a statement about
*liveness* and not about an unreadable process table — the trap the handoff
names. The pid arm is now false by construction: `$$` is the suite's own pid,
and `gates()` runs `gates.sh` as a child of it, so the owner cannot die while
there are assertions left to run. Verified in isolation before the suite was
run at all — a grandchild `bash` reports the runner's `$$` as `ALIVE` and a
reaped background pid as `DEAD`, which is the same distinction claim 4's
fixture depends on:

```
parent pid=46965 host=RyanPC
child: pid 46965 is ALIVE       # $$ - the live-owner fixture
child: pid 46986 looks DEAD     # `sleep 0 & D=$!; wait $D` - claim 4's fixture
```

No third mechanism was needed, and candidate 2 (an injected staleness signal in
`acquire_lock()`) was not used: it would have bought AC-1 at the price of an
input to the production lock that exists only for tests.

**Measured, not assumed.** Every figure below is from a run made in GREEN.

| Run | Result | Wall | Note |
|---|---|---|---|
| `immediate` probe, **unfixed** suite | `71 passed, 12 failed` | `8m50.983s` | the twelve failures are AC-1's `immediate` set verbatim; see the divergence below |
| `bash scripts/selftest.sh gates`, **fixed** | `83 passed, 0 failed` | `9m39.737s` | the arithmetic the handoff predicted: 82 + RED's one |
| `.mc044-probe-gen.sh immediate`, fixed | `injections=0` | — | copy is **byte-identical** to `gates.test.sh` (`cmp -s`), so the probe is vacuous rather than merely green |
| `.mc044-probe-gen.sh late`, fixed | `injections=1` | `8m24.368s`, `exit=143` | see below — not a passing run and not a failing one |
| `bash scripts/selftest.sh` in full | 9 suites, `0 failed` everywhere | `31m50.838s` | AC-4; `gates` was `83 passed, 0 failed` a second time |

**Divergence from the handoff's predicted counts, reported because it was
predicted rather than measured.** The handoff's table predicts `immediate` at
`70 passed, 13 failed`; it measures `71 passed, 12 failed`. The total is 83 as
predicted and the twelve **failure labels are exactly** AC-1's `immediate` set,
so AC-1's control is satisfied — AC-1 itself says `70 passed, 12 failed`, i.e.
twelve failures against the pre-RED total of 82, and the handoff's `13` was an
arithmetic guess that the new assertion would also fall. It does not, and for a
reason worth writing down: in `immediate` mode the owner is dead before the
*first* `gates --no-wait`, so **that** run takes the lock over, runs the gate
and releases it on exit. By the time `out="$(GATES_LOCK_WAIT=2 gates)"` runs
there is no lock at all, the run simply succeeds, and its `$out` — the only
thing the new assertion reads — contains no `Taking it over`. The new assertion
therefore passes *vacuously* under `immediate`. It is a real detector only
under `late`, which is the condition `## Context` reports from the field and the
one RED watched it go red in (`78 passed, 5 failed`). Benign for this story; put
here so nobody later reads `immediate` green as evidence that claim 5 holds.

**The `late` probe is vacuous in substance but not in the generator's eyes.**
Its anchor is `assert_eq "--list takes no lock" …`, which this story does not
touch, so it still injects one line — but that line is
`kill "$LIVE"; wait "$LIVE"`, and `$LIVE` is now the suite's own pid. The run
therefore terminates itself at the injection point: `Terminated`, `exit=143`,
**zero `FAIL` lines printed** and no summary. That is the criterion being met in
the strongest form available — the block starts no process that can be killed,
and the only process the late induction can now reach is the test runner, whose
death takes the assertions with it rather than making them observe a stale lock.
It is *not* a `0 failed` run and is not reported as one.

**The four things GREEN was asked to confirm.**

1. *The new assertion passes for the right reason.* In the fixed run the
   assertion directly above it, `waiting is the default`
   (`assert_contains … "Waiting for it to finish" "$out"`), passes in the same
   `0 failed` run. So the waiting run's `$out` is non-empty, the run happened,
   and it waited — `a live owner's lock is never taken over` is reading real
   output of a real wait, not an empty string.
2. *Both probes' injection counts read and reported* — `immediate` `0`,
   `late` `1`, above.
3. *Claim 4 and claim 5 still discriminate.* In the same fixed run,
   `a lock with no live owner is taken over` (dead same-host owner →
   `which is gone. Taking it over`) and `a live owner's lock is never taken
   over` (live same-host owner → no such string) both pass. They read the same
   string from the same code path and demand opposite outcomes, and the
   `kill -0` measurement quoted above shows the two fixtures are genuinely on
   opposite sides of the branch.
4. *All five claims in `## Context`'s table still carried*, by exactly the
   assertions `## Test plan` names — nothing in the block was deleted or
   weakened, and the fifth is still `a live owner's lock is never taken over`
   (AC-2).

### AC-4: `bash scripts/selftest.sh` in full

Blank lines squeezed; nothing else elided. `gates` suite wall time, measured
separately as `bash scripts/selftest.sh gates`: **`real 9m39.737s`** — recorded
for the next person and not as a threshold (AC-4). The machine was still under
the concurrent load the subsection above describes.

```
=== boundaries ===
  a return to RED has to show the red
  the story comes from GITHUB_HEAD_REF where CI sets it
  a PR is opened from REVIEW, as committed
  the same rule covers gate probes
  acceptance criteria are frozen
  a large section is read, not silently treated as empty
  a spike that escalates a gate has to have run it
boundaries: 23 passed, 0 failed
=== doctor ===
  discovery: the runner is asked what it can see
  discovery: nothing declared is reported, not skipped silently
doctor: 4 passed, 0 failed
=== gate-reminder ===
  the hook only watches GREEN and GATES
  GREEN and GATES: unverified code does not get to stop
  generated output is not a code change
  a harness prompt is not code the gates judge
  the hook never loops on itself
gate-reminder: 19 passed, 0 failed
=== gates ===
  evidence: a gate that exits 0 having done nothing
  --gate names a gate that exists
  floor: a gate that started doing much less
  floor: a floor that cannot be evaluated is a manifest error
  floor: an evidence regex that uses alternation
  the count is reported even with no floor
  no-count: an evidence regex that proves liveness and measures nothing
  no-count: a floor on it is refused
  no-count: a line that defends nothing is a manifest error
  required_gates: a story can escalate an optional gate for itself
  --fast: the subset that judges whether tests are admissible
  slow: a line that excludes nothing is a manifest error
  ci-factor: a measurement, or nothing
  one run at a time: two gate runs on one checkout
  the record: an older run does not overwrite a newer one
gates: 83 passed, 0 failed
=== lib ===
  classify: paths.conf rules
  classify: git decides what is generated
  to_rel
  mask_shell_quotes: operators inside quotes stop being operators
  mask_shell_quotes: structure outside quotes is preserved
  mask_shell_quotes: heredocs and escapes
  mask_shell_quotes: a backslash inside double quotes is usually a backslash
  mask_shell_quotes: a comment is data to the end of the line
  json_escape: a backslash is escaped, not dropped
  gate_tree_hash: agrees with the committed tree whatever autocrlf says
  portability: the harness runs on bash 3.2 and BSD tools
  gate_tree_hash: covers what the gates judge, and only that
  path_is_implausible: a failed parse is inconclusive, not a violation
lib: 85 passed, 0 failed
=== manifest ===
  integration is configured as an optional gate with a countable evidence line
  unit: the manifest declares a floor, and says what the floor defends
  integration: the manifest declares a floor, and says what the floor defends
  gates.sh --audit accepts the manifest, floor included
manifest: 25 passed, 0 failed
=== phase-guard ===
  RED: quoted arguments are not shell syntax
  RED: real writes to source are still blocked
  RED: the seven false positives reported from the field
  RED: four more false positives, all of them read-only commands
  RED: the harness's own vocabulary in a commit message
  RED: a double-quoted Windows path keeps its backslashes
  RED: three holes found by probing, closed
  RED: a paren inside a quoted argument is data, not a terminator
  RED: sed -i writes every operand, not just the last one
  RED: a file-descriptor redirect is not an operand
  RED: a parse the guard cannot believe declines rather than denies
  RED: relative paths resolve against the command's own cwd
  GREEN: tests are frozen, source is not
  Generated output is not source
  No active story means no lock
phase-guard: 115 passed, 0 failed
=== phase ===
  frontmatter readers
  new-story.sh writes a story the harness can read
  phase.sh set: the branch has to match
  phase.sh set: dependencies have to be DONE
phase: 24 passed, 0 failed
=== profiles ===
  the profiles are where the suite thinks they are
  godot.md
  node-typescript.md
  python-uv.md
  rust-cargo.md
  web-static.md
profiles: 42 passed, 0 failed
9 harness suite(s) passed.
real	31m50.838s
user	3m0.967s
sys	8m37.178s
```

### Orchestrator mutation check: does this suite actually discriminate?

RED and GREEN both claim the block discriminates. That is a claim until someone
runs a mutation, so the orchestrator ran one against the **restored** production
lock, independently of either subagent's code.

**Mutation.** One line of `acquire_lock()` in `scripts/gates.sh` (~177): the
take-over branch's liveness test dropped, so a same-host lock is taken over
whether or not its owner is alive.

```
-       && ! kill -0 "$pid" 2>/dev/null; then
+       && true; then
```

**Predicted:** 5 failures — claim 3's four, plus `a live owner's lock is never
taken over`.

**Measured:** 14 failures. `gates: 69 passed, 14 failed`, `real 7m5.690s`.

```
    FAIL a second run says what is holding it        FAIL waiting is the default
    FAIL and names the owner                         FAIL and it gives up out loud
    FAIL and refuses rather than proceeding          FAIL saying nothing ran
    FAIL with an exit code that is not a gate verdict FAIL and exits 3
    FAIL and runs nothing                            FAIL the second of two real runs is refused
    FAIL --gate is locked too                        FAIL naming the first
    FAIL --fast is locked too
    FAIL --required is locked too
```

Restored with `git checkout -- scripts/gates.sh` (`git diff --stat -- scripts/`
empty, byte-for-byte) and re-run: **`gates: 83 passed, 0 failed`,
`real 11m11.772s`**.

**The prediction was wrong, and the way it was wrong is the finding.**
`a live owner's lock is never taken over` did **not** fail. With liveness
ignored, the *first* `gates --no-wait` takes the live owner's lock over and
releases it on exit, so by the time the `GATES_LOCK_WAIT=2` waiting run happens
there is no lock at all: its `$out` contains no `Taking it over` and the
assertion passes **vacuously**. This is the same blind spot GREEN found from the
other direction under the `immediate` probe, confirmed here by a production
mutation rather than a fixture one.

**Why this is recorded rather than sent back to RED.** The assertion is not
vacuous in the sense that matters — RED *watched it fail* under the `late`
condition it was written for, which is the non-negotiable. What the mutation
shows is that it is a **narrow** detector, blind to any break that consumes the
lock before the waiting run. Claim 5 does not rest on it alone: the eight
`--no-wait` / `--gate` / `--fast` / `--required` assertions failed precisely
*because* a live owner's lock had been taken over, and they are claim 5's real
safety net. Nothing was weakened, widened, skipped or deleted, so AC-2 holds.

**For the next person.** If you want a single assertion that catches claim 5
whenever it breaks rather than only at the waiting run, it has to observe the
lock at the point of each invocation rather than reading the last `$out` — that
is a change to assertions, so it is a new story and not an edit here. Both
blind-spot observations are now on record; do not read a green `immediate`
probe, or a green `a live owner's lock is never taken over` under a mutation
that steals the lock early, as evidence for claim 5.
