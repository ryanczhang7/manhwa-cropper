---
id: MC-052
title: The viewport stage reads only the reader's own window
slug: the-viewport-stage-reads-only-the-reader
epic: EPIC-07
type: fix
status: todo
phase: PLANNED
branch: story/MC-052-the-viewport-stage-reads-only-the-reader
depends_on: []      # nothing open: MC-048 (the viewport stage) is DONE. MC-053 depends on THIS story (serial manifest and pin edits)
required_gates: [integration]  # AC-1 to AC-5 are corpus claims, and only the optional integration gate runs the corpus (MC-038's reasoning). AC-6 is in the required unit gate
---

## Context

**The bug.** MC-048's viewport stage cuts the reader's artwork on
split-screen screenshots. It reads the browser chrome and the taskbar of a
*second* window beside the reader as if they were the reader's own. The crop
then clips the art at the top, the bottom, or both. **The user's decision,
2026-09-24: "Fix story, top priority".** It runs before MC-053.

**How it was found.** MC-048 shipped without its held-out check.
(MC-048 `## Amendments`, "Open question 2 — the held-out run is not made".)
The first aggregate held-out run after it was merged, by the orchestrator on
2026-09-24, on `main` at `26eddcb` (release build, headless), read:

| Build | Marked held-out | Cropped | Not cropped | Clips | Row sides |
|---|---|---|---|---|---|
| `0f94c75` (before MC-048) | 24 | 23 | 1 | 4 | none |
| `26eddcb` (MC-048) | 24 | 21 | 3 | 7 | `bottom:103 bottom:31 bottom:2 top:3` |

The column sides (`left:3 left:6 right:2 right:5 right:5`) are the same on
both builds. They are MC-053's and are out of scope here. With the user's
approval, the orchestrator then looked at the row-clipped edges:

- **`2025-03-06 01_22_45.png`**: top cut by 3 rows and bottom by 103.
- **`2025-03-07 00_58_06.png`**: bottom cut by 31 rows.
- **`2025-09-29 14_33_15.png`**: bottom 2 rows. This is a **mark error**, not
  a clip. The user ruled on a labelled render: rows 1400–1401 are taskbar, and
  the art ends at row 1399. Whole-screen mean brightness per row is ≈ 57 over
  1396–1400 and ≈ 62.4 from 1401.

The first two are split-screen screenshots. The reader window is on the left,
from about x 0 to 1828. A second browser window (YouTube) is on the right. It
has its own bookmark bar, which ends lower, and its own bottom edge, which is
higher.

**The mechanism, measured by the Lead PO on 2026-09-24 against `26eddcb`.** It
confirms the orchestrator's hypothesis. `viewport::locate`
(`crates/core/src/viewport.rs`) scores each row by the share of **every pixel
outside the page column, across the full image width**, that is page
background. A row is page-like at 0.90 or above. On these two files:

| Rows (`01_22_45`) | Left of column | Right of column, inside the reader | Second window (x ≥ 1828) | Full width, as `locate` reads it |
|---|---|---|---|---|
| 104–114 (reader chrome) | 0.000 | 0.000 | 0.000 | 0.000 |
| 115–120 | 0.988 | 0.974 | **0.000** (its bookmark bar) | **0.627** |
| 121–1258 | 0.988–0.998 | 0.974 | 0.964–0.975 | 0.975–0.982 |
| 1259–1373 | 0.988 | 0.963–0.974 | **0.000** (its bottom panel) | **0.623–0.627** |
| 1374–1399 (below the reader window) | 0.000 | 0.000 | 0.000 | 0.000 |
| 1400– (taskbar) | 0.000 | 0.000 | 0.000 | 0.000 |

`00_58_06` reads the same. The reader window is page-like over 115–1398, and
the second window over 121–1258. The full-width share on the rows only the
reader covers is 0.634–0.637. So `locate` returns `Viewport { top: 121,
bottom: 1259 }` on **both** files. That is the second window's viewport, and
the crop's rows are clamped to it. The reader's own viewport is rows
**115..1374** on `01_22_45` and **115..1399** on `00_58_06`. (These are the
probe's numbers, from its own tone estimate. `## Model guidance` says what RED
does with them.)

**What the user decided on 2026-09-24:**

1. The two real-clip entries (`2025-03-06 01_22_45.png` and
   `2025-03-07 00_58_06.png`) **move from `held-out` to `tuning`**. They
   reproduce the bug, and the fix is designed while looking at them. That
   leaves 22 marked held-out entries.
2. **`2025-09-29 14_33_15.png`'s mark is corrected**: `h` 1231 → 1229, so the
   rect is `984,171 576x1229` (rows 171..1399). It stays `held-out`.
3. **Zero clips is required on both sets.** On held-out, the orchestrator
   checks it at GATES in one aggregate run, counts only (AC-7). MC-048 skipped
   that run and shipped this bug. **This story must not skip it.**

**The required gates that fail if this breaks.** `unit` fails through AC-6,
the synthetic two-window screenshot. `integration` fails through AC-1 to AC-5,
the corpus. That is why `integration` is in `required_gates`.

## Acceptance criteria

**Terms used below.**

- "The two" means `2025-03-06 01_22_45.png` and `2025-03-07 00_58_06.png`,
  after this story moves them to `tuning`.
- "The 21 originals" means the 21 marked `tuning` entries as they stand on
  `26eddcb`.
- "Both margins" means `process_file` run at `Tuning::default()` and at
  `Tuning { margin_px: 0, ..Tuning::default() }`.
- "The crop's rows" means `y .. y + h - 1` of the rect `process_file` returns.

- **AC-1: the bug, reproduced.** Given the two, at both margins, when
  `process_file` runs, then each crop contains its mark on the top and on the
  bottom.
  - *On `26eddcb` this fails on 3 sides of 2 entries, at both margins.* The
    measured cuts:
    - `01_22_45` top: 3 rows (118–120; the crop starts at 121).
    - `01_22_45` bottom: 103 rows (1259–1361; the crop's last row is 1258).
    - `00_58_06` bottom: 31 rows (1259–1289).
  - Once the two are `tuning`, the existing tests
    `corpus.rs::no_crop_clips_a_marked_page` and
    `corpus_accuracy.rs::no_corpus_crop_cuts_into_the_artwork_its_manifest_entry_marked`
    already fail on exactly these two and on nothing else (measured,
    `## Notes`). They become the permanent regression tests. RED adds the
    margin-0 half.
  - *Control:* the existing inset-10 containment controls stay as they are.
- **AC-2: the reader's own chrome still goes.** Given the two, at both
  margins, when `process_file` runs, then every crop row lies inside the
  reader window's own viewport: rows **115..1374** on `01_22_45` and
  **115..1399** on `00_58_06`, subject to RED's re-measurement.
  - *On `26eddcb` this holds* (rows 121..1258). This AC is not the bug. It is
    the guard against the cheapest wrong fix.
  - *Control, the fix this AC exists to rule out:* a stage that **declines** on
    a split-screen shot leaves the page column's rows, which are 0..1439 on
    both files (page column `643,0 533x1440` and `670,0 480x1440`). That fails
    this AC by 115 rows at the top (the reader's tab strip and bookmark bar)
    and by 66 or 41 rows at the bottom (the strip below the reader window and
    the taskbar).
- **AC-3: the 21 originals do not move.** Given the 21 originals, at both
  margins, when `process_file` runs, then each crop's `x`, `y`, `w` and `h`
  are exactly what `26eddcb` produces at that margin. RED pins these per file
  (`## Notes` has the probe's values).
  - MC-048's `VIEWPORT` predicate (AC-2 there) and its `COLUMNS_BEFORE` pin
    still pass on these 21, unchanged.
  - The two `2025-08-05` WebPs still decline, with crops identical to MC-048's
    `WEBPS_BEFORE`.
  - *Control:* the settled readout
    (`corpus_viewport_stage.rs`, §4 to the row on at least 18 of 19) still
    passes. On `26eddcb` it reads **19 of 19**. A fix that changes what the
    stage reads on a single-window screenshot moves this.
- **AC-4: zero clips on the tuning set.** Given all 23 marked `tuning` entries
  at both margins, when `process_file` runs, then every `Cropped` rect
  contains its mark: **0 clips**.
  - At margin 0, the 21 originals' clips on `26eddcb` are MC-049's seven mark
    errors on the **column** axis. Those are not this story's; AC-3 covers
    them. So at margin 0, this AC counts **top and bottom** sides only.
- **AC-5: nothing else moves.** All of these hold over the enlarged tuning
  set:
  - The six MC-026 AC-4 pins stay `Flagged(Detector(NoBorderFound))`.
  - `2025-02-27 22_46_15.png` is reported, not pinned. On `26eddcb` it is
    `72,3 225x710`.
  - MC-019's column window keeps its floor of 19 hits.
  - `at_least_nine_corpus_screenshots_in_ten_are_right_on_the_column_axis`
    passes over the 30 tuning entries. On `26eddcb` with the data change alone
    it reads **26 of 30 (86.7%)**. Its misses are `22_46_15`, `(93)` and the
    two, and the two miss only because they clip. Their columns sit inside the
    11 px band: `01_22_45` +8/+11, `00_58_06` +4/+8. So a correct fix reads
    **28 of 30**, and 27 is the least that passes.
  - The columns of the two do not move: `x`, `w` are `640, 539` and
    `667, 486` at margin 3, and `643, 533` and `670, 480` at margin 0.
    MC-053 and MC-049 own the column axis.
- **AC-6: the synthetic two-window case (required `unit` gate).**
  - *Case A:* the reader's window is beside a second window. The second window
    has a taller chrome strip and a shorter viewport than the reader's, and
    takes enough of the width outside the page column that the full-width
    share on the reader-only rows falls below 0.90 by construction. When
    `detect` runs, then the crop contains the page art and no row of the
    reader's chrome or taskbar.
  - *Case B:* the second window's page-background rows extend **beyond** the
    reader window's, above and below. When `detect` runs, then no crop row
    lies outside the reader window's viewport. This rules out a fix that
    treats a row as page-like when any window beside the column is flat.
  - Each case is also run mirrored (the reader on the right), and each runs at
    both margins.
  - *Control:* `26eddcb` fails case A and passes case B. MC-048's existing
    single-window scenes (`viewport_detect.rs` and `viewport.rs`) pass
    unchanged. Both results are recorded.
- **AC-7: zero row clips on held-out, one aggregate run, not skipped.** Given
  the 22 marked held-out entries, with `14_33_15`'s mark corrected, when the
  orchestrator runs the frozen implementation once at GATES, at
  `Tuning::default()`, then there are **0 top or bottom clips**.
  - It prints counts only, with no per-file line: entries, cropped, not
    cropped, clips, unattributed side:depth pairs, and the not-cropped
    reasons as a tally.
  - **Baseline on `26eddcb` with this story's data edits:** 22 entries,
    19 cropped, 3 not cropped (1 `Ambiguous`, 2 `LowContent`), and 4 clips,
    all on the column axis: `left:3 left:6 right:2 right:5 right:5`. The
    column sides must be **exactly** these after the fix. They are MC-053's,
    and the fix may not move them.
  - The not-cropped count is governed by Open question 1.
  - The run is recorded in `## Notes` with the command and its output.
  - **REVIEW is conditional on it.** `check-boundaries.sh` cannot see this
    run. The orchestrator does not move this story to REVIEW until the run is
    in `## Notes` and shows 0 row clips. A row clip is a stop-and-ask. It is
    never a re-tune, because the per-file look it would need contaminates the
    entry (`corpus.md`).
  - The run is not added to the `integration` gate: `corpus.md` says the gate
    never spends held-out.
- **AC-8: the record.**
  - `corpus.md` records the move of the two. It gives the reason (they
    reproduce this bug, and the fix is designed on them), the user's ruling of
    2026-09-24, and the new counts: tuning 30 : held-out 29, marked 23 : 22.
    It updates "`toongod` is 11 of the 21 marked tuning entries" to 13 of 23.
    It notes that both are `toongod`, a reader already in the tuning set, so
    the unseen-reader count does not change.
  - `corpus.md` records `14_33_15`'s correction. It gives the old and new
    rect, the user's ruling on the labelled render, and the brightness
    evidence above, in the MC-027 precedent's form.
  - `architecture.md` pipeline step 4 ("Browser viewport") and decision 14's
    MC-048 paragraph say which pixels the stage now reads. The entry is dated
    and cites this story.
  - This story's `## Notes` carries the before and after rects of the two, at
    both margins.

## Model guidance

Model: default. The recorded verdict (`story-authoring`, "Brief RED by
oracle") points at the brief, not the model.

**Oracle partition.**

- **Settled: read out, never re-derived.**
  - The user's rulings: the move of the two, `14_33_15` → `h` 1229, and zero
    clips on both sets with the held-out run at GATES.
  - MC-048's settled instrument: `PAGE_LIKE` 0.90, `MIN_RUN` 16, the tone
    estimate (the modal per-row margin median in 8-level bins), and
    `uniform_tolerance` 10. **This story changes which pixels the stage reads.
    It does not change these constants.** A fix that only works by moving one
    of them is not this story: stop and ask. The full-width share on the
    reader-only rows is 0.62–0.64, so lowering `PAGE_LIKE` to about 0.6 would
    pass the two corpus files. That is exactly the fit this rule exists to
    forbid.
  - §4's 19 viewports, the six flag pins, MC-019's window and floors, and the
    held-out baseline in AC-7.
- **Evidence, not settled: verify, do not assume.** The Lead PO's probe
  (`## Notes`) used its own tone estimate: the median of the left margin over
  rows 400..1000. It also used a second-window edge at x 1828, read by eye.
  RED re-measures AC-2's reader-window rows (115..1374 and 115..1399) by an
  independent means before pinning them. For example, look at renders of the
  rows around each boundary; the two are `tuning` now and may be looked at. If
  a boundary differs by more than a row, RED records it and uses its own
  value. The mechanism above may be read. It is **not** a design.
- **Oracle-free: invent, with a control that fires.** AC-6's fixture: what
  "a second window" is on a generated image. Case A must fail on `26eddcb`.
  Case B must fail against a stage that accepts a row when either side is
  flat. RED shows that second control with a stand-in, as MC-048's RED did
  with its stand-in oracle.
- **Mechanical: pin exactly.** AC-1's cut depths, AC-3's 21 × 2 rects,
  AC-5's column pins of the two, and AC-7's aggregate counts.

**Pinned constants the data move breaks, which RED changes.** Measured on
`26eddcb` with the manifest edit applied and nothing else (`## Notes`, "What
the data move alone does"). Each of these fails, and none of them is about the
bug:

| File | Constant / test | Change in RED |
|---|---|---|
| `corpus_manifest.rs` | `READER_BY_FILE` (28) | +2 rows, both `toongod` (their existing `site:` tag) |
| `corpus_manifest.rs` | `READER_LABELS` | `toongod` `(15, 11, 4, 10)` → `(17, 13, 4, 8)` |
| `corpus_manifest.rs` | `MARKED_TUNING_RECTS` (21) | +2 rects, their current marks: `648,118 520x1244` and `671,362 474x928` |
| `corpus_viewport.rs` | `VIEWPORT` (19): the names must equal the marked tuning set minus the WebPs | the two go in a **separate** constant with their reader-window rows (AC-2). They were measured by this story, not read out of §4, and the constant says so |
| `corpus_viewport.rs` | `every_marked_crop_keeps_the_columns...`: `seen == COLUMNS_BEFORE` | scope it to its 21 names. The two's columns are AC-5's |
| `corpus_viewport_stage.rs` | `SECTION_4` (19): panics on a marked tuning entry in neither table | scope the panic, or add a separate table. **Never merge the two into `SECTION_4`**, which claims to be §4 |

These are the same six MC-053 lists. **MC-053's own move changes them again**
after this story is DONE, and MC-053 re-measures on post-MC-052 `main`.

Unaffected, and checked: `PRE_EPIC_07_ENTRIES`, `MIN_HELD_OUT_MARKED` 20
(22 remain), `MIN_HELD_OUT_FLAGS`, `MIN_HELD_OUT_SITES` (`toongod` keeps 8
held-out entries), and `MIN_UNSEEN_SITES` (`toongod` is a pre-EPIC-07
reader). The failures in `corpus.rs` and `corpus_accuracy.rs` are AC-1's
reproduction and AC-5's count, not pins.

**Success condition, which can come out either way.** GREEN finds a rule that
meets AC-1 to AC-6 together, at both margins, with the settled constants
unchanged and the §4 readout still 19 of 19. If it cannot, it **stops**. It
does not tune a constant until the two pass. Record the verdict in `## Notes`.

## Out of scope

- **The column axis**: MC-053's dark-art clips and MC-049's margin and mark
  corrections. The held-out column sides in AC-7 stay exactly as measured.
- **Reader furniture** (the site's own header, navigation and footer):
  [MC-050](MC-050.md).
- The two WebPs' chrome (MC-048's named limitation).
- The 2 held-out entries newly `LowContent`, beyond what Open question 1
  decides.
- Any per-file look at the held-out set. Any mark beyond `14_33_15`'s.
- Changing `PAGE_LIKE`, `MIN_RUN`, the tone estimate or `uniform_tolerance`.

## Open questions: answer before RED

**Answered 2026-09-24 by the user, before RED.** Q1: watch, don't look. MC-052 is scoped to the row clips; its GATES held-out check reports the not-cropped count; if the two `LowContent` entries are not cropped again after the fix, a follow-up fix is filed and the user is asked then whether to look at them. Q2: the floor drops to 19 in MC-053 (see MC-053 Q6).

1. **Two held-out marked entries are newly flagged `LowContent` after
   MC-048.** The held-out "not cropped" count went from 1 to 3 between
   `0f94c75` and `26eddcb`. It is 3 with this story's data edits as well. The
   aggregate reason tally reads `1 Ambiguous` before, and `1 Ambiguous;
   2 LowContent` after.
   - Both were **cropped without a clip** on `0f94c75`. The four clips there
     are the four column-axis entries, which are still cropped (and clipped)
     on `26eddcb`, so neither `LowContent` entry is among them.
   - `LowContent` means the crop rect fell under
     `min_content_fraction` (0.05 of the image) or `min_content_side` (64 px)
     (`decide.rs`). The viewport stage is the only row-axis change between the
     two builds, so its cut is what made the rect too small.
   - For these two pages, the user now gets the file copied unchanged, browser
     chrome and all, where v1 gave a crop that contained the art. **That is a
     regression, not an intended flag.** It is not a clip, and flag-and-copy is
     the safe outcome, but a marked page is by definition croppable.
   - Why the stage cut so much is unknown. The same mechanism (a second
     window's chrome) is plausible and unconfirmed. Confirming it needs a
     per-file look, which contaminates both entries.
   - *Recommended:* keep MC-052 to the row clips, as written. AC-7 **reports**
     the not-cropped tally. If the fix brings it back to `1 Ambiguous`, record
     that the two were the same bug. If it does not, file a separate `fix`
     (EPIC-07, after MC-052): "a marked page is not flagged `LowContent` by
     the viewport stage". Its first question for you is whether to look at the
     two, which moves them to `tuning` and leaves 20 marked held-out entries,
     exactly `MIN_HELD_OUT_MARKED`, before MC-053's move.
   - *Alternative:* include it now. AC-7 then requires `1 Ambiguous` and
     nothing else not cropped, and the story designs against it. Without a
     look this can only be checked at GATES, so a miss there is a
     stop-and-ask with the same contamination question.
2. **`MIN_HELD_OUT_MARKED` breaks at MC-053, not here.** After this story,
   22 marked held-out entries remain. MC-053 moves three more, which leaves
   **19**, under the floor of 20.
   `the_held_out_set_carries_at_least_twenty_marked_entries` then fails in the
   required `unit` gate. This is MC-053's question (its Open question 6),
   recorded here because the two moves together cause it.

## Test plan

<!-- Filled by the Test Developer during RED: which tests, at which level,
     and which AC each one covers. -->

## Handoff: RED -> GREEN

<!-- Filled by the Test Developer at the end of RED. This is the ONLY channel
     to the Feature Developer, whose context is fresh. Must contain:
       * the exact command that runs the new tests
       * the failure output, and why it is the RIGHT failure
       * every file touched, and which AC each test covers
       * the EXPORT SHAPE the tests already pin: every module they import, the
         exact exported names and signatures, and the types the assertions
         destructure. Not a suggestion - a test already imports them, so a
         wrong guess is a compile error. Say what the tests do NOT constrain
         too, so it stays the implementer's choice.
       * any test that passed on arrival, and the probe or negative control
         that earns it
       * the EXPECTED VALUE of every negative control, as a table: threshold,
         candidate range, and the number the control measured. In RED the
         suite fails at import, so no assertion in it has run - the controls
         are claims until GREEN confirms them against the shipped module
       * anything discovered that changes the approach -->

## Regressions

<!-- REQUIRED if this story ever returned to RED after GREEN or GATES; omit
     otherwise. A test that is wrong is never edited into passing, and the
     return is not a footnote - it is the story failing to be one clean cycle,
     and the next person needs to know why. One block per return:
       * which test, what it asserted, and what was wrong with it
       * how the defect was found
       * what it asserts now
       * what earns it, since "watched it fail" cannot apply once the
         implementation exists - the corrected assertion passes on its first
         run and every run after, whether or not it asserts anything: either a
         PROBE (mutate the specific behaviour the test pins, paste the red,
         confirm the revert) or, where the defect was cost rather than
         correctness, a BEFORE/AFTER measurement taken under the gate command -
         not the plain test command, which is the faster one.
         PASTE THE OUTPUT. check-boundaries.sh refuses a PR whose Regressions
         or Gate probes section describes a failure without showing one
       * whether GREEN was a no-op, and the command output proving the source
         was untouched and still passes -->

## Gate results

<!-- Written by scripts/gates.sh itself on every full run, stamped with the
     commit and a hash of the code it ran against. Do not paste or edit it:
     check-boundaries.sh refuses a PR whose recorded run does not match the
     code being merged. -->

## Notes

### The probe (Lead PO, PLANNED, 2026-09-24)

**Setup.** Throwaway code, none of it committed.

- `git archive` of `0f94c75` and `26eddcb` into the ignored `target/m52pre`
  and `target/m52main`, each built release into its own target dir.
- `target/m52data` is `26eddcb` with this story's manifest edits applied and
  nothing else: the two moved to `tuning`, and `14_33_15` `h` 1229.
- A scratch test in `target/m52data/crates/engine/tests/m52probe.rs` printed:
  - per-file rects, the page column and `locate`'s viewport for the **tuning**
    entries only (the two are `tuning` in that tree);
  - per-row shares on the two only.
- Held-out: `m52_heldout.sh` in the session scratchpad, adapted from the
  orchestrator's. Aggregate only: counts, unattributed side:depth pairs, and a
  not-cropped reason tally. No per-file held-out line was printed or read.

**1. The two, on `26eddcb`.** Gap is the mark edge minus the crop edge;
negative means clipped.

| Entry | Mark | Page column | `locate` | Margin 3 crop | Gaps L / T / R / B | Margin 0 crop |
|---|---|---|---|---|---|---|
| `2025-03-06 01_22_45.png` | `648,118 520x1244` | `643,0 533x1440` | `121..1259` | `640,121 539x1138` | 8 / **−3** / 11 / **−103** | `643,121 533x1138` |
| `2025-03-07 00_58_06.png` | `671,362 474x928` | `670,0 480x1440` | `121..1259` | `667,121 486x1138` | 4 / 241 / 8 / **−31** | `670,121 480x1138` |

The orchestrator's figures reproduce exactly.

On `01_22_45`, rows 115–119 have the same row mean as 118–119 (114.8), and
the image shows the art's top at about row 115. The mark's top at 118 may be
up to 3 rows tight. This is not acted on: containment still requires the crop
to start at or above 118, and a crop from 115 contains it.

**2. Per-row page-background share on the two.** The tone is the median of
the left margin over rows 400..1000, and the tolerance is 10. The table in
`## Context` is `01_22_45`. `00_58_06` has the same structure:

- reader chrome through row 114, share 0.000;
- reader-only rows 115–120 and 1259–1398: left 0.988, right-inside-reader
  0.975, second window 0.000, full width **0.634–0.637**;
- both windows 121–1258: full width 0.984;
- row 1399 is a transition (row mean 139.9), and 1400 onward is taskbar.

**3. The 21 originals and the WebPs on `26eddcb`, both margins** (AC-3's pin
source; RED re-measures). `locate` gives §4's rows on all 19 and `None` on both
WebPs.

```
file                        margin 3               margin 0
2025-08-05 00_11_13.webp    950,15 646x1425        953,18 640x1422
2025-08-05 00_11_27.webp    1003,15 539x1425       1006,18 533x1422
2025-10-14 23_29_06.png     1000,167 546x1233      1003,167 540x1233
2025-10-14 23_30_20.png     1030,167 486x1233      1033,167 480x1233
2025-10-20 15_37_25.png     1004,167 538x1233      1007,167 532x1233
2026-01-05 13_33_41.png     1071,167 404x1233      1074,167 398x1233
2026-01-05 13_45_59.png     1036,167 473x1233      1039,167 467x1233
2026-01-05 13_49_39.png     1036,167 473x1233      1039,167 467x1233
Screenshot (67).png         1007,167 531x1225      1010,167 525x1225
Screenshot (70).jpg         1007,167 531x1225      1010,167 525x1225
Screenshot (75).png         1070,167 406x1225      1073,167 400x1225
Screenshot (93).jpg         1136,167 273x1225      1139,167 267x1225
Screenshot (103).jpg        1070,167 406x1225      1073,167 400x1225
Screenshot (1661).png       1070,133 406x1259      1073,133 400x1259
Screenshot (2582).jpg       1003,133 539x1259      1006,133 533x1259
Screenshot (2630).jpg       952,133 642x1259       955,133 636x1259
Screenshot (2698).jpg       950,133 645x1259       953,133 639x1259
Screenshot (2708).jpg       1071,133 405x1259      1074,133 399x1259
Screenshot (2744).jpg       945,133 654x1259       948,133 648x1259
Screenshot (3187).png       981,137 582x1255       984,137 576x1255
Screenshot (3538).png       972,137 602x1255       975,137 596x1255
```

**4. What the data move alone does on `26eddcb`** (`target/m52data`,
`cargo test --release --no-fail-fast -p cropper-engine --test corpus --test
corpus_accuracy --test corpus_viewport --test corpus_viewport_stage --test
corpus_manifest -- --include-ignored`):

- `corpus`: 8 passed, 1 failed. `no_crop_clips_a_marked_page` reads "2 of 23
  cropped entries clip", on exactly the two.
- `corpus_accuracy`: 3 passed, 2 failed.
  - `no_corpus_crop_cuts_into_the_artwork...` fails on the two.
  - `at_least_nine ... in ten` reads **26 of 30 (86.7%)**. Its misses are
    `22_46_15` (flag expected), `(93)` (columns, right by 9) and the two
    (clipped).
- `corpus_manifest`: 23 passed, 3 failed: `READER_BY_FILE`, `READER_LABELS`
  and `MARKED_TUNING_RECTS`.
- `corpus_viewport`: 1 passed, 3 failed. `VIEWPORT` names fail twice, and
  `COLUMNS_BEFORE` names once.
- `corpus_viewport_stage`: 0 passed, 1 failed. "`2025-03-06 01_22_45.png` is a
  marked tuning entry in neither SECTION_4 nor DECLINES".

**5. Held-out aggregate** (`m52_heldout.sh`, `Tuning::default()`):

| Build | Manifest | Entries | Cropped | Not cropped (reasons) | Clips | Sides |
|---|---|---|---|---|---|---|
| `0f94c75` | as on `main` | 24 | 23 | 1 (`Ambiguous`) | 4 | left:3 left:6 right:2 right:5 right:5 |
| `26eddcb` | as on `main` | 24 | 21 | 3 (`Ambiguous`, 2 `LowContent`) | 7 | bottom:103 bottom:2 bottom:31 left:3 left:6 right:2 right:5 right:5 top:3 |
| `26eddcb` | this story's edits | 22 | 19 | 3 (`Ambiguous`, 2 `LowContent`) | 4 | left:3 left:6 right:2 right:5 right:5 |

- The last row is AC-7's baseline. The `14_33_15` correction removes
  `bottom:2`, and the move removes the other three row sides.
- The held-out run therefore **cannot show the fix works**; the two in
  `tuning` do that. It guards against new row clips on held-out.

The scratch trees `target/m52pre`, `target/m52main`, `target/m52data` and
their target dirs are ignored scratch. They can be deleted.
