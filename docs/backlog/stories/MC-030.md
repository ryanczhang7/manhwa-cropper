---
id: MC-030
title: The status slot and the flagged row place their text where the design says
slug: the-status-slot-and-the-flagged-row-plac
epic: EPIC-06
type: feature
status: todo
phase: PLANNED
branch: story/MC-030-the-status-slot-and-the-flagged-row-plac
depends_on: [MC-016, MC-021]
required_gates: [unit]
---

## Context

`docs/wiki/audits/app-window-2026-09-13.md`, evidence **E-3**, lists eight
surviving mutants that move something rather than recolour it — the arithmetic
that centres the status slot's contents and the arithmetic that gives a flagged
row its width:

```
crates/app/src/gui.rs:481:45: replace + with - in status_slot   (SIZE_PROGRESS + SPACE_TIGHT + line height)
crates/app/src/gui.rs:481:45: replace + with *
crates/app/src/gui.rs:481:59: replace + with -
crates/app/src/gui.rs:481:59: replace + with *
crates/app/src/gui.rs:482:44: replace - with + in status_slot   ((SIZE_STATUS - content) / 2.0)
crates/app/src/gui.rs:482:55: replace / with %
crates/app/src/gui.rs:505:32: replace - with + in centred_line  ((SIZE_STATUS - content) / 2.0)
crates/app/src/gui.rs:505:43: replace / with %
crates/app/src/gui.rs:567:43: replace - with + in flagged_row   (available_width - tail width)
crates/app/src/gui.rs:567:43: replace - with /
```

This story was split out of MC-021 on 2026-09-15, when that story was re-planned
around the frame's paint list instead of a renderer. Read **MC-021's `## Context`
first**: it establishes the instrument, and this story inherits it.

**Two things to settle before sizing, both cheap and both changing the shape of
the work:**

1. **MC-022 may already have killed the `flagged_row` pair.** `567:43` (today
   `gui.rs:562`) is the budget handed to `truncate_right`, and MC-022's
   `much_narrower` check compares a truncated row's painted width against the
   same row painted where it all fits. A budget mutated upward paints a wider
   row. Apply the mutation to the shipped painter, run
   `cargo test -p manhwa-cropper --test truncation`, and report what happens. If
   it is already red, say so and drop AC-3 before RED begins rather than writing
   a second test for a survivor that no longer exists.
2. **This is the lowest-value cluster in the audit, and the audit says so.**
   E-3's own words: `269` and `428` change colours wholesale, whereas these
   "shift things by a few points", and "a story that treats them as one priority
   will over-invest". It is filed because EPIC-06's goal names offsets
   explicitly. If RED finds the honest test for one of these criteria is
   expensive or brittle, the right answer is to say so and hand that criterion
   back — not to buy it at any price.

**Line numbers drift; identify every mutation by what it does.** At this story's
base commit `status_slot`'s arithmetic is still `gui.rs:481`–`482`,
`centred_line`'s is `gui.rs:505`, and `flagged_row`'s budget has moved to
`gui.rs:562`. Confirm at your own base commit.

The required gate that would fail if this story's artifact broke is **`unit`**
(`cargo test --workspace`).

## Acceptance criteria

- **AC-1** — Given a finished run, when the status slot is painted, then the
  result line is vertically centred in the 40 pt slot: the space above it and
  the space below it are equal, within a tolerance no larger than one point
  (`layout.md`, the status slot; `centred_line`).
  *Measure both gaps in the same frame from the painted line's own height, not
  against a constant: a font update moves the line's height and must not move
  this criterion. Control: the same assertion must fail for a line painted flush
  to the top of the slot, which is what `(SIZE_STATUS - content) / 2.0` becoming
  `* 2.0` or `% 2.0` produces.*

- **AC-2** — Given a run under way, when the status slot is painted, then the
  progress bar and the count beneath it are centred **as one block** — equal
  space above the bar and below the count — and separated by `space-tight`
  (`tokens.md`), within the same tolerance.
  *This is the sum at `gui.rs:481`: a mutated sum mis-measures the block, so the
  block is centred as though it were a different height. Control: swap the two
  gaps and the assertion must fail.*

- **AC-3** — Given a flagged row whose file name is too long for the list, when
  the row is painted, then the whole painted row — name, separator and reason —
  is no wider than the width the list gives it.
  *Drop this criterion before RED if the check in `## Context` shows MC-022's
  suite already reddens on the same mutation; record the result either way.*

- **AC-4** — Given the shipped painter mutated so that `centred_line`'s
  `(SIZE_STATUS - content) / 2.0` becomes `(SIZE_STATUS + content) / 2.0`
  (today `gui.rs:505`), when the new tests run, then at least one fails. And
  given `status_slot`'s `SIZE_PROGRESS + SPACE_TIGHT + …` mutated so the first
  `+` becomes `-` (today `gui.rs:481`), when they run, then at least one fails.
  *Two named survivors as this story's negative controls, on the two different
  functions AC-1 and AC-2 cover. Record both under `## Gate probes`.*

## Model guidance

Oracle partition for RED:

| Criterion | Kind | Instruction |
|---|---|---|
| AC-1, AC-2 | **Settled** as to the rule — `layout.md` says the slot's contents are centred and `tokens.md` fixes `space-tight` — **oracle-free** as to the tolerance. Justify the tolerance from pixel rounding, not from what the current code happens to produce, and show the control failing by a wide margin. |
| AC-3 | **Mechanical** | One inequality. Pin it exactly, or drop the criterion per `## Context`. |
| AC-4 | **Mechanical** | Two named mutations. Apply, watch red, revert, paste. |

Every assertion here is a **relation between two measurements in the same
frame** — gap above against gap below, painted width against available width.
Do not assert an absolute y coordinate: it encodes the window's whole layout
above the slot, so any change anywhere above it breaks a test that is about
none of those things. MC-022 took the same approach with widths and its
`## Test plan` explains why.

Re-use MC-021's helper for finding a widget's shapes in the paint list.

## Out of scope

- Colours and text styles (MC-021) and the dashed border (MC-029).
- The truncation policy itself — which end gives way, where the U+2026 goes.
  That is MC-022's and it is done. AC-3 is about the *budget*, not the cut.
- Changing any spacing or offset. This story pins the layout that exists; a
  disagreement with `layout.md` is a finding for the product owner.
- Absolute coordinates, DPI assertions, and anything that would move when a
  font package updates.
- The idle status slot, which paints nothing at all. There is no survivor there
  and no behaviour to pin beyond the height, which `layout.md` already fixes and
  MC-015's suite already exercises through the widgets below it.

## Design notes

No new design. `docs/wiki/design/layout.md` (the status slot and its 40 pt
height) and `tokens.md` ("Spacing") are the specification, unchanged.
