---
id: MC-010
title: Output names never overwrite an existing file
slug: output-names-never-overwrite-an-existing
epic: EPIC-03
type: feature
status: in-progress
phase: GATES
branch: story/MC-010-output-names-never-overwrite-an-existing
depends_on: [MC-002]
required_gates: []
---

## Context

Decision 7 in `architecture.md`: same file name from two source folders, or
a second run into the same output folder, must never overwrite. Explorer's
convention: `name (2).png`, `name (3).png`. Because the batch (MC-011) runs
files in parallel, names are planned up front, sequentially, before any file
is written: `naming::plan_outputs(out_dir, &inputs) -> Vec<PathBuf>` returns
one distinct, currently-free path per input in input order, accounting for
files already on disk and for duplicates within the batch. `process_file`
(MC-008) gains an explicit output path parameter so the planner decides
names and the codec only writes.

Gate that fails if this breaks: `unit`.

## Acceptance criteria

- **AC-1** - Given `out_dir` does not contain `a.png`, when `plan_outputs(out_dir, ["x/a.png"])` runs, then it returns `[out_dir/a.png]`.
- **AC-2** - Given `out_dir/a.png` exists, when planned for `["x/a.png"]`, then it returns `[out_dir/a (2).png]`.
- **AC-3** - Given `out_dir/a.png` and `out_dir/a (2).png` exist, when planned, then it returns `[out_dir/a (3).png]`.
- **AC-4** - Given two inputs `x/a.png` and `y/a.png` in one batch and an empty `out_dir`, when planned, then it returns `[out_dir/a.png, out_dir/a (2).png]` - the in-batch duplicate is resolved without anything on disk.
- **AC-5** - Given an input `Shot.PNG` that collides, when planned, then the result is `Shot (2).PNG` (stem and extension case preserved); given an input with no extension `notes`, then `notes (2)`.
- **AC-6** - Given `process_file` is called twice with the same input and the planned paths, when both complete, then both output files exist with distinct names and the first is unchanged by the second.

## Model guidance

Model: default. Oracle partition: all **mechanical**. Pin
`plan_outputs(&Path, &[PathBuf]) -> Vec<PathBuf>` and the exact ` (n)`
formatting (space, parentheses, ASCII digits).

## Out of scope

Recursion into folders, a limit on `n`, Unicode normalisation of file names,
cleaning up a partially written file after a crash.

## Test plan

One new integration target, `crates/engine/tests/naming.rs` (14 tests), and a
call-site migration in the two targets that call `process_file`. Nothing is
added under `src/`: `paths.conf` classifies `#[cfg(test)] mod tests` there as
source and reserves it for GREEN.

| Level | Target | Why there |
|---|---|---|
| Integration | `tests/naming.rs` (new, 14 tests) | `plan_outputs` is pure over paths *and over what is on the filesystem* - "currently-free" is a question only a real directory can answer, and the one thing that could silently differ between a unit harness and the real thing (case folding on NTFS) is exactly what this story turns on. It also needs `process_file` for AC-6, and `tests/` is the only tree RED may write. |
| Migration | `tests/process_file.rs`, `tests/formats.rs` | `process_file` takes an output *path* now, so six call sites move. |

**Its own target rather than an extension of `tests/process_file.rs`.** That
file is MC-008's and its subject is one file in, one file out; this one's
subject is the batch's names, and twelve of its fourteen tests never decode an
image. The cost is a third copy of `tests/common/mod.rs` (cargo compiles it
into every binary in the directory), paid because AC-6 needs the measured
screenshot fixture and its rect oracle rather than a new fixture invented here.
Anything failing in `naming.rs` is MC-010's.

**The migration changes calls and nothing else.** Both files' helpers already
take `file_name`, so `process_file(&input, out, ..)` became
`process_file(&input, &out.join(file_name), ..)` - which is the path MC-008 and
MC-009 already asserted the output landed at. No assertion, expected value,
fixture or test name changed, and both targets still pass unchanged (15/15 and
14/14, measured - see the handoff). Two module doc comments that spelled out
the old signature were corrected with it.

### Each criterion, and what makes its test discriminating

Every number in the last column is measured, not predicted: each mutation was
applied to a reference implementation in a scratch copy of the workspace and
the suite run (see `### Mutation table`).

| AC | Test(s) | Why it can fail |
|---|---|---|
| AC-1 | `a_name_nothing_else_claims_is_planned_unchanged` | The free case, asserted as a whole `PathBuf` and then as "not already there", so a planner that suffixed unconditionally fails on the path and one that returned a taken name fails on the second assertion. |
| AC-1 | `every_input_gets_one_planned_path_in_input_order`, `an_empty_batch_plans_no_outputs` | One path per input, positionally - MC-011 zips the plan against the inputs, so a planner that sorted or de-duplicated would hand a file the wrong name. Zero, one and many are covered here, in AC-1 and in the five-input test below. |
| AC-1 | `an_output_folder_that_does_not_exist_yet_leaves_every_name_free` | The zero case of "what is already in `out_dir`". An implementation that opened the directory with `read_dir(..).unwrap()` panics here; decision 6 creates that folder on demand, so it genuinely may be absent. |
| AC-2 | `a_name_already_on_disk_is_planned_as_two` | The exact string `a (2).png`, plus "the planned path is free", plus the byte contents of the file that was already there - planning must not touch it. Catches every formatting mutation and both off-by-ones. |
| AC-3 | `a_name_and_its_two_both_on_disk_are_planned_as_three` | The counter has to keep walking, not stop at 2. Also asserts the directory listing is unchanged: a planner that reserved names by creating files would be visible. |
| AC-4 | `two_inputs_with_one_name_in_the_same_batch_are_planned_apart` | Nothing is on disk, so only the batch's own memory can resolve it; and the output folder is asserted still empty, so "reserve by touching" is not a way to pass. |
| AC-3/AC-4 | `five_inputs_with_one_name_continue_past_what_is_already_on_disk` | Both halves at once, and the many case: two files on disk, five inputs sharing one name, expected `a (3)` through `a (7)`. Every off-by-one on the counter prints a different list; all five are asserted distinct and all five asserted free. |
| AC-5 | `a_suffixed_name_keeps_the_case_of_its_stem_and_its_extension` | `Shot (2).PNG` compared as a string *and* as a path. This is the only test that fails when the planner lower-cases a name (measured: 1 of 14), which is a real hazard on a case-insensitive filesystem where the wrong name still writes successfully. |
| AC-5 | `a_colliding_name_with_no_extension_takes_the_suffix_at_the_end` | `notes (2)`, not `notes (2).` and not `notes.(2)`. `Path::extension` is `None` here, so this is the branch a naive `format!("{stem} ({n}).{ext}")` gets wrong. |
| AC-6 | `the_same_input_processed_twice_under_planned_names_leaves_both_files` | The planner and the writer together on the crop path: the plan is asserted first, then each run's reported output, then the directory holds exactly two files, then the first run's bytes are unchanged. A `process_file` that ignored its new parameter and re-derived MC-008's name fails here (measured: this test and the next, and nothing else in the workspace). |
| AC-6 | `a_flagged_input_processed_twice_under_planned_names_leaves_both_copies` | The same on the copy path, which is a different writer inside `process_file` and would otherwise go unpinned; both copies are asserted byte-equal to the marked fixture. |
| control | `the_naive_plan_collides_in_every_scenario_these_criteria_pin` | See below. |
| control | `two_writes_to_one_path_leave_one_file` | See below. |

### The two controls, and what they are for

Neither calls `naming`. They exist because the criteria in this story are
assertions about *absence* - no file was overwritten - and an assertion about
absence is worth nothing unless the alternative is shown to be present.

1. `the_naive_plan_collides_in_every_scenario_these_criteria_pin` measures what
   `out_dir.join(file_name)` - the answer MC-008 ships - gives in AC-2's,
   AC-3's and AC-4's scenarios: a path that already exists, twice, and then one
   path shared by two inputs. Without it, a reader cannot tell whether AC-2 to
   AC-4 demand work or restate what already happens. The matching whole-planner
   mutation was measured: 8 of 14 fail.
2. `two_writes_to_one_path_leave_one_file` measures the loss AC-6 forbids: one
   directory entry, and the first write's bytes gone. AC-6's "both output files
   exist with distinct names" is only a demand because of this.

Both were executed outside the test framework during RED, since the file does
not compile while `naming` is missing; their numbers are in the handoff for
GREEN to confirm.

### One thing measured and deliberately not tested

Two inputs differing only in case - `a.png` and `A.PNG` - are not mentioned by
any criterion, and this story does not invent one. RED measured the behaviour
instead, and the measurement is in the handoff under
`### The case question: measured, not criteria`. It found a real overwrite, so
it is escalated there rather than absorbed into a test here.

### Edges, and the non-goals

Zero, one, many: the empty batch, one input, five inputs sharing a name over a
directory that already holds two. Boundaries: the counter at its first value
(2) and at 3, and the five-input run at 7. Errors: there is no fallible path in
`plan_outputs` - it returns `Vec<PathBuf>`, not a `Result` - so the error
shapes here are "the directory does not exist" (covered) and "the file name
collides" (the whole story). Of `## Out of scope`, recursion into folders and a
limit on `n` are not pinned because pinning them would state a behaviour the
story deliberately leaves open; Unicode normalisation would need a fixture
whose correct answer this story has not decided; crash cleanup has no test that
does not invent a criterion.

## Handoff: RED -> GREEN

### The command

```
cargo test --workspace                          # from the repo root; the `unit` gate's own command
cargo test -p cropper-engine --test naming      # this story's target alone
```

Run the **workspace** command, not `-p cropper-engine`. MC-009 shipped a
control that was green on the crate alone and red under `--workspace`, because
cargo unifies features across the workspace; that mistake has been made here
once already.

### The failure, verbatim

`cargo test --workspace`, on the tree these tests are committed on. It fails to
**compile**, and that is the right failure: `naming::plan_outputs` is the one
thing this story adds that the tests import, and it does not exist.

```
   Compiling cropper-engine v0.1.0 (C:\Users\ryanc\Projects\manhwa-cropper\crates\engine)
error[E0432]: unresolved import `cropper_engine::naming`
  --> crates\engine\tests\naming.rs:54:21
   |
54 | use cropper_engine::naming::plan_outputs;
   |                     ^^^^^^ could not find `naming` in `cropper_engine`

For more information about this error, try `rustc --explain E0432`.
error: could not compile `cropper-engine` (test "naming") due to 1 previous error
```

**One diagnostic, not two.** An earlier draft of the AC-6 tests matched on
`&result.outcome`; while the import is unresolved that produced a second,
spurious type error next to the real one. The loops now take their results by
value, so the only thing the compiler complains about is the missing module.

`bash scripts/gates.sh --fast` on the same tree:

```
--- gate summary ---
PASS         format (0s)
FAIL         lint (4s, exit 101) -> .claude/state/gate-logs/lint.log
FAIL         typecheck (1s, exit 101) -> .claude/state/gate-logs/typecheck.log
FAIL         unit (1s, exit 101) -> .claude/state/gate-logs/unit.log
FAIL         coverage (18s, exit 101) -> .claude/state/gate-logs/coverage.log
```

**That shape was read, not assumed.** `lint` and `typecheck` are `--all-targets`
commands, so in Rust they compile the test targets and cannot be green while
the module is missing. Filtering `^error`/`^warning` out of all four logs leaves
exactly one diagnostic - the `E0432` above - plus rollup lines. No gate failed
on a timeout or a configuration error.

**The test files are lint-clean, which the repo cannot show while the module is
missing** (clippy lints nothing in a target that will not resolve). It was
shown instead in the scratch copy described below:
`cargo clippy --workspace --all-targets -- -D warnings` is silent there with
`tests/naming.rs`, the migrated `tests/process_file.rs` and the migrated
`tests/formats.rs` all compiled. `cargo fmt --all --check` is clean in the repo
now.

**Cost, for the budget.** `tests/naming.rs` runs its 14 tests in **0.02 s**
plain and **0.02 s** under `cargo llvm-cov` instrumentation (measured in the
scratch copy). `ci-factor | coverage | 1.63` puts that nowhere near anything;
no test in the file loops over a generated collection, and the two that decode
an image use MC-008's 147x120 fixture.

**`floor | unit | 47` does not move.** It tracks the first `test result: ok. N`
line, which belongs to the first binary that ran a test: cargo runs members in
`Cargo.toml` order (`crates/core` first) and each member's targets
alphabetically, so it is still `crates/core tests/content.rs` with 47. The new
target sorts between `formats` and `process_file` inside `crates/engine` and
cannot reach the front. Confirmed in the scratch run's ordering.

### The test counts, before and after

| | `cargo test --workspace` |
|---|---|
| Before this story (commit 9332ab0) | **205 passed**, 0 failed - `crates/core` 141, `crates/engine` 55 (lib 6, args 13, codec 6, formats 15, process_file 14, scaffold 1), `crates/app` 9 |
| Now, in the repo | **does not compile** - the E0432 above |
| After GREEN, expected | **219 passed** - the same 205, unchanged, plus `tests/naming.rs`'s 14 |

The 219 is not a guess. A scratch copy of the workspace outside the repository
(`crates/core` + `crates/engine`, `crates/app` dropped because nothing in it
changes) was given a reference `naming.rs` and the `process_file` signature
below, and ran **210 passed, 0 failed**: 141 + 55 + 14, which is 219 less the
app crate's 9. `tests/formats.rs` 15/15 and `tests/process_file.rs` 14/14 in
that run - the migration changed no behaviour those files can see. The scratch
copy was never committed and is deleted.

### What GREEN must export

Nothing below is a suggestion. A test already imports each name, so a different
choice is a compile error in a file GREEN may not edit.

```rust
// crates/engine/src/naming.rs, reached as `cropper_engine::naming::plan_outputs`
// (so `pub mod naming;` in lib.rs; a root re-export as well is GREEN's choice)
pub fn plan_outputs(out_dir: &Path, inputs: &[PathBuf]) -> Vec<PathBuf>;

// crates/engine/src/process.rs, re-exported at the crate root exactly as today
pub fn process_file(input: &Path, output: &Path, tuning: &Tuning) -> FileResult;
```

What the assertions require of them:

* `plan_outputs` takes a **slice of `PathBuf`** - the tests pass `&Vec<PathBuf>`,
  a borrowed array literal and `&[]` - and an `&Path` for the output directory,
  in that order (`out_dir` first). It returns an owned `Vec<PathBuf>` compared
  with `assert_eq!` against `vec![out.join(..), ..]`, so the order of the
  returned paths is the order of the inputs.
* It must not require the inputs to exist. Every input in AC-1 to AC-5 is a
  bare path under a folder that was never created, exactly as the criteria word
  them (`x/a.png`, `y/a.png`).
* It must not write anything: two tests read the output directory back after
  planning and assert it is unchanged (empty in AC-4's, the two occupied files
  in AC-3's).
* **`process_file`'s second parameter is the output path, not the output
  directory.** It returns that same path in `Outcome::Cropped { output }` and
  `Outcome::Flagged { output }`, and writes the file *there*, under that exact
  name - the case included.
* Unchanged and still pinned by MC-008 and MC-009: `FileResult { input,
  outcome }`, `Outcome::{Cropped { rect, output }, Flagged { reason, output },
  Failed { error }}`, `Flag::{Detector, Unsupported, DecodeFailed}`,
  `SourceFormat`, `codec::{detect_format, to_luma}`. **Do not add a field to
  any `Outcome` variant**: three test files construct and destructure them by
  field name and none may be edited in GREEN.

**Deliberately not constrained**, and GREEN's to choose: how `plan_outputs`
asks the filesystem (`Path::exists`, `try_exists`, one `read_dir` cached); what
it uses to remember the batch; whether the search from `n` is linear; whether
`naming` is re-exported at the crate root; what happens to an input with no
file name, such as `..` (no test passes one); double extensions like
`a.tar.gz`, where `Path::extension` gives `gz` and the obvious implementation
yields `a.tar (2).gz` (measured, unpinned); dotfiles; any cap on `n`
(`## Out of scope`).

### One line per test

`crates/engine/tests/naming.rs`, 14 tests:

| Test | Asserts | AC |
|---|---|---|
| `the_naive_plan_collides_in_every_scenario_these_criteria_pin` | `out_dir.join(file_name)` is a file that already exists in AC-2's and AC-3's scenarios, and one path shared by AC-4's two inputs | control for AC-2/3/4 |
| `two_writes_to_one_path_leave_one_file` | two writes to one path leave one directory entry and the first write's bytes are gone | control for AC-6 |
| `every_input_gets_one_planned_path_in_input_order` | three free names, three paths, same order, same length | AC-1 |
| `an_empty_batch_plans_no_outputs` | `plan_outputs(&out, &[])` is empty | AC-1 (zero) |
| `a_name_nothing_else_claims_is_planned_unchanged` | `[out/a.png]`, and that path does not already exist | AC-1 |
| `an_output_folder_that_does_not_exist_yet_leaves_every_name_free` | a missing `out_dir` contains nothing, so two inputs keep their own names | AC-1 (edge) |
| `a_name_already_on_disk_is_planned_as_two` | `[out/a (2).png]`; the path is free; the occupied file's bytes are untouched | AC-2 |
| `a_name_and_its_two_both_on_disk_are_planned_as_three` | `[out/a (3).png]`; the directory listing is unchanged; both occupied files untouched | AC-3 |
| `two_inputs_with_one_name_in_the_same_batch_are_planned_apart` | `[out/a.png, out/a (2).png]` with nothing on disk, and the output folder still empty afterwards | AC-4 |
| `five_inputs_with_one_name_continue_past_what_is_already_on_disk` | `a (3)` ... `a (7)`, all distinct, all free | AC-3 + AC-4 (many) |
| `a_suffixed_name_keeps_the_case_of_its_stem_and_its_extension` | the file name is exactly `Shot (2).PNG`, and the path is `out/Shot (2).PNG` | AC-5 |
| `a_colliding_name_with_no_extension_takes_the_suffix_at_the_end` | the file name is exactly `notes (2)` | AC-5 |
| `the_same_input_processed_twice_under_planned_names_leaves_both_files` | the plan is `[shot.png, shot (2).png]`; both runs report `Cropped` at the planned path with MC-008's rect; the folder holds exactly those two files; the first file's bytes are unchanged | AC-6 |
| `a_flagged_input_processed_twice_under_planned_names_leaves_both_copies` | the same on the copy path: `[flat.png, flat (2).png]`, both `Flagged` at the planned path, two entries, both byte-equal to the marked fixture | AC-6 |

### The tests that pass the moment the crate compiles

The file fails at **import**, so not one assertion in it has run in this
repository - controls included. Driven against a stub whose `plan_outputs` is
`unimplemented!()` in the scratch copy, exactly two pass:

```
test result: FAILED. 2 passed; 12 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.01s
```

Those two are the controls above, and they are meant to pass: they measure the
filesystem and `Path::join`, not this story's code. What earns them is that
each is a *measurement with a recorded number* (next section) rather than a
restatement - and that the behaviour each one describes is the behaviour a
matching mutation of the real implementation produces, which was measured:
the whole-planner-is-`join` mutation fails 8 of 14 tests, and the
writer-ignores-its-output-path mutation fails exactly the two AC-6 tests.

### Negative controls: the numbers GREEN must confirm

Measured in RED outside the test framework, with a std-only program compiled by
`rustc` (no `cropper-engine`, which did not compile), on this Windows 11 /
NTFS machine. **Confirming each of these against the shipped module is GREEN's
job**; until then they are a claim.

| # | Control | Why it exists | Measured in RED |
|---|---|---|---|
| 1 | AC-2's naive path | must already exist, or AC-2 asks for nothing | `out/a.png` exists = **true** |
| 2 | AC-3's naive paths | both must be taken | `a.png` **true**, `a (2).png` **true** |
| 3 | AC-4's naive paths | must be *equal*, or the in-batch rule asks for nothing | equal; **1** distinct path for 2 inputs |
| 4 | AC-6: two writes to one path | must leave one file and lose the first bytes | **1** entry (`a.png`); first bytes gone (`"second write, different length"`) |
| 5 | AC-6: two writes to two planned paths | must leave two | **2** entries: `a (2).png`, `a.png` |
| 6 | The stem/extension split the ` (n)` rule needs | mechanical, and AC-5 is about it | `Shot.PNG` -> stem `Shot`, ext `PNG` -> **`Shot (2).PNG`**; `notes` -> ext `None` -> **`notes (2)`**; (unpinned: `a.tar.gz` -> `a.tar (2).gz`, `.gitignore` -> `.gitignore (2)`) |
| 7 | `PathBuf` equality is case-sensitive on Windows for a file name | if it were not, AC-5's assertion would be vacuous | a `HashSet<PathBuf>` holding `out/a.png` `.contains(out/A.PNG)` = **false** |
| 8 | `Path::exists` is case-**in**sensitive here | the other half of the case measurement below | after writing `out/a.png`, `out/A.PNG.exists()` = **true** |
| 9 | The planner touches nothing | AC-3 and AC-4 assert it; a missing `out_dir` is still missing after planning | `out.exists()` = **false** before and after |
| 10 | The whole suite against an `unimplemented!()` planner | the controls must be the only tests that pass | **2 passed, 12 failed** |

### The case question: measured, not criteria

**A real overwrite is possible, and a follow-up story should be put to the
user.** This is a measurement, not a criterion; no test in this story asserts
it and no acceptance criterion was added for it.

The two halves of a planner disagree about case on Windows, which controls 7
and 8 above state separately. Driving the implementation-shaped planner
(`Path::exists` for the disk, `HashSet<PathBuf>` for the batch) over one batch
of `x/a.png` and `y/A.PNG` into an empty output folder:

| Step | Measured |
|---|---|
| the plan | `["a.png", "A.PNG"]` - two names, and they *are* distinct as `PathBuf`s |
| after MC-011's batch writes both | the folder holds **one** file, `a.png` |
| its bytes | `A.PNG`'s, not `a.png`'s - the first input's output is gone |

So a planner that is correct about everything this story pins can still hand
two inputs the same file, and the loss is silent: both writes succeed and the
run summary reports two files. That contradicts the story's own Context
("must never overwrite") for a case the criteria do not cover, which is why it
is escalated rather than fixed here.

The mitigation is one line and was measured too, for whoever writes that story:
folding the in-batch set to lower case gives `["a.png", "A (2).PNG"]` and two
files on disk. It is **not** in scope here and GREEN must not add it - it would
be an untested behaviour in a story whose criteria are frozen. Note also that
it is the *right* answer only on a case-insensitive filesystem, which is the
only one this product ships on.

### Mutation table

Predicted failure counts over the **14 tests in `tests/naming.rs`**. Every row
was applied to a reference implementation in the scratch copy and the suite run
there, so these are measurements of the tests, not of GREEN's code; a
structurally different implementation may need the mutation expressed
differently, but the count is what these assertions are worth. No row moves
`crates/core` (141), `tests/formats.rs` (15) or `tests/process_file.rs` (14)
unless it says so.

| Mutation | Predicted failures | Basis |
|---|---|---|
| The counter starts at 1: the first suffix is ` (1)` | **8**: AC-2, AC-3, both AC-5, AC-4, the five-input test and both AC-6 tests | measured |
| The counter starts at 3: the first suffix is ` (3)` | **6**: AC-2, both AC-5, AC-4 and both AC-6 tests. AC-3's test and the five-input test still pass - both expect `a (3)` first - which is why "starts at 2" needs AC-2 and AC-5 and not just AC-3 | measured |
| No space: `a(2).png` | **8**, the same set as the first row | measured |
| The suffix goes after the extension: `a.png (2)` | **7**: everything above except `a_colliding_name_with_no_extension_takes_the_suffix_at_the_end`, which cannot tell the two apart | measured |
| The planned name is lower-cased | **1**: `a_suffixed_name_keeps_the_case_of_its_stem_and_its_extension`. The single-assertion row - and a real hazard, because the wrong name still writes successfully on NTFS | measured |
| Only the disk is consulted; duplicates inside the batch are not | **4**: `two_inputs_with_one_name_in_the_same_batch_are_planned_apart`, the five-input test and both AC-6 tests | measured |
| Only the batch is consulted; what is on disk is not | **5**: AC-2, AC-3, both AC-5 tests and the five-input test | measured |
| The whole planner is `out_dir.join(file_name)` - what MC-008 ships | **8**: everything except the two controls, the two AC-1 tests, the order test and the empty-batch test | measured |
| `process_file` ignores its new parameter and re-derives `out_dir/<input name>` | **2**, and exactly the two AC-6 tests; **nothing else in the workspace moves**, because the migrated call sites pass `out.join(file_name)`, which is that same path | measured over `cargo test --workspace` |
| `plan_outputs` is `unimplemented!()` | **12**; the two controls pass | measured |

The failure the ninth row produces, for recognition:

```
---- the_same_input_processed_twice_under_planned_names_leaves_both_files stdout ----
assertion `left == right` failed: AC-6: the second run reports the path it was given to write
  left: "...\\out\\shot.png"
 right: "...\\out\\shot (2).png"
```

### What should change the implementation approach

1. **Who creates the output directory, since no AC pins it.** `plan_outputs`
   must create nothing - two tests assert the folder is untouched after
   planning, and one plans into a folder that does not exist. The parent
   directory therefore stays `process_file`'s job, exactly where MC-008 left
   it (`write_crop` and `copy::copy_unchanged` both call `create_dir_all`);
   with an output *path* that becomes `fs::create_dir_all(output.parent())`.
   No test pins it either way - `workspace()` in all three test files creates
   `out` first - so this is GREEN's call as it was MC-008's, and the
   recommendation is to keep it: decision 6's fallback `cropped` folder and
   MC-011's batch both rely on somebody creating it, and `Outcome::Failed` is
   the only channel through which failing to is reportable.
2. **`copy::copy_unchanged` does not have to change, and changing it is not
   free.** It joins the input's name onto a directory, which is no longer what
   the flagged path wants. Its six `#[cfg(test)]` tests live in
   `crates/engine/src/copy.rs` - classified *source*, so GREEN may edit them -
   and `copy_all` is used by `crates/app/src/main.rs`, whose `tests/cli.rs` is
   frozen. The cheapest route, and the one the RED reference took, is a
   sibling that copies to an exact path, leaving `copy_unchanged` and
   `copy_all` for MC-011 to retire.
3. **Three clippy errors follow from the signature change**, and `lint` is
   `-D warnings`. Measured on the reference implementation: with `output:
   &Path` in `write_crop`, `cropped.save_with_format(&output, ..)` (twice) and
   `write_jpeg(&cropped, &output)` become `needless_borrows_for_generic_args`
   and `needless_borrow` - drop the `&` - and `use crate::{codec, copy};`
   becomes an unused import if the flagged path stops calling `copy`.
4. **Plan sequentially, and keep it that way.** The whole point of a separate
   `plan_outputs` is that MC-011 runs the writes in parallel: "find a free
   name, then write it" inside a worker is a race that loses a file. The tests
   cannot catch that - they are single-threaded - so it is stated here.
5. **The five-input test is the one to read first** if the counter is wrong: it
   prints the whole planned list, so an off-by-one is visible as a list rather
   than as one path.
6. **Confirm the ten control values above before writing anything else.** They
   were measured against a std-only program and the real filesystem, not
   against the shipped module; MC-009's row 9 diverged for exactly that reason
   and cost a return to RED.

### Files touched in RED

| File | What |
|---|---|
| `crates/engine/tests/naming.rs` | new, 14 tests |
| `crates/engine/tests/process_file.rs` | four call sites migrated; module doc corrected where it spelled out the old signature. No assertion, expected value, fixture or test name changed |
| `crates/engine/tests/formats.rs` | two call sites migrated (inside `crop_file` and `flag_file`); one module doc paragraph added saying so. Same guarantee |
| `docs/backlog/stories/MC-010.md` | `## Test plan`, this handoff |

**Why this is the right failure**: the unsatisfied import is
`naming::plan_outputs`, the function every criterion in the story is about, and
the twelve assertions that depend on it are the twelve that fail against a stub
- the two that do not are the controls, which are not about this story's code.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-11T22:51:31Z
    commit: 25201e3
    tree:   725a2af777bab97ccfe37aa756d3b3547cdfcd09
    result: pass (8 ran, 0 unconfigured, 2 known)

    PASS         format (1s)
    PASS         lint (1s, observed 0)
    PASS         typecheck (1s, observed 0)
    PASS         unit (6s, observed 47, floor 47)
    PASS         coverage (28s, observed 985)
    KNOWN        integration (93s, ran but produced no evidence of work: expected /test result: ok\. [1-9][0-9]*/; no #[ignore] test exists until MC-017, so the evidence regex cannot match; MC-017 deletes this line)
    PASS         build (77s, observed 1)
    KNOWN        mutation (0s, exit 101; cargo-mutants is not installed (docs/wiki/environment.md, Optional); install it and delete this line to enable the gate) -> .claude/state/gate-logs/mutation.log

## Notes

### PLANNED -> RED (2026-09-11)

**The required gate that fails if this breaks is `unit`**, already
`gate | unit | required` in `project.conf`. `required_gates` escalates gates
that are *optional* for the repo and binding for one story; `unit` is not one,
so `required_gates: []` is correct rather than unfilled. `coverage` moves too -
a new module in `crates/engine/src/` starts at zero covered lines.

**`depends_on: [MC-002]` is satisfied, and understates the real dependency.**
This story changes `process_file`, which MC-008 wrote and MC-009 extended. Both
are DONE, so nothing is blocked and the frontmatter needs no correction, but
the next agent should know that the surface being modified arrived two stories
after the one named here.

#### This story edits tests belonging to finished stories, and that is allowed

`## Context` says `process_file` "gains an explicit output path parameter".
That changes a signature pinned by MC-008's `tests/process_file.rs` and
MC-009's `tests/formats.rs`. Sized before starting: **six call sites**, most of
them already behind test helpers (`crop_file`, `flag_file`), plus one doc
comment that spells the signature out.

The freeze in `CLAUDE.md` law 2 is "tests are frozen **during GREEN**" - within
a cycle, so that GREEN cannot edit its way to a pass. It is not a claim that a
finished story's tests are immutable forever; RED is exactly where a call-site
migration belongs. Two conditions on it, which the RED brief carries:

1. **No existing assertion changes.** Only the call, and only where the
   signature forces it. An assertion that was earned in MC-008's or MC-009's
   RED keeps its earning; one quietly rewritten here would not, and would be
   indistinguishable from one that was.
2. The suite will again fail at **import** while `naming::plan_outputs` does
   not exist, so nothing in the new file executes during RED. Negative controls
   need their expected values recorded for GREEN to confirm, per `rules.md`.

#### One thing the criteria are silent about, to be measured and not invented

AC-4 resolves an in-batch duplicate from two inputs both named `a.png`. Nothing
says what happens when two inputs differ only in **case** - `a.png` and
`A.PNG`, which is a real pairing when a user drops files from two folders.
It matters here and nowhere else in this codebase, because the two halves of
the planner disagree about case on Windows: `Path::exists` asks a
case-insensitive filesystem, while an in-batch set of `PathBuf`s compares
bytes. A planner that is right about disk can still hand two inputs the same
name.

That is **not** a criterion of this story and I am not making it one: no AC
mentions it and inventing one is the amendment path, not a shortcut through it.
RED is asked to *measure* the behaviour and record it. If it turns out that
this program can overwrite a user's file, that is a finding for a follow-up
story put to the user - not scope taken silently here.

### RED verified by the orchestrator (2026-09-11)

**The failure, reproduced from a clean `cargo test --workspace`:**

```
   Compiling cropper-engine v0.1.0 (C:\Users\ryanc\Projects\manhwa-cropper\crates\engine)
error[E0432]: unresolved import `cropper_engine::naming`
  --> crates\engine\tests\naming.rs:54:21
   |
54 | use cropper_engine::naming::plan_outputs;
   |                     ^^^^^^ could not find `naming` in `cropper_engine`

For more information about this error, try `rustc --explain E0432`.
error: could not compile `cropper-engine` (test "naming") due to 1 previous error
```

One diagnostic, and the suite again fails at **import** - so nothing in
`naming.rs` has executed and the ten negative controls in the handoff are
claims until GREEN confirms them.

#### The call-site migration, audited rather than accepted

The condition on migrating another story's tests was that **no existing
assertion changes**. I read the whole diff rather than the summary of it:

```
$ git diff HEAD~1 --stat
 crates/engine/tests/formats.rs      |  13 +-
 crates/engine/tests/naming.rs       | 551 +++++++++++++++++++++++++++++++
 crates/engine/tests/process_file.rs |  24 +-
 docs/backlog/stories/MC-010.md      | 399 ++++++++++++++++++++++++++
```

Every line of the 13 and the 24 is either a module doc comment or one of the
six calls changing `out` to `&out.join(file_name)`. **No assertion, expected
value, fixture or test name is touched**, and `out.join(file_name)` is the
exact path MC-008 and MC-009 asserted their outputs landed at - so the
migration preserves what those assertions claimed as well as that they pass.
`the_output_keeps_the_input_file_name_case_and_extension` still reads the
directory back, so a writer that mangles a name it is handed still fails there.

### The case question: measured, and it is a real defect

I asked RED to measure this and not to invent a criterion for it. It measured a
silent overwrite. **I reproduced it independently, end to end and by a
different method** - no Rust, no planner, just the filesystem this program will
run on:

```
$ printf 'FIRST FILE' > casetest/a.png
$ [ -e casetest/A.PNG ] && echo YES
YES                      <- the disk says A.PNG already exists
$ printf 'SECOND FILE' > casetest/A.PNG
$ ls -1 casetest | wc -l
1                        <- one file, not two
$ ls -1 casetest
a.png
$ cat casetest/a.png
SECOND FILE              <- the first file's bytes are gone
```

So on NTFS, `a.png` and `A.PNG` are one file. A planner that is correct about
everything this story pins still hands those two inputs distinct-looking names,
because `PathBuf` equality is byte-wise while the filesystem is not, and the
second write destroys the first. `x/a.png` + `y/A.PNG` is not a contrived
pairing - it is exactly the two-source-folders case `architecture.md` decision
7 exists for.

**It is still not a criterion of MC-010 and I have not made it one.** No AC
mentions case, and the fix (fold the batch's own set for comparison while
keeping the original case in the name) is a change to `plan_outputs`'s
behaviour that would need an `## Amendments` entry to be legitimate here.
Taking it silently is precisely the move the frozen-criteria rule exists to
prevent - and MC-009's amendment is the evidence that this project does it the
other way.

**Recorded as a finding to put to the user**, with the reproduction above so
the next agent does not have to rediscover it. MC-011 is the story that
actually runs batches, and it is where this would bite; a pointer is going into
its notes as well, so the finding survives whether or not a story is filed for
it.

### GREEN (2026-09-11)

**The failure was reproduced first, from the tree as committed**: `cargo test
--workspace` fails to compile with the single `E0432` the handoff records,
`unresolved import cropper_engine::naming` at `tests/naming.rs:54`. Nothing in
that file had executed, so the ten controls below were claims.

**What was written.** Three source files, no test file touched, no config
change, no new dependency:

* `crates/engine/src/naming.rs` (new) - `plan_outputs(out_dir, inputs)`. One
  pass over the inputs in order; for each, the candidate starts at
  `out_dir/<input file name>` and, while it is either claimed by an earlier
  input in this batch (a `HashSet<PathBuf>`) or present on disk
  (`Path::exists`), takes the next ` (n)` counting from 2. `suffixed` builds an
  `OsString` - `file_stem`, then `" (n)"`, then `"."` and the extension when
  there is one - so the stem case and the extension case are the input's own
  bytes, and a name this program cannot decode as UTF-8 still passes through
  exactly. Nothing in the module reads, writes or creates a file.
* `crates/engine/src/process.rs` - `process_file(input, output, tuning)`: the
  second parameter is the whole output path. `write_crop` takes it in place of
  `(input, out_dir)` and returns it; `flagged` copies to it; both report it in
  `Outcome::Cropped { output }` and `Outcome::Flagged { output }`. No field was
  added to any `Outcome` variant, so the three frozen test files still
  construct and destructure them unchanged.
* `crates/engine/src/copy.rs` - a new primitive `copy_to(input, output)` that
  copies to an exact path. `copy_unchanged` keeps its signature and delegates
  to it, so `copy_all`, `crates/app/src/main.rs` and the six `#[cfg(test)]`
  tests in `copy.rs` are untouched and still pass.

**Two of the three hazards the handoff measured did not fire, because
`copy_unchanged` never changed signature.** The predicted
`needless_borrows_for_generic_args` did fire, and was fixed properly - by
dropping the `&` from `save_with_format(output, ..)` (twice) and from
`write_jpeg(&cropped, output)`, not with an `#[allow]`. The "possibly unused
`copy` import" did not, because the flagged path still calls `copy::copy_to`.
`cargo clippy --workspace --all-targets -- -D warnings` is silent and
`cargo fmt --all --check` is clean.

**The counts.** `cargo test --workspace`: **219 passed, 0 failed** - the number
the handoff predicted, and the 205 from commit 9332ab0 plus `tests/naming.rs`'s
14. `tests/formats.rs` 15/15, `tests/process_file.rs` 14/14, `crates/app` 9/9:
the call-site migration changed nothing those files can see. `floor | unit | 47`
did not move (still `crates/core tests/content.rs`).

#### The ten negative controls, confirmed against the shipped module

Controls 1 to 9 were re-measured by a throwaway `rustc` program in the session
scratchpad that `#[path]`-includes the shipped `crates/engine/src/naming.rs`
verbatim - it is std-only, so it needs no cargo - and drives it over the real
filesystem on this Windows 11 / NTFS machine. It was never in the tree and it
is gone. Control 10 is an in-tree `cargo test --workspace` against a
`plan_outputs` that was `unimplemented!()`, run before the real body was
written. **All ten agree with RED.**

| # | Control | RED recorded | GREEN measured |
|---|---|---|---|
| 1 | AC-2's naive path already exists | `out/a.png` exists = true | **true** |
| 2 | AC-3's naive paths are both taken | `a.png` true, `a (2).png` true | **true**, **true** |
| 3 | AC-4's naive paths are equal | equal; 1 distinct path for 2 inputs | equal = **true**; **1** distinct |
| 4 | AC-6: two writes to one path | 1 entry (`a.png`); first bytes gone (`second write, different length`) | **1** entry, `["a.png"]`; first bytes survive = **false**; contents now `second write, different length` |
| 5 | AC-6: two writes to two planned paths | 2 entries: `a (2).png`, `a.png` | **2**: `["a (2).png", "a.png"]`, and the first file is still intact |
| 6 | The stem/extension split | `Shot.PNG` gives `Shot (2).PNG`; `notes` gives `notes (2)`; unpinned: `a.tar.gz` gives `a.tar (2).gz`, `.gitignore` gives `.gitignore (2)` | **`Shot (2).PNG`**, **`notes (2)`**, **`a.tar (2).gz`**, **`.gitignore (2)`** - all four out of the shipped planner, not re-derived |
| 7 | `PathBuf` equality is case-sensitive | a `HashSet<PathBuf>` holding `out/a.png` contains `out/A.PNG` = false | **false** |
| 8 | `Path::exists` is case-insensitive here | after writing `out/a.png`, `out/A.PNG.exists()` = true | **true** |
| 9 | The planner touches nothing | `out.exists()` = false before and after | **false** before, **false** after; and in AC-3's occupied folder the listing and both files' bytes are unchanged after planning |
| 10 | The whole suite against an `unimplemented!()` planner | 2 passed, 12 failed | `test result: FAILED. `**`2 passed; 12 failed`**`; 0 ignored`, the 2 being the two controls; every other target in the workspace was green in that same run |

**The extra control: the planner is doing work, not returning
`out_dir.join(file_name)`.** The same program ran the shipped `plan_outputs` in
each naive-collision scenario, so its answer sits next to the naive one:

| Scenario | naive `out_dir.join(name)` | shipped `plan_outputs` |
|---|---|---|
| AC-2, `a.png` on disk | `a.png` - occupied | `["a (2).png"]` |
| AC-3, `a.png` and `a (2).png` on disk | `a.png` - occupied | `["a (3).png"]` |
| AC-4, two inputs named `a.png`, empty folder | one path for both, 1 distinct | `["a.png", "a (2).png"]` - **2** distinct |
| Many: five inputs, two of the name on disk | one path for all five | `["a (3).png", "a (4).png", "a (5).png", "a (6).png", "a (7).png"]` |

#### Who creates the output directory: the writers, not the planner

No acceptance criterion pins it, so it is a decision and it is recorded here.
**`plan_outputs` creates nothing; `write_crop` and `copy::copy_to` each create
the parent of the path they are handed.** Three reasons:

1. Two frozen tests assert the output folder is untouched after planning, and
   one plans into a folder that does not exist. A planner that created the
   folder would fail them, and it would also make "a plan is not a write" false
   for MC-011.
2. `Outcome::Failed` is the only channel this engine has for reporting an
   output folder it cannot create or write, and only `process_file` produces
   one. If the planner created directories, that failure would surface as a
   panic or a dropped name rather than as a line in MC-011's run summary.
3. For MC-011's parallel batch this is the safe split: planning is the
   sequential phase and stays pure, while `create_dir_all` is idempotent and
   returns `Ok` when the directory already exists, so N workers racing to
   create one parent lose nothing. Racing to *name* a file is what loses one,
   and that is precisely the race this story moves off the worker.

Both writers spell it
`fs::create_dir_all(output.parent().unwrap_or(Path::new("")))` rather than with
an `if let Some(parent)`: `Path::parent` is `Some("")` for a bare file name and
`None` only for a path that is nothing but a root, so the `if let`'s else arm
was a branch no caller can reach - and it showed up as an uncovered line in
both files. `create_dir_all("")` is a documented no-op, verified here as
`Ok(())`, so the one-liner is the same behaviour with one branch fewer. That is
a simplification, not a coverage shim: it removed the branch, not the
measurement of it.

#### Coverage

Under the gate's own command: **99.31% of 578 lines**, against 99.10% of 553
before this story, floor 95. `engine/src/naming.rs` is **25/25 lines, 100%**,
and `engine/src/copy.rs` went to 100% (the change above). The only uncovered
lines left in the engine are the two named in the brief, both pre-existing and
both merely shifted by the edits: `process.rs:164-166` is now **170-172** (the
`Outcome::Failed` I/O arm) and `:264` is now **271** (the JPEG encode error
edge). Nothing was written to chase a line: no shim, no `unreachable!()`, no
pragma, no test.

#### No gate was added or changed, so there is no `## Gate probes` section

This story adds no gate and changes no gate command, evidence line, floor or
waiver. There is nothing to break and watch fail, and a `## Gate probes`
section that described a failure without showing one is what
`check-boundaries.sh` refuses - MC-008 and MC-009 both recorded the same thing
here in `## Notes` instead. The gate that fails if this behaviour breaks is
`unit`, and it was observed failing in RED (the `E0432`) and again in GREEN
against the `unimplemented!()` stub (control 10).

#### The case collision is still out of scope, and still unfixed

`x/a.png` plus `y/A.PNG` plans two distinct-looking names and lands one file on
NTFS. RED measured it, the orchestrator reproduced it independently, and the
shipped planner reproduces it by construction: controls 7 and 8 above - byte-wise
`PathBuf` equality against a case-insensitive `Path::exists` - are its two
halves, and both are confirmed. **GREEN did not fix it.** No acceptance
criterion mentions case, and folding the batch's own set would be an un-amended
change to a frozen criterion's behaviour. It stays a follow-up story for the
user, exactly as the RED notes leave it.

#### One behaviour no test pins, chosen rather than discovered

`plan_outputs` must answer one path per input, positionally - MC-011 zips the
plan against the inputs - and an input with no file name of its own (`..`, or a
path that is nothing but a root) has no name to plan. It is given the
placeholder `output` inside `out_dir`, where it collides and suffixes like any
other name. Nothing is ever written there: `process_file` on such an input
fails at `ImageReader::open` and reports `Outcome::Failed` before it writes
anything, so the placeholder is not a file a user sees. The alternatives were
dropping the input from the plan, which breaks the positional pairing MC-011
depends on, and panicking, which kills a batch over one odd path. The handoff
leaves this case to GREEN explicitly.

The `file_stem` fallback in `suffixed` is reachable only by that same input.
Both fallbacks sit on lines the suite executes, so they do not show as
uncovered, but **their values are not exercised by any test** and should not be
read as pinned behaviour. Pinning them belongs to a story that has a criterion
for them.
