---
id: MC-049
title: The crop's side edges carry no page background
slug: the-crop-s-side-edges-carry-no-page-back
epic: EPIC-08
type: fix
status: in-progress
phase: GATES
branch: story/MC-049-the-crop-s-side-edges-carry-no-page-back
depends_on: [MC-052, MC-053] # added 2026-09-24: margin 0 exposes real held-out clips (dark art, MC-053; the viewport stage reading another window, MC-052); see "Parked" in ## Notes
required_gates: [integration]  # AC-1, AC-2, AC-5 are corpus claims only the optional integration gate runs
---

## Context

The user, 2026-09-23: *"The cropping from the side isnt tight enough and I
still see the page on the right and left side."* This reopens the column axis
that `EPIC-07` had closed at 20 of 21, and it overturns
`architecture.md` decision 5's *"a leftover 3 px border is the brief's
accepted cost"* — see [`EPIC-08`](../epics/EPIC-08.md) for why this is a new
epic rather than an amendment of `EPIC-07`.

**What the strip is, measured on 2026-09-23 over the 21 marked `tuning`
entries** (Lead PO probe, outside the repository, reading each column's median
luma and the share of its pixels within `uniform_tolerance` of that median over
the mark's rows; recorded in `## Notes`):

- **The locator is already on the right boundary.** Before `margin::expand`,
  the column rect's edges sit exactly where the columns change from **flat
  page background** (share 1.00) to **textured** (share ≈ 0.1–0.4). On every
  entry checked, *crop gap − 3* equals the number of textured columns outside
  the mark.
- **The visible strip is the margin.** `margin::expand` then adds
  `Tuning::margin_px` = 3 columns on each side, and those three are flat page
  background on every entry — white on a light reader, near-black on a dark
  one. That is the high-contrast band the user sees. 42 of 42 sides carry it.
- **The margin cannot simply go to 0 against today's marks.** Seven entries
  have a crop edge 0–2 px outside the mark, which means the locator is 1–3 px
  *inside* it — and on each of the seven, the mark's own outermost 1–3
  columns are **flat page background** (share 1.00 over every marked row):

  | Entry | Side | flat columns inside the mark |
  |---|---|---|
  | `2025-10-14 23_30_20.png` | right | 3 (all 255) |
  | `Screenshot (2630).jpg` | right | 2 (one at luma 1, one at 11 — see Open question 2) |
  | `Screenshot (67).png` | right | 1 (255) |
  | `Screenshot (70).jpg` | right | 1 (251) |
  | `Screenshot (3187).png` | right | 1 (26) |
  | `2026-01-05 13_33_41.png` | left | 1 (11) |
  | `Screenshot (2708).jpg` | left | 1 (11) |

  Those marks contain page margin against `corpus.md`'s marking rule, which is
  exactly the case MC-027 met on `2026-01-05 13_45_59.png` (five flat columns;
  the user saw the image and **corrected the manifest**, `w` 471 → 466). With
  margin 0 and the marks as they stand, the existing zero-clip test would
  report seven "clips" of nothing but page background.
- **What remains after the margin goes: textured columns outside the mark.** On
  about fifteen entries the page's textured region extends 1–7 columns past
  the mark (17 on `Screenshot (93).jpg`, the diagonal-gutter entry). Its median
  matches the art beside it. Whether the user sees that as "page" is Open
  question 3; this story does not remove it.

The required gate that fails if this story's artifact breaks: `unit`, through
AC-6. The corpus half is `integration`-only, hence `required_gates`.

## Acceptance criteria

A column is **page background** when at least **0.95** of its pixels over the
mark's rows lie within `uniform_tolerance` of that column's median luma.

- **AC-1** — *The bug, reproduced.* Given the 21 marked `tuning` entries, when
  `process_file` runs at `Tuning::default()`, then **no column of the crop that
  lies outside the mark is page background**, on either side. On `cb2deef`
  this fails on **42 of 42 sides** (predicted from the probe; RED confirms the
  count and records it). *Control on the number:* the crop widened by one
  column on one side fails that side on at least 19 of 21 entries per side,
  because the next column out is page background on all but the near-flat
  transition edges (`2025-10-20 15_37_25.png` left, `Screenshot (3538).png`
  both sides; see `## Amendments`). *Control on the metric:* the predicate
  applied to the mark's own first and last columns must call them **not** page
  background on at least 19 of 21 entries after Open question 2's corrections
  (the probe measured art-edge columns at share 0.01–0.73), so a predicate that
  calls everything background is caught as surely as one that calls nothing.
- **AC-2** — *Zero clips, still, against corrected marks.* Given the 21 marked
  `tuning` entries **after the manifest corrections the user approves under
  Open question 2**, when `process_file` runs, then every `Cropped` rect
  contains its mark — **0 clips**. *Control:* the crop narrowed by one column
  on each side clips on **at least 8** entries (predicted: the seven above plus
  `Screenshot (3538).png`'s left, where the locator already sits on the mark
  edge; RED measures). The existing inset-10 controls in `corpus.rs` and
  `corpus_accuracy.rs` stay as they are.
- **AC-3** — *Flags do not move.* The six MC-026 AC-4 pins stay `Flagged` for
  `Detector(NoBorderFound)`; `2025-02-27 22_46_15.png` is reported, not pinned.
- **AC-4** — *The rows do not move beyond the margin change.* Given the 21
  marked entries, each crop's top and bottom edge moves by exactly the change
  in `margin_px` on that side and no more (clamped at the image edge), and
  still contains the mark.
- **AC-5** — *MC-019's column window still holds.* The existing
  `the_left_and_right_edges_of_every_marked_page_land_inside_the_window` and
  `at_least_nine_corpus_screenshots_in_ten_are_right_on_the_column_axis`
  still pass, reading `margin_px` from `Tuning` as they already do. Leftover
  **textured** columns outside the mark are therefore at most 8 on at least
  19 of 21, which the probe predicts as at most 7 on 20 of 21 with
  `Screenshot (93).jpg` the named diagonal exception.
- **AC-6** — *On synthetic images (required `unit` gate).* Given a generated
  page column of textured art on a flat background, when `detect` runs at
  `Tuning::default()`, then the crop's left and right columns are exactly the
  art's first and last columns. Given the art's outermost column just on the
  art side of the rule the locator already applies to call a column page
  margin, it is **kept**; just on the other side, it is **trimmed** — pinning
  the boundary to the existing rule and its settled constant (RED reads which
  rule and which constant out of `trim.rs` / `flat.rs`; it does not invent a
  new one). *Control:* at `margin_px` = 1 the
  first case fails by one column on each side.
- **AC-7** — *The record.* `architecture.md` decision 5 and the `Tuning` table
  carry the new value with the user's words and date; `product-brief.md`
  §4 item 2 carries the amendment; `corpus.md` records each corrected mark
  with its old and new rectangle and the user's ruling, as MC-027 did for
  `13_45_59`.

## Amendments

### AC-1, the widened-by-one control: approved by the user, 2026-09-23 (RED)

- **Was:** "the crop widened by one column on one side fails that side on
  **21 of 21** entries, because the next column out is page background on
  every one."
- **Now:** "... fails that side on **at least 19 of 21 entries per side**,
  because the next column out is page background on all but the near-flat
  transition edges."
- **Why:** the Test Developer measured 19 left and 20 right under AC-1's own
  0.95 predicate, at `margin_px` 0. It escalated this rather than loosening
  the predicate. Three edges sit on a near-flat transition column that the
  locator already leaves outside the crop. The orchestrator reproduced this
  independently, with different code (PowerShell `System.Drawing`, luma
  0.299/0.587/0.114, every row of the mark, median ± 10):

  | Entry | Widened column | Share |
  |---|---|---|
  | `2025-10-20 15_37_25.png` | 1006, left | 0.937 |
  | `Screenshot (3538).png` | 974, left | 0.864 |
  | `Screenshot (3538).png` | 1571, right | 0.888 |
  | `2025-10-20 15_37_25.png` (the next column out) | 1005 | 1.000 |
  | `2025-10-14 23_29_06.png` (a normal side, for comparison) | 1002 | 1.000 |

- **Approved by:** the user, 2026-09-23. Offered the choice between this
  and loosening the predicate to 0.85 so the count reads 21/21, they chose
  this one. The page-background predicate and AC-1's main assertion are
  unchanged.

## Model guidance

Model: default.

**Oracle partition.**

- **Settled — read out.** `uniform_tolerance` 10; the column locator as MC-027
  built it (it is not being changed); MC-019's window; the six flag pins; the
  marking rule; the user's rulings on the corrected marks once given — those
  are a person's call and are copied, not re-derived.
- **Evidence, not settled — verify.** The probe's two claims: that the
  pre-margin locator sits on the flat/textured boundary on all 21, and that
  the margin columns are page background on 42 of 42 sides. RED re-measures
  both from `process_file`'s output before writing the corpus tests.
- **Oracle-free — invent, with a control.** The page-background column
  predicate is written here (0.95 share within tolerance); RED may sharpen it
  but must keep AC-1's two controls and report both measured values.
- **Mechanical.** The margin value once the user answers Open question 1, the
  corrected marks, AC-3's names.

**Success condition, which can come out either way.** If RED's re-measurement
finds the pre-margin locator **inside** the art (a textured column trimmed) on
any entry whose mark is *not* one of the seven above, the premise is wrong —
margin 0 would clip real art — and the story stops for re-planning rather than
reaching for a smaller margin. Record the verdict in `## Notes`.

## Out of scope

- **Textured columns beyond the mark** (1–7 px on ~15 entries). Open
  question 3; if the user says they are page, that is a new story with a
  locator change, not this one.
- **`Screenshot (93).jpg`'s +17 on the right** — the diagonal-gutter entry,
  MC-019's accepted column miss (`corpus.md`).
- Any change to the column **locator** (MC-027's `page_column`). This story
  changes the margin and the marks, nothing else.
- The row axis beyond AC-4 — `EPIC-07`, [MC-048](MC-048.md), [MC-050](MC-050.md).
- Re-marking anything other than the flat columns listed in `## Context`, and
  only as the user rules.

## Open questions — answer before RED

**Answered 2026-09-23: the user accepted every recommended default below, before RED. For MC-049, `Screenshot (2630).jpg`'s 1 px dark column still needs the user's look when the marks are corrected.**

1. **The margin.** Does `margin_px` go to **0 on all four sides**, or to 0 on
   the columns only with the rows keeping 3? *Recommended: 0 on all four.* On
   the columns the margin only ever adds flat page background (measured); on
   the rows it only adds three rows of whatever sits above and below —
   gutter today, and after MC-048 and MC-050 the furniture those stories
   remove — while the row edges sit 97+ px from any art, so it protects
   nothing there. One constant also keeps `Tuning` simple. The cost: decision
   5's safety net is gone, and never-clip on the columns rests on the
   locator's boundary being exact, which is why Open question 4 matters.
2. **The seven marks.** May the manifest be corrected to exclude the flat
   columns listed in `## Context`, on the MC-027 precedent? *Recommended: yes,
   with the images open*, one ruling per entry. One needs a real look:
   **`Screenshot (2630).jpg`**'s inner flat column is uniform at luma **1**
   against a page background of **11** — within `uniform_tolerance`, so the
   locator trims it, but it may be a one-pixel dark panel border that belongs
   to the art. If the user says it is art, that entry keeps its mark, the
   locator clips it by one column at margin 0, and the story stops (the
   success condition above fires).
3. **The textured columns outside the mark.** On ~15 entries the crop will
   still carry 1–7 columns that are textured, not page background, and that
   the mark excludes. Are these what you meant by "the page on the right and
   left side"? *Recommended: look at `2025-10-14 23_29_06.png` (5 left, 7
   right) after this story lands and decide then*; the white or black band is
   by far the more visible artefact, and removing textured columns needs a
   different locator.
4. **Held-out.** Same question as MC-048's Open question 2: one aggregate
   zero-clip run over the 24 marked held-out entries at the end of GATES,
   counts only. *Recommended: yes* — dropping the margin is precisely the
   change that could clip an entry no one has tuned against.

## Test plan

Two new targets. Both are self-contained and neither touches `corpus.rs`, so
there is nothing to merge with MC-048. Seven existing test files are edited
because they encoded `margin_px = 3` as a literal.

**Unit: `crates/core/tests/page_edges.rs` (new; AC-6, required `unit` gate).**
The rule that makes a column page margin, read out of the source:
`flat::page_column` keeps the widest run of columns whose spread (MAD over
`central_band`) is `>= Tuning::min_line_spread` (8.0,
`edges::widest_textured_run`). The fixture is MC-027's page-in-margins
geometry, with speckled page margins and a seam that makes `textured_box`
stand down. The one change is that the art ramps up from exactly
`min_line_spread` at columns 40 and 259. Its twin nudges one pixel in each
of those columns, which puts their spread at `8 - 1/144`.

| Test | Asserts | AC |
|---|---|---|
| `the_fixtures_outermost_art_column_sits_exactly_on_the_rule_and_its_twin_just_below_it` | premise: edge columns have spread exactly 8.0 (twin: in (7.99, 8)), inner art is above 8, page margins are below it, the seam is the only strong column line, nothing is peeled | AC-6 premise |
| `at_the_default_tuning_the_crop_edges_are_the_arts_own_first_and_last_columns` | `detect` at default: columns `(40, 259)`, the art's own first and last | AC-6 s.1, "kept" |
| `an_outermost_column_just_below_the_rule_is_trimmed_as_page_margin` | twin at default: `(41, 258)` | AC-6 "trimmed" |
| `the_side_boundary_is_min_line_spread_read_from_the_tuning` | kept fixture with `min_line_spread.next_up()`: `(41, 258)`. The boundary is that constant | AC-6 "the existing rule and its settled constant" |
| `at_a_one_pixel_margin_the_crop_is_one_column_wide_of_the_art_on_each_side` | kept fixture at `margin_px: 1`: `(39, 260)` | AC-6 control |

**Unit: the margin value (mechanical).** `detect.rs::the_default_margin_px_is_three`
becomes `the_default_margin_px_is_zero`. `process_file.rs` and `formats.rs`
pin the MC-008 fixture's rect at the new margin: `{15,22,120,90}`, the art
alone, where it was `{12,19,126,96}`.

**Unit: margin-parametric fixtures (no change in meaning).** `decide.rs`'s
five low-content fixtures and the `band_over_art` area assertion now take
their size from `Tuning::default().margin_px`. They used to have literals
that only worked at 3: `bordered(74, 74, 163, 163)` and so on, and
`141_856`. The detected rects keep the exact geometry each test names. In
`headless.rs`, four `(126, 96)` output dimensions become `crop_dims()`,
derived from `common::crop_rect()` like the file's `rect()` already is.
Against a candidate at margin 0 these fail 13 tests if left alone: decide 9,
headless 4. With the change they pass at both margins.

**Unit: `corpus_manifest.rs::MARKED_TUNING_RECTS`.** The seven corrected
rects. This is the pin forcing a visible edit, as designed.

**Integration: `crates/engine/tests/corpus_sides.rs` (new; `#[ignore]`).**

| Test | Asserts | AC |
|---|---|---|
| `no_column_the_crop_keeps_outside_the_mark_is_page_background` | `process_file` at default: no crop column outside the corrected mark is page background (0.95 predicate). Controls: the mark's own first and last columns are not background on >= 19/21; one extra column on a side is caught on >= 19/21 per side | AC-1 + both controls |
| `every_crop_contains_its_corrected_mark_and_one_column_narrower_clips` | 0 clips, all four sides, against the corrected marks; control: crop narrowed by 1 column each side clips on >= 8 | AC-2 + control |
| `the_top_and_bottom_edges_move_by_exactly_the_margin_change` | per entry, `detect` at margin 0, at 3 and at default: both crops' rows are the margin-0 rows grown by their own margin, with test-side clamping. Default crop contains the mark's rows | AC-4 |

**Covered by existing tests, not restated:** AC-3 is
`corpus.rs::the_six_pages_flagged_today_are_still_flagged_for_no_border_found`.
AC-5 is `corpus.rs::the_left_and_right_edges_of_every_marked_page_land_inside_the_window`
and `corpus_accuracy.rs::at_least_nine_corpus_screenshots_in_ten_are_right_on_the_column_axis`.
AC-2's inset-10 controls are unchanged. AC-7's `corpus.md` record is written:
`docs/wiki/corpus.md`, "Corrected marks". `architecture.md` and
`product-brief.md` are GREEN's.

## Handoff: RED -> GREEN

<!-- Filled by the Test Developer at the end of RED. This is the ONLY channel
     to the Feature Developer, whose context is fresh. Must contain:
       * the exact command that runs the new tests
       * the failure output, and why it is the RIGHT failure
       * every file touched, and which AC each test covers
       * the EXPORT SHAPE the tests already pin: every module they import, the
         exact exported names and signatures, and the types the assertions
         destructure. Not a suggestion - a test already imports them, so a
         wrong guess is a compile error. Say what the tests do NOT constrain
         too, so it stays the implementer's choice.
       * any test that passed on arrival, and the probe or negative control
         that earns it
       * the EXPECTED VALUE of every negative control, as a table: threshold,
         candidate range, and the number the control measured. In RED the
         suite fails at import, so no assertion in it has run - the controls
         are claims until GREEN confirms them against the shipped module
       * anything discovered that changes the approach -->

### ESCALATION: AC-1's "control on the number" does not measure 21 of 21

AC-1 says a crop widened by one column fails that side on **21 of 21**,
"because the next column out is page background on every one". Under the
AC's own predicate (0.95 share), that premise is **false on three sides**.
Against the margin-0 crop, the column just outside the locator is a
near-flat transition column:

| Entry | Side | Column | Share within 10 of median | Median |
|---|---|---|---|---|
| `2025-10-20 15_37_25.png` | left | 1006 | 0.94 | 12 |
| `Screenshot (3538).png` | left | 974 | 0.86 | 16 |
| `Screenshot (3538).png` | right | 1571 | 0.89 | 16 |

Measured: **19 of 21 left, 20 of 21 right.** Before this story the same
control reads 21/21 both sides, because at margin 3 the widened column is
the 4th one out, which is solid background. So a test asserting 21/21 would
pass in RED and **fail in GREEN**. That is a trap, and GREEN could not fix it.

**What I did:** kept the AC's predicate at 0.95 verbatim and asserted the
control at `>= 19` per side (`WIDENED_CONTROL_REQUIRED_PER_SIDE`, the same
floor the metric control uses), with the three sides named in the constant's
doc. That differs from the AC text's "21 of 21", **so it needs the product
owner's ruling and an `## Amendments` entry** before this can merge. The
alternative is to "sharpen" the predicate, which Model guidance permits:
any share threshold in (0.73, 0.86] gives 21/21. I did not take it, because
it re-tunes an invented metric to fit a wrong prediction. The one-line
switch is either constant at the top of `corpus_sides.rs`.

This does **not** fire the success condition: those columns are *outside*
the locator, and margin 0 excludes them. That verdict is below.

### Success condition: does not fire (the premise holds)

Re-measured from `detect` at `margin_px: 0` (the pre-margin locator) over
the 21, in a scratch copy outside the repository:

- **The locator is inside the old mark on exactly the seven named entries,
  and nowhere else.** On each of the seven, the trimmed columns are exactly
  the flat ones in `## Context`. Each locator edge column is art (share
  0.02–0.73), and each column it trims is page background (1.00).
- **Pre-margin locator on the flat/textured boundary:** each locator edge
  column is art on 42/42 sides. The column just outside it is background
  (>= 0.95) on 39/42. The other 3 are the transition columns above, flat by
  MAD (the locator's own rule) but not uniform by the 0.95 share.
- **Margin columns are background:** at margin 3 all three margin columns
  are background on 39/42 sides, and at least one is on 42/42 (hence AC-1's
  42/42 red).

### Commands

```
cargo test --workspace                                         # unit: AC-6 + margin pins
cargo test -p cropper-core --test page_edges                   # AC-6 alone
cargo test -p cropper-engine --release --test corpus_sides -- --ignored --nocapture   # AC-1, AC-2, AC-4
cargo test --workspace --release -- --ignored                  # the integration gate
```

### Failure output (RED, this tree: `cb2deef` + test edits, margin 3)

Unit: 6 tests, all off by exactly the margin. `cargo test --workspace --no-fail-fast`:

```
test the_default_margin_px_is_zero ... FAILED
  assertion `left == right` failed: MC-049: the default margin is 0 on all four sides ...
  left: 3
 right: 0
test at_the_default_tuning_the_crop_edges_are_the_arts_own_first_and_last_columns ... FAILED
  the crop must start on the art's first column (40) and end on its last (259), with no page background on either side; margin_px is 3
  left: (37, 262)
 right: (40, 259)
test an_outermost_column_just_below_the_rule_is_trimmed_as_page_margin ... FAILED
  columns 40 and 259 sit just below min_line_spread (8), so they are page margin by the locator's own rule and the crop must start at 41 and end at 258; margin_px is 3
  left: (38, 261)
 right: (41, 258)
test the_side_boundary_is_min_line_spread_read_from_the_tuning ... FAILED
  at min_line_spread 8.000001 the edge columns (spread exactly 8) are below the rule and must be trimmed; margin_px is 3
  left: (38, 261)
 right: (41, 258)
test every_container_of_the_fixture_decides_to_crop_the_same_rect ... FAILED   (formats.rs)
  MC-008's rect: the art plus a margin of 3
  left: Rect { x: 12, y: 19, w: 126, h: 96 }
 right: Rect { x: 15, y: 22, w: 120, h: 90 }
test the_fixture_decides_to_crop_the_art_plus_the_margin ... FAILED           (process_file.rs)
  the settled default margin  left: 3  right: 0
```

Integration, `corpus_sides` run directly (AC-4 passes, see below):

```
no_column_the_crop_keeps_outside_the_mark_is_page_background:
AC-1: the crop must carry no page background outside the mark, on either side. 42 of 42 sides do
(margin_px is 3); the first is 2025-08-05 00_11_13.webp left: page-background columns [950, 951, 952]
outside the mark's 958.
every_crop_contains_its_corrected_mark_and_one_column_narrower_clips:
AC-2's control: the crop narrowed by one column on each side must clip on at least 8 entries, or the
crop still carries slack beside the art and 'zero clips' is measured through a margin. It clipped on
0 ([]); margin_px is 3.
test result: FAILED. 1 passed; 2 failed
```

`bash scripts/gates.sh --gate integration` fails **before** reaching
`corpus_sides`. Cargo stops at the first failing target, and that target
is an existing MC-019 control that the corrected marks turn red at margin 3:

```
widening_every_expected_rect_one_pixel_on_the_columns_makes_the_zero_clip_check_fail:
AC-1's control did not fire: widening every expected rect by 1 px on the left and right made 0 of 21
compared crops fail the containment check ... Column headroom ... smallest first:
       2 px  2025-10-14 23_30_20.png  ... (nine entries at 2 px)
--- gate summary ---
FAIL         integration (required by story MC-049) (122s, exit 101)
```

That is the right failure. With the marks corrected, the only thing
between the crop and the mark is the 3 px margin. `corpus.rs`'s 9 tests
pass (the floor target is unaffected).

`bash scripts/gates.sh --fast`: format PASS, lint PASS (clippy `-D warnings`
over the new tests), typecheck PASS, unit FAIL (the 6 above), coverage FAIL
(same tests, instrumented). No timeout, config or lint failure.

**Why these are the right failures:** every red is the 3-column margin. The
unit crops sit exactly 3 wide on each side. The corpus sides carry 3 (or
1–2) columns of share-1.00 background. The narrowed crop cannot clip
through 3 columns of slack. Nothing fails to compile or import.

### What GREEN should expect (measured against a candidate)

I measured a scratch copy (`git archive HEAD` plus these test edits, outside
the repository, target dir under the ignored `target/mc049-scratch`) with
**one production change: `margin_px: 3` → `0` in `Tuning::default()`**.
Results: `cargo test --workspace` all green (43 targets ok);
`cargo test --workspace --release -- --ignored` all green (corpus 9,
corpus_accuracy 5, corpus_sides 3, perf 1). GREEN needs nothing else in
source. The rest of GREEN is AC-7's docs: `architecture.md` decision 5 and
the `Tuning` table, `product-brief.md` §4 item 2, and the `margin.rs` /
`lib.rs` doc comments that say 3.

### Files touched

| File | What | AC |
|---|---|---|
| `crates/core/tests/page_edges.rs` (new) | 5 tests above | AC-6 |
| `crates/engine/tests/corpus_sides.rs` (new) | 3 tests above | AC-1, AC-2, AC-4 |
| `crates/core/tests/detect.rs` | default-margin pin 3 → 0 | margin value |
| `crates/engine/tests/process_file.rs`, `formats.rs` | fixture rect pin → `{15,22,120,90}` | margin value |
| `crates/core/tests/decide.rs` | fixtures + one area sized from `margin_px` | none new; keeps AC-3/4 of MC-007 valid at 0 |
| `crates/app/tests/headless.rs` | `(126, 96)` → `crop_dims()` | none new; same |
| `crates/engine/tests/corpus_manifest.rs` | `MARKED_TUNING_RECTS`: seven rects | corrected marks |
| `fixtures/corpus/manifest.json` | seven rects (table in `corpus.md`) | AC-2 (corrected marks) |
| `docs/wiki/corpus.md` | "Corrected marks" section: old/new rect, user rulings | AC-7 (corpus half) |

### Export shape the tests pin

Nothing new. All of it already exists and must not change:
`cropper_core::{Luma, Rect, Tuning, detect}`, `Tuning { margin_px: u32,
min_line_spread: f32, uniform_tolerance: u8, .. }` built by struct-update,
`cropper_core::flat::central_band(Rect, &Tuning) -> Rect`,
`cropper_core::edges::{col_profile, strong_lines}` (`Line { start, end }`),
`cropper_engine::{process_file, Outcome}` (`Outcome::Cropped { rect, .. }`,
`Flagged { reason, .. }`, `Failed { error }`), `cropper_engine::codec::to_luma`.
**Not constrained:** how the margin reaches 0, whether `margin::expand`
stays (the tests only read `Tuning::default().margin_px` and the rects),
and any doc wording.

### Tests that pass on arrival, and what earns each one

| Test | Why green now | What earns it |
|---|---|---|
| `corpus_sides::the_top_and_bottom_edges_move_by_exactly_the_margin_change` | at margin 3 the default and the "before" crop are the same | **Probe, run.** Scratch copy at margin 0, `margin::expand`'s `let y = rect.y.saturating_sub(by)` → `saturating_sub(3)` (rows ignore the tuning). Output: `AC-4: the rows may move by the change in margin_px and nothing else. 21 entries moved otherwise: 2025-08-05 00_11_13.webp: located rows (15, 1440); at margin 3 (15, 1440) (want (12, 1440)); ...` Reverted; `grep` confirms `saturating_sub(by)` restored. |
| AC-2's zero-clip half (same test as the red control) | margin 3 hides everything | **Probe, run.** Margin 0, `let x = rect.x.saturating_sub(by) + 1` (left edge one column in): `3 of 21 clip; the first is 2026-01-05 13_33_41.png: crop 1075,4 397x1436 does not contain the mark 1074,233 396x1060` (also 2708, 3538). Reverted. |
| `page_edges::the_fixtures_outermost_art_column_sits_exactly_on_the_rule...` | fixture premise, test-side arithmetic | It pins the fixture, not the code. It would fail if `min_line_spread` stopped being a whole number or the band went odd |
| `page_edges::at_a_one_pixel_margin_...` | explicit `margin_px: 1` | Negative control for the default-tuning test. It shows that assertion is tight to one column |
| `decide.rs` (margin-parametric fixtures), `headless.rs` (`crop_dims`) | green at 3 and at 0 | They are the same assertions at a margin-independent size. Measured: at margin 0 **without** these edits, 13 of them fail (decide 9, headless 4). With them, 0 fail |
| AC-3 / AC-5 existing tests | unchanged; MC-026, MC-027 and MC-019 earned them | Measured at margin 0: 6 flags still `NoBorderFound`; window 20/21 (only `Screenshot (93).jpg` misses, +17 right); MC-019 26/28 = 92.9 %, misses `(93)` and `2025-02-27 22_46_15.png` |

Existing tests whose outcome changes **today** from the corrected marks
alone: one. `corpus_accuracy::widening_every_expected_rect_one_pixel...`
goes red at margin 3 (above) and green at 0. At margin 0 it fires on 9
entries, where before it fired on 1. `MARKED_TUNING_RECTS` would have gone
red if not updated. Every other test keeps its outcome.

### Negative controls: thresholds and measured values

All measured by running the suite against this tree (margin 3) and against
the margin-0 scratch candidate. **GREEN confirms the margin-0 column against
the shipped change.**

| Control | Threshold | Margin 3 (today) | Margin 0 (candidate) |
|---|---|---|---|
| AC-1 metric: mark's first and last columns NOT background | >= 19 of 21 | 21 (14 with the uncorrected marks) | 21 |
| AC-1 number: one extra column caught, per side | >= 19 per side (**escalated**, AC says 21) | 21 L / 21 R | **19 L / 20 R** (misses: `15_37_25` L, `3538` L+R) |
| AC-1 main: sides carrying background outside the mark | 0 | 42 of 42 | 0 |
| AC-2 control: crop narrowed by 1 clips | >= 8 | 0 | **9**: the seven + `3538` L + `13_45_59` R |
| AC-2 main: clips | 0 | 0 | 0 |
| AC-4 clamped entries (so clamping is exercised) | printed | 11 | 11 |
| MC-019 widening control (existing) | >= 1 | 0 (red) | 9 |
| AC-6 control: margin 1 → `(39, 260)` | exact | `(39, 260)` | `(39, 260)` |
| AC-6 edge-column spread, kept / twin | = 8.0 / in (7.99, 8) | 8.0 / 7.993 | same |

### Anything that changes the approach

- The implementation is one constant. The seven existing-test edits above
  are RED's, so GREEN touches no test.
- MC-048 is concurrent. If it moves a stage before `page_column`, the rows
  the central band is measured over change. MC-048's AC-8 pins the columns,
  and that is what keeps this story's numbers valid. Re-run `corpus_sides`
  after rebasing on it.
- `corpus_sides.rs` sorts after `corpus.rs`, so the `integration` floor
  (read off `corpus.rs`, 9) does not move.

## Regressions

<!-- REQUIRED if this story ever returned to RED after GREEN or GATES; omit
     otherwise. A test that is wrong is never edited into passing, and the
     return is not a footnote - it is the story failing to be one clean cycle,
     and the next person needs to know why. One block per return:
       * which test, what it asserted, and what was wrong with it
       * how the defect was found
       * what it asserts now
       * what earns it, since "watched it fail" cannot apply once the
         implementation exists - the corrected assertion passes on its first
         run and every run after, whether or not it asserts anything: either a
         PROBE (mutate the specific behaviour the test pins, paste the red,
         confirm the revert) or, where the defect was cost rather than
         correctness, a BEFORE/AFTER measurement taken under the gate command -
         not the plain test command, which is the faster one.
         PASTE THE OUTPUT. check-boundaries.sh refuses a PR whose Regressions
         or Gate probes section describes a failure without showing one
       * whether GREEN was a no-op, and the command output proving the source
         was untouched and still passes -->

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-24T05:11:29Z
    commit: 0f94c75 (working tree had uncommitted changes)
    tree:   58b0f54b6a64f822c1b5f2ced91b3553947bd7c5
    result: pass (7 ran, 0 unconfigured, 0 known)

    PASS         format (1s)
    PASS         lint (10s, observed -)
    PASS         typecheck (5s, observed -)
    PASS         unit (40s, observed 47, floor 47)
    PASS         coverage (76s, observed 1717)
    PASS         integration (179s, observed 9, floor 9)
    PASS         build (96s, observed -)

## Gate probes

<!-- REQUIRED if this story adds or changes a gate, its command, or its
     evidence line. Omit the section entirely otherwise.
     A gate that has never been observed to fail is not a gate: break the thing
     it guards, run the gate, paste the failure, revert. One block per gate:
       * what was broken, and where
       * the gate output proving it failed
       * confirmation the probe was reverted -->

## Scaffold inventory

<!-- REQUIRED for a bootstrap or chore story that writes production code under
     SCAFFOLD, where nothing forces a test to exist first, and for a spike that
     commits its throwaway code. Omit otherwise.
     One line per production file written, and for anything with behaviour
     rather than configuration, the test that covers it:
       src/core/palette.ts        - src/core/palette.test.ts
       vite.config.ts             - configuration, no behaviour
     check-boundaries.sh refuses the PR if any changed source file is not
     named here. -->

## Notes

**2026-09-23, Lead PO, at PLANNED — the side probe behind `## Context`.**
Release build of `cb2deef`, headless `--no-gui`, 28 `tuning` entries. Gap =
crop edge minus mark edge, positive = page left in; the crop includes
`margin_px` = 3.

```
file                       leftgap rightgap   textured cols outside mark (L / R)
2025-08-05 00_11_13.webp         8      7     5 / 4
2025-08-05 00_11_27.webp         5      6     2 / 3
2025-10-14 23_29_06.png          8     10     5 / 7
2025-10-14 23_30_20.png         10      0     7 / mark has 3 flat cols
2025-10-20 15_37_25.png          4      6     1 / 3
2026-01-05 13_33_41.png          2      5     mark has 1 flat col / 2
2026-01-05 13_45_59.png          4      3     1 / 0
2026-01-05 13_49_39.png          8      6     5 / 3
Screenshot (67).png              5      2     2 / mark has 1 flat col
Screenshot (70).jpg              9      2     6 / mark has 1 flat col
Screenshot (75).png              7      6     4 / 3
Screenshot (93).jpg              7     20     4 / 17 (diagonal gutter)
Screenshot (103).jpg             7     10     4 / 7
Screenshot (1661).png            7      6     4 / 3
Screenshot (2582).jpg            5     10     2 / 7
Screenshot (2630).jpg            6      1     3 / mark has 2 flat cols
Screenshot (2698).jpg            4     10     1 / 7
Screenshot (2708).jpg            2     10     mark has 1 flat col / 7
Screenshot (2744).jpg            5     10     2 / 7
Screenshot (3187).png            6      2     3 / mark has 1 flat col
Screenshot (3538).png            3      9     0 / 6
```

The third column is read from a second probe: per column, the median luma and
the share of pixels within 10 of it over the mark's rows, from 10 columns
outside each mark edge to 2 inside. Page background reads share 1.00 (luma
255, 251–254, 26, 24–25 or 10–12 depending on the reader); art reads
0.01–0.73. On every entry the boundary between the two sits exactly
`gap − 3` columns outside the mark. Both probes are throwaway code outside the
repository (scratchpad `sideprobe/`, built with `CARGO_TARGET_DIR` under the
worktree's ignored `target/`); nothing here is a committed test, and RED
re-measures.

### Orchestrator reproduction of the mark-overreach claim (PLANNED, 2026-09-23)

The orchestrator checked this independently, with different code and a different
statistic: PowerShell `System.Drawing` reading raw pixels, and the mean and
standard deviation of grey level for each column over the mark's rows, sampling
every second row. None of the probe under `target/sideprobe` was used.

| Entry | Column | Role | Mean | SD |
|---|---|---|---|---|
| `Screenshot (67).png` | 1534 | last detector column | 122.9 | 111.0 |
| `Screenshot (67).png` | 1535 | **last mark column** | 255.0 | 0.9 |
| `Screenshot (3187).png` | 1559 | last detector column | 61.1 | 45.6 |
| `Screenshot (3187).png` | 1560 | **last mark column** | 27.0 | 0.0 |

On both entries, the mark's outermost column is plain page background, flat to
within sensor noise. The column just inside it is art. A 0 px margin would
therefore "clip" these marks by exactly the one background column. That is a
marking error, not missing art, so the claim reproduces on 2 of the 7 entries.
The other five were not re-measured; Open question 2 has the user look at them.

### User ruling on `Screenshot (2630).jpg`, Open question 2 (PLANNED, 2026-09-23)

The user was shown two renders of the mark's right edge: an 8x
nearest-neighbour zoom of columns 1560–1607 over rows 600–759, and the edge at
1x over the full mark height. Column statistics over the mark's rows 224–1112,
measured by the orchestrator with `System.Drawing`:

| Column | Mean luma | Min | Max | Role |
|---|---|---|---|---|
| 1589 | 204.0 | 6 | 241 | art |
| 1590 | 70.2 | 2 | 139 | the grey step down from the white art |
| 1591 | 3.1 | 2 | 141 | dark line, inside the mark |
| 1592 | 12.2 | 12 | 13 | flat, inside the mark |
| 1593–1597 | 12.4–13.1 | 12 | 14 | page background, outside the mark |

Asked whether column 1591 is art (the panel's own black border) or page
background, the user ruled it **page background**. The mark is corrected to
drop both 1591 and 1592 (`w` 635 → 633), on the MC-027 precedent. The success
condition's stop does not fire for this entry.

### Success-condition verdict (RED, 2026-09-23)

**Does not fire.** The pre-margin locator (`detect` at `margin_px: 0`) sits
inside the old mark on exactly the seven entries in `## Context`, and nowhere
else. On each of them the columns it leaves out are the flat ones the user
approved removing: 1.00 share, and for `Screenshot (2630).jpg` both 1591 and
1592, per the user's ruling. Everywhere else the locator lands 0–7 columns
outside the mark (17 on `Screenshot (93).jpg`), always on a textured column.
The corrected marks and the measurements behind them are in
`docs/wiki/corpus.md`, "Corrected marks". One prediction in AC-1's control
did not hold (19/20 of 21 against "21 of 21"). It is escalated in
`## Handoff`, and it is not a clip.

### GREEN (2026-09-24)

**Source change: exactly the one RED predicted.** `margin_px: 3` → `0` in
`Tuning::default()` (`crates/core/src/lib.rs`). `margin::expand` and the
column locator are untouched. The rest is doc comments and AC-7:

- `crates/core/src/margin.rs`: the module doc no longer says "3 by default".
  It now records the 0, the user's words and date, and what the safety net
  gives up.
- `crates/core/src/lib.rs`: the `margin_px` field doc carries the new value.
  The `min_content_fraction` doc also gets a note. Its ceiling (0.080186,
  `Screenshot (93).jpg` at 252x1173) was measured on the mark *plus* the
  margin, and at margin 0 it is 246x1167 = 0.077876. 0.05 is still 1.56x
  below it, and the value does not move. The corpus test that measures this
  prints `Screenshot (93).jpg 246x1167 0.07788 admit`.
- `docs/wiki/architecture.md`: decision 5 is rewritten, and the `Tuning`
  table reads 0 (MC-049), with the user's words and 2026-09-23.
- `docs/wiki/product-brief.md`: §4 item 2 has an "Amended 2026-09-23"
  paragraph in the brief's existing amendment style. Open question 3 got a
  one-line pointer to it.
- `docs/wiki/corpus.md` (written in RED): checked. All seven entries are
  there, and each old→new rect matches `git diff fixtures/corpus/manifest.json`
  exactly. The general approval and the separate `Screenshot (2630).jpg`
  ruling are both recorded.

**Tests.** `cargo test --workspace --no-fail-fast`: 43 targets, 434 passed,
0 failed, 18 ignored (RED: 6 failed). `cargo test --workspace --release
--no-fail-fast -- --ignored --nocapture`: exit 0, corpus 9/9,
corpus_accuracy 5/5, corpus_sides 3/3, perf 1/1 (RED: 3 corpus failures).

**Negative controls, against the shipped change.** Most are read from the
tables the ignored tests print under `--nocapture`. The metric control prints
its count only in its failure message. It was measured from a throwaway copy
of the workspace (outside the repository, target dir under the ignored
`target/`, deleted afterwards). The only edit in that copy was the assertion
`metric_ok >= METRIC_CONTROL_REQUIRED` → `metric_ok >= 22`, to force the
message. Its output: `...must call them not page background on at least 19
of 21 entries; it did on 21.`

| Control | Threshold | RED-predicted (margin-0 candidate) | Measured (shipped) |
|---|---|---|---|
| AC-1 metric: mark's first and last columns NOT background | >= 19 of 21 | 21 | **21** |
| AC-1 number: widened by one column, caught per side | >= 19 per side (amended) | 19 L / 20 R (misses `15_37_25` L, `3538` L+R) | **19 L / 20 R**, the same three misses |
| AC-1 main: sides with background outside the mark | 0 | 0 | **0** (every row `bg 0`) |
| AC-2 control: crop narrowed by 1 clips | >= 8 | 9 (the seven + `3538` L + `13_45_59` R) | **9**, the same nine |
| AC-2 main: clips | 0 | 0 | **0** (21 `holds`) |
| AC-4 clamped entries | printed | 11 | **11** |
| MC-019 widening control (existing) | >= 1 | 9 | **9** (the same nine) |
| MC-019 column window (AC-5) | existing | 20/21, `(93)` +17 R misses | **20/21**, `(93)` +17 R the only MISS |
| MC-019 accuracy | >= 9 in 10 | 26/28, misses `(93)` and `2025-02-27 22_46_15.png` | **26/28**, the same two |
| AC-3 flag pins | 6 `NoBorderFound` | 6 | **6**; `22_46_15` reported (`Cropped 75,6 219x850`) |
| AC-6 margin-1 control | `(39, 260)` | `(39, 260)` | passes (exact assertion) |

Nothing diverges from RED's candidate. That is expected: RED's candidate
*was* this one-constant change.

**`bash scripts/gates.sh --fast`** (not recorded; a partial run):

```
PASS         format (3s)
PASS         lint (15s, observed -)
PASS         typecheck (6s, observed -)
PASS         unit (29s, observed 47, floor 47)
PASS         coverage (79s, observed 1717)
--fast skipped: integration build
All required gates passed (5 ran, 0 unconfigured, 0 known).
```

The full `bash scripts/gates.sh` has not been run yet. It is GATES's, and
`integration` is this story's required gate.

### Orchestrator verification (GREEN, 2026-09-24)

These checks were run by the orchestrator, separately from the subagents.

- **The source diff** is `margin_px: 3` → `0` in `Tuning::default()`, plus doc
  comments. The column locator is untouched.
- **RED re-run by the orchestrator:**
  - the unit suite had 6 failures, each off by exactly 3 per side;
  - the integration suite had 3 corpus failures: AC-1, the AC-2 control, and
    the MC-019 widening control, which the corrected marks turned red at
    margin 3.
- **Boundary mutation, AC-6.** The `min_line_spread` comparison in
  `widest_textured_run` (`edges.rs`, the one `page_column` uses) was changed
  from `>=` to `>`. It was applied with an exact-string edit and reverted with
  `git checkout`, after which `cargo test -p cropper-core` had 0 failures.
  - **Prediction:** the art column sitting exactly on 8.0 is trimmed, so
    `at_the_default_tuning_the_crop_edges_are_the_arts_own_first_and_last_columns`
    goes red, while `an_outermost_column_just_below_the_rule_is_trimmed_as_page_margin`
    and `the_side_boundary_is_min_line_spread_read_from_the_tuning` stay green.
  - **Measured:**
    ```
    page_edges: 3 passed; 2 failed
      at_a_one_pixel_margin_the_crop_is_one_column_wide_of_the_art_on_each_side ... FAILED
      at_the_default_tuning_the_crop_edges_are_the_arts_own_first_and_last_columns ... FAILED
    flatness: 25 passed; 4 failed (existing MC-027 tests of the same comparison)
    ```
  - **Reading:** the second red is the AC-6 margin-1 control. It rests on the
    same kept edge column, and it was simply not in the orchestrator's
    prediction. The two "trimmed" tests stayed green as predicted, so AC-6 pins
    the boundary from both sides.
- An earlier attempt at this mutation matched two sites, so the edit refused
  and nothing changed. That run tested unmodified code and was discarded.

### Held-out zero-clip check, Open question 4 (GATES, 2026-09-24): FAILS, story held at GATES

This is one aggregate run over the 24 marked `held-out` entries. It reports
counts and unattributed side:depth pairs only, never a per-file line. The
script is `heldout_clips.sh` in the orchestrator's scratchpad. The baseline is
a clean build of `origin/main` (`0f94c75`, `margin_px` 3), built into the
git-ignored `target/m3base`.

| Build | Cropped | Not cropped | Clips | Clipped sides (side:px, sorted) |
|---|---|---|---|---|
| `origin/main`, margin 3 | 23 | 1 | **4** | left:3 left:6 right:2 right:5 right:5 |
| MC-049, margin 0 | 23 | 1 | **5** | left:6 left:9 right:1 right:5 right:8 right:8 |

- **v1 already clips 4 of 23 held-out crops, on the column axis, by 2–6 px.**
  Nothing measured this before: the held-out set had no clip check. It is a
  pre-existing defect, not one this story introduced.
- Removing the margin adds one clip (5), and deepens each existing one by the
  3 px that the margin used to absorb.
- Every clip is on the left or right side. None is on the top or bottom.
- It is not yet known whether these are real clips of art or held-out marks
  that include flat page columns, as seven tuning marks did. Answering that
  means looking at the entries. The story is held at GATES pending the user's
  decision.

#### The clipped held-out edges, looked at (user-approved, 2026-09-24)

The user chose to look at the marks. The orchestrator measured each clipped
edge at margin 0: column median and the share of the column within ±10 of its
median, over every second row of the mark. It then rendered each edge at 4x
width over the full mark height.

| Entry | Side | Cut columns | What they are | Verdict |
|---|---|---|---|---|
| `Screenshot (56).png` | right | 1512–1519 (8) | share 1.00, median 255 | **mark error**: 8 columns of white page inside the mark |
| `Screenshot (59).png` | right | 1506 (1) | share 1.00, median 255 | **mark error**: 1 column of white page |
| `2025-07-17 14_41_58.png` | left | 931–936 (6) | dark purple textured art, share 0.81–0.83, the same as the kept art beside it | **real clip** |
| `2025-07-17 14_41_58.png` | right | 1604–1611 (8) | the same art | **real clip** |
| `2025-07-17 14_55_10.png` | right | 1594–1598 (5) | dark red art (chains), share 0.82–0.84 | **real clip** |
| `Screenshot (73).png` | left | 1007–1015 (9) | dark scene art, share 0.84–0.88 | **real clip** |

On all four real clips the art is dark and has little texture at its outer
columns, and the page column locator trims it as page. At margin 3, v1 already
cut these same three entries by 2–6 px; the margin used to absorb part of the
cut. On `14_41_58` both sides and on `14_55_10`, the art visibly extends a few
columns **past** the mark as well, so those marks are slightly tight. That does
not change the verdict: the crop cuts art either way.

### Parked at GATES, waiting on MC-053 (2026-09-24, the user's decision)

The user ruled on the held-out findings above:

- **Correct the two white-page marks.** `Screenshot (56).png` right, `w`
  480 → 472, and `Screenshot (59).png` right, `w` 464 → 463. This is done in
  MC-053, which owns the held-out manifest changes.
- **The real clips are fixed first, in a new fix story, MC-053.** The three
  entries (`2025-07-17 14_41_58.png`, `2025-07-17 14_55_10.png`,
  `Screenshot (73).png`) move from `held-out` to `tuning`, because the fix will
  be designed while looking at them. That leaves 21 marked held-out entries.

MC-049's code and tests are committed on its branch as they stand. All 7 gates
passed on the tuning set, and `## Gate results` records it. When MC-053 is
DONE, merge `main` into this branch, re-run the gates, and re-run the held-out
zero-clip check. Resolve the `fixtures/corpus/manifest.json` conflict by
keeping both sets of corrections. MC-049 then goes to REVIEW only if the
held-out check shows 0 clips at margin 0.

**Renumbered 2026-09-24.** The dark-art fix was drafted as MC-051. Another
session filed a different MC-051 on `main` at the same time (the held-out
scoring spike), so the fix is now **MC-053**. MC-049 also waits on **MC-052**,
which fixes the held-out row clips MC-048 introduced; MC-053 depends on it.
