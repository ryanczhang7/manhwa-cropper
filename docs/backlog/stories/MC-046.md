---
id: MC-046
title: A gate command that reads stdin silently deletes every later gate
slug: a-gate-command-that-reads-stdin-silently
epic: EPIC-07
type: fix
status: done
phase: DONE
branch: story/MC-046-a-gate-command-that-reads-stdin-silently
depends_on: [MC-045] # DONE; listed because this story edits the gate loop MC-045 rewrote
required_gates: []    # no project.conf gate covers the harness; see "Gate decision" in ## Notes
---

## Context

<!-- Why this story exists. Link the epic and any wiki pages that constrain it.
     Name the REQUIRED gate that would fail if this story's artifact broke. If
     only an optional gate can, put it in required_gates above before RED:
     every required gate once passed over a story none of them exercised. -->

The user reported this, and it was first reproduced during [MC-045](MC-045.md)
against `scripts/gates.sh` at b820614. It is harness work filed under
[EPIC-07](../epics/EPIC-07.md), like MC-043 to MC-045, because that is the epic
in flight.

### The defect, in the current code (HEAD 1fa9815, after MC-045)

MC-045 rewrote how the manifest is *parsed* (builtins in place of
`trim`/`cut`). It did not change how the manifest is *read* or how gates are
*run*. The gate loop in `scripts/gates.sh` still reads the manifest on the
loop's stdin (`while IFS= read -r line; do … done < "$CONF"`, ~line 418 to
~line 677), and runs each gate inside that loop without redirecting stdin
(~line 611):

```bash
( cd "$ROOT/$cwd" && eval "$cmd" ) 2>&1 | tee "$log"
```

The gate command therefore inherits the loop's stdin, which is the unread rest
of `project.conf`. A gate that reads stdin (`cat`, a tool that drains its
input, anything that waits for a prompt answer) consumes every later line. The
loop then sees EOF and ends. The later gates never run, never appear in the
summary and are never counted, and nothing reports that they are missing.

### Reproduced at PLANNED (2026-09-23, HEAD 1fa9815)

Fixture from `make_project_fixture` / `write_conf` (`.claude/tests/_lib.sh`),
full run, no flags.

**Case A, the user's report.**

```
gate | eats  | required | . | cat >/dev/null; printf 'Tests 1 passed\n'
gate | later | required | . | printf 'Tests 2 passed\n'
```

```
All required gates passed (1 ran, 0 unconfigured, 0 known).
exit=0
```

`later` does not appear anywhere in the output.

**Case B, worse than reported: a failing required gate is swallowed.**

```
gate | first | required | . | printf 'Tests 2 passed\n'
gate | eats  | required | . | cat >/dev/null; printf 'Tests 1 passed\n'
gate | later | required | . | exit 1
```

```
PASS         first (0s) -- no evidence line: a vacuous pass would go unnoticed
PASS         eats (0s) -- no evidence line: a vacuous pass would go unnoticed
All required gates passed (2 ran, 0 unconfigured, 0 known).
exit=0
```

A required gate that exits 1 turns into exit 0 and "All required gates
passed". A full run with `--story` records that as `pass` in the story's
`## Gate results`, and `check-boundaries.sh` accepts it, because the record is
genuine for a run that silently skipped a gate. `--list` on the same config
shows all three gates, because it never runs a command.

**Case C, `--gate later`, is not affected.** The loop reaches `continue` for
`eats` before running it, so nothing reads the manifest's stdin. Output:
`All required gates passed (1 ran …)`, and `later` is the gate that ran.

### Two traps in the obvious fix, also reproduced at PLANNED

The obvious one-line fix reads the manifest on another descriptor
(`read -u 3 … done 3< "$CONF"`). Applied to a scratch copy of the fixture's
`gates.sh` (never to the repo), it gives this:

| Gate command | Next gate: `later`, required, `exit 1` | Result |
|---|---|---|
| `cat >/dev/null; …` | `FAIL later (exit 1)`, `1 required gate(s) failed.` | fixed |
| `cat <&3 >/dev/null 2>&1; …` | not run; `All required gates passed (1 ran …)`, exit 0 | **still broken** |
| `printf 'got:'; cat` with `printf 'CALLER-DATA\n' \| bash scripts/gates.sh` | the gate's log shows `got:CALLER-DATA` | **the caller's stdin leaks into gates** |

So moving the manifest to fd 3 relocates the defect instead of removing it.
The eval'd command inherits fd 3 and can still drain it. The gate also
inherits whatever `gates.sh` was given on stdin. In a terminal that is the TTY,
where a stdin-reading gate blocks forever, which is the "looks like a hang"
failure MC-043 and MC-044 already lost time to. The criteria below are written
as behaviour so that they reject this fix, not so that they prescribe another
one.

### Which required gate would fail if this broke

None in `project.conf`, and that is expected for harness work. See "Gate
decision" in `## Notes`. The regression tests live in
`.claude/tests/gates.test.sh`, which CI runs through `bash scripts/selftest.sh`
(`.github/workflows/gates.yml` line 37, a blocking step).

## Acceptance criteria

<!-- Each AC is independently testable and phrased as observable behaviour.
     The Test Developer writes at least one failing test per AC.
     An AC that names a statistic, a metric or a threshold names a NEGATIVE
     CONTROL too: the deliberately broken input the metric must reject, and
     roughly what it should score. A criterion can be perfectly testable and
     still be blind to the defect it exists to catch, and that is more
     dangerous than a vague one - it survives review and goes green. -->

All fixtures come from `make_project_fixture` / `write_conf`. Every gate
command is a `printf`/`cat`/`wc` one-liner, so no real toolchain is involved.
"Runs" means the gate's `=== gate: <id>` banner appears and its outcome line
appears in `--- gate summary ---`.

- **AC-1** — *The reported bug, as its permanent regression test.* Given a
  manifest whose first required gate reads all of its stdin
  (`cat >/dev/null; printf 'Tests 1 passed\n'`), when a full `gates.sh` run
  executes, then:
  (a) if the second required gate prints `Tests 2 passed` and exits 0, both
  gates appear as `PASS` in the summary and it reads
  `All required gates passed (2 ran, 0 unconfigured, 0 known).`;
  (b) if the second required gate exits 1, the summary shows `FAIL` for it,
  the run prints `1 required gate(s) failed.` and exits 1.
  *Control:* on today's `gates.sh`, (a) prints `(1 ran, …)`, (b) exits 0 with
  `All required gates passed`, and the second gate's id is absent from the
  output (measured, see Context, cases A and B). Red on arrival.
- **AC-2** — *No inherited descriptor reaches the manifest.* Given a manifest
  whose first required gate tries to drain stdin and every descriptor from 3
  to 9 (`for fd in 0 3 4 5 6 7 8 9; do cat <&$fd >/dev/null; done 2>/dev/null;
  printf 'Tests 1 passed\n'`, or an equivalent that reads each one), followed
  by a required gate that exits 1, when a full run executes, then the second
  gate is reported `FAIL` and the run exits 1.
  *Control:* red on arrival on today's `gates.sh` through fd 0. The criterion
  exists because of its second control, the fd-3-only fix from Context: it
  passes AC-1 and must fail AC-2 (measured: `1 ran`, exit 0). RED shows the
  test tells the two apart. Apply the fd-3-only edit to a **fixture copy** of
  `gates.sh`, confirm that the AC-1 test goes green and the AC-2 test stays
  red, and paste both results into `## Handoff`.
- **AC-3** — *A gate reads end-of-input, not the caller's stdin.* Given a
  manifest with a required gate `printf 'stdin-bytes=%s\n' "$(wc -c | tr -d
  ' ')"` followed by one more gate line, when `gates.sh` is invoked with data
  piped to it (`printf 'CALLER-DATA\n' | bash scripts/gates.sh`), then the
  gate's log (`.claude/state/gate-logs/<id>.log`) contains `stdin-bytes=0`,
  and the run finishes (the test runs it under the suite's normal timeout
  discipline and does not block).
  *Control:* on today's `gates.sh` the gate reads the rest of the manifest, so
  the count is the byte length of the following line(s), not 0. On the fd-3-only
  fix it reads `CALLER-DATA\n`, which is 12 bytes. Both are red. The TTY case,
  where a stdin-reading gate would block forever, cannot be exercised in the
  selftest harness. This criterion is its proxy: a gate that sees end-of-input
  on a pipe also sees end-of-input when the caller is a terminal.
- **AC-4** — *The subsets use the same loop, so they get the same guarantee.*
  Given the AC-1(b) manifest plus a third gate `slow_one` carrying a `slow`
  line with a reason, when `gates.sh --fast` runs, then `later` is reported
  `FAIL`, `--fast skipped: slow_one` is printed, and the run exits 1. And
  when `gates.sh --required` runs on the AC-1(b) manifest, `later` is
  reported `FAIL` and the run exits 1.
  *Control:* on today's `gates.sh`, both runs exit 0 and never mention `later`.
  Red on arrival. (`--gate <id>` is deliberately not here, see Context case C.
  A filtered-out gate hits `continue` before anything runs, so it has no defect
  to reproduce, and a test for it would pass on arrival and earn nothing.)
- **AC-5** — Given `bash scripts/selftest.sh`, when it runs in full after
  GREEN, then every suite passes, and the output tail is pasted into
  `## Notes`. *Control:* `selftest` is a CI step, not a `project.conf` gate,
  and never appears in `## Gate results`. That makes this the story's only
  in-story evidence that the rest of the harness still works: the lock tests,
  the `--list`/`--audit` tests and the MC-045 exec-count tests all run
  `gates.sh` (see "Gate decision" in `## Notes`).

## Amendments

<!-- Acceptance criteria are frozen once the story leaves PLANNED. If one turns
     out to be wrong or unsatisfiable, stop, put it to the product owner, and
     record the change here: which AC, what it said, what it says now, who
     approved it and why. check-boundaries.sh fails a PR whose criteria differ
     from the base branch without an entry here. Omit the section if unused.
     Where the change came from a subagent's claim that the criterion was
     wrong, record the ORCHESTRATOR'S OWN reproduction of it - different
     inputs, not the subagent's code. That claim is also what an agent says
     when it wants to stop failing. -->

## Model guidance

<!-- Optional, written by the Lead PO BEFORE the phase it applies to. Use it
     when a phase of this story is worth running on a different model from the
     default, and make it falsifiable rather than folklore:
       * which phase, which model, and why that phase specifically
       * what the orchestrator should stay on
       * HOW to brief it differently - a model chosen for judgement wants the
         criteria and the constraints, not a pre-decided test design
       * the ORACLE PARTITION of the criteria: which are settled (read the
         numbers out, do not calibrate), which are oracle-free (invent the
         metric and demand a negative control that fires hard), which are
         mechanical (pin exactly). Measured to matter more than the model
       * a success condition that could come out either way
     Then record the VERDICT against that condition when the phase ends, with
     evidence. The verdict is the part that gets skipped, and without it a model
     choice becomes a habit nobody can argue with. -->

No model change: every phase runs on the default. This section exists only to
carry the oracle partition into the RED dispatch.

**Oracle partition.**

- **Mechanical, pin exactly:** AC-1, AC-2, AC-4. The oracle is the summary
  lines, the `(N ran, …)` count and the exit code, and the expected strings
  are quoted in the criteria. Do not loosen them to "contains FAIL" when the
  gate id is available. A summary that says FAIL for the wrong gate must not
  pass.
- **Settled, read the numbers out:** each control's measured value on today's
  code (`1 ran`/exit 0 for AC-1, AC-2 and AC-4, `stdin-bytes=43` for AC-3 on the PLANNED fixture,
  meaning the byte length of the rest of the fixture manifest, and 12 bytes
  under the fd-3-only fix). Those were measured at PLANNED on HEAD 1fa9815. Do
  not re-derive them. If a number differs, report it rather than adjusting the
  test.
- **Oracle-free:** none.

**The one judgement call RED makes.** The AC-2 test must be shown to
discriminate between today's code, the fd-3-only fix and a real fix, using the
fixture-copy mutation that AC-2 prescribes. A test that goes green against the
fd-3-only fix fails the criterion, however it is written.

## Out of scope

<!-- Explicit non-goals. Prevents the Feature Developer from over-building. -->

- **An accounting invariant in the summary** ("refuse when fewer gates ran than
  the manifest declares"). It is a real idea, and it belongs in its own story,
  not here. The reasons:
  1. *It is a different behaviour with a different oracle.* This story makes
     a gate unable to consume the manifest. The invariant makes the summary
     refuse to certify a run whose accounting does not close, whatever the
     cause.
  2. *After this fix it has no reproducible failure to be red against.* The
     only known way to lose a gate line is the one this story removes. A test
     for the invariant would pass on arrival, and it could be earned only by
     mutating `gates.sh` or by adding a fault-injection seam. Choosing between
     those is a design decision this story should not make in passing.
  3. *Its definition is not settled.* The categories it would have to close
     over are `ran`, `unconfigured`, `known`, config-error FAILs that
     `continue` before running (bad `floor`, `ci-factor`, a waiver on a
     required gate, a reasonless `slow`/`no-count`), `--fast` skips,
     `--gate`/`--required` filters and `--list`/`--audit`. Each of these is a
     product decision about which subsets it may assert over. Folding that into
     a one-line fix would make the story two cycles.

  Recommended as a follow-up `fix` or `feature` under the same epic. It is not
  filed yet: the user decides whether they want it.
- **`--gate <id>`.** Not affected, because filtered-out gates `continue` before
  running (Context, case C). No change and no test.
- **`--list` and `--audit`.** Neither runs a gate command, so neither is
  affected. Their output must not change: the MC-045 byte-identical and
  exec-count tests already pin them, and AC-5 runs them.
- **The first parse loop** (the tables, ~line 239). It runs no command, and
  nothing in it can read stdin.
- **Any other change to how a gate runs.** That covers its cwd, the
  `2>&1 | tee` pipeline, `PIPESTATUS`, evidence and floor evaluation, the
  lock and `record_in_story`. The one change is what the gate command's stdin
  and inherited descriptors can reach.
- **Sandboxing gate commands in general.** A gate can still `rm` the manifest,
  `exec` over the shell and so on. `project.conf` is trusted input written by
  the Lead PO. The defect here is an *accidental* read by an ordinary tool,
  not a hostile gate. Descriptors 0 and 3 to 9 are the ones an ordinary shell
  command can reach by accident, and they are the boundary.
- **The `fd 255` script descriptor, the lock's descriptor if any, and other
  high-numbered fds.** A gate does not reach these by accident, so no criterion
  covers them.
- **`lib.sh`, `phase.sh`, `selftest.sh`, `check-boundaries.sh` and
  `project.conf`.** Only `scripts/gates.sh` changes on the source side.

## Design notes

<!-- Filled by the Lead Designer for user-facing stories: layout, states,
     tokens, accessibility requirements. Omit for non-UI stories. -->

## Test plan

<!-- Filled by the Test Developer during RED: which tests, at which level,
     and which AC each one covers. -->

Level: integration. Every test runs the real `scripts/gates.sh`, copied into a
`make_project_fixture` repository, against a `write_conf` manifest whose gate
commands are `printf`/`cat`/`wc` one-liners. The bug is in how the loop wires
a gate's descriptors, so nothing smaller can falsify it. All tests are in
`.claude/tests/gates.test.sh`, in four new `describe` blocks at the end, just
before `summary "gates"`.

Two helpers are local to the block. `gates_isolated` runs `gates.sh` with stdin
from `/dev/null` and fds 3 to 9 closed. That way a gate that drains a
descriptor can reach only what `gates.sh` itself opened, and never the test
process's TTY or pipes, which would hang the suite instead of failing it.
`assert_gate_failed <what> <id>` pins the summary line
`FAIL         <id> (<n>s, exit 1) -> .claude/state/gate-logs/<id>.log`. Only
the duration is left free.

| describe / assertion | AC |
|---|---|
| **"a gate that reads its stdin cannot delete the gates after it"**, manifest `eats` (`cat >/dev/null; printf 'Tests 1 passed\n'`) then `later` (`printf 'Tests 2 passed\n'`) | |
| the gate after a stdin-reading gate still runs (`=== gate: later (required) ===`) | AC-1(a) |
| the stdin-reading gate passes (`PASS         eats (`) | AC-1(a) |
| and so does the gate after it (`PASS         later (`) | AC-1(a) |
| and both are counted as having run (`All required gates passed (2 ran, 0 unconfigured, 0 known).`) | AC-1(a) |
| a run of two passing gates exits 0 | AC-1(a) |
| same block, manifest `eats` then `later` = `exit 1` (the "AC-1(b) manifest") | |
| a failing required gate after a stdin-reading gate is reported FAIL (`assert_gate_failed … later`) | AC-1(b) |
| and the run says a required gate failed (`1 required gate(s) failed.`) | AC-1(b) |
| and does not certify the run (no `All required gates passed`) | AC-1(b) |
| and exits 1 | AC-1(b) |
| **"a gate cannot reach the manifest through any inherited descriptor"**, `eats` = `for fd in 0 3 4 5 6 7 8 9; do cat <&$fd >/dev/null; done 2>/dev/null; printf 'Tests 1 passed\n'`, then `later` = `exit 1` | |
| a gate after one that drains fds 0 and 3-9 still runs and is reported FAIL | AC-2 |
| and the run says a required gate failed / and does not certify the run / and exits 1 | AC-2 |
| **"a gate reads end-of-input, not the manifest and not the caller's stdin"**, `count` = `printf 'stdin-bytes=%s\n' "$(wc -c \| tr -d ' ')"` then `after`, invoked as `printf 'CALLER-DATA\n' \| bash scripts/gates.sh` | |
| a run given data on stdin still finishes (`--- gate summary ---` printed) | AC-3 |
| a gate reads zero bytes of stdin, whatever gates.sh was given (the `stdin-bytes=` line of `count.log` equals exactly `stdin-bytes=0`) | AC-3 |
| **"--fast and --required give the same guarantee as a full run"**, AC-1(b) manifest plus `slow_one` and a `slow` line with a reason | |
| --fast: `later` reported FAIL / `--fast skipped: slow_one` / `1 required gate(s) failed.` / exits 1 | AC-4 |
| --required (AC-1(b) manifest only): `later` reported FAIL / `1 required gate(s) failed.` / exits 1 | AC-4 |

AC-5 is the full `bash scripts/selftest.sh` after GREEN and has no test of its
own.

## Handoff: RED -> GREEN

<!-- Filled by the Test Developer at the end of RED. This is the ONLY channel
     to the Feature Developer, whose context is fresh. Must contain:
       * the exact command that runs the new tests
       * the failure output, and why it is the RIGHT failure
       * every file touched, and which AC each test covers
       * the EXPORT SHAPE the tests already pin: every module they import, the
         exact exported names and signatures, and the types the assertions
         destructure. Not a suggestion - a test already imports them, so a
         wrong guess is a compile error. Say what the tests do NOT constrain
         too, so it stays the implementer's choice.
       * any test that passed on arrival, and the probe or negative control
         that earns it
       * the EXPECTED VALUE of every negative control, as a table: threshold,
         candidate range, and the number the control measured. In RED the
         suite fails at import, so no assertion in it has run - the controls
         are claims until GREEN confirms them against the shipped module
       * anything discovered that changes the approach -->

### Command

```bash
bash scripts/selftest.sh gates
```

This takes about 3.5 minutes on this Windows machine. The HEAD suite takes
2m50s, and the six new `gates.sh` invocations add about 50s. `selftest` has no
timeout.

### Result on today's `gates.sh` (HEAD dcd3c0d, unmodified): 91 passed, 19 failed

Before this story the suite had 88 assertions, all passing. The 22 new ones
split 19 red and 3 green (see "Passing on arrival" below).

```
  a gate that reads its stdin cannot delete the gates after it
    FAIL the gate after a stdin-reading gate still runs
         expected to contain: === gate: later (required) ===
         actual:
         === gate: eats (required) ===
         cat >/dev/null; printf 'Tests 1 passed\n'
         Tests 1 passed

         --- gate summary ---
         PASS         eats (0s, observed 1)

         (not recorded: no active story; use --story <id> to record it in one)

         All required gates passed (1 ran, 0 unconfigured, 0 known).
    FAIL and so does the gate after it
    FAIL and both are counted as having run
    FAIL a failing required gate after a stdin-reading gate is reported FAIL
         expected a summary line: FAIL         later (<n>s, exit 1) -> .claude/state/gate-logs/later.log
         actual:
         === gate: eats (required) ===
         ...
         PASS         eats (1s, observed 1)
         ...
         All required gates passed (1 ran, 0 unconfigured, 0 known).
    FAIL and the run says a required gate failed
    FAIL and does not certify the run
    FAIL and exits 1
         expected: 1
         actual:   0
  a gate cannot reach the manifest through any inherited descriptor
    FAIL a gate after one that drains fds 0 and 3-9 still runs and is reported FAIL
    FAIL and the run says a required gate failed
    FAIL and does not certify the run
    FAIL and exits 1
  a gate reads end-of-input, not the manifest and not the caller's stdin
    FAIL a gate reads zero bytes of stdin, whatever gates.sh was given
         expected: stdin-bytes=0
         actual:   stdin-bytes=57
  --fast and --required give the same guarantee as a full run
    FAIL --fast: a failing gate after a stdin-reading gate is reported FAIL
    FAIL --fast: the slow gate after them is still seen, and skipped
         (actual: "--fast skipped nothing: no gate carries a `slow` line", because the slow line was eaten too)
    FAIL --fast: the run says a required gate failed
    FAIL --fast: and exits 1
    FAIL --required: a failing gate after a stdin-reading gate is reported FAIL
    FAIL --required: the run says a required gate failed
    FAIL --required: and exits 1

gates: 91 passed, 19 failed
```

**Why this is the right failure.** Every red is the defect itself. `later`
never gets a banner or a summary line, the count says `1 ran`, a required
`exit 1` becomes exit 0 with "All required gates passed", and the gate's stdin
carries the rest of the manifest. None of it is a fixture, syntax or harness
error. The 17 existing describe blocks (88 assertions) still pass.

### AC-2 discrimination: the fd-3-only fix on a fixture copy

I copied `scripts/`, `.claude/tests`, `.claude/harness` and `.claude/hooks` into
the scratchpad (`…/scratchpad/fd3/`), so `_lib.sh`'s `REPO_ROOT` resolves to
that copy and `make_project_fixture` copies the mutated `gates.sh`. The repo's
`gates.sh` was never touched. The mutation, as `diff mutated repo`:

```
418c418
< while IFS= read -r -u 3 line; do
---
> while IFS= read -r line; do
677c677
< done 3< "$CONF"
---
> done < "$CONF"
```

Result (`bash .claude/tests/gates.test.sh` in the copy):

```
  a gate that reads its stdin cannot delete the gates after it
  a gate cannot reach the manifest through any inherited descriptor
    FAIL a gate after one that drains fds 0 and 3-9 still runs and is reported FAIL
    FAIL and the run says a required gate failed
    FAIL and does not certify the run
    FAIL and exits 1
  a gate reads end-of-input, not the manifest and not the caller's stdin
    FAIL a gate reads zero bytes of stdin, whatever gates.sh was given
         expected: stdin-bytes=0
         actual:   stdin-bytes=12
  --fast and --required give the same guarantee as a full run
gates: 105 passed, 5 failed
```

So the AC-1 block goes green (and AC-4 with it), while the AC-2 block stays
red, as does AC-3 at 12 bytes, which matches PLANNED. The test separates the
two fixes.

I ran two more fixture-copy variants, neither committed:

- **fd-3-only plus `</dev/null` on the gate subshell**: 106 passed, 4 failed.
  AC-3 goes green, but all four AC-2 assertions stay red, because fd 3 is
  still inherited. Closing stdin alone does not satisfy AC-2 if the manifest
  has moved to fd 3.
- **Candidate real fix, feasibility only**: line 611 becomes
  `( cd "$ROOT/$cwd" && eval "$cmd" ) </dev/null 2>&1 | tee "$log"`, with the
  manifest left on fd 0. Result: **110 passed, 0 failed**. The suite can be
  satisfied with a one-line change. This is not a prescription (see below).

### Passing on arrival (3 of the 22 new assertions)

None of these is the assertion its AC exists for. Each is a companion that
keeps a fix from over-correcting.

- `the stdin-reading gate passes` (AC-1(a)). `eats` passes today. It is here
  so that a fix which breaks the stdin-reading gate itself goes red.
- `a run of two passing gates exits 0` (AC-1(a)). Today's run also exits 0,
  for the wrong reason. The red assertions next to it (`later` banner, `2 ran`)
  are what carry AC-1(a).
- `a run given data on stdin still finishes` (AC-3). A proxy for "does not
  block". Today it finishes because the gate reads the manifest. The fd-3-only
  copy also finishes, because the pipe is finite. The fixture cannot reproduce
  a TTY block, as the AC itself says.

No probe is needed for these, because they are not the pinning assertions, and
each red sibling was watched to fail above.

### Negative controls: expected and measured values

The assertions ran for real (a harness suite has no import step), so these
numbers are measured, not estimated. GREEN must confirm the last column.

| Control | Assertion / threshold | Today (HEAD, measured) | fd-3-only fix (measured) | Real fix (expected; candidate measured) |
|---|---|---|---|---|
| AC-1(a) run count | exactly `(2 ran, 0 unconfigured, 0 known)` | `1 ran`, exit 0, no `later` | `2 ran`, exit 0 (green) | `2 ran`, exit 0 |
| AC-1(b) | `FAIL later (…exit 1)`, `1 required gate(s) failed.`, exit 1 | `All required gates passed (1 ran …)`, exit 0 | green | green |
| AC-2 | same as AC-1(b) | `1 ran`, exit 0 | `1 ran`, exit 0 (**red**) | green |
| AC-3 stdin bytes | `stdin-bytes=0` exactly | **57** (see note) | **12** (`CALLER-DATA\n`) | 0 |
| AC-4 `--fast` | `FAIL later`, `--fast skipped: slow_one`, exit 1 | `1 ran`, exit 0, "skipped nothing" | green | green |
| AC-4 `--required` | `FAIL later`, exit 1 | `1 ran`, exit 0 | green | green |

**Note on 57 versus PLANNED's 43.** The count is the byte length of whatever
manifest follows the `count` line, so it depends on the fixture's text. This
fixture's next line is
`gate  | after | required | . | printf 'Tests 2 passed\n'` plus a newline:
`printf '%s\n' "<that line>" | wc -c` gives 57. PLANNED used a different
following line. The meaning is the same ("the rest of the manifest"), so it is
not a discrepancy. The 12 under fd-3-only matches PLANNED exactly, and every
`1 ran`/exit 0 control matches.

### Export shape / what the tests pin

This is a harness shell script, so there are no imports. The tests pin:

- **CLI:** `bash scripts/gates.sh [--fast|--required]`, run from the fixture
  root.
- **Summary lines:**
  - `PASS         <id> (`
  - `FAIL         <id> (<n>s, exit 1) -> .claude/state/gate-logs/<id>.log`
  - `All required gates passed (N ran, 0 unconfigured, 0 known).`
  - `1 required gate(s) failed.`
  - `--fast skipped: slow_one`
- **Banner:** `=== gate: <id> (required) ===`.
- **Gate log path:** `.claude/state/gate-logs/<id>.log`.
- **Exit codes:** 0 and 1.

All of these are today's formats. **Do not change any of them.**

The tests do **not** constrain how the isolation is done. Any of these is
acceptable to them, subject to the story's Out of scope:

- `</dev/null` on the gate subshell with the manifest left on fd 0;
- moving the manifest to fd N and closing it for the command;
- reading the manifest into an array first.

One requirement does hold. A gate must see end-of-input on fd 0, whatever
`gates.sh` was given, and must not be able to reach the manifest on 0 or 3-9.

### Files touched

- `.claude/tests/gates.test.sh`: four new `describe` blocks plus the local
  helpers `gates_isolated`, `assert_gate_failed` and `assert_lacks`, appended
  before `summary "gates"`.
- `docs/backlog/stories/MC-046.md`: `## Test plan` and this section only.

### `bash scripts/gates.sh --fast` (admissibility)

Run from the repo root, exit 0:

```
--- gate summary ---
PASS         format (1s)
PASS         lint (42s, observed -)
PASS         typecheck (2s, observed -)
PASS         unit (146s, observed 47, floor 47)
PASS         coverage (168s, observed 1717)

--fast skipped: integration build
All required gates passed (5 ran, 0 unconfigured, 0 known).
```

This is green, as expected: no `project.conf` gate runs the harness suites (see
"Gate decision"). The red for this story is in `selftest.sh gates`, which is
CI's blocking step, not a gate.

### Things that bear on the implementation

- **Scope boundary on fds 3-9.** The tests close 3-9 in the *caller*, so they
  pin only descriptors that `gates.sh` itself opens. A caller's own open fds
  are not tested, per the Out of scope.
- **Placement of the redirect.** `</dev/null` has to be applied to the
  subshell that runs the gate, before `| tee`. The candidate measured above
  puts it on `( … )`. If it went on `tee` instead, fd 0 would stay the
  manifest.
- **Other suites.** The existing `--list`/`--audit` and MC-045 exec-count
  suites were not run in RED (AC-5 is GREEN's). Of the gates suite, 88 of 88
  pre-existing assertions still pass with the candidate fix.

## Regressions

<!-- REQUIRED if this story ever returned to RED after GREEN or GATES; omit
     otherwise. A test that is wrong is never edited into passing, and the
     return is not a footnote - it is the story failing to be one clean cycle,
     and the next person needs to know why. One block per return:
       * which test, what it asserted, and what was wrong with it
       * how the defect was found
       * what it asserts now
       * what earns it, since "watched it fail" cannot apply once the
         implementation exists - the corrected assertion passes on its first
         run and every run after, whether or not it asserts anything: either a
         PROBE (mutate the specific behaviour the test pins, paste the red,
         confirm the revert) or, where the defect was cost rather than
         correctness, a BEFORE/AFTER measurement taken under the gate command -
         not the plain test command, which is the faster one.
         PASTE THE OUTPUT. check-boundaries.sh refuses a PR whose Regressions
         or Gate probes section describes a failure without showing one
       * whether GREEN was a no-op, and the command output proving the source
         was untouched and still passes -->

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-23T19:12:18Z
    commit: dcd3c0d (working tree had uncommitted changes)
    tree:   0293fe4d0ad31ced954a27fa94144e243944c556
    result: pass (7 ran, 0 unconfigured, 0 known)

    PASS         format (2s)
    PASS         lint (7s, observed -)
    PASS         typecheck (2s, observed -)
    PASS         unit (19s, observed 47, floor 47)
    PASS         coverage (58s, observed 1717)
    PASS         integration (238s, observed 9, floor 9)
    PASS         build (82s, observed -)

## Gate probes

<!-- REQUIRED if this story adds or changes a gate, its command, or its
     evidence line. Omit the section entirely otherwise.
     A gate that has never been observed to fail is not a gate: break the thing
     it guards, run the gate, paste the failure, revert. One block per gate:
       * what was broken, and where
       * the gate output proving it failed
       * confirmation the probe was reverted -->

## Scaffold inventory

<!-- REQUIRED for a bootstrap or chore story that writes production code under
     SCAFFOLD, where nothing forces a test to exist first, and for a spike that
     commits its throwaway code. Omit otherwise.
     One line per production file written, and for anything with behaviour
     rather than configuration, the test that covers it:
       src/core/palette.ts        - src/core/palette.test.ts
       vite.config.ts             - configuration, no behaviour
     check-boundaries.sh refuses the PR if any changed source file is not
     named here. -->

## Notes

### Gate decision (PLANNED, 2026-09-23)

`required_gates: []`. I re-derived this rather than inheriting it, following
the reasoning MC-045 used. The only executable lines this story changes are in
`scripts/gates.sh`. Every `gate` line in `project.conf` (lines 192–198:
`format`, `lint`, `typecheck`, `unit`, `coverage`, `integration`, `build`)
runs `cargo`, and none runs `scripts/selftest.sh` or reads
`.claude/tests/**`. So no gate, required or optional, can go red if this
story's artifact breaks, and escalating one would bind nothing.

What does verify it is `.github/workflows/gates.yml` line 37 (re-checked
2026-09-23), which runs `bash scripts/selftest.sh` as a blocking step, followed
by `gates.sh --list` and `gates.sh --audit` against the real config. AC-5
brings the full selftest into the story as evidence.

`## Gate probes`: not required. This story changes no `gate`, `evidence`,
`floor` or other manifest line, and no gate command. It changes what a running
gate command can read. The criteria are red on arrival against the real
`gates.sh`, and AC-2 carries its own discriminating mutation (the fd-3-only
fix, applied to a fixture copy). That is the gate-probe discipline applied to
the runner.

### Tests and commands

- Tests go in `.claude/tests/gates.test.sh`, under a new `describe`, using the
  suite's existing `gates()` helper and `make_project_fixture` / `write_conf`.
- Run: `bash scripts/selftest.sh gates`. MC-045 made that suite fast; it was
  5–13 minutes before.
- Full: `bash scripts/selftest.sh` (AC-5).
- `gates.sh` flags are `--fast`, `--required`, `--gate <id>`, `--list`,
  `--audit`, `--no-wait` and `--story <id>`. There is no `--required-only`.

### Split decision

One story, not split. AC-1 to AC-4 are one behaviour: nothing a gate command
reads can change which gates the loop runs. They are four views of it (the
report, the descriptor generalisation, the caller's stdin and the subset
flags), and one change to one call site in one file satisfies all of them. The
accounting invariant was the candidate second behaviour, and it is split out
(see `## Out of scope`, first bullet).

### depends_on

`[MC-045]`, which is DONE, so `phase.sh set` is not blocked. It is listed
rather than omitted because this story edits the gate loop MC-045 rewrote. The
line numbers and the loop's shape in Context are those after MC-045, and a
reader should know which version the reproduction was taken against.

### Reproduction scripts (PLANNED)

These were run from the scratchpad, not committed. Case A, case B, the three
fd-3-only rows and every AC control above were measured on fixtures built by
`make_project_fixture` at HEAD 1fa9815. The fd-3-only variant was produced by
editing the **fixture's** copy of `gates.sh` only. The repo's `gates.sh` was
not touched.

### GREEN (2026-09-23)

**Change.** One line in `scripts/gates.sh`, the gate subshell (~line 615),
plus a comment: `( cd "$ROOT/$cwd" && eval "$cmd" ) </dev/null 2>&1 | tee "$log"`.
The manifest stays on the loop's fd 0. This is the handoff's candidate. I
checked whether `gates.sh` opens any other descriptor that a gate would inherit.
It does not. The lock is `mkdir`-based (~line 135), not a flock fd, and nothing
in the script uses `exec N<` or an `N<` redirect for 3-9. The only
redirections into loops are `done < "$CONF"` (lines 271 and 677) and
here-strings. So fd 0 was the only route to the manifest, and `</dev/null`
closes it. fd 255 was left alone (Out of scope).

**Suite.** `bash scripts/selftest.sh gates`: **110 passed, 0 failed**
(RED: 91 passed, 19 failed). Wall time 2m21s.

**Negative controls.** Each was re-run outside the suite on a
`make_project_fixture` fixture, with the test's own manifests and the same
isolation as `gates_isolated` (stdin `/dev/null`, fds 3-9 closed). AC-3 was
invoked as `printf 'CALLER-DATA\n' | bash scripts/gates.sh`.

| Control | RED-recorded value (today / fd-3-only / real fix expected) | Measured after fix |
|---|---|---|
| AC-1(a) run count | `1 ran`, exit 0, no `later` / `2 ran` / `2 ran`, exit 0 | `PASS eats`, `PASS later`, `All required gates passed (2 ran, 0 unconfigured, 0 known).`, exit 0 |
| AC-1(b) | `All required gates passed (1 ran …)`, exit 0 / green / green | `FAIL         later (0s, exit 1) -> .claude/state/gate-logs/later.log`, `1 required gate(s) failed.`, exit 1 |
| AC-2 (drains 0, 3-9) | `1 ran`, exit 0 / `1 ran`, exit 0 (red) / green | `FAIL         later (1s, exit 1) -> …/later.log`, `1 required gate(s) failed.`, exit 1 |
| AC-3 stdin bytes | 57 / 12 / 0 | `stdin-bytes=0`. The run finished, `2 ran`, exit 0 |
| AC-4 `--fast` | `1 ran`, exit 0, "skipped nothing" / green / green | `FAIL later … exit 1`, `--fast skipped: slow_one`, `1 required gate(s) failed.`, exit 1 |
| AC-4 `--required` | `1 ran`, exit 0 / green / green | `FAIL later … exit 1`, `1 required gate(s) failed.`, exit 1 |

Every measured value matches the "real fix" column. There is no divergence.
Discrimination still holds: with the fix in place the suite is green, and RED
already recorded it red on HEAD and red on the fd-3-only mutation (not redone
here).

**`bash scripts/gates.sh --fast`** (repo root, exit 0):

```
--- gate summary ---
PASS         format (1s)
PASS         lint (1s, observed -)
PASS         typecheck (1s, observed -)
PASS         unit (9s, observed 47, floor 47)
PASS         coverage (60s, observed 1717)

--fast skipped: integration build
This is a subset, not a verdict. The full run before REVIEW is what judges the story.

(not recorded in the story: a partial run is not evidence of anything)

All required gates passed (5 ran, 0 unconfigured, 0 known).
```

The lint and typecheck times are well below RED's (42s and 2s). That comes
from a warm cargo cache, not from lost work: `unit` still observes 47 against
floor 47. This story changes no Rust.

### AC-5: full `bash scripts/selftest.sh` (GREEN, 2026-09-23)

Exit 0, wall time 22m26s. Per-suite totals, then the output tail:

```
boundaries: 23 passed, 0 failed
doctor: 4 passed, 0 failed
gate-reminder: 19 passed, 0 failed
gates-parse: 43 passed, 0 failed
gates: 110 passed, 0 failed
lib: 85 passed, 0 failed
manifest: 25 passed, 0 failed
phase-guard: 115 passed, 0 failed
phase: 24 passed, 0 failed
profiles: 42 passed, 0 failed

10 harness suite(s) passed.
```


### Orchestrator verification (GREEN, 2026-09-23)

I checked this separately from the subagents' reports. `bash scripts/selftest.sh gates` on the
shipped change gave `gates: 110 passed, 0 failed`. The test file diff was the same 120
lines RED wrote. I then ran two mutations of `scripts/gates.sh` in the repo, one
run each. After each run I restored the file from a byte copy and confirmed it
with `cmp`.

**Mutation 1, the fd-3 manifest plus `</dev/null` variant from the handoff.**
The handoff predicted 4 failures, all AC-2.

```
418c418
< while IFS= read -r line; do
---
> while IFS= read -r -u 3 line; do
681c681
< done < "$CONF"
---
> done 3< "$CONF"

    FAIL a gate after one that drains fds 0 and 3-9 still runs and is reported FAIL
    FAIL and the run says a required gate failed
    FAIL and does not certify the run
    FAIL and exits 1
gates: 106 passed, 4 failed
```

It matches the prediction. My first attempt at this mutation changed only the
`read` line, because a sed escape failed. That run gave 48/62, since nothing
opened fd 3. It was invalid and I discarded it; it is noted here so the count is
not mistaken for a result.

**Mutation 2, a single assertion: the gate gets the manifest by path instead of
`/dev/null`.** The loop is unaffected, so I predicted exactly one failure, AC-3.

```
615c615
<   ( cd "$ROOT/$cwd" && eval "$cmd" ) </dev/null 2>&1 | tee "$log"
---
>   ( cd "$ROOT/$cwd" && eval "$cmd" ) <"$CONF" 2>&1 | tee "$log"

    FAIL a gate reads zero bytes of stdin, whatever gates.sh was given
         expected: stdin-bytes=0
         actual:   stdin-bytes=154
gates: 109 passed, 1 failed
```

It matches the prediction. AC-3's lone assertion is not vacuous. After both
mutations, `cmp` against the pre-mutation copy was clean.
