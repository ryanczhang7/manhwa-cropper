---
id: MC-011
title: A batch runs every file and reports a run summary
slug: a-batch-runs-every-file-and-reports-a-ru
epic: EPIC-03
type: feature
status: in-progress
phase: RED
branch: story/MC-011-a-batch-runs-every-file-and-reports-a-ru
depends_on: [MC-009, MC-010, MC-020]
required_gates: []
---

## Context

The brief's features 4 and 7 in the engine: many files in, every one
processed, one `RunSummary` out with counts and the flagged names.
`batch::run(&[PathBuf], out_dir, &Tuning, progress) -> RunSummary` plans
names (MC-010), processes files on the rayon pool (decision 12), keeps
results in input order, and never lets one file's failure stop the others.
`progress` is `&(dyn Fn(Progress) + Sync)` with `Progress { done, total }`,
called once per completed file. `RunSummary` serialises to JSON with a
stable shape because MC-012 writes it to disk and MC-015 renders it.

Gate that fails if this breaks: `unit`.

## Acceptance criteria

- **AC-1** - Given five inputs - three croppable PNGs, one uniform PNG and one `.txt` - when `run` completes, then `results` has five entries in input order with outcomes `Cropped, Cropped, Cropped, Flagged(Detector(Uniform)), Flagged(Unsupported)`, and `cropped() == 3`, `flagged() == 2`, `failed() == 0`.
- **AC-2** - Given one of the inputs does not exist, when `run` completes, then that entry is `Failed { error }`, every other input was still processed, and `failed() == 1`.
- **AC-3** - Given `n` inputs, when `run` completes, then the progress callback was called exactly `n` times, each call carried `total == n`, and the sequence of `done` values is `1..=n` (strictly increasing by one).
- **AC-4** - Given `out_dir` does not exist, when `run` starts, then it is created; given it cannot be created (a path under a file), then every result is `Failed` and nothing panics.
- **AC-5** - Given a `RunSummary`, when serialised with `serde_json`, then it has the shape `{"cropped": u32, "flagged": u32, "failed": u32, "results": [{"input": str, "outcome": "cropped" | "flagged" | "failed", "output": str | null, "rect": {x,y,w,h} | null, "reason": str | null, "error": str | null}]}` with `reason` being the `Flag` variant name (`Uniform`, `NoBorderFound`, `LowContent`, `Ambiguous`, `Unsupported`, `DecodeFailed`); and `flagged_names()` returns the flagged inputs' file names in order.
- **AC-6** - Given 32 inputs, when `run` completes, then the wall time is less than the sum of the per-file times measured by processing them sequentially with `process_file` (the batch is parallel; this is checked on a machine with more than one core and skipped with a recorded note otherwise).

## Model guidance

Model: default. Oracle partition:

- **Mechanical**: everything in AC-1..AC-5; pin the JSON shape exactly - it is a contract with MC-012 and MC-015.
- **Oracle-free**: AC-6 is a weak timing claim; its purpose is to catch a batch that is accidentally sequential, not to measure performance (MC-017 does). Use small images so it runs in well under a second.

## Out of scope

The `--summary` file and exit codes (MC-012), the window's progress bar
(MC-015), the 100-image budget (MC-017), cancellation, retrying a failed
file, recursion into folders (a directory input is `Failed`).

## Test plan

> **RED IS NOT FINISHED. AC-6 IS ESCALATED, NOT WRITTEN.** AC-1 to AC-5 are
> written, run and admissible. AC-6 is not in the tree, because as the
> criterion is worded it cannot be satisfied under the required `coverage`
> gate by *any* correct implementation. The evidence is below under "AC-6".
> Do not read this phase as complete and do not move the story to GREEN
> without answering it.

### Level

Integration, in `crates/engine/tests/`. `batch::run` is not a pure function:
its whole subject is what happens when real files are planned, processed in
parallel and reported together, and the failures the story cares about -
a missing input, an output folder under a file, a name collision between two
inputs - only exist against a filesystem. There is nothing below this level
that could falsify AC-1 to AC-5; the pure parts (`decide`, `plan_outputs`)
are already pinned by MC-007 and MC-010 and are not re-tested here.

### Target layout

One new target, `crates/engine/tests/batch.rs`, holding AC-1 to AC-5 - and
**not** a second target for AC-6, which the layout would otherwise have
demanded. The reason is worth recording, because it is the right answer if
AC-6 comes back in a runnable form:

* `common/mod.rs` is compiled into every target in the directory, so a new
  target costs a recompile of 1,046 lines of fixtures and nothing else. That
  is cheap enough that it is not an argument either way.
* The argument that *does* bite is scheduling. `cargo test` runs the tests
  **inside one binary concurrently** on as many threads as it has cores, and
  runs the binaries themselves one after another. A timing test sharing a
  binary with eleven filesystem tests is measured while they compete with it
  for the cores it is trying to prove it can use. So a timing test belongs
  alone in its own target - `batch_parallel.rs` - and the eleven others
  belong together in `batch.rs`. AC-1 to AC-5 have no such constraint and
  share one target.

`args.rs`, `codec.rs`, `formats.rs`, `naming.rs`, `naming_case.rs`,
`process_file.rs` and `scaffold.rs` are untouched. No existing assertion was
changed.

### What each criterion is tested by, and what makes it a real test

* **AC-1** is two tests. The first compares `summary.results` **as a whole
  vector**, `input` paths, rects and output paths included, against the five
  it must be; the second reads the three counts. Comparing the vector rather
  than the outcomes is what makes "input order" mean something: see the
  negative control below.
* **AC-2** puts the missing input at index 1, so that "the files after it were
  still processed" is a claim about a file the failure could have swallowed,
  and reads the output folder to confirm it.
* **AC-3** compares the whole log of progress calls against
  `[(1,5), (2,5), (3,5), (4,5), (5,5)]`, plus a batch of one and a batch of
  none. Zero, one, many.
* **AC-4** is two tests: the folder that does not exist yet (created, and
  holding all three outputs), and the folder under a file (five results, all
  `Failed`, five progress calls, and the blocking file unharmed). "Nothing
  panics" needs no assertion: rayon resumes a worker's panic on the calling
  thread, so reaching the assertions at all is the proof.
* **AC-5** is two tests: one `assert_eq!` of `serde_json::to_value(&summary)`
  against a `json!` literal covering all four outcome shapes -
  `Cropped`, `Flagged(Detector(Uniform))`, `Flagged(Unsupported)`,
  `Flagged(DecodeFailed(..))` and `Failed` - and one on `flagged_names()`.
  `Value` equality compares the key *set*, so the literal pins the absence of
  extra keys and the presence of every `null` as well as every value.
* **AC-6** - see below. Not written.

Two tests are not criteria and are here deliberately:
`two_inputs_with_the_same_file_name_both_reach_the_output_folder` is the
MC-020 dependency made real (this story is the first place two files arrive at
once, and the first place the writes are parallel), and
`a_folder_given_as_an_input_is_failed_rather_than_recursed_into` pins one line
of `## Out of scope` for the price of six.

### AC-6: measured, and it does not work

AC-6 asks for `batch_wall < sum(per-file times measured sequentially)` on 32
inputs. Two separate problems, both measured on this machine (12 cores,
`profile.test` at `opt-level = 2`), with 32 copies of the MC-008 screenshot
scene at 1x (147x120) and at 3x (441x360):

**1. Under the `coverage` gate a correct parallel batch is two to three times
SLOWER than the same work done sequentially, so the assertion fails.** LLVM's
source-based coverage counters are shared globals incremented without
atomics; twelve threads running the same instrumented `cropper-core`
functions contend on the same cache lines. The sequential side pays the
documented ~1.6-1.9x instrumentation cost. The parallel side pays ~20x.

| | plain | instrumented | |
|---|---|---|---|
| 32 x 441x360, sequential sum | 112-127 ms | 221-226 ms | 1.9x slower |
| 32 x 441x360, 12-thread batch | 31-33 ms | 605-614 ms | **20x slower** |
| ratio `seq_sum / batch` | 3.35-4.08 | **0.36-0.37** | |

Twelve instrumented runs, two scales x two thread counts x three repetitions:
the assertion failed 10 of 12 times, and the two that passed read 1.01 and
1.02 - noise, not a pass. Plain, it passed 12 of 12.

**2. Even where it runs, the criterion's own comparison is a weak control.**
For a sequential implementation the two sides are the same quantity measured
twice, so the result is decided by run-to-run noise. Measured over five
repetitions against a deliberately sequential batch, the assertion *wrongly
passed* 3/5 at 147x120, 2/5 at 294x240 and 1/5 at 441x360.

So AC-6 cannot be written as specified without either failing a required gate
or shipping a flaky test. This is put to the product owner rather than
decided here. The shapes an answer could take, none of them chosen:

1. **Move the timing check to the `integration` gate** - `#[ignore]`,
   `--release`, uninstrumented - which is where MC-017's timing budget already
   lives, and which `project.conf`'s waiver
   (*"no `#[ignore]` test exists until MC-017"*) already anticipates. This is
   an `## Amendments` change to AC-6 and to the story's "Gate that fails if
   this breaks: `unit`".
2. **Replace the timing claim with a non-timing observation of parallelism** -
   that the batch's work is seen on more than one thread. Instrumentation
   cannot change that, and it fails *deterministically* for a sequential
   batch, where the timing claim fails 20-80% of the time. Strictly the better
   control, and a different criterion.
3. **Drop AC-6** and let MC-017 own every timing claim.
4. Keep AC-6 and give up a required gate. Not acceptable under the law.

Whichever is chosen, the fixture work is already done: a 3x nearest-neighbour
upscale of the MC-008 scene (441x360) is where the plain signal is widest
(3.35-4.08 against a sequential band of 0.92-1.06), it is still `Cropped`
(`Rect { x: 42, y: 63, w: 366, h: 276 }`), and 32 of them cost ~180 ms plain
end to end.

**To reproduce (1) independently:** write a test that writes 32 PNGs, times a
`std::thread::scope` pool over `process_file` and then times a sequential
pass, then run it under `cargo test -p cropper-engine --test <name>` and under
`cargo llvm-cov --package cropper-engine --test <name> --summary-only`. The
plain run beats the sum; the instrumented run loses to it by roughly 2.5x.

## Handoff: RED -> GREEN

> **Read `## Test plan` first.** AC-6 has no test and is escalated to the
> product owner. Everything below covers AC-1 to AC-5 only.

### The command

```
cargo test --workspace
```

The workspace command, never `-p cropper-engine` alone: `project.conf`'s
`unit` gate is `cargo test --workspace`, and a `bmp` decoder is present in one
build and absent in the other (`tests/common/mod.rs`, `write_bmp`).

Only this story's tests:

```
cargo test --workspace --test batch
```

### The failure, verbatim

```
   Compiling cropper-engine v0.1.0 (C:\Users\ryanc\Projects\manhwa-cropper\crates\engine)
error[E0432]: unresolved import `cropper_engine::batch`
  --> crates\engine\tests\batch.rs:52:21
   |
52 | use cropper_engine::batch::{Progress, RunSummary, run};
   |                     ^^^^^ could not find `batch` in `cropper_engine`

error[E0432]: unresolved import `serde_json`
  --> crates\engine\tests\batch.rs:54:5
   |
54 | use serde_json::json;
   |     ^^^^^^^^^^ use of unresolved module or unlinked crate `serde_json`
   |
   = help: if you wanted to use a crate named `serde_json`, use `cargo add serde_json` to add it to your `Cargo.toml`

error[E0433]: cannot find module or crate `serde_json` in this scope
   --> crates\engine\tests\batch.rs:457:9
    |
457 |         serde_json::to_value(&summary).expect("a RunSummary serialises"),
    |         ^^^^^^^^^^ use of unresolved module or unlinked crate `serde_json`
    |
    = help: if you wanted to use a crate named `serde_json`, use `cargo add serde_json` to add it to your `Cargo.toml`

Some errors have detailed explanations: E0432, E0433.
For more information about an error, try `rustc --explain E0432`.
error: could not compile `cropper-engine` (test "batch") due to 3 previous errors
```

**Why this is the right failure, and what it is not.** It is the first test of
a module that does not exist, which is the one case `tdd-cycle` allows an
import error to stand for RED - and `crates/engine/Cargo.toml` has neither
`rayon` nor `serde`/`serde_json`, which is config and therefore GREEN's to
add, not RED's. It is *not* evidence that any assertion in the file has been
observed to fail, because the file never loaded: see "Controls" below, which
is how that debt is discharged.

`bash scripts/gates.sh --fast` reports `format` PASS and `lint`, `typecheck`,
`unit` and `coverage` FAIL. (`format` WARNed on the first run - this file was
not `cargo fmt` clean as written; `cargo fmt --all` fixed it and it is clean
now, which is the whole reason RED ends with a gate run.) **`lint` and
`typecheck` fail with the same three unresolved imports and nothing else**,
because both are `--all-targets` and so compile the test tree; in Rust that is
unavoidable for a new module's first test and it is not a lint finding against
this file. Proof that it is not: the file was type-checked and run under
`cargo clippy --all-targets -- -D warnings` against a stub with the export
shape below, outside the repository, and is clean.

### One row per test

| Test | Asserts | Covers |
|---|---|---|
| `results_are_one_per_input_in_input_order_with_the_outcome_each_input_earned` | the whole `results` vector - five entries, input paths, `Cropped x3` with `crop_rect()`, `Flagged(Detector(Uniform))`, `Flagged(Unsupported)` - in input order | AC-1 |
| `the_counts_are_three_cropped_two_flagged_and_none_failed` | `cropped() == 3`, `flagged() == 2`, `failed() == 0` | AC-1 |
| `a_missing_input_is_the_only_failure_and_the_files_after_it_are_still_processed` | index 1 is `Failed` with a non-empty error; `failed()==1`, `cropped()==2`, `flagged()==1`; the output folder holds the other three | AC-2 |
| `progress_counts_one_to_n_and_every_call_carries_the_batch_total` | the call log equals `[(1,5),(2,5),(3,5),(4,5),(5,5)]` | AC-3 |
| `a_batch_of_one_reports_one_result_and_one_progress_call` | one result, one call reading `(1, 1)` | AC-3 |
| `an_empty_batch_reports_nothing_and_never_calls_progress` | empty `results`, three zero counts, zero calls | AC-3 |
| `an_output_folder_that_does_not_exist_is_created_and_holds_every_output` | `out.is_dir()` afterwards and the three names in it | AC-4 |
| `an_output_folder_that_cannot_be_created_fails_every_result_without_panicking` | five results, all `Failed`, counts `(0,0,5)`, five progress calls, the blocking file unchanged | AC-4, AC-3 |
| `the_summary_serialises_to_the_json_shape_mc012_writes_and_mc015_renders` | `serde_json::to_value` equals a `json!` literal over all five entries and every one of the six keys | AC-5 |
| `flagged_names_are_the_flagged_inputs_file_names_in_input_order` | `["flat.png", "notes.txt", "junk.png"]` | AC-5 |
| `two_inputs_with_the_same_file_name_both_reach_the_output_folder` | outputs are `a.png` and `a (2).png` and both exist | MC-010/MC-020 composed; `## Context` |
| `a_folder_given_as_an_input_is_failed_rather_than_recursed_into` | one `Failed`, two results not three, nothing from inside the folder written | `## Out of scope` |

### The export shape the tests already pin

Nothing below is a suggestion. Each name and signature is already imported by
a test, so getting it wrong is a compile error rather than a debate. All of it
was compiled and run against a stub before this handoff was written.

```
crates/engine/src/batch.rs   (module path `cropper_engine::batch`, so `pub mod batch;` in lib.rs)

  pub fn run(
      inputs: &[PathBuf],
      out_dir: &Path,
      tuning: &Tuning,
      progress: &(dyn Fn(Progress) + Sync),
  ) -> RunSummary

  pub struct Progress { pub done: _, pub total: _ }
      // derives at least Debug + PartialEq; the tests build one and compare
      // vectors of them. Copy/Clone/Eq are yours.
      // The integer type is NOT constrained: every literal in the tests infers
      // from the field.

  pub struct RunSummary { pub results: Vec<FileResult> }
      // `results` is a public field, read directly and compared as a whole.
      // impl RunSummary:
      //   pub fn cropped(&self) -> _        (compared against 3, 2, 1, 0)
      //   pub fn flagged(&self) -> _
      //   pub fn failed(&self) -> _
      //   pub fn flagged_names(&self) -> Vec<String>
      // The three counts' integer type is NOT constrained; AC-5's JSON pins
      // them as JSON numbers, which u32 and usize both produce.
      // RunSummary: serde::Serialize  (by any means - a hand-written impl is
      // what the reference did, because the JSON shape is flatter than the
      // types)
```

Unchanged and already imported by the tests: `cropper_engine::{FileResult,
Flag, Outcome}`, `cropper_engine::naming::plan_outputs`,
`cropper_core::{FlagReason, Tuning}`.

**Not constrained, and yours to choose:** whether `Progress` / `RunSummary`
are also re-exported at the crate root (the tests import them from
`cropper_engine::batch`); how the summary is serialised; how the pool is
driven; the order of the JSON's keys (the tests compare `serde_json::Value`,
which is order-insensitive); whether `out_dir` is created up front or left to
the writers - **except** that AC-4's second test requires one result per input
even when the folder cannot be created, so an early return with an empty
summary is out.

### Config GREEN has to add

`crates/engine/Cargo.toml` needs `rayon` (decision 12), `serde` and
`serde_json`; all three are already pinned in the workspace root. `serde_json`
may be a dev-dependency if the summary's `Serialize` is hand-written;
`cropper-core` already takes `serde` as a regular dependency for exactly this
reason and `Rect` and `FlagReason` already derive `Serialize`.

### Controls: the numbers GREEN must confirm

The suite failed at **import**, so not one assertion in it ran - controls
included. Every number below was produced outside the framework, by driving
the same scenarios through `process_file` and `plan_outputs` directly (which
do exist) and, for the whole suite, against a throwaway reference
implementation of `batch::run` built outside the repository.
**Confirming them against the shipped module is GREEN's job.**

| Control | What it guards | Expected | Measured in RED |
|---|---|---|---|
| completion order of AC-1's five inputs, run at the same time | that "input order" is a real demand | different from input order | input indices `[4, 3, 0, 1, 2]`, i.e. `notes.txt, flat.png, a.png, b.png, c.png` |
| per-file cost of AC-1's five inputs, sequential | that they do not all take the same time, so the two orders cannot coincide by luck | a clear spread | `[744, 704, 899, 312, 250] us` |
| progress log of a batch reporting once at the end | that AC-3 catches it | not equal to expected | `[(5,5)] != [(1,5)..(5,5)]` |
| progress log of `n` calls with a constant `done` | that AC-3 catches it | not equal to expected | `[(5,5) x5] != [(1,5)..(5,5)]` |
| progress log whose `total` is the running index | that AC-3 pins `total`, not just `done` | not equal to expected | `[(1,1),(2,2),...] != [(1,5)..(5,5)]` |
| `fs::create_dir_all` under a file | that AC-4's second half is reachable | an error, not a panic | `Err(Os { code: 183, kind: AlreadyExists, "Cannot create a file when that file already exists." })` |
| `process_file` for each input with that folder | that every result really is `Failed` | five `Failed` | `a.png`, `flat.png`, `notes.txt`: os error 183; `gone.png`: os error 2; a directory input: os error 5 |
| `process_file` on a directory input | that `## Out of scope`'s "a directory input is `Failed`" holds | `Failed` | `Failed { error: "Access is denied. (os error 5)" }` |
| `common::crop_rect()` at `Tuning::default()` | that AC-1's and AC-5's rect is the fixture's, not a literal | the MC-008 value | `Rect { x: 12, y: 19, w: 126, h: 96 }` |
| the missing-file error string | that AC-5's `error` is a real string | non-empty | `"The system cannot find the file specified. (os error 2)"` |

### Mutation table

Measured, not predicted: each row was applied to a reference implementation of
`batch::run` outside the repository and the suite of 12 run against it. The
predicted catch for the shipped implementation is the same, because none of
these depends on how the pool is driven. **Prefer M3, M4, M6, M7, M8 or M10
for an independent check: each is a single assertion.**

| # | Mutation | Predicted | Measured |
|---|---|---|---|
| M1 | results collected in completion order, not input order | 3-4 | **4** — order, AC-2, folder, JSON |
| M2 | progress fired once, at the end | 3 | **3** — progress, empty batch, AC-4b |
| M3 | progress fired `n` times with `done` always the total | 1 | **1** — progress |
| M4 | `reason` is the outer variant name (`"Detector"`) — the nested shape, one level too shallow | 1 | **1** — JSON |
| M5 | the `null` fields omitted rather than written (`skip_serializing_if`) | 1 | **1** — JSON |
| M6 | `out_dir.join(file_name)` instead of `plan_outputs` | 1 | **1** — same-name |
| M7 | early return with an empty summary when `out_dir` cannot be created | 1 | **1** — AC-4b |
| M8 | `flagged_names()` returns whole paths | 1 | **1** — flagged names |
| M9 | the batch stops at the first failure | 2-3 | **2** — AC-4b, progress |
| M10 | `flagged_names()` also lists the failures | 1 | **1** — flagged names |

M4's failure message names the defect directly:

```
  left: ... "reason": String("Detector") ...
 right: ... "reason": String("Uniform") ...
```

### Notes for the implementer

* **The progress callback must be called under the same lock that increments
  the counter.** AC-3 pins the *observed sequence* as `1..=n`, strictly
  increasing. An `AtomicUsize::fetch_add` followed by an unsynchronised
  `progress(..)` is a race: two workers can take 1 and 2 and call in the order
  2, 1. That would make the test flaky rather than wrong, which is worse. The
  reference implementation took one `Mutex<usize>`, incremented it and called
  the callback while still holding it; 12 runs, no reordering.
* **`plan_outputs` must be called once, before any file is written**, and its
  result indexed positionally. It already documents why (MC-010's module
  docs); `two_inputs_with_the_same_file_name_both_reach_the_output_folder` is
  what fails if a worker chooses its own name.
* **`process_file` reports, it does not unwind.** Nothing in AC-4's second
  half needs a guard: `fs::create_dir_all` under a file returns
  `Err(os error 183)`, `run` turns it into `Outcome::Failed`, and the batch
  carries on. The only way that test panics is if the batch does.
* **AC-5's JSON is flatter than the types.** `Outcome` has an `output` on two
  variants and an `error` on the third; the wire shape has all six keys on
  every entry, three of them usually `null`, and `reason` is a bare variant
  name with `Flag::DecodeFailed`'s message discarded. A derive on `Outcome`
  will not produce it. The reference used a private `#[derive(Serialize)]`
  shadow struct built from `&FileResult`, which is about twenty lines.
* **`floor | unit | 47` does not move.** It tracks the first test binary with
  at least one test, which is `crates/core tests/content.rs`; this story adds
  a target to `crates/engine`, which cargo runs after every `crates/core`
  target. Confirmed still 47 with `cargo test -p cropper-core`.
* Nothing in `args.rs`, `codec.rs`, `formats.rs`, `naming.rs`,
  `naming_case.rs`, `process_file.rs` or `scaffold.rs` was changed.

### Files touched

* `crates/engine/tests/batch.rs` — new, the only file this phase wrote.

## Gate results

## Notes

### MC-020 inserted ahead of this story (2026-09-12)

`depends_on` gained **MC-020**, which fixes a case-collision defect in
`naming::plan_outputs`: two inputs whose names differ only by letter case are
planned as two names and written as one file, because `PathBuf` equality is
byte-wise and NTFS is not. Reproduced on the filesystem alone; the evidence is
in MC-020's `## Context` and MC-010's `## Notes`.

**It is a dependency of this story specifically**, not merely an earlier one.
MC-011 is the first point at which a user hands the program two files at once,
so it is where the overwrite becomes reachable - and it is also the story that
parallelises the writes, which makes a lost file harder to notice, not easier.

### PLANNED -> RED (2026-09-12)

**The required gate is `unit`**, already `gate | unit | required`. Nothing to
escalate: `required_gates` is for gates the repo marks *optional*. `coverage`
moves too, and see the warning about AC-6 below - it is the one criterion in
this story that the coverage gate could treat differently from the test
command.

**Sized before starting, because six criteria and two new dependencies is the
largest story yet.** It stays one cycle: the criteria are one behaviour - a
batch runs every file and reports what happened - and they are not
independently demonstrable. A batch with no summary reports nothing; a summary
with no batch has nothing to summarise; ordering, failure isolation and
progress are properties *of* that one run, not separate features. The tests can
be named before writing them, which is the skill's own test for a story that
is not too big.

#### AC-6 is a timing assertion, and timing assertions are how suites become flaky

This machine has **12 cores**, so AC-6 runs rather than skips here. Three
things about it that RED must get right, and they are the difference between a
control and a coin flip:

1. **It must compare two measurements taken in the same run**, as the criterion
   words it - the batch's wall time against the *sum of per-file times measured
   sequentially in that same test*. Never an absolute threshold. The `coverage`
   gate runs this suite instrumented and `ci-factor | coverage | 1.63` says a
   test there is roughly 1.6x its local self; a ratio survives that, a constant
   does not. The same goes for CI's slower, fewer-core hardware.
2. **The skip on a single-core machine is authorised by the criterion itself**
   and by nothing else. It is conditioned on the core count and it records a
   note. It is *not* a way to make a red go away: if the assertion fails on a
   multi-core machine, that is a real failure. GitHub's `windows-latest`
   runners have more than one core, so CI exercises it.
3. Small images, well under a second, per `## Model guidance`.

#### What GREEN will have to add, so RED is not surprised by it

`crates/engine/Cargo.toml` has only `cropper-core` and `image` today. This
story needs `rayon` (decision 12) and `serde`/`serde_json` (AC-5), all three
already pinned in the workspace root. Adding them is **config**, which is
GREEN's to write and not RED's - RED gets the compile error, which is the
correct RED.

`cropper-core` already derives `serde::Serialize` on `Rect` and `FlagReason`
(MC-007), so AC-5's `rect` object and its `reason` string have a foundation.
Note that AC-5 asks for `reason` to be the **variant name**, which means
`Flag::Detector(Uniform)` serialises as `"Uniform"` and not as a nested object
- the engine's `Flag` is one level deeper than the JSON is. That flattening is
the part of AC-5 most likely to be got wrong quietly, because a nested shape
still round-trips and still looks like JSON.
