---
id: MC-047
title: A gate run never certifies a manifest it did not fully account for
slug: a-gate-run-never-certifies-a-manifest-it
epic: EPIC-07
type: fix
status: in-review
phase: REVIEW
branch: story/MC-047-a-gate-run-never-certifies-a-manifest-it
depends_on: [MC-046] # builds on the gate loop MC-046 fixes; AC-1's control must be re-measured on it
required_gates: []    # no project.conf gate covers the harness; see "Gate decision" in ## Notes
---

## Context

<!-- Why this story exists. Link the epic and any wiki pages that constrain it.
     Name the REQUIRED gate that would fail if this story's artifact broke. If
     only an optional gate can, put it in required_gates above before RED:
     every required gate once passed over a story none of them exercised. -->

This is the follow-up that [MC-046](MC-046.md) recommended in its
`## Out of scope`. The user asked for it on 2026-09-23. It is harness work
filed under [EPIC-07](../epics/EPIC-07.md), like MC-043 to MC-046, because that
is the epic in flight.

MC-046 stops a gate command from consuming the manifest through stdin or an
inherited descriptor. This story adds the defence behind it. **A gate run must
not print `All required gates passed`, exit 0 or record `pass` unless every
`gate` line that `project.conf` declares has been accounted for.** That holds
whatever the cause of a loss. Today nothing compares what ran against what was
declared. The summary counts only what reached it, so a gate the loop never
reached leaves no trace.

### Why this is a `fix` and not a `feature`: a loss path MC-046 does not close

MC-046's Out of scope expected that after its fix "no known path loses a
gate". That turned out to be wrong. The following was measured at PLANNED on
2026-09-23, HEAD 1fa9815, with a fixture from `make_project_fixture`:

```
gate | first | required | . | printf 'BOOTSTRAPPED=yes\n' > .claude/harness/project.conf; printf 'Tests 1 passed\n'
gate | later | required | . | exit 1
```

```
PASS         first (0s) -- no evidence line: a vacuous pass would go unnoticed
All required gates passed (1 ran, 0 unconfigured, 0 known).
exit=0
```

`first` reads no stdin. It rewrites the manifest **in place**, by path. The
gate loop streams `project.conf` from an open descriptor, so its next `read`
lands past the new end of file and the loop ends. `later` exits 1, yet it never
runs and never appears in the output, and the run certifies a pass.

The same fixture was then run against the **smallest fix that satisfies
MC-046's criteria**: `</dev/null` on the gate subshell (line 611), applied to
a **fixture copy** of `gates.sh` and never to the repo. That fix turns MC-046
case B into `FAIL later … exit=1` and MC-046 AC-2's fd-draining gate into
`FAIL later … exit=1`, so it passes both. The in-place rewrite still prints
`All required gates passed (1 ran, 0 unconfigured, 0 known).` and exits 0.
An atomic rewrite (`> p.tmp && mv p.tmp project.conf`) does *not* lose the
gate, because the loop keeps the old inode. Only an in-place truncating write
does.

This is consistent with MC-046, which declares "a gate can still `rm` the
manifest" out of scope. It is also exactly the class of loss the invariant
exists to catch. It is the reason this story is a `fix`: AC-1 reproduces it.

### How the tests earn their failure: a fault-injection seam

Rules.md requires every test to be watched to fail for the right reason. After
MC-046, a stdin-based loss no longer exists to be red against. The in-place
rewrite above is red today, but whether it stays red depends on how MC-046 is
implemented: a loop that snapshots the manifest before running anything
closes it. A test of the invariant that relies on some *real* way of losing a
gate therefore guards the invariant only as long as that way exists. The day
someone closes it, the test passes whether or not the invariant is present,
and it passes forever.

Three options were considered:

1. **Run RED against pre-MC-046 code** (order this story first). Rejected. It
   earns the red exactly once. Once MC-046 lands, the stdin fixture loses
   nothing, so the test is vacuous from then on. It also contradicts
   `depends_on: [MC-046]`, and it would change the controls MC-046 measured.
2. **A one-off mutation recorded in `## Regressions`** (break `gates.sh` so it
   loses a gate, watch the test go red, revert). Rejected as the *primary*
   mechanism. A test still needs some way to lose a gate on every run. Without
   one, it has nothing to be red against, and the mutation proves only that
   the test *was* able to fail once.
3. **A fault-injection seam in `gates.sh`: chosen.** An environment variable,
   `GATES_FAULT_LOSE`, names gate ids that the run loop must lose: treat their
   lines as though they had never been read. This gives a deterministic,
   permanent loss that does not depend on MC-046's implementation or on any
   future rewrite of the loop. A test that sets it is red whenever the
   invariant is missing or broken, on every run, forever.

Why the seam is safe to ship. Its only effect is to make a run **fail**, never
pass: that is AC-3, the invariant itself. It announces itself on every run
that uses it (AC-2). It reads nothing that CI sets. It is the smallest
production surface that makes the invariant testable, and one test demands it,
so it is production code with a failing test, as the law requires.

What the seam does *not* prove is that the invariant's "declared" side is
independent of the loop. An implementation that recomputed "declared" by
re-reading `project.conf` after the loop would pass every seam test and still
certify the in-place rewrite. AC-1 is the test that closes that gap, using a
real loss rather than a simulated one.

The RED failure of the seam-based tests is "the seam does not exist yet": the
env var is ignored, both gates run, and the run passes. That is the right
reason, but it is not the whole proof, because it is the analogue of a suite
failing at import. So each control's expected value goes in the handoff, and
after GREEN the orchestrator runs the mutations listed under each AC below
against the committed implementation. Those runs are recorded in `## Notes`
(see "Verification plan").

### Which required gate would fail if this broke

None in `project.conf`, as for MC-044 to MC-046. See "Gate decision" in
`## Notes`. The tests live in `.claude/tests/gates.test.sh`, which CI runs
through `bash scripts/selftest.sh` (`.github/workflows/gates.yml` line 37, a
blocking step). The invariant also runs on every CI job, because line 49 runs
`bash scripts/gates.sh` in full against the real manifest. A false refusal
there would block every PR, which is why AC-6 exists.

## Acceptance criteria

<!-- Each AC is independently testable and phrased as observable behaviour.
     The Test Developer writes at least one failing test per AC.
     An AC that names a statistic, a metric or a threshold names a NEGATIVE
     CONTROL too: the deliberately broken input the metric must reject, and
     roughly what it should score. A criterion can be perfectly testable and
     still be blind to the defect it exists to catch, and that is more
     dangerous than a vague one - it survives review and goes green. -->

**Definitions used by every criterion.** These are the counting rules, and
each one is also listed for confirmation under "Open questions for the PO/user"
in `## Notes`.

- **Declared.** One entry per `gate` line in `project.conf`, counted per
  *line*, not per unique id. The list is read **before any gate command
  runs**. It applies only to runs that execute gates, not to `--list` or
  `--audit`.
- **Accounted for.** A declared entry is accounted for when the run loop
  reached its line and gave it exactly one of these dispositions:
  1. *ran*: reported `PASS`, `FAIL`, `WARN` or `KNOWN`;
  2. *unconfigured*: reported `UNCONFIGURED`, or `FAIL … (required gate has no
     command in project.conf)`;
  3. *refused before running*: one of the existing manifest-error `FAIL`s
     (`slow` or `no-count` with no reason, a broken `floor`, a broken
     `ci-factor`, a waiver on a required gate);
  4. *left out by `--fast`*: named in `--fast skipped:`;
  5. *not selected*: excluded by `--gate <id>` or `--required`. This is
     accounted for without being printed, but only if the loop actually
     reached the line and the filter excluded it.
- **Lost.** A declared entry that is not accounted for. A lost gate is lost
  whether it is required or optional, and whether or not a filter would have
  excluded it.
- **Refusal.** For each lost gate, in manifest order, the summary shows the
  line `FAIL         <id> (declared in project.conf but never accounted for)`,
  with the same 13-column prefix as every other summary line. It is followed
  by the line `<N> declared gate(s) never accounted for; this run certifies
  nothing.` The run exits **1** and does not print `All required gates
  passed`. `.claude/state/last-gate-run` records `RESULT=fail`. A full run
  records `result: fail (…)` in the story.
- **Unchanged on a healthy run.** When nothing is lost, every existing line is
  byte-for-byte what it is today. That includes
  `All required gates passed (<ran> ran, <u> unconfigured, <k> known).`, which
  MC-046's tests pin.

All fixtures come from `make_project_fixture` / `write_conf`. Every gate
command is a `printf` / `exit` one-liner.

- **AC-1** — *The reproduction: a gate that rewrites the manifest in place
  cannot make a later gate vanish from a certified run.* Given the manifest
  ```
  gate | first | required | . | printf 'BOOTSTRAPPED=yes\n' > .claude/harness/project.conf; printf 'Tests 1 passed\n'
  gate | later | required | . | printf 'Tests 2 passed\n'
  ```
  when a full run executes, then **either** `later` runs (its `=== gate: later`
  banner appears, `PASS         later` is in the summary, and the run reports
  `(2 ran, 0 unconfigured, 0 known)`), **or** the run refuses and names
  `later`. In no case does it print `All required gates passed` while
  `later` is absent from the output. A second case uses the same manifest
  with `later` changed to `exit 1`. That run exits 1, and its summary contains
  a `FAIL         later` line, either the gate's own failure or the refusal.
  *Control:* measured at PLANNED on HEAD 1fa9815, and again on the
  `</dev/null` MC-046 candidate: `All required gates passed (1 ran, 0
  unconfigured, 0 known).`, exit 0, `later` absent, in both cases. Red on
  arrival **if** MC-046's merged loop still streams the manifest. RED
  re-measures this on the post-MC-046 code *first*. If it is green on arrival,
  stop and report to the PO, per the escalation note in `## Notes`.
  *Why a disjunction:* snapshotting the manifest so that it cannot be lost is
  an acceptable fix as well as refusing. The product claim is "never a false
  certification", not a particular mechanism. (Open question 7.)
- **AC-2** — *The seam loses what it names, and says so.* Given a healthy
  three-gate manifest (`a`, `b` and `c`, all required, each printing
  `Tests 1 passed`), when a full run executes with
  `GATES_FAULT_LOSE='b'` in the environment, then no `=== gate: b` banner
  appears, the output contains a line beginning
  `note: GATES_FAULT_LOSE is set` that names `b`, and it is printed before the
  first `=== gate:` banner. `a` and `c` run. With `GATES_FAULT_LOSE='a c'`,
  neither `a` nor `c` has a banner, and `b` runs.
  *Control:* on today's code the variable is ignored and all three banners
  appear. That is red on arrival, for the right reason.
- **AC-3** — *The invariant: a lost gate refuses the run.* Given the AC-2
  manifest, when a full run executes:
  (a) with `GATES_FAULT_LOSE='b'`, the output contains `FAIL         b
  (declared in project.conf but never accounted for)` and `1 declared gate(s)
  never accounted for; this run certifies nothing.`, it does not contain
  `All required gates passed`, and it exits 1;
  (b) with `GATES_FAULT_LOSE='a c'`, both refusal lines appear, `a` before
  `c`, with `2 declared gate(s) never accounted for; …`, and it exits 1;
  (c) with `b` changed to `optional` and `GATES_FAULT_LOSE='b'`, the same
  refusal applies and it exits 1. A lost optional gate is not a WARN.
  *Control:* this must be earned twice. On today's code the seam is absent,
  so all three gates run, the run passes and it exits 0. That is red for the
  wrong half of the reason. After GREEN, the orchestrator mutates the
  committed `gates.sh` so the refusal never fires, keeping the seam. Every
  case (a) to (c) must go red, and a seam-only implementation must print
  `All required gates passed (2 ran, …)` / `(1 ran, …)`, exit 0.
- **AC-4** — *The refusal reaches the record.* Given the AC-2 manifest, a
  story file in the fixture, and `GATES_FAULT_LOSE='b'`, when
  `gates.sh --story <id>` runs in full, then the story's `## Gate results`
  block has `result: fail`, and its summary lines include the refusal line for
  `b`. `.claude/state/last-gate-run` contains `RESULT=fail`.
  *Control:* with the refusal mutated away (the AC-3 mutation), the record
  reads `result: pass (2 ran, 0 unconfigured, 0 known)` and the stamp reads
  `RESULT=pass`. On today's code it reads `result: pass (3 ran, …)`.
- **AC-5** — *The subsets refuse on a lost gate, and never blame one they
  excluded.* Given the AC-2 manifest plus a required gate `s`
  (`printf 'Tests 1 passed\n'`) with `slow | s | fixture: stands in for a
  release build`, and an optional gate `o` (`printf 'Tests 1 passed\n'`):
  (a) `--fast` with `GATES_FAULT_LOSE='b'`: `b` is refused, `--fast skipped:
  s` is printed, no refusal line names `s`, and it exits 1;
  (b) `--required` with `GATES_FAULT_LOSE='b'`: `b` is refused, no refusal
  line names `o`, and it exits 1;
  (c) `--gate b` with `GATES_FAULT_LOSE='b'`: `b` is refused, and it exits 1,
  not 2 (`b` is declared, so the "no gate named" error must not fire);
  (d) `--gate a` with `GATES_FAULT_LOSE='b'`: `b` is refused, and it exits 1,
  because a lost line was never reached to be excluded (open question 5).
  *Control:* on today's code all four exit 0 (the seam is absent). After
  GREEN, (a) to (d) go red under the AC-3 mutation, and the AC-6 subset cases
  go red under their own mutations.
- **AC-6** — *No false refusal: every disposition counts as accounted for.*
  These are the negative controls on the invariant itself. The invariant also
  runs on every CI job, so a false refusal blocks every PR. Two manifests,
  both **measured at PLANNED on HEAD 1fa9815**, with outputs that must stay
  exactly as they are, minus timings:
  - *Healthy*:
    ```
    gate     | a | required | . | printf 'Tests 3 passed\n'
    evidence | a | Tests [1-9]
    gate     | k | optional | . | exit 1
    waiver   | k | fixture: known red
    gate     | w | optional | . | exit 1
    gate     | u | optional | . |
    gate     | s | required | . | printf 'Tests 2 passed\n'
    evidence | s | Tests [1-9]
    slow     | s | fixture: stands in for a release build
    ```
    | Flags | Summary line | Exit |
    |---|---|---|
    | none | `All required gates passed (4 ran, 1 unconfigured, 1 known).` | 0 |
    | `--fast` | `(3 ran, 1 unconfigured, 1 known)`, plus `--fast skipped: s` | 0 |
    | `--required` | `(2 ran, 0 unconfigured, 0 known)` | 0 |
    | `--gate w` | `(1 ran, 0 unconfigured, 0 known)` | 0 |
    | `--gate u` | `(0 ran, 1 unconfigured, 0 known)` | 0 |
  - *Manifest errors*: one gate per pre-run refusal (`e_slow` with a
    reasonless `slow`, `e_nocnt` with a reasonless `no-count`, `e_floor` with
    `floor | e_floor | abc`, `e_cif` with a sourceless `ci-factor | e_cif |
    3.4`, `e_waive` required with a waiver, `e_nocmd` required with no
    command), plus `ok`. A full run shows exactly the six existing `FAIL` lines
    with their current reasons, `PASS ok`, `6 required gate(s) failed.`, and
    exit 1.
  In every one of these runs, `never accounted for` appears nowhere in the
  output.
  *Control:* these pass on arrival, because there is no invariant yet, and
  they earn their red after GREEN by mutation. The orchestrator makes the
  invariant forget one disposition at a time, restoring the file between
  runs:
  (m1) `--fast` skips are not counted, so the `--fast` row must refuse `s`;
  (m2) filtered gates are not counted, so the `--required` and `--gate` rows
  must refuse;
  (m3) pre-run manifest-error FAILs are not counted, so the error manifest must
  add refusals;
  (m4) `UNCONFIGURED` is not counted, so the full and `--gate u` rows must
  refuse.
  Each mutation must turn at least one AC-6 case red.
- **AC-7** — Given `bash scripts/selftest.sh`, when it runs in full after
  GREEN, then every suite passes, including MC-046's tests and the MC-045
  byte-identical and exec-count tests. The output tail is pasted into
  `## Notes`. *Control:* `selftest` is a CI step, not a `project.conf` gate,
  so this is the story's only in-story evidence that the rest of the harness
  still works.

## Amendments

<!-- Acceptance criteria are frozen once the story leaves PLANNED. If one turns
     out to be wrong or unsatisfiable, stop, put it to the product owner, and
     record the change here: which AC, what it said, what it says now, who
     approved it and why. check-boundaries.sh fails a PR whose criteria differ
     from the base branch without an entry here. Omit the section if unused.
     Where the change came from a subagent's claim that the criterion was
     wrong, record the ORCHESTRATOR'S OWN reproduction of it - different
     inputs, not the subagent's code. That claim is also what an agent says
     when it wants to stop failing. -->

## Model guidance

<!-- Optional, written by the Lead PO BEFORE the phase it applies to. Use it
     when a phase of this story is worth running on a different model from the
     default, and make it falsifiable rather than folklore:
       * which phase, which model, and why that phase specifically
       * what the orchestrator should stay on
       * HOW to brief it differently - a model chosen for judgement wants the
         criteria and the constraints, not a pre-decided test design
       * the ORACLE PARTITION of the criteria: which are settled (read the
         numbers out, do not calibrate), which are oracle-free (invent the
         metric and demand a negative control that fires hard), which are
         mechanical (pin exactly). Measured to matter more than the model
       * a success condition that could come out either way
     Then record the VERDICT against that condition when the phase ends, with
     evidence. The verdict is the part that gets skipped, and without it a model
     choice becomes a habit nobody can argue with. -->

No model change: every phase runs on the default. This section exists only to
carry the oracle partition into the RED dispatch.

**Oracle partition.**

- **Mechanical, pin exactly:** AC-2, AC-3, AC-4, AC-5. The refusal line, the
  count line, the `note:` prefix, the exit code and the record's `result:`
  field are quoted in the criteria. Do not loosen them to "contains FAIL" when
  the gate id is available. A refusal that names the wrong gate must not pass.
- **Settled, read the numbers out:** AC-1's control (`1 ran`, exit 0, `later`
  absent) and every AC-6 row, all measured at PLANNED on HEAD 1fa9815. Do not
  re-derive them. If a number differs on the post-MC-046 code, report it
  rather than adjusting the test. A difference in AC-6 means MC-046 changed a
  healthy summary, which is a finding in its own right.
- **Oracle-free:** none.

**The judgement call RED makes.** AC-1 is a disjunction. Its test must be red
on today's code, and it must accept both a refusal and a genuine run of
`later`. It must not accept "`later` absent and the run passed". Check the
test against all three outcomes; the first is measured, and the other two can
be produced by hand-editing the fixture's expected output in a scratch copy.

## Out of scope

<!-- Explicit non-goals. Prevents the Feature Developer from over-building. -->

- **Closing the in-place-rewrite path itself.** Snapshotting the manifest is
  *permitted* by AC-1, but not required. MC-046's Out of scope already says
  "a gate can still `rm` the manifest": `project.conf` is trusted input. This
  story makes such a loss *visible*. It does not have to make it impossible.
- **Surplus accounting.** A declared entry accounted for twice, or an
  accounted id that was never declared. No known path produces either, and the
  seam cannot simulate them, so any test would pass on arrival and earn
  nothing. An implementation that compares the two lists exactly will catch
  them anyway. That is allowed, but it is not claimed and not tested. (Open
  question 8.)
- **Duplicate gate ids in `project.conf`.** Accounting is per line (see
  Definitions), so two `gate | unit` lines are two entries. Whether `--audit`
  should reject duplicates is a separate story.
- **`--list` and `--audit`.** Neither runs a gate command, and neither applies
  the invariant or reads `GATES_FAULT_LOSE`. Their output must not change: the
  MC-045 byte-identical and exec-count tests pin it, and AC-7 runs them.
- **A new format for the pass line.** No `(N of M declared)` suffix, and no
  change to `All required gates passed (…)` on a healthy run. MC-046 pins that
  string. (Open question 6.)
- **A distinct exit code for a lost gate.** It exits 1, like a required
  failure. 2 is a usage error and 3 is "lock not obtained"; see open
  question 4.
- **A gate that kills `gates.sh` itself** (`kill $PPID`, an out-of-memory
  kill). No summary is printed and the exit is non-zero already. That is not a
  false certification.
- **`check-boundaries.sh`, `lib.sh`, `phase.sh`, `selftest.sh`,
  `project.conf`.** Only `scripts/gates.sh` changes on the source side, and
  only `.claude/tests/gates.test.sh` on the test side.
- **Any other change to how a gate runs**: cwd, `tee`, `PIPESTATUS`,
  evidence, floors, the lock, `record_in_story`, or what MC-046 changed about
  stdin and descriptors.
- **Using `GATES_FAULT_LOSE` for anything but tests.** Do not document it in
  `--help`, and do not add any other fault modes (duplicate, reorder, crash).

## Design notes

<!-- Filled by the Lead Designer for user-facing stories: layout, states,
     tokens, accessibility requirements. Omit for non-UI stories. -->

## Test plan

<!-- Filled by the Test Developer during RED: which tests, at which level,
     and which AC each one covers. -->

One level: integration. `scripts/gates.sh` runs as a process against a fixture
from `make_project_fixture` / `write_conf`, and every gate command is a
`printf` / `exit` one-liner. All tests are in `.claude/tests/gates.test.sh`,
appended after the MC-046 blocks, as six new `describe` blocks. That makes 66
new assertions; the 110 that were there at HEAD are unchanged.

New helpers, local to the suite:

- `seam <ids> [args]` runs gates.sh with `GATES_FAULT_LOSE=<ids>` set for that
  one process. The suite never exports the variable, and it `unset`s it before
  the first MC-047 block.
- `assert_line` / `assert_no_line` check for a *whole* line (`grep -Fx`), so a
  refusal naming `ab` cannot pass for `b`.
- `first_line_no` gives the number of the first line that starts with a prefix,
  for the ordering checks.
- `refusal <id>` and `lost_count <n>` return the exact pinned strings.
- `ac1_outcome` sorts a run into `ran`, `refused`, `false-cert` or `other`.
- `verdict_of` extracts every status line, the `--fast skipped` line and the
  verdict line, and masks `(<n>s` as `(Ns`.

| Block | AC | Assertions | What they pin |
|---|---|---|---|
| a gate that rewrites project.conf in place cannot make a later gate vanish from a certified run | AC-1 | 5 | Case 1 (`later` passes): the outcome is `ran` or `refused`, and is never `false-cert`. Case 2 (`later` exits 1): exit 1, a line starting `FAIL         later (`, and no `All required gates passed`. Each case calls `write_conf` afresh and runs through `gates_isolated`. |
| GATES_FAULT_LOSE loses exactly the gates it names, and says so before any gate runs | AC-2 | 9 | With `b` lost: no `=== gate: b (`, the `a` and `c` banners present, a line starting `note: GATES_FAULT_LOSE is set` that contains the token `b`, and that note on an earlier line than the first `=== gate:`. With `'a c'` lost: no `a` or `c` banner, and the `b` banner present. |
| a lost gate refuses the run | AC-3 | 15 | (a) The exact refusal line for `b`, `1 declared gate(s) never accounted for; this run certifies nothing.`, no pass line, exit 1. (b) Refusals for `a` and `c`, with `a` on an earlier line, the count `2`, no pass line, exit 1. (c) `b` optional: the refusal, the count `1`, no `WARN         b`, no pass line, exit 1. (a) and (b) reuse the AC-2 runs. |
| the refusal reaches the story record and the stamp | AC-4 | 3 | `gates.sh --story T-47` with `b` lost. `## Gate results` has a line matching `^ *result: fail( \|$)` and the line `    FAIL         b (declared in project.conf but never accounted for)`, and `last-gate-run` contains the line `RESULT=fail`. |
| --fast, --required and --gate refuse a lost gate, and never blame one they excluded | AC-5 | 16 | For each of `--fast`, `--required`, `--gate b` and `--gate a` with `b` lost: the refusal for `b`, the count line with **1**, and exit 1. `--fast` also has the exact line `--fast skipped: s` and no refusal for `s`. `--required` has no refusal for `o`. `--gate b` has no `no gate named`. |
| no false refusal: every disposition counts as accounted for | AC-6 | 18 | Six runs, three assertions each: the whole `verdict_of` block (exactly the PLANNED measurement), the exit code, and no `never accounted for` anywhere. The runs are healthy full, `--fast`, `--required`, `--gate w`, `--gate u`, and the manifest-error run. |
| (the whole suite) | AC-7 | none | `bash scripts/selftest.sh` in full, after GREEN. That is the orchestrator's job, and the tail goes in `## Notes`. |

Choices a reader might question:

- **The AC-5 count line.** AC-5 does not quote it, but the Definitions fix it:
  in every AC-5 run exactly one declared line (`b`) goes unaccounted for. Pinning
  `1` is what lets m1 and m2 turn the subset cases red: a filter that forgets a
  disposition inflates the count.
- **AC-6 pins the whole block, not just the verdict line.** "Byte-for-byte what
  it is today" is the claim. Every refusal line starts `FAIL`, so the block
  catches one even if the implementation put its count elsewhere.
- **The note's position.** It is compared by line number in the merged
  stdout+stderr capture. Both streams are written unbuffered by bash, so the
  order is reliable whichever stream the note goes to.

## Handoff: RED -> GREEN

<!-- Filled by the Test Developer at the end of RED. This is the ONLY channel
     to the Feature Developer, whose context is fresh. Must contain:
       * the exact command that runs the new tests
       * the failure output, and why it is the RIGHT failure
       * every file touched, and which AC each test covers
       * the EXPORT SHAPE the tests already pin: every module they import, the
         exact exported names and signatures, and the types the assertions
         destructure. Not a suggestion - a test already imports them, so a
         wrong guess is a compile error. Say what the tests do NOT constrain
         too, so it stays the implementer's choice.
       * any test that passed on arrival, and the probe or negative control
         that earns it
       * the EXPECTED VALUE of every negative control, as a table: threshold,
         candidate range, and the number the control measured. In RED the
         suite fails at import, so no assertion in it has run - the controls
         are claims until GREEN confirms them against the shipped module
       * anything discovered that changes the approach -->

### Command

```bash
bash scripts/selftest.sh gates      # ~4 min 13 s on this machine; 176 assertions
```

### Failure output (RED, on 50e475b plus this test file)

```
  a gate that rewrites project.conf in place cannot make a later gate vanish from a certified run
    FAIL the gate after an in-place rewrite of project.conf either runs or is refused by name
         outcome: false-cert (exit 0); wanted 'ran' or 'refused'
    FAIL and the run never certifies itself with that gate absent from the output
    FAIL a failing required gate after an in-place rewrite fails the run
         expected: 1
         actual:   0
    FAIL and the summary has a FAIL line for it, its own or the refusal
    FAIL and the run is not certified

  GATES_FAULT_LOSE loses exactly the gates it names, and says so before any gate runs
    FAIL a gate the seam names never runs
    FAIL the seam announces itself with a note: GATES_FAULT_LOSE is set line
    FAIL and the note names the gate it will lose
    FAIL and the note comes before the first gate banner
    FAIL a seam naming two gates loses the first
    FAIL and the second

  a lost gate refuses the run
    FAIL (a) a lost gate gets its own refusal line
    FAIL (a) and the refusals are counted
    FAIL (a) and the run is not certified
    FAIL (a) and exits 1
    FAIL (b) two lost gates: the first is refused
    FAIL (b) and the second
    FAIL (b) in manifest order
    FAIL (b) and both are counted
    FAIL (b) and the run is not certified
    FAIL (b) and exits 1
    FAIL (c) a lost optional gate is refused like a required one
    FAIL (c) and counted
    FAIL (c) and the run is not certified
    FAIL (c) and exits 1

  the refusal reaches the story record and the stamp
    FAIL a full run that lost a gate records result: fail
    FAIL and the record carries the refusal line
    FAIL and the stamp says RESULT=fail
         expected a line exactly: RESULT=fail
         actual:   RESULT=pass

  --fast, --required and --gate refuse a lost gate, and never blame one they excluded
    FAIL (a) --fast: the lost gate is refused
    FAIL (a) --fast: exactly one gate is counted lost
    FAIL (a) --fast: and exits 1
    FAIL (b) --required: the lost gate is refused
    FAIL (b) --required: exactly one gate is counted lost
    FAIL (b) --required: and exits 1
    FAIL (c) --gate b: the lost gate it selected is refused
    FAIL (c) --gate b: exactly one gate is counted lost
    FAIL (c) --gate b: and exits 1, not 2
    FAIL (d) --gate a: a lost gate it did not select is still refused
    FAIL (d) --gate a: exactly one gate is counted lost
    FAIL (d) --gate a: and exits 1

  no false refusal: every disposition counts as accounted for

gates: 136 passed, 40 failed
```

The AC-1 body, under the first failure, is the defect itself:
`PASS first … / All required gates passed (1 ran, 0 unconfigured, 0 known).`
with `later` absent, exit 0.

**Why these are the right failures.** AC-1 fails on the real loss: the
in-place rewrite truncates the manifest, the streaming loop never reaches
`later`, and the run certifies a pass. AC-2 to AC-5 fail because the seam does
not exist yet. `GATES_FAULT_LOSE` is ignored, `b` runs, and every run passes
with exit 0. On current code the summaries read `(3 ran, …)` for AC-3 and
`(4 ran, …)` / `(1 ran, …)` for AC-5, and AC-4 records `result: pass (3 ran, 0
unconfigured, 0 known)`. As the story says, this is the analogue of failing at
import: it shows the seam is missing, not that the refusal works. That is why
the seam-only control below was measured separately. No failure comes from a
harness error, a timeout or a typo: every one of the 110 assertions that were
there at HEAD still passes (136 = 110 + 26 new that pass on arrival).

### Files touched

- `.claude/tests/gates.test.sh`: six new `describe` blocks and their helpers,
  appended before `summary "gates"`. Nothing above them changed.
- `docs/backlog/stories/MC-047.md`: `## Test plan` and this handoff.

`scripts/gates.sh` in the repo is untouched (`git status`: only the two files
above). Every variant below lived in the scratchpad.

### What each test asserts, by AC

The assertion-by-assertion list is in `## Test plan`. In short, per block:
AC-1 has 5 assertions (2 cases), AC-2 has 9, AC-3 has 15 ((a) 4, (b) 6,
(c) 5), AC-4 has 3, AC-5 has 16 ((a) 5, (b) 4, (c) 4, (d) 3), and AC-6 has 18
(6 runs × 3).

### The interface the tests already pin (this is fact, not suggestion)

These are CLI and text contracts; nothing is sourced from `gates.sh`.

- **Seam:** env var `GATES_FAULT_LOSE`, a space-separated list of gate ids,
  e.g. `'b'` or `'a c'`. It is set only on the gates.sh process. A named gate's
  line is treated as never read: no `=== gate: <id> (` banner, and no status
  line of its own.
- **`b` stays declared.** `--gate b` with `b` lost must exit **1**, not 2, so
  the seam must not remove `b` from the pre-read `GATE_IDS` list that the "no
  gate named" check uses.
- **Note:** a line that *starts with* `note: GATES_FAULT_LOSE is set`. It must
  appear on an earlier line of the merged stdout+stderr than the first line
  starting `=== gate:`. When `b` is lost it contains `b` as a token delimited
  by characters outside `[A-Za-z0-9_.-]` (`lose: b`, `'b'` and `(b)` all pass).
- **Refusal:** one whole line per lost entry, in manifest order:
  `FAIL         <id> (declared in project.conf but never accounted for)` (FAIL
  plus 9 spaces).
- **Count:** the whole line
  `<N> declared gate(s) never accounted for; this run certifies nothing.`
- **Exit and verdict:** exit **1**, and `All required gates passed` appears
  nowhere in the output.
- **Record:** the story's `## Gate results` has a line matching
  `^ *result: fail( |$)` and the line
  `    FAIL         b (declared in project.conf but never accounted for)`
  (the refusal in `$results`, indented 4 by `record_in_story` as today).
  `.claude/state/last-gate-run` has a line exactly `RESULT=fail`.
- **Healthy runs:** every status line, the `--fast skipped:` line and the
  verdict line are byte-for-byte as today (see the AC-6 table below).
  `never accounted for` appears nowhere.
- **AC-1:** either `later` runs (`=== gate: later (required) ===`, a line
  starting `PASS         later (`, `All required gates passed (2 ran, 0
  unconfigured, 0 known).`, exit 0) or it is refused (its refusal line, no pass
  line, exit 1). In the `exit 1` case, a line starting `FAIL         later (`
  and exit 1.

**What the tests do NOT constrain** (the implementer's choice):

- The note's wording after its prefix, and which stream it goes to.
- Where the count line prints relative to the summary, and whether it has a
  blank line before it.
- Whether a loss also increments `fails` or prints `N required gate(s)
  failed.` alongside the count line. (This changes the mutation arithmetic
  for *existing* tests; see the note under the mutation table.)
- The text inside `result: fail (…)`, and any extra fields in the stamp.
- Refusal versus snapshot for AC-1. Both pass (demonstrated below).
- How `--list` and `--audit` treat the variable. No new test covers it; the
  Out of scope says they must not read it, and the MC-045 byte-identical and
  exec-count tests run without it set.

### Tests passing on arrival, and what earns each

26 new assertions pass on today's code, where there is no seam and no
invariant:

| Assertions | Why they pass today | What turns them red (measured on scratch variants) |
|---|---|---|
| AC-6: 18 (6 rows × 3) | there is no invariant to refuse anything | m1 to m4 below. Each row goes red under at least one mutation. |
| AC-2: `a` and `c` banners present (2), and `b` present when `'a c'` is lost (1) | nothing is lost | They guard against a seam that loses more than it names. Not independently mutated; they are the positive half of AC-2. |
| AC-3(c): no `WARN         b` (1) | `b` runs and passes | It pins "a lost optional gate is not a WARN". It is only meaningful once the seam loses `b`, and it guards an implementation that turns the loss into a WARN. |
| AC-5: `--fast skipped: s` (1), no refusal for `s` (1), no refusal for `o` (1), no `no gate named` (1) | there is no refusal and no seam | m1 turns red "no refusal for `s`". m2 turns red "no refusal for `o`". "No `no gate named`" goes red if the seam drops `b` from `GATE_IDS` (it exits 2). That one I have not run as a mutation. |

### Negative controls: expected values

The controls were measured outside the suite with scratch scripts: on the repo
code (50e475b) and on scratch copies of `gates.sh`. The copies were a
reference implementation, a snapshot fix, and the reference with the refusal
disabled. **GREEN confirms these against the shipped `gates.sh`.**

| Control | Threshold / pinned value | Measured |
|---|---|---|
| AC-1 control, `later` passes (repo, 50e475b) | must be `ran` or `refused` | `false-cert`: `All required gates passed (1 ran, 0 unconfigured, 0 known).`, exit 0, `later` absent, same as PLANNED |
| AC-1 control, `later` exits 1 (repo) | exit 1 plus `FAIL         later (` | the same `(1 ran, …)`, exit 0, `later` absent, same as PLANNED |
| AC-1 classifier, canned outputs | ran/refused accepted; everything else rejected | false-cert text, exit 0 → `false-cert`; real run, exit 0 → `ran`; real run, exit 1 → `other`; refusal of `later`, exit 1 → `refused`; refusal, exit 0 → `other`; refusal naming `first` instead, exit 1 → `other` |
| AC-1, scratch snapshot fix (loop reads a copy of project.conf taken before the loop; no seam, no refusal) | pass | both AC-1 cases pass (0 AC-1 failures; the suite's 35 failures are all AC-2 to AC-5, as expected with no seam) |
| AC-1, scratch refusal implementation | pass | both cases pass (176/176) |
| AC-3/4/5, seam kept and refusal disabled (the "seam-only" control) | must fail | AC-3(a) `All required gates passed (2 ran, 0 unconfigured, 0 known).` exit 0; (b) `(1 ran, …)` exit 0; (c) `(2 ran, …)` exit 0; AC-4 `result: pass (2 ran, 0 unconfigured, 0 known)`, `RESULT=pass`; AC-5 `--fast` `(3 ran…)`, `--required` `(3 ran…)`, `--gate b` `(0 ran…)`, `--gate a` `(1 ran…)`, all exit 0. Matches the story's AC-3/AC-4 controls. |
| AC-3/4/5 on today's code (no seam) | must fail | AC-3 (a)(b)(c) `(3 ran, …)` exit 0; AC-4 `result: pass (3 ran, 0 unconfigured, 0 known)`, `RESULT=pass`; AC-5 `--fast`/`--required` `(4 ran, …)`, `--gate b`/`--gate a` `(1 ran, …)`, exit 0 |
| AC-6 healthy, full | `All required gates passed (4 ran, 1 unconfigured, 1 known).`, exit 0 | identical on 50e475b, with the block `PASS a (Ns, observed 3)` / `KNOWN k (Ns, exit 1; fixture: known red) -> …k.log` / `WARN w (Ns, exit 1, optional) -> …w.log` / `UNCONFIGURED u` / `PASS s (Ns, observed 2)` |
| AC-6 healthy, `--fast` | `(3 ran, 1 unconfigured, 1 known)` plus `--fast skipped: s`, exit 0 | identical |
| AC-6 healthy, `--required` | `(2 ran, 0 unconfigured, 0 known)`, exit 0 | identical |
| AC-6 healthy, `--gate w` | `(1 ran, 0 unconfigured, 0 known)`, exit 0 | identical |
| AC-6 healthy, `--gate u` | `(0 ran, 1 unconfigured, 0 known)`, exit 0 | identical |
| AC-6 manifest errors | six FAILs with their current reasons, `PASS ok`, `6 required gate(s) failed.`, exit 1 | identical |

**No settled number moved.** The AC-1 control and every AC-6 row re-measured
on 50e475b match the PLANNED measurements on 1fa9815 exactly, so MC-046 did not
change any healthy summary.

### Mutation predictions (for the Verification plan)

These counts are *measured*, not only predicted. Each mutation was applied to a
scratch reference implementation, and the full suite was run against it. That
implementation passes 176/176 unmutated. It accounts for a gate at each
disposition, compares against `GATE_IDS` after the loop, appends refusals to
`$results`, and does **not** add losses to `fails`.

| Mutation | Red assertions | Which |
|---|---|---|
| refusal never fires (seam kept) | **34** | AC-1 5 (while the loop streams; **0** if GREEN snapshots the manifest, making it 29), AC-3 14 (all but "(c) not merely warned about"), AC-4 3, AC-5 12 (refusal, count and exit in each of (a) to (d)). AC-2 0. |
| m1: `--fast` skips not counted | **5** | AC-6 `--fast` row (3); AC-5(a) "and is not blamed as lost", AC-5(a) "exactly one gate is counted lost" (2) |
| m2: filtered gates not counted (both `--gate` and `--required`) | **13** | AC-6 `--required`, `--gate w` and `--gate u` rows (9); AC-5(b) "not blamed" and count (2); AC-5(c) count (1); AC-5(d) count (1). `--gate` filter only: 8. `--required` filter only: 5. |
| m3: pre-run manifest-error FAILs not counted | **2** | AC-6 manifest errors: the summary block, and "nothing is called unaccounted for". The exit code is 1 either way. |
| m4: `UNCONFIGURED` not counted (the `UNCONFIGURED $id` branch only) | **9** | AC-6 full, **`--fast`** and `--gate u` rows (3 each) |

Two differences from the story's Verification plan table, both expected:

- **m4 also turns the `--fast` row red.** `u` is `UNCONFIGURED` in `--fast`
  too. The plan names only the full and `--gate u` rows. More red, not less.
- **m1 is 5 assertions across 2 tests**, not 1. The story calls m1 the
  single-test spot check. Only one *AC-6* test catches it, but AC-5(a)'s "no
  refusal names `s`" and its count line catch it as well. For a true
  single-assertion spot check, use m3 (2 assertions, both in one AC-6 test), or
  compare m1 against "5".

**This arithmetic depends on the implementation.** If GREEN's implementation
adds each lost gate to `fails`, some *existing* assertions that expect `1
required gate(s) failed.` can also go red under m1 or m3. These are the MC-046
`--fast` block ("--fast: the run says a required gate failed") and, under m3,
the `required_gates` waiver case ("and it fails"). That would add at most 1 to
each count. The orchestrator should check the red *names* against this table,
not only the totals. Where `e_nocmd` (a required gate with no command) is
counted is also the implementation's choice. The Definitions class it as
"unconfigured", but in the reference it has its own accounting line, which
none of m1 to m4 removes. If GREEN shares one line between it and
`UNCONFIGURED`, then m4 also reddens the manifest-error row, for 11 instead
of 9.

### Admissibility

`bash scripts/gates.sh --fast` from the repo root, with this test file in
place, exit 0:

```
PASS         format (3s)
PASS         lint (11s, observed -)
PASS         typecheck (3s, observed -)
PASS         unit (29s, observed 47, floor 47)
PASS         coverage (76s, observed 1717)

--fast skipped: integration build
All required gates passed (5 ran, 0 unconfigured, 0 known).
```

As expected, the cargo gates do not touch `.claude/tests`, so they cannot see
this suite. Its only runner is `selftest.sh` (CI, gates.yml line 37). The suite
took 4 min 13 s, the same order as before. It adds 19 gates.sh invocations,
with no sleeps and no lock waits.

### Discoveries that bear on the implementation

- **"Declared" already exists.** The pre-read loop at `gates.sh` lines
  239–271 builds `GATE_IDS` *before any gate runs*. It has one entry per
  `gate` line, duplicates included, which is exactly the Definition. Comparing
  against it passes AC-1 by refusal. Re-reading `project.conf` after the loop
  would see only `BOOTSTRAPPED=yes` in AC-1 and certify the pass. AC-1 is the
  test that catches that.
- **The seam check belongs right after `id="$F2"` in the run loop**, skipped
  for `--list`/`--audit`. It must stay out of the pre-read loop, or `--gate b`
  exits 2 (AC-5(c)).
- **In the reference, a disposition is recorded at each existing `continue`**
  (the filters, the `--fast` skip, the four pre-run FAILs, the waiver FAIL, no
  command, UNCONFIGURED) and after `ran=$((ran+1))`. That keeps m1 to m4
  meaningful as single-line mutations. Marking "reached" once at the top of the
  loop would also pass every test, but then m1 to m4 cannot be expressed
  against the code: there would be no per-disposition line to delete. The
  orchestrator's Verification plan assumes those lines exist, so GREEN should
  say in `## Notes` which shape it chose.
- The refusal lines must go into `$results` **before**
  `printf '\n--- gate summary ---%b\n'`, so that they reach both the printed
  summary and `record_in_story` (AC-4).

## Regressions

<!-- REQUIRED if this story ever returned to RED after GREEN or GATES; omit
     otherwise. A test that is wrong is never edited into passing, and the
     return is not a footnote - it is the story failing to be one clean cycle,
     and the next person needs to know why. One block per return:
       * which test, what it asserted, and what was wrong with it
       * how the defect was found
       * what it asserts now
       * what earns it, since "watched it fail" cannot apply once the
         implementation exists - the corrected assertion passes on its first
         run and every run after, whether or not it asserts anything: either a
         PROBE (mutate the specific behaviour the test pins, paste the red,
         confirm the revert) or, where the defect was cost rather than
         correctness, a BEFORE/AFTER measurement taken under the gate command -
         not the plain test command, which is the faster one.
         PASTE THE OUTPUT. check-boundaries.sh refuses a PR whose Regressions
         or Gate probes section describes a failure without showing one
       * whether GREEN was a no-op, and the command output proving the source
         was untouched and still passes -->

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-23T22:33:06Z
    commit: 50e475b (working tree had uncommitted changes)
    tree:   865d08ec856b764ba8fe39f22723bc9fe824569f
    result: pass (7 ran, 0 unconfigured, 0 known)

    PASS         format (1s)
    PASS         lint (5s, observed -)
    PASS         typecheck (1s, observed -)
    PASS         unit (12s, observed 47, floor 47)
    PASS         coverage (57s, observed 1717)
    PASS         integration (97s, observed 9, floor 9)
    PASS         build (75s, observed -)

## Gate probes

<!-- REQUIRED if this story adds or changes a gate, its command, or its
     evidence line. Omit the section entirely otherwise.
     A gate that has never been observed to fail is not a gate: break the thing
     it guards, run the gate, paste the failure, revert. One block per gate:
       * what was broken, and where
       * the gate output proving it failed
       * confirmation the probe was reverted -->

## Scaffold inventory

<!-- REQUIRED for a bootstrap or chore story that writes production code under
     SCAFFOLD, where nothing forces a test to exist first, and for a spike that
     commits its throwaway code. Omit otherwise.
     One line per production file written, and for anything with behaviour
     rather than configuration, the test that covers it:
       src/core/palette.ts        - src/core/palette.test.ts
       vite.config.ts             - configuration, no behaviour
     check-boundaries.sh refuses the PR if any changed source file is not
     named here. -->

## Notes

### Open questions for the PO/user (answer before RED; the ACs use the recommended default)

**Answered 2026-09-23.** The user accepted all ten recommended defaults as
written before RED, so the criteria stand unchanged.

**AC-1 re-measured on post-MC-046 code (50e475b), as the escalation note
requires.** The in-place-rewrite manifest, with `later` as `exit 1` and a full
run with stdin at `/dev/null`, printed `All required gates passed (1 ran, 0
unconfigured, 0 known).` and exited 0, with `later` absent. MC-046 shipped
`</dev/null` and the loop still streams the manifest, so AC-1 is still red on
arrival and the escalation does not apply.

Each item is a counting or reporting rule the criteria currently assume. If
you change one, the criteria are edited while the story is still PLANNED, so
no amendment is needed.

1. **What counts as declared.** *Recommended:* every `gate` line, counted per
   line, read before any gate command runs. The alternative is counting per
   unique id, which would hide a duplicated line.
2. **Manifest-error FAILs count as accounted for** (reasonless
   `slow`/`no-count`, a broken `floor`/`ci-factor`, a waiver on a required
   gate, a required gate with no command). *Recommended:* yes. They already
   fail the run with their own reason, and a second "never accounted for" line
   would be noise.
3. **`UNCONFIGURED` and `KNOWN` count as accounted for.** *Recommended:* yes.
   Both are reported outcomes.
4. **A lost gate exits 1**, the same as a required failure. *Recommended:* 1.
   Every caller (CI, `record_in_story`, the stamp) already treats 1 as "not a
   pass". A distinct code (such as 4) would say more, but every consumer would
   have to learn it.
5. **A lost gate is refused even when a filter would have excluded it**
   (`--gate a` losing `b`, `--required` losing an optional gate).
   *Recommended:* refuse. A lost line means the loop's integrity is broken,
   whatever was selected. The alternative is to apply the filter to the
   declared list, so a filtered run only checks what it selected. That is
   quieter, but it would hide a loss on exactly the runs RED and GREEN use
   most (`--fast`, `--gate unit`).
6. **The healthy pass line is unchanged** (no `N of M declared` suffix).
   *Recommended:* unchanged. MC-046's tests pin it, and a suffix is noise when
   the numbers always agree.
7. **AC-1 allows either fix** (refuse, or snapshot the manifest so that
   `later` runs). *Recommended:* allow either. The alternative is to require
   the refusal specifically, which would forbid the snapshot.
8. **Surplus accounting** (a gate accounted for twice, or never declared) is
   not claimed. *Recommended:* out of scope, because nothing can make it red
   (see `## Out of scope`).
9. **The refusal and seam wording** (`FAIL         <id> (declared in
   project.conf but never accounted for)`, `<N> declared gate(s) never
   accounted for; this run certifies nothing.`, `note: GATES_FAULT_LOSE is
   set …`) and the **seam's name**, `GATES_FAULT_LOSE`. *Recommended:* as
   written. Changing any of them is a PLANNED edit.
10. **Optional lost gates are refused (exit 1), not WARNed.** *Recommended:*
    refuse. The optional gate might well have WARNed, but losing it means the
    runner itself is broken.

### Escalation: if AC-1 is green on arrival

If MC-046 is merged with a loop that snapshots the manifest, AC-1's control no
longer reproduces, and AC-1's test passes on arrival. In that case, do not
write around it. Stop at the start of RED and report to the PO. The likely
re-plan is `type: feature`, with AC-1 either dropped under `## Amendments` or
kept as MC-046 regression coverage earned by a recorded mutation (restore
streaming in a scratch copy and watch it go red). AC-2 to AC-6 are unaffected
either way, because they do not depend on any real loss path.

### Verification plan (orchestrator, after GREEN)

These are the mutations named in the controls of AC-3 to AC-6. Each is run
against the committed `scripts/gates.sh`, with the red count compared to the
Test Developer's prediction in `## Handoff`. The file is restored
byte-for-byte after each run, and `bash scripts/selftest.sh gates` is
confirmed green. The outputs are pasted here.

| Mutation | Must turn red |
|---|---|
| refusal never fires (seam kept) | AC-3 (a)(b)(c), AC-4, AC-5 (a)–(d) |
| m1: `--fast` skips not counted | AC-6 `--fast` row |
| m2: filtered gates not counted | AC-6 `--required`, `--gate w`, `--gate u` rows |
| m3: manifest-error FAILs not counted | AC-6 error manifest |
| m4: `UNCONFIGURED` not counted | AC-6 full and `--gate u` rows |
| "declared" re-read from `project.conf` after the loop | AC-1 (only while the loop streams the manifest) |

The single-assertion mutation to prefer for the ladder's spot check is m1,
because only one test is predicted to catch it.

### Gate decision (PLANNED, 2026-09-23)

`required_gates: []`, re-derived the same way as MC-045 and MC-046. Every
`gate` line in `project.conf` (lines 192–198: `format`, `lint`, `typecheck`,
`unit`, `coverage`, `integration`, `build`) runs `cargo`. None of them runs
`scripts/selftest.sh` or reads `.claude/tests/**`, so no gate can go red if
this story's artifact breaks, and escalating one would bind nothing. What
verifies it is `.github/workflows/gates.yml` line 37 (`bash
scripts/selftest.sh`, blocking), re-checked on 2026-09-23. Line 49 (`bash
scripts/gates.sh`, full) runs the invariant over the real manifest on every
CI job. That is the live form of AC-6's no-false-refusal control.

`## Gate probes`: not required. No `gate`, `evidence`, `floor` or other
manifest line changes. The seam plays the role a gate probe would, on every
test run.

### Tests and commands

- Tests go in `.claude/tests/gates.test.sh`, under new `describe` blocks,
  using the suite's `gates()` helper (it `cd`s into `$FIX` and merges stderr).
  Fixtures use `make_project_fixture` / `write_conf` from `.claude/tests/_lib.sh`.
  AC-4 needs a story file in the fixture; use `story` from `_lib.sh`.
- Set the seam per call (`GATES_FAULT_LOSE='b' bash scripts/gates.sh …`
  inside the fixture), never exported suite-wide. Later `describe` blocks must
  not inherit it.
- AC-1 rewrites the fixture's `project.conf`, so each AC-1 case must call
  `write_conf` afresh.
- Run: `bash scripts/selftest.sh gates`. Full: `bash scripts/selftest.sh`
  (AC-7).
- The harness stays bash, awk and coreutils. No python.
- The `gates.sh` flags are `--fast`, `--required`, `--gate <id>`, `--list`,
  `--audit`, `--no-wait` and `--story <id>`.

### Split decision

One story, not split. The seam and the invariant are one behaviour. The seam
has no purpose without the invariant, and the invariant cannot be tested
without the seam, so splitting them would give a first story with nothing to
be red against. AC-1 is the same invariant observed through a real loss. The
change is to one source file and one test file.

### depends_on

`[MC-046]`, which is PLANNED at the time of writing, so `phase.sh set MC-047
RED` will refuse until MC-046 is DONE. That is intended. This story's
invariant sits on the loop MC-046 changes, and AC-1's control has to be
re-measured on that loop before RED writes a line.

### Reproduction scripts (PLANNED)

These were run from the scratchpad, not committed, on fixtures built by
`make_project_fixture` at HEAD 1fa9815. The `</dev/null` MC-046 candidate was
produced by editing the **fixture's** copy of `gates.sh` only. The repo's
`gates.sh` was not touched. Measured: the in-place rewrite (lost, exit 0),
the atomic rewrite (not lost, `FAIL later`, exit 1), MC-046 case B and MC-046
AC-2 under the candidate (both `FAIL later`, exit 1), the in-place rewrite
under the candidate (lost, exit 0), and every AC-6 row.

### GREEN (2026-09-23)

Only `scripts/gates.sh` changed (+56/-4). `.claude/tests/**` untouched.
`bash scripts/selftest.sh gates`: **176 passed, 0 failed** (RED: 136/40).

**Shape: per-disposition accounting, refusal by comparison with the pre-read
`GATE_IDS`.** AC-1 is met by *refusal*; the loop still streams the manifest,
so the "declared re-read after the loop" mutation stays meaningful. Each
disposition appends the id to `accounted` with its own statement, on its own
line, at the site that disposes of the gate. Nothing is marked at the top of
the loop. After the `--list`/`--audit` exits, each `GATE_IDS` entry (per line,
manifest order) is paid off by one occurrence in `accounted`; anything unpaid
is refused. A lost gate does **not** increment `fails`: the counts in the
handoff's mutation table apply as written.

Line numbers in the uncommitted `scripts/gates.sh` as of this note (they are
what the orchestrator should target; each is `accounted="$accounted $id"`
unless stated):

| Disposition | Line(s) | Mutation |
|---|---|---|
| seam: `case "$fault_lose" in *" $id "*) continue ;; esac` | 441 | - |
| seam note (`note: GATES_FAULT_LOSE is set; this run will lose: …`), printed only when set and not `--list`/`--audit` | 424-428 | - |
| not selected by `--gate` | 458 | m2 |
| not selected by `--required` | 462 | m2 |
| left out by `--fast` | 473 | m1 |
| pre-run FAIL: `slow` with no reason | 505 | m3 |
| pre-run FAIL: `no-count` with no reason | 518 | m3 |
| pre-run FAIL: broken `floor` | 548 | m3 |
| pre-run FAIL: broken `ci-factor` | 579 | m3 |
| pre-run FAIL: waiver on a required gate | 591 | m3 |
| `FAIL … (required gate has no command …)` | 627 | none of m1-m4 (own line, not shared with UNCONFIGURED) |
| `UNCONFIGURED` | 631 | m4 |
| ran (after `ran=$((ran+1))`) | 648 | - |
| **the refusal** (the unpaid branch: refusal line into `$results`, `lost++`) | **764** | "refusal never fires": replace with `*) : ;;` |
| stamp/record `fail` when `lost > 0` | 789-791 | - |
| count line and exit 1 | 835-840 | - |

m3 can delete all five lines or any one of them: each error gate is a single
line, so either form turns the error manifest red.

**Mutation predictions: unchanged from the handoff.** No count moves under this
shape: refusal-never-fires **34**, m1 **5**, m2 **13** (`--gate` line alone 8,
`--required` line alone 5), m3 **2**, m4 **9**. Losses are kept out of `fails`,
and `e_nocmd` has its own accounting line (627), so the "+1" and "11 instead of
9" caveats in the handoff do not apply.

A scratch pre-check. This is not the orchestrator's run. I applied each
mutation to a fixture copy of `gates.sh` (the repo file was never mutated) and
ran each control manifest directly, outside the suite. Under every mutation,
the rows that gained a refusal were the ones the Verification plan names, and
no others: m1 `--fast`; m2 `--required`, `--gate w`, `--gate u`; m3 the error
manifest; m4 full, `--fast`, `--gate u`. The AC-1 to AC-5 seam runs refuse
under all four, as they should.

**Negative controls, re-measured on the shipped code.** Measured with a
scratch script, `controls.sh`, on fixtures from `make_project_fixture`.

| Control | RED-recorded | Measured on shipped `gates.sh` |
|---|---|---|
| AC-1, `later` passes, repo 50e475b (pre-GREEN) | `false-cert`: `(1 ran, 0 unconfigured, 0 known)`, exit 0, `later` absent | not re-run (code replaced); shipped: `FAIL         later (declared in project.conf but never accounted for)`, `1 declared gate(s) never accounted for; …`, exit 1 → `refused` |
| AC-1, `later` exits 1 | same false-cert, exit 0 | shipped: same refusal of `later`, exit 1 |
| seam-only (refusal line 764 disabled), AC-1 | (not recorded for seam-only) | `(1 ran, 0 unconfigured, 0 known)`, exit 0, both cases. The streaming loss is still real. |
| seam-only, AC-3 (a)/(b)/(c) | `(2 ran…)` / `(1 ran…)` / `(2 ran…)`, exit 0 | identical |
| seam-only, AC-4 | `result: pass (2 ran, 0 unconfigured, 0 known)`, `RESULT=pass` | identical |
| seam-only, AC-5 `--fast` / `--required` / `--gate b` / `--gate a` | `(3 ran…)` / `(3 ran…)` / `(0 ran…)` / `(1 ran…)`, exit 0 | identical |
| AC-6 healthy full | `(4 ran, 1 unconfigured, 1 known)`, exit 0, block as listed | identical, block identical |
| AC-6 `--fast` | `(3 ran, 1 unconfigured, 1 known)` + `--fast skipped: s`, exit 0 | identical |
| AC-6 `--required` | `(2 ran, 0 unconfigured, 0 known)`, exit 0 | identical |
| AC-6 `--gate w` | `(1 ran, 0 unconfigured, 0 known)`, exit 0 | identical |
| AC-6 `--gate u` | `(0 ran, 1 unconfigured, 0 known)`, exit 0 | identical |
| AC-6 manifest errors | six FAILs, `PASS ok`, `6 required gate(s) failed.`, exit 1 | identical: six FAILs with the same reasons, `6 required gate(s) failed.`, exit 1, no `never accounted for` |

No divergence. One benign difference: my scratch error manifest had no
`evidence` line for `ok`, so the scratch run shows `PASS ok (Ns) -- no evidence
line …` where the suite shows `(Ns, observed 1)`. The suite's own fixture
passes.

Also measured: `--list` and `--audit` print byte-identical output with
`GATES_FAULT_LOSE=a` set and unset. The seam is not read on either path. The
record for a lost gate reads `result: fail (1 declared gate(s) never accounted
for, 0 required gate(s) failed)`. The tests leave that text unconstrained.

The note goes to stdout, before the first banner. A healthy run over the real
manifest is unchanged: the `--fast` summary below is byte-for-byte the one RED
recorded under Admissibility, except for the timings.

The parse path gained no fork. The only per-gate additions are a `case` and a
string append, and the comparison after the loop is builtins only (a `for`
under `set -f`, and `${var/pat/rep}`).

**`bash scripts/gates.sh --fast`** from the repo root, exit 0:

```
--- gate summary ---
PASS         format (2s)
PASS         lint (3s, observed -)
PASS         typecheck (3s, observed -)
PASS         unit (20s, observed 47, floor 47)
PASS         coverage (92s, observed 1717)

--fast skipped: integration build
This is a subset, not a verdict. The full run before REVIEW is what judges the story.

(not recorded in the story: a partial run is not evidence of anything)

All required gates passed (5 ran, 0 unconfigured, 0 known).
```

### AC-7: `bash scripts/selftest.sh` in full, after GREEN

On the uncommitted GREEN tree (50e475b plus `gates.test.sh`, `gates.sh` and
this story), exit 0:

```
boundaries: 23 passed, 0 failed
doctor: 4 passed, 0 failed
gate-reminder: 19 passed, 0 failed
gates-parse: 43 passed, 0 failed
gates: 176 passed, 0 failed
lib: 85 passed, 0 failed
manifest: 25 passed, 0 failed
phase-guard: 115 passed, 0 failed
phase: 24 passed, 0 failed
profiles: 42 passed, 0 failed

10 harness suite(s) passed.
```

`gates-parse` holds the MC-045 byte-identical and exec-count tests, and
`gates` holds MC-046's. All of them pass.

Not yet run: the full `bash scripts/gates.sh` (the recorded run). The
orchestrator's brief for GREEN stopped at `--fast`, and the gate record must be
stamped against the committed code, so it is left for GATES.

### Orchestrator verification (GREEN, 2026-09-23)

These checks were run by the orchestrator, separately from the subagents.

- **The test file is frozen.** `.claude/tests/gates.test.sh` has not been
  modified since the orchestrator's RED run (its mtime is older than that run's
  output). `bash scripts/selftest.sh gates` on the shipped code gave
  `gates: 176 passed, 0 failed`.
- **Mutations, run against the committed implementation.** Each mutation was
  applied with an exact-string edit. The file was restored from a byte copy
  after each one and confirmed with `cmp`.

**Refusal never fires, with the seam kept.** Line 764's `*)` branch was
replaced with `*) : ;;`. Predicted: 34.

```
gates: 142 passed, 34 failed
```

The 34 failures are exactly the predicted set: AC-1 (5), AC-3 (a)(b)(c) (14),
AC-4 (3) and AC-5 (a)–(d) (12). No AC-6 test and no pre-existing test went
red.

**m3: manifest-error FAILs not counted.** The five `accounted=` lines at 505,
518, 548, 579 and 591 were deleted. Predicted: 2, both in one test.

```
gates: 174 passed, 2 failed
    FAIL manifest errors: the summary is exactly what it was
    FAIL manifest errors: and nothing is called unaccounted for
```

Both counts match the handoff's predictions. The mutation table is now
evidence, not a claim. An earlier attempt at the first mutation used `sed`,
and the Bash tool collapsed the backslashes in the pattern, so `sed` matched
nothing and that run tested the unmutated file (176/0). It was discarded and
re-done with an exact-string edit.
