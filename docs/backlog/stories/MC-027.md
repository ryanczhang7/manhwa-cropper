---
id: MC-027
title: The page column is located by its flat page margins
slug: the-page-column-is-located-by-its-flat-p
epic: EPIC-05
type: feature
status: in-review
phase: REVIEW
branch: story/MC-027-the-page-column-is-located-by-its-flat-p
depends_on: [MC-026]
required_gates: [integration]
---

## Context

Half of the locator problem is solved and has been for two stories; nobody has
shipped it because it was measured only as a by-product of a rule that failed on
the other axis.

A reader screenshot has a **page margin** on each side - the flat browser
background to the left and right of the page column - and a **panel gutter**
between panels, which is a different thing with a different detector
(`docs/wiki/architecture.md`, "Two kinds of blank space"). This story is the
page margin only. MC-028 is the panel gutter, and the row axis stays exactly
where MC-025 left it.

**The measurement that makes this a `feature` and not a spike** is MC-026's
finding 7. Starting from the rect the **real** pipeline hands the flat stage -
`trim_uniform` -> `content_box` -> `trim_within`, no marked rect anywhere - and
measuring column flatness over a **central band of that rect's rows** rather
than all of them, the widest textured band places the page's left and right
edges inside MC-019's window on **19 of 21** corpus entries:

```
central fraction of the rect's rows    1.0    0.8    0.6    0.5    0.4   0.25
widest textured band                     2     19     19     18     18     16
outermost textured columns               0     15     15     12     13     14
```

Three things in that table are the whole story. The central-band restriction is
what makes it work at all (1.0 scores 2, because the browser chrome and taskbar
rows that survive inside the rect are textured out to column 0 - MC-025's
diagnosis, from the other side). The **widest-band** selector beats
`textured_span`'s **outermost** rule by four entries, where on the row axis
every selector scored identically. And the parameter has a real window, 0.6 to
0.8, unlike the bracket depth of MC-026's retired premise, which was inert at
every value.

**The hard part is the same one MC-025 hit**, and it is where this story's
engineering content is:

1. `flat::narrow` stands down on any axis where `strong_lines` found anything,
   and on a real screenshot it always has. Two frozen assertions in
   `crates/core/tests/decide.rs` enforce that guard - MC-025's orchestrator
   confirmed both by mutation - so it cannot simply be deleted. Read MC-025's
   `## Notes`, "GREEN: the wiring, and the alternatives the frozen suite
   rejected", before writing a line: it lists four wirings and the exact
   assertion that killed each.
2. `edges::textured_span` takes the **outermost** textured indices and does not
   stop at a flat run between them. That is deliberate - it is how MC-005
   decision 13 is honoured - and it is why the column axis scores 15 rather than
   19. Adding a selector is in scope; changing what `textured_span` means is
   not.

`Screenshot (1661).png` is the known awkward file: a second textured region to
the right of the page. MC-025 recorded it as a `+1086 px` error under the
outermost rule and MC-026 finding 7 records it resolving correctly, `-4,+3`,
under a widest-band rule from the pipeline's own rect. It is a case, not an
obstacle.

Gate that fails if this breaks: `unit` (required) for the locator itself, and
`integration` for AC-4 and AC-5, which are corpus criteria - `integration` is
**optional for the repo**, so it is named in `required_gates` above. AC-4 and
AC-5 are `#[ignore]`d tests in `crates/engine/tests/corpus.rs`, which MC-026
creates; add to that target rather than making a second one.

## Acceptance criteria

- **AC-1** — Given a `Luma` plane, a rect and a `Tuning`, when the column locator runs, then it measures each column's spread over only a **central band** of the rect's rows, whose height is `Tuning`'s central-band fraction of the rect's height, centred on the rect - and a column outside that band's rows cannot change the answer. *Pinned by construction:* a fixture with art in the central band and full-width texture in the top and bottom sixths of the rect must give the same answer as the same fixture with those sixths blanked.
- **AC-2** — Given a column spread profile with two textured runs separated by a flat run, when the locator selects, then it returns the **widest** run and not the span from the first to the last. *Contrast with `edges::textured_span`, which is required to keep returning the outermost pair on the same profile* - both in one test, because the difference between them is what buys the four entries.
- **AC-3** — Given a fixture whose page column sits inside flat page margins, when `decide` runs at `Tuning::default()`, then the result is `Crop(rect)` whose rect excludes both page margins beyond `margin_px` and contains the full width of the page column, where before this story the same fixture yields a rect spanning the whole image width.
- **AC-4** — *The corpus, on the column axis only.* Given every corpus entry with an expected rect, when `process_file` runs at `Tuning::default()`, then the produced rect's **left and right** edges lie within MC-019's window of the expected rect's - `[e.x - 8, e.x + 3]` and `[e.x + e.w - 1 - 3, e.x + e.w - 1 + 8]` - on at least **19 of 21** entries. The test prints a per-file table of both offsets. Rows are explicitly not measured by this criterion; they are MC-028's. *Control on the metric:* with the expected rect's left edge shifted 40 px inward before comparison, the check must fail on at least 15 entries - a test that passes whatever the locator returns is the failure mode here, and the measured offsets are all within 7 px.
- **AC-5** — *Zero clips, still.* Given every corpus entry with an expected rect, when `process_file` runs, then no entry whose outcome is `Cropped { rect, .. }` has a `rect` that fails to contain the expected rect entirely. The count is **0** before this story. **One file is known to be at risk and it is 2 px:** `2026-01-05 13_45_59.png` is located `1039..1505` against a marked `1040..1510`, which after `margin_px` is `1036..1508`. If the implementation reproduces that, **stop** - do not widen the margin, do not special-case the file, and do not edit the manifest. Report it, and the orchestrator opens the image with the user and records which of the two is wrong (MC-019's `## Model guidance` is the procedure).
- **AC-6** — Given the workspace suite, when it runs, then every assertion still holds: **361 pass on `main` at `a42f8e8`, 1 ignored**, plus whatever MC-026 leaves. In particular `no_border_found_beats_ambiguous_on_an_image_that_is_both` and `a_detection_with_a_nearly_chrome_edge_strip_is_flagged_ambiguous` in `crates/core/tests/decide.rs` still hold - they are what MC-025's stand-down guard exists for, and loosening the guard is the obvious way to break them.
- **AC-7** — Given the central-band fraction this story introduces, when the story ends, then `docs/wiki/architecture.md`'s tuning table carries it, and `## Notes` carries its derivation: the window it must lie in, the measurement at each end, and the control that fires outside it.

## Amendments

No acceptance criterion's text changed. Two things recorded here anyway, because
both are decisions taken during the cycle that a reader of the criteria alone
would not be able to reconstruct.

### 1. `## Out of scope` relaxed for one number in the manifest, by the user

**What it said, and still says:** "Changing the corpus files or the manifest,
including the 2 px question in AC-5, which is the user's to answer."

**What happened:** AC-5 names `2026-01-05 13_45_59.png` as a known 2 px risk and
sets the procedure - *if the implementation reproduces that, stop; do not widen
the margin, do not special-case the file, and do not edit the manifest; report
it, and the orchestrator opens the image with the user and records which of the
two is wrong.* The orchestrator followed that procedure exactly. The case
reproduced; the image and the measurement were put to the user; the user chose to
**correct the manifest** as part of this story.

**The change:** that entry's `"w"` from `471` to `466`, moving its right edge from
1510 to 1505. Same `x`, `y`, `h`, same tags. Nothing else in the manifest moved
and no corpus image was touched.

**Why, measured over all 1167 marked rows** (the orchestrator's own harness,
outside the repository, the mean absolute deviation written from the definition):

```
   col  mad(marked)      min      max   rows off-median
  1504      41.1884        7      254              906
  1505      41.1630        7      254              898   <- the locator's right edge
  1506       0.0000      255      255                0
  1507       0.0000      255      255                0
  1508       0.0000      255      255                0
  1509       0.0000      255      255                0
  1510       0.0000      255      255                0   <- the mark's old right edge
  ... 1511..1525 identical: 255/255, zero deviation
```

Columns 1506 to 1510 are exactly 255 on every one of the 1167 rows - not
near-white, exactly 255, zero deviation. The artwork ends at 1505, so the old
mark carried five columns of blank page margin, against MC-018's own marking rule
("the tightest axis-aligned rectangle containing all the artwork"). **The
detector was right and the mark was wrong**, which is the outcome MC-019's
`## Model guidance` anticipates and the reason it reserves the call for a person.

**AC-5 itself is unchanged and was not weakened.** No margin was widened, no file
is special-cased in either corpus test, and the criterion still demands zero
clips over the whole corpus. Approved by the user (`ryanczhang7`) on 2026-09-15.

### 2. AC-4's window: which coordinates the bracket is in

**Not a change - a reading, recorded because the criterion admits two and they
give different code.** AC-4 writes the window as `[e.x - 8, e.x + 3]` and
`[e.x + e.w - 1 - 3, e.x + e.w - 1 + 8]` and asks it of *the produced rect's*
edges. Those are two different coordinate systems: `detect` grows whatever its
last stage returns by `margin_px`, so a bracket written around the located edge
is 3 px off one written around the produced one.

MC-019 is what reconciles them, and MC-026's `## Notes` states the convention
outright: "the pipeline expands by `margin_px` (3) and MC-019 allows
`margin_px + 8` (11) of slack, so a located low edge must land in
`[marked_lo - 8, marked_lo + 3]`". In produced coordinates that is
`[e.x - 11, e.x]`, which is AC-4's bracket applied to the mark **grown by
`margin_px`** - the `expanded()` helper `corpus.rs` already uses for AC-1, and
the convention MC-026 finding 7's own "19 of 21" was measured under.

Both readings were scored, by RED against its candidate and by the orchestrator
independently from its own harness at central fraction 0.6:

| reading | score |
|---|---|
| bracket on the mark grown by `margin_px` (MC-019's convention) | **20 of 21** |
| bracket on the raw mark | **11 of 21** |

The two agree to the entry. AC-4 requires at least 19, so the raw-mark reading is
not a reading the criterion can have been written in - it is unsatisfiable by any
locator, including a perfect one. The tests use the grown mark. No criterion text
moved and nothing was loosened to reach a number: the tighter reading is simply
not the one MC-019 defines.

## Model guidance

Model: default. Oracle partition:

- **Settled - read out, do not re-derive**: `min_line_spread` 8.0 and its floor/ceiling derivation (MC-025), `margin_px` 3, MC-005 decision 13, MC-007 AC-6's flag order, whatever MC-026 settles for `min_content_fraction` and `ambiguity_band`, and MC-019's hit window (`margin_px + 8` of slack, which is `[-8, +3]` on a low edge and `[-3, +8]` on a high one). This story adds a selector and a row restriction; it retunes nothing.
- **Oracle-free, and the one place judgement is needed**: the **central-band fraction**. The corpus is its oracle and MC-026 finding 7 is evidence, not the answer - the window `[0.6, 0.8]` is measured on 21 files at one threshold with one selector, and the value should be re-derived with its own controls. MC-025's derivation of `min_line_spread` is the standard: a floor that fires, a ceiling that fires, a corpus-backed value between them, and a test that tells a later story loudly when it tries to leave the window. `Screenshot (2708).png` is the file that fires at the low end (`+1,+7` at 0.6, `+85,+7` at 0.5); `1.0` is the file-independent ceiling, where the score collapses from 19 to 2.
- **Mechanical**: AC-1, AC-2, AC-6, AC-7, and the per-file tables.

**Where to spend the thinking.** Not on the statistic - it is MC-025's, shipped
and measured. On the **wiring**: when does the column locator get to speak, given
that `flat::narrow`'s stand-down guard is load-bearing for two frozen tests and
unconditional on real screenshots? MC-025's `## Notes` lists four wirings and the
assertion that rejected each; a fifth is what this story needs, and the frozen
suite is the arbiter, exactly as it was there.

**Sizing.** One RED -> GREEN cycle: one behaviour - the page column's left and
right edges are found - with one new `Tuning` field, one new selector and one
wiring decision. It is a *large* one cycle, comparable to MC-025. If RED finds
it cannot be stated without also changing what `textured_span` means, that is the
signal to stop and split, not to widen the story.

## Out of scope

**The row axis, entirely.** This story does not locate a panel gutter, does not
change `flat`'s row behaviour, and does not improve MC-019's whole-rect metric -
after it lands, most corpus entries will still be cropped to something far too
tall, and that is the expected outcome. MC-028 owns the rows.

Changing what `edges::textured_span` returns (the outermost pair) or the
`min_line_spread` threshold. Retuning any `Tuning::default()` field other than
the one this story adds. Colour or chroma signals - measured and ruled out in
MC-025's `## Context`. Per-reader special cases. Changing the corpus files or the
manifest, including the 2 px question in AC-5, which is the user's to answer.
Meeting the 90% bar, which stays MC-019's.

## Test plan

### The surface, stated as fact

Three additions and one wiring line. Every name, signature and default below is
already imported by a test, so a different choice is a compile error rather than
a preference.

```rust
// crates/core/src/lib.rs, the eleventh Tuning field, after min_line_spread
pub struct Tuning {
    // ...
    pub min_line_spread: f32,
    /// Share of a rect's rows the column locator measures over, centred.
    pub central_band_fraction: f32,   // default 0.6
}

// crates/core/src/edges.rs
/// The widest run of consecutive indices of `spread` at or above
/// `t.min_line_spread`, both bounds inclusive; the FIRST of any that tie.
#[must_use]
pub fn widest_textured_run(spread: &[f32], t: &Tuning) -> Option<(usize, usize)>;

// crates/core/src/flat.rs
/// `rect`, restricted to the middle `t.central_band_fraction` of its rows.
#[must_use]
pub fn central_band(rect: Rect, t: &Tuning) -> Rect;

/// `within`, narrowed on the column axis to the page column inside it.
#[must_use]
pub fn page_column(img: &Luma, within: Rect, t: &Tuning) -> Rect;
```

`edges::textured_span`, `edges::spread_profile`, `edges::Axis`,
`flat::textured_box` and every existing `Tuning` field keep their current
signatures and their current meanings. `textured_span` in particular **still
returns the outermost pair** - AC-2 asserts that in the same test as the new
rule, and `## Regressions` below carries the mutation that proves the assertion
discriminates.

**Not constrained, and GREEN's to choose:** whether `central_band` is a free
function or a method; whether `page_column` calls `spread_within` (crate-private
today) or something else; the internal shape of `widest_textured_run`; every doc
comment; where the `Tuning` field sits in `Default`. Only the names, the
argument order, the return types and the observable answers are pinned.

**Two arithmetic details are pinned because they are not free.**

- `central_band`'s height is `floor(rect.h * fraction)` and its top is
  `rect.y + (rect.h - that) / 2`, with a band of at least one row whenever the
  rect has one. `0.6f32` is `0.60000002384185791015625`, so the product must be
  taken in `f64` and truncated; taken in `f32` and rounded, a height of 170
  gives 102 either way but a fraction of 0.7 gives 168 instead of the 167 the
  `f32` product actually yields, and a test that pinned one would fail against
  the other. The nine heights
  `the_central_band_is_the_middle_share_of_the_rects_rows_and_nothing_else`
  pins are 0, 1, 2, 3, 10, 100, 101, 170 and 240.
- `widest_textured_run` breaks a tie by taking the **first** run. Nothing on the
  corpus turns on it - `Screenshot (1661).png`, the file with a second textured
  region beside the page, has a strictly wider page - and it is pinned so the
  answer is the same on every machine.

### The wiring, and why the frozen suite permits it

**Chosen: a fifth stage in `detect`, on the column axis only, immediately after
`textured_box`, with no `strong_lines` guard - and a rule of its own instead.**

```rust
let second = trim_within(img, found.rect, t)?;
let textured = textured_box(img, second, t);
let column = page_column(img, textured, t);          // <- new
...
rect: margin::expand(column, t.margin_px, dims),
trimmed: first != whole || second != found.rect || textured != second
    || column != textured,
```

MC-025's stand-down guard is **left exactly as it is**, and so is the row axis.
`textured_box` still refuses to speak on any axis where `strong_lines` found
something; this stage is a different stage with a different rule. That rule is:

> **`page_column` narrows the rect only where the widest textured run has a flat
> column on both sides of it.** Where the run reaches column 0 or the rect's last
> column, there is no page *margin* on that side and there is nothing for a
> page-margin locator to remove.

That is the fifth wiring the story asks for, and it is not a tuning knob: it is
the definition of the thing being located, restated as code. `## Handoff` carries
the measurement that says the frozen suite permits it; the four alternatives
MC-025 rejected are all still rejected, because none of them is this.

Why each frozen assertion survives, structurally rather than by luck:

| Frozen assertion | Why it is unaffected |
|---|---|
| `decide.rs::no_border_found_beats_ambiguous_on_an_image_that_is_both` | `unbordered_band_over_art(679/800)` is art edge to edge; its widest run over the central band is `(0, 799)`, which reaches **both** ends, so `page_column` returns the rect untouched, `trimmed` stays false and the answer stays `NoBorderFound`. Measured: the flattest column of that band is 78.15 against a threshold of 8.0, so the run is the whole profile with an order of magnitude to spare. |
| `decide.rs::a_detection_with_a_nearly_chrome_edge_strip_is_flagged_ambiguous` | `band_over_art(679/800)`, identically: widest run `(0, 799)`, rect untouched, so the exact area assertion `(141_856, 176_400)` is untouched too. |
| `content.rs::the_edge_threshold_is_read_from_the_tuning` | The stage lives in `detect`. Nothing reachable from `content_box` can see it - MC-025's first guard, unchanged. |
| `corpus.rs::the_six_pages_flagged_today_are_still_flagged_for_no_border_found` | The six `expect: flag` entries are bare artwork files. Five have a widest run reaching both ends of the rect; `2025-05-12 22_55_40.png` has `(79, 1679)` in a 1680-wide rect, which reaches the right end. All six stand down, `trimmed` stays false, and all six stay `Flagged(NoBorderFound)`. **Without this rule that one file starts being cropped and this frozen test fails** - `## Handoff` records the measurement. |
| `corpus.rs::no_crop_clips_a_marked_page` | 21 entries cropped, 0 clips. |

The rule separates the corpus exactly: of the 28 entries, the widest run is
interior on the 21 that carry a marked rect and on none of the six flagged ones.
The single disagreement is `2025-02-27 22_46_15.png`, which is `expect: flag`,
is cropped on `main` already, and is the entry MC-026's AC-4 names as
`NOT_THIS_STORYS_TO_FIX` and deliberately asserts nothing about.

### Deriving `central_band_fraction = 0.6`

Both ends are fixtures in `crates/core/tests/flatness.rs`, so the **required**
`unit` gate is what tells a later story it has left the window - not a corpus
score, and not the optional `integration` gate.

| | Value | Control that fires | What it measures |
|---|---|---|---|
| **Ceiling** | 2/3 | `a_band_that_reaches_the_chrome_rows_gives_the_page_back_whole` | Above it the band reaches the full-width chrome rows in the outer sixths, every column reads textured, and the page is given back whole |
| **Value** | **0.6** | the corpus, 20 of 21 | 0.1 above the floor, 0.0667 below the ceiling, within 3% of the window's arithmetic centre (0.5833) |
| **Floor** | 0.5 | `a_page_whose_outer_panels_miss_the_middle_needs_a_band_wider_than_half` | At or below it the band fits inside a page's waist and the outer panels are missed by 60 px a side |

**At the ceiling**, on `common::page_in_margins_with_chrome_bands` (measured
outside the framework, and again against the candidate implementation):

```
fraction   band rows   page_column           column 0 spread
2/3        40..199     x 47 w 206  correct   0.750000
0.7        36..202     x 46 w 208  2 px out  3.088673
0.8        24..215     x  0 w 300  page lost 8.958333   <- threshold is 8.0
```

**At the floor**, on `common::page_with_a_waist`:

```
fraction   band rows   page_column           outer-panel column spread
0.4        72..167     x 100 w 100  60 px out  0.824653
0.5        60..179     x 100 w 100  60 px out  0.991667
0.6        48..191     x  40 w 220  correct    9.137731   <- threshold is 8.0
```

**In between**, the corpus, scored by AC-4's window over the 21 marked entries
with the candidate implementation:

```
central fraction   0.25  0.4  0.5  0.55  0.6  2/3  0.7  0.8  0.9  1.0
entries inside       17   19   19    19   20   20   20   20    6    2
```

0.6 is the smallest fraction that reaches the maximum. Below 0.6 the entry that
falls out is `Screenshot (2708).jpg`, at `+85` on the left - which is exactly the
defect the waist fixture reproduces, and is why the synthetic floor is where it
is. Above 2/3 the corpus is still fine until 0.9; the fixture is the tighter of
the two ends, deliberately, because a fixture fires in the required gate and a
corpus score does not.

### Which test covers which criterion

| AC | Test | File |
|---|---|---|
| AC-1 | `the_central_band_is_the_middle_share_of_the_rects_rows_and_nothing_else` | `crates/core/tests/flatness.rs` |
| AC-1 | `a_row_outside_the_central_band_cannot_change_where_the_page_column_is` | `crates/core/tests/flatness.rs` |
| AC-2 | `the_widest_textured_run_is_not_the_outermost_textured_pair` | `crates/core/tests/flatness.rs` |
| AC-2 | `zero_one_and_many_runs_and_the_tie_between_two_of_the_same_width` | `crates/core/tests/flatness.rs` |
| AC-2 | `the_widest_run_reads_its_threshold_from_the_tuning_like_the_span_does` | `crates/core/tests/flatness.rs` |
| AC-3 | `a_page_column_between_flat_page_margins_is_cropped_to_the_page` | `crates/core/tests/decide.rs` |
| AC-4 | `the_left_and_right_edges_of_every_marked_page_land_inside_the_window` | `crates/engine/tests/corpus.rs` |
| AC-5 | `no_crop_clips_a_marked_page_on_the_column_axis` | `crates/engine/tests/corpus.rs` |
| AC-6 | the workspace suite, unchanged, plus MC-026's seven `#[ignore]`d corpus tests | everywhere |
| AC-7 | the derivation above, the header of `flatness.rs`, and `architecture.md` (GREEN's) | - |
| the value | `the_default_central_band_fraction_is_three_fifths` | `crates/core/tests/flatness.rs` |
| the window | `the_default_central_band_fraction_sits_inside_the_window_its_controls_measure` | `crates/core/tests/flatness.rs` |
| the floor | `a_page_whose_outer_panels_miss_the_middle_needs_a_band_wider_than_half` | `crates/core/tests/flatness.rs` |
| the ceiling | `a_band_that_reaches_the_chrome_rows_gives_the_page_back_whole` | `crates/core/tests/flatness.rs` |
| control | `the_locator_leaves_a_rect_that_is_art_edge_to_edge_alone` | `crates/core/tests/flatness.rs` |
| control | `the_locator_finds_no_page_column_in_a_rect_that_is_all_gutter` | `crates/core/tests/flatness.rs` |
| the rule | `a_run_that_reaches_an_end_of_the_rect_is_not_a_page_column_and_is_left_alone` | `crates/core/tests/flatness.rs` |

`AC-7`'s other half - `docs/wiki/architecture.md`'s tuning table - is GREEN's to
write; RED does not touch it.

## Handoff: RED -> GREEN

### The commands

```bash
cargo test --workspace                                   # the unit gate; fails at compile today
cd crates/core  && cargo test --test decide              # AC-3, genuinely red today
cd crates/engine && cargo test --test corpus -- --ignored --nocapture   # AC-4 red, AC-5 green
bash scripts/gates.sh --fast
```

`cargo test --workspace` cannot run a single test today: `crates/core/tests/
flatness.rs` names the three items that do not exist yet, so the whole workspace
test build fails and cargo never reaches the other targets. `tests/decide.rs`
and `tests/corpus.rs` build on their own, which is why AC-3 and AC-4 can be
watched failing.

### Files written

| File | What changed |
|---|---|
| `crates/core/tests/common/mod.rs` | +4 fixtures, 1 rect helper, 4 column helpers, 12 constants: the page fixture and its two band variants, the waist fixture |
| `crates/core/tests/flatness.rs` | +12 tests, +1 helper (`column_spreads`), the MC-027 header and the derivation |
| `crates/core/tests/decide.rs` | +1 test (AC-3) and its imports |
| `crates/engine/tests/corpus.rs` | +2 `#[ignore]`d tests (AC-4, AC-5), 7 constants, 3 helpers |
| `fixtures/corpus/manifest.json` | `2026-01-05 13_45_59.png` `w` 471 -> 466; see "The manifest correction" below |

`git diff -- crates/*/src` is **empty**. No production source, no config, no
`Cargo.toml` was touched.

### Verbatim failure output

`cargo test --workspace`, the whole of the first screen:

```
   Compiling cropper-core v0.1.0 (C:\Users\ryanc\Projects\manhwa-cropper\crates\core)
error[E0432]: unresolved import `cropper_core::edges::widest_textured_run`
   --> crates\core\tests\flatness.rs:131:75
    |
131 |     Axis, Line, row_profile, spread_profile, strong_lines, textured_span, widest_textured_run,
    |                                                                           ^^^^^^^^^^^^^^^^^^^ no `widest_textured_run` in `edges`

error[E0432]: unresolved imports `cropper_core::flat::central_band`, `cropper_core::flat::page_column`
   --> crates\core\tests\flatness.rs:133:26
    |
133 | use cropper_core::flat::{central_band, page_column};
    |                          ^^^^^^^^^^^^  ^^^^^^^^^^^ no `page_column` in `flat`
    |                          |
    |                          no `central_band` in `flat`

error[E0609]: no field `central_band_fraction` on type `Tuning`
   --> crates\core\tests\flatness.rs:921:27
    |
921 |         Tuning::default().central_band_fraction,
    |                           ^^^^^^^^^^^^^^^^^^^^^ unknown field
    |
    = note: available fields are: `uniform_tolerance`, `edge_threshold`, `min_content_stddev`, `chrome_flat_fraction`, `chrome_max_extent` ... and 5 others
...
Some errors have detailed explanations: E0432, E0560, E0609.
error: could not compile `cropper-core` (test "flatness") due to 13 previous errors
```

`cd crates/core && cargo test --test decide` - AC-3, which is the one criterion
that can be watched failing today:

```
test a_page_column_between_flat_page_margins_is_cropped_to_the_page ... FAILED

---- a_page_column_between_flat_page_margins_is_cropped_to_the_page stdout ----

thread 'a_page_column_between_flat_page_margins_is_cropped_to_the_page' (37832) panicked at crates\core\tests\decide.rs:1383:9:
AC-3: a page column sitting inside flat page margins must be cropped, not flagged. decide returned Flag(NoBorderFound) and detect returned Some(Detection { rect: Rect { x: 0, y: 0, w: 300, h: 240 }, trimmed: false, removed: [], ambiguous: false })

failures:
    a_page_column_between_flat_page_margins_is_cropped_to_the_page

test result: FAILED. 28 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.02s
```

The three premise assertions in that test ran and passed before the panic, which
is the "before this story" state AC-3 describes: `w: 300` is the whole image
width.

`cd crates/engine && cargo test --test corpus -- --ignored` - AC-4:

```
AC-4: left and right offsets from the marked rect expanded by margin_px (3), at min_line_spread 8
file                                produced      expected     low    high window
2025-08-05 00_11_13.webp             0..2559     955..1591    -955    +968   MISS
2025-08-05 00_11_27.webp             0..2559    1005..1538   -1005   +1021   MISS
2025-10-14 23_29_06.png              0..2547    1005..1538   -1005   +1009   MISS
2025-10-14 23_30_20.png              0..2547    1037..1518   -1037   +1029   MISS
2025-10-20 15_37_25.png              0..2547    1005..1538   -1005   +1009   MISS
2026-01-05 13_33_41.png              0..2559    1070..1472   -1070   +1087   MISS
2026-01-05 13_45_59.png              0..2559    1037..1508   -1037   +1051   MISS
2026-01-05 13_49_39.png              0..2559    1041..1505   -1041   +1054   MISS
Screenshot (67).png                  0..2547    1009..1538   -1009   +1009   MISS
Screenshot (70).jpg                  0..2547    1013..1538   -1013   +1009   MISS
Screenshot (75).png                  0..2547    1074..1472   -1074   +1075   MISS
Screenshot (93).jpg                  0..2547    1140..1391   -1140   +1156   MISS
Screenshot (103).jpg                 0..2547    1074..1468   -1074   +1079   MISS
Screenshot (1661).png                0..2547    1074..1472   -1074   +1075   MISS
Screenshot (2582).jpg                0..2547    1005..1534   -1005   +1013   MISS
Screenshot (2630).jpg                0..2547     955..1595    -955    +952   MISS
Screenshot (2698).jpg                0..2547     951..1587    -951    +960   MISS
Screenshot (2708).jpg                0..2547    1070..1468   -1070   +1079   MISS
Screenshot (2744).jpg                0..2547     947..1591    -947    +956   MISS
Screenshot (3187).png                0..2547     984..1563    -984    +984   MISS
Screenshot (3538).png                0..2547     972..1567    -972    +980   MISS

thread 'the_left_and_right_edges_of_every_marked_page_land_inside_the_window' panicked at crates\engine\tests\corpus.rs:1031:5:
AC-4: the page's left and right edges must land inside MC-019's window on at least 19 of the 21 marked entries; 0 did.

test result: FAILED. 8 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 3.72s
```

**AC-4's "before" number is 0 of 21**, and the table says why: with no column
locator, every produced rect spans the whole image width.

### One line per test

| Test | What it asserts | AC |
|---|---|---|
| `the_default_central_band_fraction_is_three_fifths` | `Tuning::default().central_band_fraction == 0.6` | the value |
| `the_default_central_band_fraction_sits_inside_the_window_its_controls_measure` | `0.5 < fraction <= 2/3`, stated over the waist's and the chrome bands' own constants | the window |
| `the_central_band_is_the_middle_share_of_the_rects_rows_and_nothing_else` | `central_band` is `floor(h * fraction)` rows centred on the rect, columns untouched, at nine heights; a fraction of 1.0 is the rect itself | AC-1 |
| `a_row_outside_the_central_band_cannot_change_where_the_page_column_is` | the chrome-band and blank-band fixtures - identical inside the middle two thirds, different in every row of the outer sixths - give the same `page_column`, and that answer is the page | AC-1 |
| `the_widest_textured_run_is_not_the_outermost_textured_pair` | on `[0, 20, 0, 0, 30, 30, 30, 0]`, `widest_textured_run` is `(4, 6)` and `textured_span` is still `(1, 6)` | AC-2 |
| `zero_one_and_many_runs_and_the_tie_between_two_of_the_same_width` | empty -> None, no textured index -> None, one index -> `(i, i)`, a tie -> the first, a run open at the profile's end -> closed at the last index | AC-2 |
| `the_widest_run_reads_its_threshold_from_the_tuning_like_the_span_does` | "at or above" is `>=`, and the same profile at `min_line_spread` 25.0 gives a different run | AC-2 |
| `a_page_whose_outer_panels_miss_the_middle_needs_a_band_wider_than_half` | the waist fixture is located whole at the default and 60 px in on each side at a fraction of 0.5 | the floor |
| `a_band_that_reaches_the_chrome_rows_gives_the_page_back_whole` | the chrome-band fixture is located at the default and lost entirely at a fraction of 0.8 | the ceiling |
| `the_locator_leaves_a_rect_that_is_art_edge_to_edge_alone` | `soft_art(100, 94, 7)`'s flattest band column is above `min_line_spread`, the widest run is the whole profile, and `page_column` returns the rect | control |
| `the_locator_finds_no_page_column_in_a_rect_that_is_all_gutter` | `flat_gutter_only(200, 120)`'s widest band column is below `min_line_spread`, there is no run, and `page_column` returns the rect | control |
| `a_run_that_reaches_an_end_of_the_rect_is_not_a_page_column_and_is_left_alone` | the same fixture over three rects: both margins -> located; the page alone -> untouched; one margin only -> untouched | the rule |
| `a_page_column_between_flat_page_margins_is_cropped_to_the_page` | the fixture's three premises, then `decide` gives `Crop` whose rect contains the full-amplitude core, excludes both margins beyond `margin_px`, and leaves the rows alone | AC-3 |
| `the_left_and_right_edges_of_every_marked_page_land_inside_the_window` | at least 19 of 21 corpus entries land inside MC-019's window on both column edges, with the shifted-left control failing on at least 15 | AC-4 |
| `no_crop_clips_a_marked_page_on_the_column_axis` | no cropped entry's left or right edge cuts into the marked rect, with the 10 px inset control detecting a clip on all 21 | AC-5 |

### The export shape these tests already pin

Stated as fact: a test imports each of these, so a different choice is a compile
error.

**`cropper_core::edges`** - `use cropper_core::edges::{Axis, Line, row_profile,
spread_profile, strong_lines, textured_span, widest_textured_run};`

- `pub fn widest_textured_run(spread: &[f32], t: &Tuning) -> Option<(usize, usize)>`
  - **new**. Compared with `assert_eq!` against `Option<(usize, usize)>`
    literals, so the tuple is `(usize, usize)` and both bounds are inclusive.
- `pub fn textured_span(spread: &[f32], t: &Tuning) -> Option<(usize, usize)>` -
  unchanged, and asserted to still return the outermost pair.
- `pub fn col_profile(img: &Luma, rect: Rect) -> Vec<f32>` and
  `pub fn strong_lines(profile: &[f32], t: &Tuning) -> Vec<Line>` - unchanged;
  `tests/decide.rs` now imports both, and reads `Line::start` and `Line::end`
  as `usize`.
- `pub fn spread_profile(img: &Luma, axis: Axis) -> Vec<f32>` - unchanged.
  `tests/flatness.rs`'s `column_spreads` helper crops a plane and calls it, so
  the two-argument public form must stay.

**`cropper_core::flat`** - `use cropper_core::flat::{central_band, page_column};`

- `pub fn central_band(rect: Rect, t: &Tuning) -> Rect` - **new**. Takes the
  rect **by value** (`Rect` is `Copy`), returns a `Rect` compared with
  `assert_eq!`, and the test reads `.x`, `.y`, `.w`, `.h` off it.
- `pub fn page_column(img: &Luma, within: Rect, t: &Tuning) -> Rect` - **new**.
  The argument order is `(image, rect, tuning)`, which is AC-1's sentence in
  order. Returns a `Rect` compared with `assert_eq!` against
  `Rect { x, w, ..within }` literals, so **`y` and `h` must be the rect's own**.
- `pub fn textured_box(img: &Luma, within: Rect, t: &Tuning) -> Rect` -
  unchanged.

**`cropper_core::Tuning`**

- `pub central_band_fraction: f32` - **new**, default `0.6`. It is `f32` because
  the tests build `Tuning { central_band_fraction: 0.5, ..Tuning::default() }`
  and compare `Tuning::default().central_band_fraction` against the literal
  `0.6` with `assert_eq!`, and because `min_content_fraction` and
  `chrome_flat_fraction`, the other two share-like fields, are `f32`.
- Every other field keeps its name, type and default. `Tuning` stays
  non-`non_exhaustive` and stays constructible by struct literal with `..`.

**Nothing else is constrained.** `spread_within` may stay crate-private or not;
`page_column` may be built out of `central_band` and `widest_textured_run` or
not; the module each lives in is fixed only by the `use` lines above.

### The wiring measurement GREEN must not skip

RED built the whole implementation in a throwaway copy of the tree outside the
repository to find out whether any wiring satisfies the frozen suite, because
the story says that is the engineering content. Three results, and the third is
the one that decides it:

1. **The workspace suite passes with no guard at all.** 361 of 361, including
   both frozen `decide.rs` assertions. The reason is measured: every scene that
   reaches `detect` is built from `common::Recipe`'s art, whose flattest column
   over the central band is 73.8 to 78.2 against a threshold of 8.0, so the
   widest run is the whole profile and the stage moves nothing.
2. **The `integration` gate does not.** With no guard,
   `2025-05-12 22_55_40.png` - an `expect: flag` entry, and one of the six
   MC-026's AC-4 pins by name - stops being `Flagged(NoBorderFound)` and becomes
   `Cropped 76,0 1604x3136`. It is a bare webtoon strip 1680 px wide whose left
   79 columns are blank through the middle of the image, so an unguarded column
   locator finds a "page margin" there. **This is invisible to
   `cargo test --workspace`**, because that test is `#[ignore]`d; it is exactly
   the hole `required_gates: [integration]` exists for.
3. **The interior rule closes it, and it closes it exactly.** Over all 28
   entries, "the widest run over the central band touches neither end" agrees
   with "this entry carries a marked rect" on 27, the single disagreement being
   `2025-02-27 22_46_15.png`, which MC-026's AC-4 explicitly excludes. With the
   rule in place: workspace 374 passed / 0 failed / 10 ignored, and all ten
   `#[ignore]`d integration tests pass, MC-026's seven included.

The per-entry table, measured against the candidate:

```
file                            marked  widest run over the band   interior?
2025-02-27 22_46_15.png          false  (75, 293)   of 553          true  <- AC-4 excludes it
2025-03-03 11_06_04.png          false  (0, 571)    of 572          false
2025-03-03 11_24_19.png          false  (0, 474)    of 475          false
2025-05-12 22_55_40.png          false  (79, 1679)  of 1680         false
2025-05-12 22_58_53.png          false  (0, 1415)   of 1416         false
Screenshot (3455).png            false  (0, 528)    of 529          false
Screenshot (3465).png            false  (0, 646)    of 647          false
all 21 marked entries            true   strictly interior           true
```

### AC-4's window, and a reading of the criterion GREEN should be aware of

AC-4 writes the window as `[e.x - 8, e.x + 3]` and `[e.x + e.w - 1 - 3,
e.x + e.w - 1 + 8]`, and asks it of the rect `process_file` produced. Those are
different coordinates, and MC-019 is what reconciles them: AC-1 there requires
the produced rect to **contain** the mark and AC-2 requires it to sit inside the
mark grown by `margin_px + 8`, so in produced coordinates MC-019's window is
`[e.x - 11, e.x]` - which is AC-4's bracket applied to the mark **grown by
`margin_px`**, because `detect` grows whatever its last stage returns by exactly
that. The test uses `expanded()`, the helper `corpus.rs` already has for AC-1,
and so measures MC-019's window exactly and AC-4's numbers exactly.

Applying AC-4's bracket to the **raw** mark instead would be a window 3 px
tighter on each side than MC-019's, and RED measured both against the candidate:

```
central fraction     0.25  0.4  0.5  0.55  0.6  2/3  0.7  0.8  0.9  1.0
vs the grown mark      17   19   19    19   20   20   20   20    6    2
vs the raw mark        10   11   11    11   11   11   11   11    5    2
```

The raw-mark reading maxes out at 11 of 21 and AC-4 asks for 19, so it is not a
reading the criterion can have been written in; the grown-mark reading reaches
20. Both numbers are recorded so the choice is visible rather than assumed. If
the orchestrator disagrees, only `column_offsets`'s second argument in
`the_left_and_right_edges_of_every_marked_page_land_inside_the_window` changes.

### The manifest correction: `2026-01-05 13_45_59.png`

AC-5 named this file as the one 2 px risk and reserved the question for the user.
The orchestrator measured it independently and the user, shown the image and the
measurement, chose to **correct the manifest**. Done here, since
`fixtures/corpus/manifest.json` classifies as `test`:

```
"expect": { "x": 1040, "y": 204, "w": 471, "h": 1167 }   ->   "w": 466
```

That moves the mark's right edge from 1510 to 1505 and changes nothing else -
same `x`, `y`, `h`, same tags. The evidence, over all 1167 marked rows:

```
   col  mad(marked)      min      max   rows off-median
  1504      41.1884        7      254              906
  1505      41.1630        7      254              898   <- locator's right edge
  1506       0.0000      255      255                0
  1507       0.0000      255      255                0
  1508       0.0000      255      255                0
  1509       0.0000      255      255                0
  1510       0.0000      255      255                0   <- manifest's old right edge
  ... 1511..1525 identical: 255/255, zero deviation
```

Columns 1506..1510 are exactly 255 on every one of the 1167 rows. The artwork
ends at 1505, so the old mark included five columns of blank page margin against
MC-018's own rule - "the tightest axis-aligned rectangle containing all the
artwork". The detector was right and the mark was wrong. **AC-5 is unchanged as a
criterion:** there is no special case for this file in either corpus test and no
margin was widened.

- `crates/engine/tests/corpus_manifest.rs` still passes after the edit: 10
  passed, 0 failed, in the repository as it now stands.
- **Other places that quote the old number, which are historical records and
  must not be rewritten:** MC-026's `## Notes` finding 7 quotes
  `marked 1040..1510, located 1039..1505 ... 2 px short on the right`, and its
  `ambiguity_band` bisection names this file as one of the binding pair at
  0.00324973. Both were true measurements at the time they were taken. The
  bisection figure does **not** move - it is about a horizontal chrome strip's
  flat fraction and does not read the mark's width at all, and RED confirmed it:
  `the_two_pages_closest_to_chrome_are_ambiguous_again_one_step_above_the_band`
  still passes, still naming this file and `2026-01-05 13_49_39.png`.
- No other test reads this entry's width by name. The entry's own AC-4 offsets
  move from `-1,-5` to `-1,+0`, which is why the corpus score is 20 of 21 here
  where MC-026 finding 7 recorded 19.

### Every negative control, with the value RED measured

`crates/core/tests/flatness.rs` fails at **compile**, so **not one assertion in
it has executed**, controls included. Every number below was measured outside
the test framework - a separate crate outside the repository, with the mean
absolute deviation written from the definition in `f64` and calling nothing in
`cropper-core` - and then confirmed a second time against the candidate
implementation. **Confirming them against the shipped module is GREEN's job**,
and it is the only phase that can.

| Control | Threshold | Must be | RED measured | Headroom |
|---|---|---|---|---|
| `soft_art(100, 94, 7)` flattest column over the band | `min_line_spread` 8.0 | above | **8.252551** | +0.2526 |
| `soft_art(100, 94, 7)` widest column over the band | - | - | **10.563138** | - |
| `flat_gutter_only(200, 120)` widest column over the band | 8.0 | below | **1.095679** | -6.90 |
| `flat_gutter_only(200, 120)` flattest column over the band | - | - | **0.555556** | - |
| `page_in_margins` left-margin columns over the band | 8.0 | below | **0.827546 .. 0.833333** | -7.17 |
| `page_in_margins` column at amplitude 7 (x = 46, 253) | 8.0 | below | **exactly 7.0** | -1.0 |
| `page_in_margins` column at amplitude 8 (x = 47, 252) | 8.0 | at or above | **exactly 8.0** | 0 (the boundary) |
| `page_with_a_waist` outer-panel column, fraction 0.5 | 8.0 | below | **0.991667** | -7.01 |
| `page_with_a_waist` outer-panel column, fraction 0.6 | 8.0 | above | **9.137731** | +1.14 |
| `page_in_margins_with_chrome_bands` column 0, fraction 2/3 | 8.0 | below | **0.750000** | -7.25 |
| `page_in_margins_with_chrome_bands` column 0, fraction 0.7 | 8.0 | below | **3.088673** | -4.91 |
| `page_in_margins_with_chrome_bands` column 0, fraction 0.8 | 8.0 | above | **8.958333** | +0.96 |
| `band_over_art(679/800)` flattest column over the band | 8.0 | above | **78.147059** | +70.1 |
| `unbordered_band_over_art(679/800)` flattest column over the band | 8.0 | above | **78.147059** | +70.1 |
| `short_band_over_short_art(679/800)` flattest column over the band | 8.0 | above | **73.760000** | +65.8 |
| AC-4's shifted-left control | fails on >= 15 of 21 | - | **fails on 21 of 21** | +6 |
| AC-5's 10 px inset control | detects a clip on 21 of 21 | - | **21 of 21** | exact |

The two tightest are `soft_art`'s 8.2526 and the chrome band's 8.9583; both are
within 1.0 of the threshold, and both are supposed to be, because they are what
makes 8.0 and 2/3 mean something. If GREEN measures either on the other side of
8.0, stop and say so rather than adjusting anything.

Two exact values are worth checking first because they are exact by
construction, not by luck: an art column of `page_in_margins` has its mean
exactly on `FADE_TONE` over an even row count and every pixel exactly its
amplitude away from it, so its spread is **exactly** its amplitude - which is
why the band must be an even number of rows starting on an even row. At the
settled fraction it is rows 48..191 of 240, which is both.

### `bash scripts/gates.sh --fast`, read for shape

```
PASS         format (0s)
FAIL         lint (3s, exit 101) -> .claude/state/gate-logs/lint.log
FAIL         typecheck (2s, exit 101) -> .claude/state/gate-logs/typecheck.log
FAIL         unit (3s, exit 101) -> .claude/state/gate-logs/unit.log
FAIL         coverage (11s, exit 101) -> .claude/state/gate-logs/coverage.log

--fast skipped: integration build
```

`lint` and `typecheck` cannot be green in Rust while an item is missing:
`cargo clippy --all-targets` and `cargo check --all-targets` both compile the
test target. What matters is that they are red for **that and nothing else**.
Filtering every `^error` and `^warning` line in all four logs for anything that
is not `E0432`, `E0560`, `E0609` or a rollup line leaves **nothing, in all
four**.

**Admissibility was checked properly, because a file that does not compile
cannot be linted.** The throwaway copy of the tree outside the repository
carries the four test files byte for byte, plus a candidate implementation, so
it compiles - and under the gates' own commands:

```
$ cargo clippy --workspace --all-targets -- -D warnings      # clean
$ cargo fmt --all --check                                    # clean
$ cargo test --workspace                                     # 374 passed, 0 failed, 10 ignored
$ cargo test --workspace --release -- --ignored               # 10 passed, 0 failed
$ cargo llvm-cov --workspace --fail-under-lines 95 ...        # TOTAL 99.73% lines
```

It earned its keep on the first run: four `clippy::manual_range_contains`
errors - three in the new `common/mod.rs` fixtures, one in `flatness.rs` - and
two `rustfmt` diffs, all of which would have failed the `lint` and `format`
gates the moment the surface existed. All six are fixed in the repository. The
throwaway tree was deleted before RED ended and is not in this repository.

**Cost.** `tests/flatness.rs` is 29 tests and finishes in **0.02 s under
coverage instrumentation**, 0.00 s plain; the largest fixture is 300x240. The
two new corpus tests are `#[ignore]`d, decode 21 images each and run only under
`integration`, whose whole corpus binary is 2.7 s in release. At the recorded
`ci-factor` of 1.63 for `coverage` nothing here is within three orders of
magnitude of a timeout, and Rust's harness has no per-test timeout anyway.

### Tests that passed on arrival

**`no_crop_clips_a_marked_page_on_the_column_axis` (AC-5)** passes today: with
no column locator every produced rect spans the whole image width, so nothing
can clip on the column axis. It is the same vacuity MC-026 recorded for its own
`no_crop_clips_a_marked_page`, and it is earned twice over:

- its **negative control ran and fired**: each marked rect pulled in 10 px on
  the left and the right is reported as clipping, on all 21 - so the predicate
  can detect a clip;
- and it was **probed by mutation**: see `## Regressions`, P2.

Every other new test is red today, either by the compile failure (`flatness.rs`)
or by its own assertion (`decide.rs`, and AC-4 in `corpus.rs`).

### What RED discovered that should change the implementation approach

1. **Do not start from "delete or loosen MC-025's guard".** The guard is not
   what is in the way on the column axis - `strong_lines` on the column gradient
   profile is **empty** on every one of the six flagged corpus entries and on
   the page fixture's margins, so loosening it would change behaviour in exactly
   the wrong places. The column axis needs a stage of its own with a rule of its
   own, and the row axis needs nothing.
2. **The `integration` gate is the one that constrains the wiring**, not the
   `unit` gate. Anything that looks right under `cargo test --workspace` should
   be run against `cargo test --workspace --release -- --ignored` before it is
   believed. RED found a wiring that passed all 361 unit tests and broke a
   frozen corpus control.
3. **`2025-02-27 22_46_15.png` changes behaviour and no test pins it.** On
   `main` it is `Cropped 0,3 553x853`; with the candidate it is
   `Cropped 72,3 225x853`. It is an `expect: flag` entry, so both are wrong, and
   MC-026's AC-4 names it `NOT_THIS_STORYS_TO_FIX`. Expect to see it in the
   AC-4 table output and do not chase it.
4. **`Screenshot (93).jpg` is the one AC-4 miss** and is expected to stay one:
   `-4,+17` on the high edge, the same miss MC-026 finding 7 records. AC-4 asks
   for 19 of 21 and the candidate reaches 20.
5. **Coverage.** With the candidate the new code is fully covered by these tests
   and the workspace total is 99.73% of lines. If GREEN adds a branch these
   tests do not reach - a `saturating_sub` that cannot underflow, a `count == 0`
   guard - it will show up against the 95% floor as `edges::deviation`'s
   unreachable guard already does.

## Regressions

Two probes, both run in a throwaway copy of the tree **outside** the repository
(the phase lock forbids editing production source in RED, and editing it would
be a violation whether or not the lock caught it). The copy carried the four
test files from this branch byte for byte plus a candidate implementation, so
every test could run. `git diff -- crates/*/src` in this repository is empty.

### P1 - `textured_span` made to stop at the first flat run

`the_widest_textured_run_is_not_the_outermost_textured_pair` (AC-2) asserts two
things about the same profile, and one of them - that `textured_span` still
returns the **outermost** pair - is about code that already exists and would
pass whatever this story did. Mutating exactly that behaviour, in
`crates/core/src/edges.rs`:

```rust
-    let last = spread.iter().rposition(textured)?;
+    // P1 MUTATION: stop at the first flat index after the first textured one.
+    let last = spread[first..]
+        .iter()
+        .position(|v| !textured(v))
+        .map_or(spread.len() - 1, |k| first + k - 1);
```

```
---- the_widest_textured_run_is_not_the_outermost_textured_pair stdout ----

thread 'the_widest_textured_run_is_not_the_outermost_textured_pair' (9328) panicked at crates\core\tests\flatness.rs:1126:5:
assertion `left == right` failed: AC-2: and `textured_span` still spans the outermost textured pair on the same profile - MC-005's decision 13, unchanged by this story
  left: Some((1, 1))
 right: Some((1, 6))

---- the_span_reaches_the_outermost_textured_lines_across_a_flat_gap_between_them stdout ----

thread 'the_span_reaches_the_outermost_textured_lines_across_a_flat_gap_between_them' (29584) panicked at crates\core\tests\flatness.rs:628:5:
assertion `left == right` failed: a flat gutter between two panels must not end the span
  left: Some((1, 1))
 right: Some((1, 4))

test result: FAILED. 27 passed; 2 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.00s
```

The right assertion, with the right message. The `widest_textured_run` half of
the same test is unaffected, which is the point: the two halves discriminate
separately. MC-025's own assertion on the same behaviour goes red beside it, as
it should. Reverted; the mutation is not in the tree.

### P2 - the column locator made to over-crop the left edge by 20 px

`no_crop_clips_a_marked_page_on_the_column_axis` (AC-5) passes on arrival
because nothing is cropped narrowly today. Mutating the narrowing in
`crates/core/src/flat.rs`:

```rust
+    // P2 MUTATION: take 20 columns more off the left than the page has margin.
     Rect {
-        x: within.x + first as u32,
-        w: (last - first + 1) as u32,
+        x: within.x + first as u32 + 20,
+        w: (last - first + 1) as u32 - 20,
         ..within
     }
```

```
---- no_crop_clips_a_marked_page_on_the_column_axis stdout ----
AC-5: column-axis containment over the twenty-one marked entries (21 cropped)

thread 'no_crop_clips_a_marked_page_on_the_column_axis' (14848) panicked at crates\engine\tests\corpus.rs:1151:5:
AC-5: a crop whose left or right edge cuts into the page a person marked is the worst defect this product has, and locating those two edges is exactly what this story does. 21 of 21 cropped entries clip; the first is 2025-08-05 00_11_13.webp: cropped to columns 970..1595, which does not contain the marked 958..1588.

failures:
    no_crop_clips_a_marked_page
    no_crop_clips_a_marked_page_on_the_column_axis
    the_left_and_right_edges_of_every_marked_page_land_inside_the_window

test result: FAILED. 6 passed; 3 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.74s
```

The assertion fires, names the file and prints the two column ranges, and
MC-026's whole-rect `no_crop_clips_a_marked_page` fires beside it. Reverted; the
mutation is not in the tree.

## Gate probes

**Something is owed, and it is not the usual thing.** This story adds no gate
and changes no gate command: `.claude/harness/project.conf` is untouched, and
`integration` has existed since MC-017. What it does is put `integration` in its
frontmatter's `required_gates`, which is the harness saying *this story's
criteria are invisible to every required gate*. AC-4 and AC-5 are `#[ignore]`d
and only `integration` runs them, so on this story `integration` is doing the
job a required gate does - and `rules.md`'s rule is that a gate which has never
been observed to fail is not a gate. It had not been observed to fail against
**this** code. Two probes, both run in the repository and both reverted.

The second one is the interesting one only in combination with the first: G1
shows the gate catching something **no required gate can see**, which is the
whole argument for the `required_gates` line.

### G1 - `page_column` made to select with `textured_span` (the outermost pair)

The story's `## Context` says the widest-run selector "beats `textured_span`'s
outermost rule by four entries". That is AC-4's content, and AC-4 runs only
under `integration`. In `crates/core/src/flat.rs`:

```rust
-    let Some((first, last)) = widest_textured_run(&spread, t) else {
+    // G1 MUTATION: select with `textured_span` (the outermost pair) instead.
+    let Some((first, last)) = textured_span(&spread, t) else {
```

**`cargo test --workspace` stays completely green** - 374 passed, 0 failed, 10
ignored, one `unused import: widest_textured_run` warning and nothing else. The
unit fixtures each carry a single textured run, so the two selectors agree on
every one of them. The `integration` gate does not:

```
running 9 tests
test the_six_pages_flagged_today_are_still_flagged_for_no_border_found ... ok
test no_crop_clips_a_marked_page_on_the_column_axis ... ok
test no_crop_clips_a_marked_page ... ok
test the_left_and_right_edges_of_every_marked_page_land_inside_the_window ... FAILED

---- the_left_and_right_edges_of_every_marked_page_land_inside_the_window stdout ----
thread '...' (27472) panicked at crates\engine\tests\corpus.rs:1031:5:
AC-4: the page's left and right edges must land inside MC-019's window on at
least 19 of the 21 marked entries; 15 did. The misses are:
2025-10-14 23_29_06.png: produced 1000..1884 against expected 1005..1538, offsets -5,+346, ...
2025-10-14 23_30_20.png: produced 1030..1804 against expected 1037..1518, offsets -7,+286, ...
2026-01-05 13_33_41.png: produced 1071..2559 against expected 1070..1472, offsets +1,+1087, ...
2026-01-05 13_45_59.png: produced 1036..1576 against expected 1037..1508, offsets -1,+68, ...
2026-01-05 13_49_39.png: produced 1036..1775 against expected 1041..1505, offsets -5,+270, ...
Screenshot (93).jpg: produced 1136..1408 against expected 1140..1391, offsets -4,+17, ...

test result: FAILED. 8 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.97s
error: test failed, to rerun pass `-p cropper-engine --test corpus`
```

**15 of 21 against the widest run's 20** - MC-026 finding 1's "19 against 15"
reproduced on the shipped module, at the corrected manifest and the settled
fraction. Five of the six misses are the outermost rule annexing a second
textured region to the right of the page, the worst of them by 1087 px. The
sixth, `Screenshot (93).jpg` at `-4,+17`, is the miss the correct
implementation has too.

This is the probe that justifies `required_gates: [integration]` on this story,
and it is the exact hole the handoff's finding 2 describes: a change that passes
every required gate and breaks a corpus criterion. Reverted; the mutation is not
in the tree.

### G2 - the interior rule, removed

The rule the `## Test plan` argues the whole wiring on. In the same file:

```rust
     // The interior rule. ...
-    if first == 0 || last + 1 == spread.len() {
+    if false {  // G2 MUTATION: the interior rule, removed.
         return within;
     }
```

The `integration` gate fires, on the frozen control MC-026 owns, and reproduces
RED's measured rect **to the pixel** (`Cropped 76,0 1604x3136`):

```
---- the_six_pages_flagged_today_are_still_flagged_for_no_border_found stdout ----
AC-4: process_file over the seven `expect: flag` entries
file                              AC-4 outcome
2025-02-27 22_46_15.png              - Cropped 72,3 225x853
2025-05-12 22_55_40.png         pinned Cropped 76,0 1604x3136
2025-05-12 22_58_53.png         pinned Flagged Detector(NoBorderFound)

thread '...' (26812) panicked at crates\engine\tests\corpus.rs:709:5:
AC-4: opening the two decision gates must not start cropping the pages the
corpus says to leave alone. 1 of the six changed; the first is
2025-05-12 22_55_40.png: Cropped 76,0 1604x3136.
```

**One honest correction to the handoff, in this story's favour.** Finding 3
says this defect is "invisible to `cargo test --workspace`". That was true in
RED's throwaway tree, which carried no MC-027 unit tests. In the repository as
it now stands it is **not** invisible: the `unit` gate catches it too, on the
fixture RED wrote for exactly this rule, and it is the one-margin-only case that
does it rather than the both-ends one:

```
---- a_run_that_reaches_an_end_of_the_rect_is_not_a_page_column_and_is_left_alone stdout ----
thread '...' (22280) panicked at crates\core\tests\flatness.rs:1418:5:
assertion `left == right` failed: and a rect with a margin on the left but none
on the right is the same answer: this stage removes page margins in pairs or not
at all, which is what keeps it off a bare artwork file that happens to start
with a blank column
  left: Rect { x: 47, y: 0, w: 206, h: 240 }
 right: Rect { x: 0, y: 0, w: 253, h: 240 }
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 28 filtered out
```

So the interior rule is pinned in both gates and the `required_gates` line does
not rest on it - it rests on G1. Reverted; the mutation is not in the tree, and
`cargo test --workspace`, the corpus target under `--ignored`, `cargo fmt --all
--check` and `cargo clippy --workspace --all-targets -- -D warnings` were all
re-run clean afterwards.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-15T17:20:53Z
    commit: 0e25b9f (working tree had uncommitted changes)
    tree:   eaf6d9d2634fb23de68736efa613b33a10c99289
    result: pass (7 ran, 0 unconfigured, 0 known)

    PASS         format (0s)
    PASS         lint (2s, observed -)
    PASS         typecheck (2s, observed -)
    PASS         unit (14s, observed 47, floor 47)
    PASS         coverage (42s, observed 1710)
    PASS         integration (116s, observed 9)
    PASS         build (79s, observed -)

## Notes

### The required gate (PLANNED -> RED)

Two artifacts, two gates, and only one of them is required for the repo, which
is why `required_gates` is not empty on this story.

- **The locator itself** - the new selector, the new `Tuning` field and the
  wiring - lives in `crates/core/**` and is judged by **`unit`**
  (`cargo test --workspace`), which is **required** in
  `.claude/harness/project.conf`. Break the selector and `unit` goes red.
- **AC-4 and AC-5 are corpus criteria.** They are `#[ignore]`d tests in
  `crates/engine/tests/corpus.rs` and only the **`integration`** gate
  (`cargo test --workspace --release -- --ignored`) ever runs them. That gate
  is **optional** for the repo, so AC-4 and AC-5 could break without a single
  required gate noticing - `unit` never executes them. `required_gates:
  [integration]` in the frontmatter is what closes that hole, and
  `check-boundaries.sh` enforces it as "story-required gate 'integration'
  passed in the recorded run".

`bash scripts/gates.sh --list` on this branch, the two gates that matter:

```
unit         required  .      cargo test --workspace
                              evidence: test result: ok\. [1-9][0-9]*
                              floor:    47
integration  optional  .      cargo test --workspace --release -- --ignored
                              evidence: test result: ok\. [1-9][0-9]*
                              slow:     ... (left out of --fast)
```

### RED, verified by the orchestrator

Not taken on the Test Developer's word. Re-run here, in the repository, after
RED reported:

* `git diff --stat -- 'crates/*/src'` is **empty**. Only test paths, the
  manifest and this story file changed.
* `cargo test --workspace` -> **13 compile errors**, every one in
  `crates/core/tests/flatness.rs` and every one the missing surface: 8 x E0609
  (`no field central_band_fraction`), 3 x E0560 (the same field in a struct
  literal), 2 x E0432 (`widest_textured_run`, and `central_band` / `page_column`
  in `flat`). Nothing else in the workspace fails to compile.
* `cargo test -p cropper-core --test decide` -> **AC-3 fails as a real,
  executed assertion**, which is the watched failure this story is entitled to:

  ```
  thread 'a_page_column_between_flat_page_margins_is_cropped_to_the_page'
  panicked at crates\core\tests\decide.rs:1383:9:
  AC-3: a page column sitting inside flat page margins must be cropped, not
  flagged. decide returned Flag(NoBorderFound) and detect returned
  Some(Detection { rect: Rect { x: 0, y: 0, w: 300, h: 240 }, trimmed: false,
  removed: [], ambiguous: false })
  test result: FAILED. 28 passed; 1 failed
  ```

  `w: 300` is the whole image width, which is AC-3's "before this story" clause
  verbatim.
* `bash scripts/gates.sh --fast` -> `PASS format`, `FAIL lint / typecheck /
  unit / coverage`, all exit 101. **Admissibility checked rather than assumed:**
  filtering every `^error` and `^warning` line in all four gate logs for anything
  that is not E0432 / E0560 / E0609 or a rollup leaves **nothing, in all four**.
  So nothing fails on a lint rule, a timeout or a configuration error, and the
  instrumented coverage build reached the same compile error in 7 s. A `--fast`
  run is not recorded in `## Gate results`; it is not a full run.

### The premise, reproduced by the orchestrator

MC-026 finding 7 is what makes this a `feature` rather than a spike, so it was
re-measured here rather than read out. The harness is a cargo crate **outside the
repository**, with path dependencies on `cropper-core` and `cropper-engine`, at

```
C:\Users\ryanc\AppData\Local\Temp\claude\C--Users-ryanc-Projects-manhwa-cropper\
  5c63fcc1-4a2c-4892-8300-1118d29c08a2\scratchpad\colcheck
```

so that no phase and no committed test depends on it. The mean absolute deviation
is written from the definition in `f64` rather than by calling
`edges::spread_within` - which is crate-private in any case - independence being
the point. The rect is the real pipeline's: `trim_uniform` -> `content_box` ->
`trim_within`, no marked rect anywhere.

**Finding 7's table reproduces cell for cell**, against the manifest as it stood
before this story's correction, and three fractions are added:

```
central fraction   1.0   0.9   0.8   0.7   0.6  0.55   0.5   0.4  0.25
widest run           2     6    19    19    19    18    18    18    16
outermost columns    0     2    15    16    15    14    12    13    14
```

MC-026 recorded `2, 19, 19, 18, 18, 16` and `0, 15, 15, 12, 13, 14` at the six
fractions it sampled. Every one is reproduced. 0.9, 0.7 and 0.55 are new, and the
0.9 column is what shows the top of the window is a cliff and not a slope.

### The window, measured at 1 % resolution - and the floor is tighter than RED sampled

MC-026's `[0.6, 0.8]` came off a six-point grid, and RED sampled 0.5, 0.55, 0.6.
A grid gives a window and not a margin - the same defect MC-026's own
`ambiguity_band` correction records. Swept here from 0.20 to 1.00 in steps of
0.01, printing only the fractions where the **set of missing files** changes,
against the corrected manifest:

```
frac   hits   misses
0.57     19   Screenshot (93).jpg (-4,+17); Screenshot (2708).jpg (+82,+7)
0.58     20   Screenshot (93).jpg (-4,+17)
0.81     18   2026-01-05 13_45_59.png (-678,+1039);
              2026-01-05 13_49_39.png (-682,+1042); Screenshot (93).jpg (-4,+17)
```

Every fraction from **0.58 to 0.80** gives the same miss set and the same score,
20 of 21. So the corpus window is `[0.58, 0.80]`, and both ends fire:

* **0.81** - `2026-01-05 13_45_59.png` and `13_49_39.png` collapse from a few
  pixels of error to the whole rect. The band has grown far enough to take in the
  browser chrome rows that survive inside the pipeline's rect, which are textured
  out to the rect's own edges - MC-025's diagnosis, arriving from the other side.
  By 0.90 it has taken fifteen of the twenty-one.
* **0.57** - `Screenshot (2708).jpg` goes from a hit to `+82` on its left edge.
  MC-026 named this file as the one that fires at the low end and measured `+85`
  at 0.5; the transition is at 0.58, and RED's waist fixture reproduces the
  mechanism synthetically.

**This is the one number in RED's derivation the orchestrator would write
differently, and it is recorded rather than changed.** RED's floor control is a
fixture at 0.5 and its ceiling control a fixture at 2/3, with 0.6 sitting within
3 % of that window's centre - a sound derivation, and the right one to put under
the **required** `unit` gate, since a fixture fires there and a corpus score does
not. But the *corpus* floor is 0.58, not 0.5, so the shipped 0.6 has **0.02 of
corpus margin below it and 0.067 of fixture margin above it**. That is a real
asymmetry and it is thinner than it looks in RED's table, which never sampled
between 0.55 and 0.6.

It was not changed, for three reasons. The corpus files are committed and
deterministic, so 0.58 is a fixed cliff and not drift; `min_line_spread` ships at
8.0 against a measured ceiling of 8.26, a 3.2 % margin, and MC-025 defended
exactly that reasoning; and every value in `[0.58, 2/3]` scores the same 20 of
21, so moving to the centre of the intersection (0.62) would buy margin without
buying a single entry, at the cost of re-deriving four named tests. **A later
story that wants to lower this fraction should read this paragraph first: below
0.58 the corpus loses an entry, and the fixture floor at 0.5 will not tell it
so.**

### AC-4's control, and AC-4's coordinate system, checked independently

* **The control.** AC-4 requires that shifting the expected left edge 40 px
  inward before comparison makes the check fail on at least 15 entries. At
  central fraction 0.6 the twenty-one measured left offsets run `-7` to `+1`;
  subtracting 40 puts every one of them in `[-47, -39]`, outside MC-019's
  `[-8, +3]`. The control fires on all twenty-one against a bar of fifteen.
* **The coordinate system.** RED escalated that AC-4's bracket is written in two
  coordinate systems and that only one reading is satisfiable. That claim was
  **reproduced here on the orchestrator's own harness, without reusing RED's
  code**: scoring the produced rect against the *raw* mark at central fraction
  0.6 gives **11 of 21** - hits on `00_11_13`, `00_11_27`, `15_37_25`,
  `13_33_41`, `13_45_59`, `13_49_39`, `(67)`, `(75)`, `(1661)`, `(2630)`,
  `(3187)` - against RED's independently measured 11. The same rects scored the
  way MC-019 defines the window give 20. The escalation is correct and the
  resolution is recorded in `## Amendments`.

### GREEN: the recorded control values, confirmed against the shipped module

`tests/flatness.rs` failed at **compile** in RED, so no assertion in it -
controls included - had ever executed. Every value in `## Handoff`'s table was
re-measured here against the **shipped** `cropper-core`, using the frozen
`crates/core/tests/common/mod.rs` verbatim (`#[path]`-included into a throwaway
crate outside the repository, deleted afterwards) and the frozen
`flatness.rs::column_spreads` helper copied byte for byte, so the statistic is
computed the way the tests compute it and not some second way.

**Every value is confirmed, every verdict is on the side RED required, and
three differ in the 7th significant digit.**

| Control | Threshold | Must be | RED | GREEN | |
|---|---|---|---|---|---|
| `soft_art(100, 94, 7)` flattest column over the band | 8.0 | above | 8.252551 | **8.252551** | = |
| `soft_art(100, 94, 7)` widest column over the band | - | - | 10.563138 | **10.563138** | = |
| `flat_gutter_only(200, 120)` widest column over the band | 8.0 | below | 1.095679 | **1.095679** | = |
| `flat_gutter_only(200, 120)` flattest column over the band | - | - | 0.555556 | **0.555556** | = |
| `page_in_margins` left-margin columns over the band | 8.0 | below | 0.827546 .. 0.833333 | **0.827546 .. 0.833333** | = |
| `page_in_margins` column at amplitude 7 (x = 46, 253) | 8.0 | below | exactly 7.0 | **exactly 7.0**, both | = |
| `page_in_margins` column at amplitude 8 (x = 47, 252) | 8.0 | at or above | exactly 8.0 | **exactly 8.0**, both | = |
| `page_with_a_waist` outer-panel column, fraction 0.5 | 8.0 | below | 0.991667 | **0.991667** | = |
| `page_with_a_waist` outer-panel column, fraction 0.6 | 8.0 | above | 9.137731 | **9.137732** | 1e-6 |
| `page_in_margins_with_chrome_bands` column 0, fraction 2/3 | 8.0 | below | 0.750000 | **0.750000** | = |
| `page_in_margins_with_chrome_bands` column 0, fraction 0.7 | 8.0 | below | 3.088673 | **3.088673** | = |
| `page_in_margins_with_chrome_bands` column 0, fraction 0.8 | 8.0 | above | 8.958333 | **8.958333** | = |
| `band_over_art(679/800)` flattest column over the band | 8.0 | above | 78.147059 | **78.147057** | 2e-6 |
| `unbordered_band_over_art(679/800)` flattest column over the band | 8.0 | above | 78.147059 | **78.147057** | 2e-6 |
| `short_band_over_short_art(679/800)` flattest column over the band | 8.0 | above | 73.760000 | **73.760002** | 2e-6 |
| AC-4's shifted-left control | fails on >= 15 of 21 | - | 21 of 21 | **21 of 21** | = |
| AC-5's 10 px inset control | detects a clip on 21 of 21 | - | 21 of 21 | **21 of 21** | = |

**The two tightest are on the right side of 8.0, and by RED's exact margins.**
`soft_art`'s flattest band column is **8.252551**, 0.2526 above
`min_line_spread`; the chrome fixture's column 0 at a fraction of 0.8 is
**8.958333**, 0.9583 above it. Neither moved, so the ceiling on
`min_line_spread` and the ceiling on `central_band_fraction` both still mean
what MC-025 and this story's `## Test plan` say they mean. Nothing was adjusted
to reach either.

**The two exact-by-construction values are exact.** `page_in_margins`'s columns
at amplitude 7 and 8 measure exactly 7.0 and exactly 8.0 on the shipped module,
at both of the two columns each occurs at (x = 46 and 253, x = 47 and 252). The
condition the handoff names holds on the shipped `central_band`: at a fraction
of 0.6 the band of the 300x240 fixture is **rows 48..=191, 144 rows** - an even
count starting on an even row, which is what makes a column's mean land exactly
on `FADE_TONE` and its deviation exactly its amplitude.

**The three divergences are the same arithmetic MC-025's GREEN recorded, and
they are benign.** All three are one `f32` ulp at that magnitude, none is a
behaviour change, and none is within four orders of magnitude of the threshold
it is measured against. They sit at the ulp boundary of the same two-pass `f32`
accumulation MC-025's GREEN adjudicated in exact `i128` arithmetic and found the
shipped module correct on; that adjudication is in MC-025's `## Notes` and is
not repeated here.

**One measurement had to be taken on a different rect than a naive reading
suggests, and it matters.** `band_over_art` and `short_band_over_short_art` are
**bordered** fixtures, and a solid border column has a spread of exactly 0.0, so
measured over the whole 840-wide image their flattest band column is 0.0 and not
78.15. RED's numbers are over the rect the pipeline hands the stage -
`trim_uniform` to `content_box` to `trim_within` to `textured_box` - which is
800 columns wide, and the `## Test plan`'s frozen-assertion table says so
implicitly by quoting a run of `(0, 799)`. Measured on that rect all three
fixtures reproduce, and so does the table's claim in full:

```
fixture                             pipeline rect                          cols  widest run  page_column
band_over_art(679/800)              Rect { x: 20, y: 20, w: 800, h: 170 }   800  (0, 799)    unchanged
unbordered_band_over_art(679/800)   Rect { x:  0, y:  0, w: 800, h: 170 }   800  (0, 799)    unchanged
short_band_over_short_art(679/800)  Rect { x: 20, y: 20, w: 800, h:  26 }   800  (0, 799)    unchanged
```

So the two frozen `decide.rs` assertions survive **structurally**, exactly as
the `## Test plan` predicted: the widest run reaches both ends of the rect, the
interior rule declines, `trimmed` stays false and the answers stay
`NoBorderFound` and `Ambiguous`. Not by luck and not by a threshold.

**The two corpus controls, and where their numbers come from.** AC-5's inset
control is asserted directly - `control_missed.is_empty()` over all 21 - and it
passed, which *is* the 21-of-21 statement. AC-4's shifted control is asserted as
a floor of 15, so its exact count is read off the shipped run's own printed
table rather than from a second measurement: the twenty-one left offsets run
`-7` to `+1`, and subtracting 40 puts every one of them in `[-47, -39]`, wholly
outside `LOW_WINDOW` (`-8..=3`). All 21 fail, against a bar of 15.

### GREEN: the wiring, and the alternative the frozen suite rejected

The fifth wiring the `## Test plan` specifies, shipped unchanged. What was
written:

| File | What changed |
|---|---|
| `crates/core/src/lib.rs` | the eleventh `Tuning` field, `central_band_fraction: f32`, defaulting to `0.6`, and its window |
| `crates/core/src/edges.rs` | `widest_textured_run`, a private `wider` tie-break helper, and a module-header paragraph on the contrast with `textured_span` |
| `crates/core/src/flat.rs` | `central_band`, `page_column`, the "Stage 3c" section, and a module header saying the module now holds two stages |
| `crates/core/src/decide.rs` | one call in `detect`, one clause in `trimmed`, and the documentation for both |
| `docs/wiki/architecture.md` | AC-7: the tuning table row, and a pointer to the derivation here |

`textured_span`, `spread_profile`, `spread_within`, `textured_box`, the
`strong_lines` stand-down guard, the row axis and every existing
`Tuning::default()` value are untouched. No test file and no fixture was
touched: `git diff -- crates/*/tests fixtures` carries only RED's work.

**It went green on the first run.** 374 passed, 0 failed, 10 ignored - the same
374/0/10 RED measured against its candidate - and all ten `#[ignore]`d
integration tests pass, MC-026's seven included. AC-4 lands at **20 of 21**, the
one miss being `Screenshot (93).jpg` at `-4,+17`, and AC-5 at **0 clips of 21
cropped**. Both are RED's numbers to the entry and to the pixel.

**The one alternative tried and rejected**, and the frozen suite is again the
arbiter rather than taste: `page_column` selecting with `textured_span`. It is
the smaller change - one fewer function - and it passes the **entire** workspace
suite, because every fixture in `tests/flatness.rs` carries a single textured
run and the two selectors cannot be told apart on one. It is rejected by
`corpus.rs::the_left_and_right_edges_of_every_marked_page_land_inside_the_window`,
which scores it 15 of 21 against a floor of 19; the output is G1 in
`## Gate probes`. That is `## Context`'s "beats `textured_span`'s outermost rule
by four entries", measured here on the shipped module rather than read out, and
it is the reason `widest_textured_run` exists as a separate function instead of
`textured_span` being changed - which `## Out of scope` forbids and AC-2's
second assertion pins.

Three implementation choices the `## Test plan` explicitly left to GREEN:

* **`central_band` is a free function**, taking `Rect` by value, in `flat`.
* **`page_column` is built out of `central_band` and `widest_textured_run`**,
  over the crate-private `spread_within`, which **stays crate-private** - the
  public two-argument `spread_profile` is MC-025 AC-1 and nothing here needed to
  widen it.
* **The tie-break lives in a three-line private `wider`**, because
  `Iterator::max_by_key` returns the **last** maximum and the tie is pinned to
  the **first** run. `zero_one_and_many_runs_and_the_tie_between_two_of_the_same_width`
  is the test that would have caught the lazy spelling, and writing that helper
  with `>` rather than `>=` is precisely the bug it pins.

**Coverage.** `crates/core/src/flat.rs` is at 100.00% of lines and of regions;
`edges.rs` at 99.30% of lines, its single uncovered line being `deviation`'s
`count == 0` guard, which was already uncovered before this story and is
unreachable by construction. Workspace total 99.06% of regions and 99.72% of
lines against a floor of 95. No new branch was added that these tests do not
reach.

### GREEN: the numbers this story changes on the corpus, for MC-028's benefit

Recorded because MC-028 inherits this tree and `## Out of scope` says most
entries are still cropped far too tall. At `Tuning::default()`, over the 21
marked entries: every left offset is in `[-7, +1]` and every right offset in
`[-3, +17]`, twenty of the twenty-one inside MC-019's window on both. The
tightest containment margin over the mark is **0 px, on the right of
`2025-10-14 23_30_20.png`** - produced `1030..1515` against marked `1040..1515`.
It holds, and it is the one entry in the corpus with no slack at all on the
column axis; a later story that moves this locator outward by a pixel on the
right will find out there first. `2025-02-27 22_46_15.png` changes from
`Cropped 0,3 553x853` on `main` to `Cropped 72,3 225x853`; it is an
`expect: flag` entry that MC-026's AC-4 names `NOT_THIS_STORYS_TO_FIX`, both
answers are wrong, and this story does not chase it.

Gates run in GREEN: `cargo test --workspace`, `cargo test --workspace --release
-- --ignored`, and `bash scripts/gates.sh --fast` (PASS on format, lint,
typecheck, unit and coverage; `format` WARNed on the first attempt over two
rustfmt diffs of this story's own code, which `cargo fmt --all` fixed - the
class of thing RED's `--fast` note said to expect). The **full**
`bash scripts/gates.sh` was deliberately **not** run here - MC-026 recorded a
concurrent-gate-run incident - and is the orchestrator's to run alone in the
tree at GATES. Nothing above is a substitute for it.

### GREEN, verified by the orchestrator - and the mutation, run against the shipped implementation

**The tests were not touched in GREEN, checked rather than assumed.** The phase
lock forbids it, but a lock that is trusted without evidence is a lock nobody has
tested. Modification times separate the two phases cleanly: every test file and
the manifest was last written between 10:40 and 11:27 (`fixtures/corpus/
manifest.json` 10:40, `tests/decide.rs` 11:17, `tests/flatness.rs`,
`tests/common/mod.rs` and `tests/corpus.rs` 11:27), `phase.sh set MC-027 GREEN`
ran at 11:48, and every production file was written after it (`src/lib.rs`,
`src/edges.rs`, `src/decide.rs` 12:03, `src/flat.rs` 12:06). No test write falls
on the GREEN side of that line.

`cargo test --workspace` re-run here: **374 passed, 0 failed, 10 ignored**, which
is RED's predicted number to the test.

**The mutation.** A mutation table in a handoff is a claim until somebody runs
one, and RED's two probes were run in a throwaway tree against a *candidate*
implementation, which is not the same thing as the code being merged. One was
re-run here against the shipped module. The probe chosen is the one whose
predicted catch is a **single assertion**, because a single-assertion test is
exactly where a vacuous one hides.

`crates/core/src/edges.rs`, the tie-break in `wider`, loosened from strict to
non-strict so that a later run of equal width displaces the earlier one - which
turns "the first of any tie" into "the last of any tie":

```rust
-        Some(best) if best.1 - best.0 >= run.1 - run.0 => Some(best),
+        Some(best) if best.1 - best.0 > run.1 - run.0 => Some(best),
```

```
test zero_one_and_many_runs_and_the_tie_between_two_of_the_same_width ... FAILED

thread 'zero_one_and_many_runs_and_the_tie_between_two_of_the_same_width'
panicked at crates\core\tests\flatness.rs:1159:5:
assertion `left == right` failed: two runs of the same width: the first wins
  left: Some((2, 2))
 right: Some((0, 0))

test result: FAILED. 28 passed; 1 failed
```

**Exactly one test, and exactly the predicted one**, with an assertion message
that names the rule rather than the values. The tie-break is pinned and it is not
vacuous - which matters more than it looks, because nothing on the corpus turns
on it (`Screenshot (1661).png`, the file with a second textured region beside the
page, has a strictly wider page), so an unpinned tie-break would be a silent
platform dependency rather than a visible one.

Reverted immediately. `crates/core/src/edges.rs` is back to SHA-256
`9e64f78af3f427192378d64371ffbe7925ad2fc52c0909057bdf543cc85784d6`, byte for
byte the hash it carried before the mutation, and
`cargo test -p cropper-core --test flatness` is green at 29 of 29. The tree the
gates judge and the tree this mutation was run against are the same tree.
