# Project quality gates and tasks. THE SINGLE PLACE the harness learns how to
# build, test and run *this* project. Written by the bootstrap story once
# /plan-product has chosen a stack.
#
# Format:  <kind> | <id> | <required|optional> | <cwd> | <command>
#
#   kind     gate  -> run by scripts/gates.sh, must pass before a story is done
#            task  -> convenience command, run by scripts/task.sh <id>
#   id       stable name the agents refer to ("the coverage gate")
#   required required gates block story completion; optional ones only report
#   cwd      directory to run in, relative to repo root ("." for root)
#   command  the shell command; leave EMPTY to mark the gate as unconfigured
#
# Unconfigured required gates are reported as UNCONFIGURED. They are a warning
# before the bootstrap story lands and a hard failure after it (see BOOTSTRAPPED
# below).
#
# Format:  evidence | <gate id> | <extended regex>
#
# A gate that exits 0 has not necessarily done anything. `cargo test` in a
# workspace root tests one package and skips the members; a linter aimed at a
# directory that no longer exists checks nothing; a coverage threshold on a glob
# matching no files is satisfied vacuously. All of them exit 0, and exit 0 is
# not proof of work - it is the absence of a complaint, and a tool with nothing
# to do does not complain.
#
# An `evidence` line is the liveness assertion for one gate: after that gate
# exits 0, its captured output must match this regex or the gate FAILS with
# "ran but produced no evidence of work", at the gate's own severity.
#
#   evidence | unit  | test result: ok\. [1-9]
#   evidence | lint  | Checked [1-9][0-9]* files
#
# Rules:
#   - Assert VOLUME OF WORK OBSERVED, never success. Success is the exit code's
#     job. Regexes that assert success are fragile and encode the wrong idea.
#   - `|` inside the regex is fine; the parser keeps everything after field 2.
#   - `evidence | <id> | -` declares that no such output exists for this tool.
#     Use it rarely, and say why in the story that added the gate.
#   - A gate with no evidence line behaves exactly as it always did, but once
#     BOOTSTRAPPED=yes a required gate without one is reported as a warning.
#   - Proving liveness and measuring volume are two different jobs, and one
#     regex does both. Where a regex can only do the first, say so with a
#     `no-count` line below. In THIS file that is `lint`, `typecheck` and
#     `build`: see the note above their evidence lines.
#
# Canonical evidence lines per ecosystem live in the stack-profiles skill.
#
# Format:  floor | <gate id> | <minimum>
#
# An `evidence` line catches a gate that did NOTHING. A `floor` catches one that
# quietly started doing much less: a test project dropped from the workspace, a
# lint target narrowed to one directory, a suite that went from 47 tests to 3.
# All of those exit 0 and match the evidence regex.
#
# The number is measured out of the evidence match - the first run of digits at
# or after the start of it - so a floor needs an evidence line, and that regex
# must cover the whole number. `gates.sh --audit` refuses a floor without one,
# or a floor that is not a number.
#
#   evidence | unit | Tests +[1-9][0-9]* passed
#   floor    | unit | 40
#
# NOT EVERY EVIDENCE REGEX HAS A NUMBER UNDER IT, and the ones that do not are
# the trap. `Finished .* profile` - the regex `lint`, `typecheck` and `build`
# use in this file - proves cargo ran and measures nothing: the digits that
# follow it are the ELAPSED TIME. A floor of 5 on `lint` would therefore pass
# or fail on how warm the build cache is, and it would look, in this file,
# exactly like the floor on `unit`.
#
# Format:  no-count | <gate id> | <why its evidence regex measures nothing>
#
# So a gate whose evidence regex is liveness-only says so out loud. gates.sh
# then reports `observed -` for it instead of a meaningless number, and refuses
# any floor declared on it - at audit time and at run time both.
#
#   no-count | lint | `Finished .* profile` is liveness-only; the digits are seconds
#
# This is declared and not inferred because it cannot be inferred. "The regex
# contains no digit class" is the obvious test and it is wrong in both
# directions: `Finished .* profile` has none and matches a stopwatch, while
# `TOTAL` also has none and is followed by a real region count that a floor
# could legitimately use.
#
# Rules:
#   - Raise a floor deliberately, in the story that adds the tests, and never
#     lower it to make a gate pass. Lowering a floor is the gate equivalent of
#     deleting a test case; if the drop is legitimate - a suite genuinely
#     split in two - say so in the story that changes it.
#   - Every full run prints `observed <n>` for any gate with an evidence line,
#     floor or not, so the number is visible before anyone commits to one -
#     except a `no-count` gate, which prints `observed -`.
#   - The reason on a `no-count` line is required, and `gates.sh --audit`
#     refuses a line without one, or one naming a gate that does not exist.
#     A misspelt id defends nothing while looking like it does.
#   - Removing a `no-count` line is a claim that the regex now measures
#     something. Show the count in the story that removes it.
#
# Format:  discovery | <id> | <cwd> | <command>
#
# Run by scripts/doctor.sh, never by the gates. Each one asserts that a test
# runner can actually SEE a place tests are expected to live. A coverage
# threshold on a directory no test project includes is satisfied vacuously; a
# workspace member dropped from an include list takes its whole suite with it.
# Both are invisible in the configuration and obvious the moment the runner is
# asked what it can find, which is why these are commands and not globs:
#
#   discovery | platform | . | pnpm exec vitest list | grep -q "src/platform/"
#   discovery | crates   | . | cargo test --workspace --no-run 2>&1 | grep -q "Compiling world-core"
#
# Add one for every directory carrying a coverage threshold, and for every
# workspace member whose tests must run. A claim about what a runner discovers
# is verified by running the runner.
#
# Format:  slow | <gate id> | <why it is too slow to run every loop>
#
# `bash scripts/gates.sh --fast` runs every gate EXCEPT the ones named here.
#
# It exists because RED and GREEN only ever run the test command, while the
# gates judge the tests with a different and slower one - the same suite under
# coverage instrumentation, a linter, a type checker. A suite can pass RED,
# pass GREEN, pass every local gate and still fail a required gate in CI,
# because the instrumented run is slower and CI hardware is slower again. That
# happened: a property test at 1,835 ms plain and 2,644 ms instrumented, against
# a 5,000 ms default timeout, which is comfortable on a desktop and a failure on
# a runner. --fast is what lets RED ask "are these tests admissible to the gates
# that will judge them" without paying for a release bundle every loop.
#
# A gate is FAST unless a line here says otherwise, so the subset is right by
# default in a new project and wrong only where somebody said so out loud.
#
# Rules:
#   - The reason is required. `gates.sh --audit` refuses a `slow` line without
#     one, and refuses one naming a gate that does not exist - a typo there
#     excludes nothing and says nothing.
#   - Never mark a gate slow to stop it failing. That is what a waiver is for,
#     and waivers are refused on required gates for the same reason.
#   - A --fast run is never recorded in the story. It is not a full run, and
#     only a full run is evidence.

# Format:  ci-factor | <gate id> | <per-test factor> | <the CI run it came from>
#
# How much slower ONE TEST is on CI than on a developer machine, under this
# gate. Measured once from a real CI log and written down, because the obvious
# way to derive it is wrong and reads plausible.
#
# The trap: extrapolating from the gate's own wall time. A coverage gate at 3 s
# locally and 41 s in CI looks like 14x, so a 367 ms test looks like it will
# take 5,000 ms there and blow the framework's default timeout. It does not.
# Most of a coverage gate's wall time is fixed overhead - process start,
# transform, instrumentation, report generation - which does not scale with
# test compute. Measured per test, the same gate was 3.4x. Someone acting on
# the 14x would have spent a day making a test faster that was already fine.
#
#   ci-factor | coverage | 3.4 | actions run 412, "AC-4/AC-5 file 1,262 ms"
#
# Rules:
#   - Take it from a real CI log: one test's duration there over the same
#     test's duration locally, both under the same gate. `gates.sh --audit`
#     refuses a factor that is not a number, one with no source, and one
#     naming a gate that does not exist.
#   - Budget, not limit: keep any single test under a QUARTER of the
#     framework's timeout under local instrumentation. 4,275 ms against a
#     5,000 ms timeout is not a pass, it is a pending failure on slower
#     hardware.
#   - Re-measure when the runner image or the test command changes. A stale
#     factor is a guess with a citation.

# Format:  waiver | <gate id> | <why this optional gate is expected to fail>
#
# An optional gate that is known to fail - a mutation tool the stack cannot
# run yet, an integration suite waiting on a service - would otherwise WARN on
# every run forever, and the next agent learns that WARNs are ignorable. A
# waiver names the failure and where the decision is recorded; gates.sh then
# reports it as KNOWN, and WARN is reserved for something that changed. A
# waived gate still runs: when it passes, the summary says to remove the line.
# Waivers are refused on required gates - that would be a bypass, not a waiver.
#
#   waiver | mutation | stryker needs a TS compiler API TS 7 lacks; stack.md s4

# Set to yes by the bootstrap story once the stack is real.
BOOTSTRAPPED=yes

# --- Gates ------------------------------------------------------------------
gate | format      | optional | . | cargo fmt --all --check
gate | lint        | required | . | cargo clippy --workspace --all-targets -- -D warnings
gate | typecheck   | required | . | cargo check --workspace --all-targets
gate | unit        | required | . | cargo test --workspace
gate | coverage    | required | . | cargo llvm-cov --workspace --fail-under-lines 95 --ignore-filename-regex 'crates[/\\]app[/\\]src[/\\](main|gui)\.rs'
gate | integration | optional | . | cargo test --workspace --release -- --ignored
gate | build       | required | . | cargo build --release --workspace


# --- Left out of `gates.sh --fast` ------------------------------------------
# Defaults a bootstrap story should revisit against its real stack: keep a gate
# here only if it is genuinely too slow to run on every RED and GREEN loop.
slow | integration | release rebuild of the workspace plus the 100-image timing run and the corpus; minutes
slow | build       | fat-LTO release build of the workspace; minutes, and RED has no use for it

# --- Evidence of work -------------------------------------------------------
# Filled in by the bootstrap story alongside the gate commands, from the stack
# profile. `bash scripts/gates.sh --audit` reports which are still missing.

# --- Tasks ------------------------------------------------------------------
task | install | - | . | cargo fetch
task | dev     | - | . | cargo run -p manhwa-cropper
task | test    | - | . | cargo test --workspace

# --- Evidence of work (MC-001, run and read on 2026-09-10) -----------------
# Output shapes observed on cargo 1.98.1 / cargo-llvm-cov 0.9.1 on Windows.
# `cargo fmt --check` prints nothing when clean and a diff otherwise, so its
# liveness is declared unassertable. The three `Finished` regexes are weak by
# design (a warm cache prints nothing else); `--workspace` and the `unit`
# gate carry the real protection. The coverage table ends in a `TOTAL` row.
#
# Those three are also COUNT-FREE, and the `no-count` lines below say so. Cargo
# has no count to offer here: on a cold cache it prints one `Compiling <crate>`
# line per crate, on a warm one it prints none at all, so no regex over that
# output can both hold as an evidence assertion and yield a stable number. The
# single line a warm run does print is
#   Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.39s
# whose first digits are 0 - the seconds. Read on MC-026: `typecheck` reported
# observed 0, `lint` 2 then 6 across runs, `build` 1 (from `in 1m 22s`). All
# passed correctly; the numbers were noise, and would have become a coin-toss
# gate the moment anyone put a floor under one.
evidence | unit        | test result: ok\. [1-9][0-9]*
evidence | lint        | Finished .* profile
evidence | typecheck   | Finished .* profile
evidence | build       | Finished .* profile
evidence | coverage    | TOTAL
evidence | integration | test result: ok\. [1-9][0-9]*
evidence | format      | -

no-count | lint      | `Finished .* profile` is liveness-only; the digits after it are the elapsed seconds, not a count of crates, files or targets
no-count | typecheck | `Finished .* profile` is liveness-only; the digits after it are the elapsed seconds, not a count of crates, files or targets
no-count | build     | `Finished .* profile` is liveness-only; the digits after it are the elapsed seconds, not a count of crates, files or targets

# The first `test result: ok. N` line cargo prints belongs to the first test
# binary that ran at least one test. Cargo runs the workspace members in the
# order `Cargo.toml` lists them (`crates/core` first) and each member's test
# targets alphabetically, so that binary is `crates/core tests/content.rs`
# today. Observed 1 at MC-001 (then crates/core tests/scaffold.rs); raised to
# 47 at MC-005, which is content.rs's whole suite. Probed both ways in MC-005's
# `## Gate probes`.
#
# Note for whoever adds the next core test target: this number tracks ONE
# binary, not the workspace's 124 tests, and a new target sorting before
# `content` would move it. If that happens the drop is legitimate - say so in
# your story, per the "never lower it to make a gate pass" rule above.
floor | unit | 47

# `integration` runs `cargo test --workspace --release -- --ignored`, where
# almost every target reports `test result: ok. 0 passed` - which the evidence
# regex above does NOT match, because of its leading `[1-9]`. So the counted
# line is not simply the first target cargo runs: it is the first target that
# has any ignored test at all. Measured on MC-040 from the real gate log: twelve
# `test result: ok. 0` lines go by unmatched (all of crates/core, then engine's
# args/batch/codec), and the number comes from `crates/engine tests/corpus.rs`
# and its nine tests - the suite that scores the corpus. `corpus_accuracy.rs`
# (5) and `perf.rs` (1) run after it and contribute nothing to this number.
#
# So this number tracks ONE binary, not the whole --ignored run. It would move
# if an engine test target with ignored tests started sorting before `corpus`,
# if a crates/core target gained an ignored test (core runs first), or if
# corpus.rs itself gained or lost a test. Each of those is a legitimate change:
# say so in your story and set the number the next full run observes. What is
# not legitimate is lowering it to make the gate pass - that is the gate
# equivalent of deleting a test case, and this gate is the only thing that runs
# the two suites every accuracy number in the product is denominated by.
floor | integration | 9

# `cargo test --no-run` prints one `Executable ... (target\debug\deps\<crate>-<hash>.exe)`
# line per test binary on every run, warm or cold; the crate name appears in
# its underscore form. Confirmed at MC-001.
discovery | core   | . | cargo test --workspace --no-run 2>&1 | grep -q "cropper_core"
discovery | engine | . | cargo test --workspace --no-run 2>&1 | grep -q "cropper_engine"
discovery | app    | . | cargo test --workspace --no-run 2>&1 | grep -q "manhwa_cropper"

# --- Known failures on optional gates ---------------------------------------

# --- CI cost, measured on MC-006's PR #7 -----------------------------------
# Per-test, against the same binary (crates/core tests/detect.rs), not from
# gate wall time - MC-006's story has the working and the reason the wall-time
# reading (22m17s CI against 201s local, "6.7x") is wrong. Almost all of the
# gate's CI time is fixed overhead: checkout, toolchain, and rebuilds across
# four profiles.
ci-factor | unit     | 1.42 | actions run 34568382246, tests/detect.rs 3.93s local -> 5.58s
ci-factor | coverage | 1.63 | actions run 34568382246, tests/detect.rs 12.41s local -> 20.28s
